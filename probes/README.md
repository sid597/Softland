# probes/ — archived measurement harnesses

Throwaway rigs that answered their questions; kept for resurrection, OFF
every classpath (deps.edn paths = src/resources — nothing here compiles
or loads). Findings live in `docs/current-mental-model/build/<pkg>/`
(write-echo, islands-probe, block-write, scene-substrate). The ns forms
still name their original homes — copy a file back under `src/` and
restore its require/mount lines (git history has them) to run one again.
