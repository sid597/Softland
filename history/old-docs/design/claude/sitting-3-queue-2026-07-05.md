# Sitting-3 queue — Track C · captured 2026-07-05 at Window-1 close

References below to `BETS.md` are historical; that document is retired.
For vision orientation, use [carry-on](../../../../docs/carry-on.md) as a reference summary
and [the vision log](../../../../vision/LOG.md) as the primary source.

**Standing:** queue, not rulings. Delivered by the sitting-2 designer at window
close (relayed via Sid, verbatim below); filed by HQ so it survives the window.
Sitting 3 boots from this file + `room-card-lane-2026-07-05.md` + the
do-not-preclude ledger.

## The seven OPEN items (designer's words, designer's order)

1. **The naming fork (founder's)** — ground / rim / room / band / kraft as
   shipped words; recommendation: keep them as working names until
   user-facing copy exists, so the cost of deciding late stays near zero.
2. **Persistent trail-presence-in-rooms** — what of the trail survives on
   screen when a room (e.g. the editor) is open; the one honest tension R3
   deferred between the full-screen swap and the "trail always on screen" LAW.
3. **Band-2 semantic text** — where authored decision/2-liner forms come from
   for real material; this is the render half of the already-queued
   "commit-gesture for thought" question, and the two should be designed
   together. *(Cross-link: baton queue item 7 / trail-view INPUTS item 5.)*
4. **Doors** — the gesture by which a card opens a room at its address
   (editor at a commit's file, transcript at an entry); the first half of the
   read→write bridge and the thing that makes R1's "rooms behind doors" real
   rather than rhetorical.
5. **Calibration lane** *(from the track queue — not yet designed)* — how
   truth-states/calibration get a lane-level render channel; design it
   against R6's two-materials ruling so calibration doesn't become a third
   material by accident.
6. **Newcomer's first day** *(from the track queue — not yet designed)* — the
   visitable test: first entry into someone else's land; exercises
   resume-in-place, the rim's scope slot, and
   watching-cheaper-than-contributing at once. *(Cross-link: BETS H3.)*
7. **Rim delta semantics at the write milestone** — the delta slot currently
   reads the arrival clock, honestly labeled; once walk/attestation rows
   exist (D-008 milestone 2) it should switch to walk-cursor delta, and that
   switch deserves a ruling rather than a drift.

## Designer's sequencing instinct (recorded, non-binding)

2 and 4 are the load-bearing pair (they define what leaving the ground
means); 3 rides the commit-gesture question wherever that lands; 1 can wait
for Sid's whim indefinitely.

**2026-07-07 addendum (sitting 3):** item 3 ACTIVATED and promoted — Sid
independently re-derived the commit-gesture-for-thought from the felt
failure of the whole-land mockup ("the granularity is just too big … this
whole tripple gives me nothing"). It is no longer a render question riding a
gesture question: it is the trail's data atom. See
`atomic-unit-2026-07-07.md` + `vision/LOG.md` 2026-07-07 (verbatim + both
notebook pages). Sequencing instinct updated: 3 now leads the queue; 2 and 4
render against units, not containers.
