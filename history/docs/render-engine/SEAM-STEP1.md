# Render step 1 — the Electric-native landing begins (starter + register)

The first code act of the render-seam constitution (decisions.md "The
render seam", settled + amended 2026-08-03). In Sid's words: the
Electric-native arc starts landing here — the maintained tape is the
store that "electric native and equivalent" stands on. The diff touches
no Electric file: transport stays untouched; the work lives in our
renderer, editor, and pick paths.

**Register + runner:** BUILD session. Recommended: Fable implements
directly (settled role split — fastest path; the surface is
reactive-subtle: which flows split, where the sharing points are — and
the oracle fence is being BUILT here, so an implementer cannot lean on
it). Sid may route to an implementer-under-contract instead; that
requires a contract first (work-package skill).

**Close conditions:** render tests green · BOTH fences green (the
existing text-layout ownership fence AND the new maintained-≡-batch
equivalence oracle) · hand-feel pan/wheel/inspector · falsification
pass run · the pre-registered prediction checked (did the plumbing
dialects start converging?).

## Opening prompt (paste into a fresh session)

    Session register: BUILD. Boot per CLAUDE.md; read decisions.md "The
    render seam" (settled + amended) + the board's engine block +
    build/render-engine/SEAM-STEP1.md. Implement render step 1 (staged
    beside Package 2's image atom):
    Act 1 — maintained ordered view: persistent sorted map keyed
    [order-key entry-id] beside the slot map in !scene-store, patched in
    the existing keyed mutations; draw-frame! walks it forward, pick-at
    walks rseq; compile-frame-tape leaves the frame/pick paths — this
    act IS the growth law's first crossing: the batch compiler is
    DEMOTED to oracle behind a NEW equivalence fence (maintained view ≡
    batch compile; a new instrument — do not extend the text-layout
    fence, it is a static ownership fence, different genus);
    validate-entry! moves to the upsert edge; :frame-idx /
    :world/revision must no longer reach scene derivation.
    Act 2 — editor: split stable layout flows from the 530ms ticker
    overlays (!caret-visible, !shimmer-phase); unify the forked flow
    copies — the real sharing points (m/signal there). Begins
    fenced-view instance #2: upgrade the existing :line/id rescan
    consumers (electric_flow / renderer lookup sites) into keyed ones,
    consuming :line/id + :source-revision as keys — begun here, not
    finished. When the flow split relocates the text fence's hardcoded
    anchors (<editor-content, the fn-args marker), move the fence's
    pins with it — never delete the fence.
    Act 3 — the door: quarantine the !camera watch, measure pan; then
    the honest fix — :screen space in pick; the camera→registry edge
    dies.
    Standing guards (keep the open register open): diffs minted as
    canonical facts, as values, in one namespace — destination deltas
    derived behind their doors · blob assets by id+revision, bytes
    never in the scene value · store internals private behind the two
    interfaces · no new watch, no clock-typed derivation input.
    Close: render tests + both fences green; hand-feel
    pan/wheel/inspector; check the dialect-convergence prediction.
    Note: workshop_playground / studio / image_material are uncommitted
    and live in the tree — never sweep them into a commit.
