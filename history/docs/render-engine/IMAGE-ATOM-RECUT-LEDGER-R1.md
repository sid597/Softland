# image-atom — recut ledger R1 → R2 candidate

Maps every `IMAGE-ATOM-CONTRACT-VALIDATION-R1.md` finding to its consumption
in the recut CONTRACT.md (2026-08-05). R2 validates the recut against this
map: a finding is consumed only if its repair is executable substance in the
contract, not a banner.

## FAILs (recut-class)

- **F1 (per-instance entries corrupt GPU payload)** → §7 REWRITTEN: the
  per-instance store-entry design is REFUSED with the R1 trace recorded;
  images ride the pool-lane pattern (fourth op lane + renderer producer
  `[:frame/store vi :images]`); the scene_runtime payload derivation +
  count/order maps extension is a NAMED deliverable (§4 + §7); the
  maintained view is explicitly untouched.
- **F2 (slot-entry fan-out unnamed; maintained ≡ oracle impossible)** →
  dissolved by the same §7 recut: `slot-entry`, `remove-slot`, the batch
  oracle, and the equivalence law are UNCHANGED and G8 re-asserts them.
- **F3 (no pick dispatch for `:geometry :image-quad`)** → §5 pick bullet
  recut: product-pick route DECLARED as the existing slot-tree route
  (rt/hit-test through scene_store/pick inverse-affine — the transformed-
  quad test), divergence-sentinel precedent named; a central
  `:geometry`-keyed pick branch REFUSED; G4 overlap receipts re-scoped to
  the product route; no new dispatch mechanism in this wave.
- **F4 (strict C4 unsatisfiable under direct-present; candidate
  presentation encode absent from tree)** → §5 color block recut into TWO
  DECLARED REGIMES: candidate-strict (ingress transfer + presentation
  encode made code-real behind the default-OFF seam, zero-diff when
  disabled per G2) · legacy declared zero-transfer passthrough + no-double-
  transfer sentinel; G6 recut to match; inter-regime divergence recorded,
  never equated.
- **F5 (G4 decisive-pass vacuous at zero boundary pixels)** → §5 coverage
  bullet gains the ramped-edge obligation; G4 + §8 gain explicit floors
  (`boundary-pixel-count > 0`, `decisive-count > 0` per regime, reported in
  the receipt).

## MINORs (fixed in place)

- **M1 (fixture bytes 404 on synthetic origin)** → §8 fixture-delivery
  bullet: `serveSyntheticOrigin` extended to serve
  `test/app/fixtures/render_engine/images/*`; named as implementer
  disclosure.
- **M2 (single-system capture + hardcoded clear)** → §8 capture-path
  bullet: image cases get their own capture routine; the existing path
  serves the 21 cases unchanged (edit = S4).
- **M3 (21-byte assertion breaks on appended rows)** → §8 bank-append
  bullet: compare/classification tolerance for appended rows is part of
  the deliverable, receipted by G2 green-on-21 with rows present.
- **M4 (`--update-goldens` wholesale + unauthorized-without-imagePass)** →
  §8: wholesale update FORBIDDEN; scoped `--append-image-goldens` road
  named.
- **M5 (fence self-test vacuous literal)** → T12 recut: self-test seeds
  into a copy of the ACTUAL extracted slice; must still reject.
- **M7 (T10 fence script needs package.json)** → T9→T10 recut + §12: the
  producer-purity check lives INSIDE `verify_scene_tape_fence.mjs`; the
  new-script line removed from MAY CREATE.
- **M11 (no ICC/PNG road for the profiled fixture)** → §8 fixture-
  provenance bullet: deterministic JVM generator via `javax.imageio` +
  `java.awt.color.ICC_Profile`, committed bytes + digests, no npm
  dependency, no network.
- Remaining R1 MINORs/ADVISORIES not named above travel to the phase
  artifact as implementation notes; none alters a §-level law
  (R1 artifact preserved unmodified — the log is primary).

## R1 clean-traces carried forward (the R2 validator need not re-derive)

All 30 §11 locators substance-correct · Act-0 fingerprint matches on-disk
environment.json · the fence's renderer-slice extraction would see the
image producer · `frame-contract-registry` forces tape+registry edits to
land together · candidate-seam fixtures need no `runtime/render.cljs` edit
(init systems take `:scene-color` directly).
