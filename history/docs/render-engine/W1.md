# W1 — shared render contracts

The contract text moved VERBATIM into `docs/render-engine/W1/`, one file per
contract. Those files are the binding text — read them PRIMARY; this page is a
locator only and restates no law. Section numbering (§1–§9) is unchanged inside
the shards, so every existing "W1 §N" citation still resolves.

- **`W1/GROUND.md`** — title + intro · "1. Current-byte ground" ·
  "2. Shared vocabulary and fail-closed admission".
  Also bound by: `CHROME-ATOM-CONTRACT.md` (clip participation, absence-is-not-a-policy),
  `IMAGE-ATOM-RULING-PACKET.md` (new-family admission precedent).

- **`W1/CONTRACT-O.md`** — "3. Contract O — one ordered scene truth".
  Also bound by: `CHROME-ATOM-CONTRACT.md` (chrome's stratum/part-rank ladder),
  `CONNECTOR-ATOM-CONTRACT.md` (connector order token),
  `IMAGE-ATOM-CONTRACT.md` + its VALIDATION/RECUT ledgers (batching law, reverse-order overlap),
  `W4-FRAME-RUNTIME-CONTRACT.md` (pass-splitting under forward order),
  `T2-INPUT-FLOOR-CONTRACT.md` (pick filters the tape, reverse),
  `W2-A-T0.md` (canonical nested stack-path).

- **`W1/CONTRACT-G.md`** — "4. Contract G — unconditional geometry truth".
  Also bound by: `CHROME-ATOM-CONTRACT.md` (per-form declaration, hit-slop),
  `CONNECTOR-ATOM-CONTRACT.md` (reference-edge authority),
  `PATH-ATOM-CONTRACT.md` (path-material mirrors G),
  `IMAGE-ATOM-CONTRACT.md` (geometry descriptor + pick policy),
  `W4-FRAME-RUNTIME-CONTRACT.md` (effects declare geometry),
  `T2-INPUT-FLOOR-CONTRACT.md` (§4.9 text pick row), `ENGINE.md`, `W2-B-T1.md`.

- **`W1/CONTRACT-M.md`** — "5. Contract M — material citizenship".
  Also bound by: `CHROME-ATOM-CONTRACT.md` (ephemeral-citizenship pin),
  `CONNECTOR-ATOM-CONTRACT.md` (fail-closed grammar, versioned semantics),
  `IMAGE-ATOM-CONTRACT.md` (M-template instantiation, M-receipt coverage),
  `W4-FRAME-RUNTIME-CONTRACT.md` (M10 target-pool lifetime/budget),
  `T2-INPUT-FLOOR-CONTRACT.md` (session-ephemeral citizenship).

- **`W1/D2-INK.md`** — "6. D2=A ink citizen".
  Also bound by: `PATH-ATOM-CONTRACT.md` + `PATH-ATOM-NOW.md` (the path family sits on D2=A),
  `docs/packages/path-atom.md` (package scope), `ENGINE.md` (template binds ink to D2=A),
  `IMAGE-ATOM-CONTRACT.md` (names it as the next wave).

- **`W1/CONTRACT-T.md`** — "7. Contract T — one text layout result".
  Also bound by: `docs/shaping-correction/CONTRACT.md` + `NOW.md` + `P1.md` +
  `VALIDATION_R1–R4` + `RECUT_LEDGER_R1–R2` (primary downstream: tagged indices,
  no aliased offsets, one-geometry-truth),
  `T2-INPUT-FLOOR-CONTRACT.md` (every editing/preedit layout rides the one seam),
  `CONNECTOR-ATOM-CONTRACT.md` (label consumes one layout result),
  `CHROME-ATOM-CONTRACT.md` (no new text consumers; T1 fence untouched).

- **`W1/CONTRACT-C.md`** — "8. Contract C — color and alpha".
  Also bound by: `PATH-ATOM-CONTRACT.md` + `CONNECTOR-ATOM-CONTRACT.md` +
  `CHROME-ATOM-CONTRACT.md` (straight → tagged linear-premultiplied seam),
  `IMAGE-ATOM-CONTRACT.md` + `-RULING-PACKET.md` + VALIDATION/RECUT ledgers
  (untagged refusal, §8.2.5 transfer regimes),
  `W4-FRAME-RUNTIME-CONTRACT.md` (intermediates, gradient law),
  `T2-INPUT-FLOOR-CONTRACT.md` (editing visuals as ordinary rects),
  `ENGINE.md`, `W2-B-T1.md`.

- **`W1/EXECUTION.md`** — "9. W1 execution receipt and downstream gate".
  Also bound by: `docs/shaping-correction/*` (G10 pins the §9.1 verifier state
  and environment fingerprint), `IMAGE-ATOM-CONTRACT.md` + `-VALIDATION-R1.md`
  (§9.1 counts, §9.2 registration gates), `ENGINE.md` (W1 SETTLED marker).

Boot lists that name `W1.md` whole (`W2-A-T0.md`, `W2-B-T1.md`,
`PATH-/CHROME-/CONNECTOR-/IMAGE-ATOM-CONTRACT.md`, `W4-FRAME-RUNTIME-CONTRACT.md`,
`IMAGE-ATOM-NOW.md`) should boot the specific shards their family touches.
