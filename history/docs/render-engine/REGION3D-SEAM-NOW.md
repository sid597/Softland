# REGION3D SEAM (Atom B) — NOW

STANDING (frozen at cut, 2026-08-07): Package 3 Atom B — the coexistence
seam + THE SEAM DEMO; the FINAL atom of the render-engine campaign, and its
close carries the ONE seam courtroom over Packages 2+3 (A-contract §10 format
+ the latency trace, contract §9). Contract:
`REGION3D-SEAM-CONTRACT.md` (one-pass law; cutter `claude-fable-5` effort
max; two Sid-touches: the cut + the accept at the demo; the cut closes
through ONE bounded fresh-eyes Codex-class falsification round at Sid's hand
— findings return to a Fable session for in-session repairs, no verdict
authority, no recut). Implementer: Codex, one session, whole atom, after the
round's repairs land. Discharges the eight frozen §9 obligations of the
A-contract, none shrunk. The observed object-interaction slowness stays
observed/unmeasured/unattributed until the courtroom's adapter-pinned frame
trace — no perf work rides it. Focused suite:
`region3d_placement_test` + `region3d_scene_test` + `region3d_material_test`
+ the connector test home + `scene_tape_test` + `frame_graph_test` +
`maintained_view_test`; `npm run verify:render-engine` RED only on the
preserved MSDF counterexample. Genuine forks and strongest-default notes land
HERE as one-liners. This baton never outranks the contract or decisions.md —
flag drift, keep moving.

---
(NOW entries below, ≤15 lines each, newest first; close entry includes the
process-vs-code self-audit line.)

## 2026-08-08 — TREE BANKED `43ae9d9`; budget wedge repaired; one composed-tree RED
- Banked the composed working tree (both atoms + repairs) at Sid's OOF word — a logistics bank, NOT the demo acceptance; FELT VERDICT stays PENDING.
- Frame-target budget wedge repaired (live zoom refusal loop, error observed 2026-08-08): stale region leases now die at frame start BEFORE the new generation acquires; failed frames bump the reclaim epoch; lease size clamps to the attachment (unclamped, the 4096-quant MSAA color target alone equals the whole 512MB pool); `destroy-target!` idempotent + after-submit identity guard. Functional receipt = Sid's next zoom; `__softlandFrameCompositor.receipt()` shows refusals if any survive.
- S5 placement leg re-spelled through the Contract-T §7.5 accessor door (`line-glyphs`; raw `:glyphs` get-in is the amendment's named crime); numeric `==` since the view deliberately longs integral unshaped values on the JVM.
- COMPOSED-TREE RED (S4 replay leg): plane-backed layout results do not survive `pr-str`→`edn/read-string` ("No reader function for tag object"); the road is real — T2 text-ops carry whole results into the store. NOT repaired here (weakening the frozen tripwire would hide the finding). Owner: the durable-custody ruling (cutover contract vs staged).
- Verifier note: `run_verifier.mjs` T1 fails in this sandbox environment with or without the bank (stash-isolated receipt) — environment-shaped, not tree-shaped; unresolved.

## 2026-08-08 — ATOM B BUILT; mechanical courtroom closed; SID ACCEPTANCE PENDING
- BUILT: grammar v2 + migrations, pure ref/placement/pick derivations, store resolution, placed MSDF/ink GPU, region-object connector doors, session-layout mirror, and `seam_demo.cljs` through thin renderer/runtime hooks.
- DEMO: `?live-atoms=1&region3d=1&seam-demo=1`; Codex drove orbit → mesh/placed-text picks → T2 type+IME echo → gizmo drag with following labeled anchor → cold replay; no page errors.
- LIVE RECEIPT: 2/2 placements, 190 glyphs, 90 ink vertices; placed pick kept address + shared layout identity; each T2/IME transition gave the same 2D/3D `:layout/id`; idle repack/projection deltas were zero.
- MOTION RECEIPT: orbit and eight-step gizmo drag moved only the region-bound edge proportionally; settled mesh translation changed, placement packs/uploads/layout calls stayed zero; reload preserved region value, order hash, and picks while session dress disappeared.
- TRIPWIRES/GOLDENS: S1–S5 frozen in placement/connector homes; exactly 3 seam goldens appended (50 total), deterministic 6/6 Region3D; the prior 47 PNG bytes and known MSDF RED stayed unchanged.
- FOCUSED: 50 tests / 459 assertions green across every contract-named Region3D, connector, scene-tape, frame-graph, and maintained-view home; T1 fence green (27 owners); dev/verifier compile green.
- COURTROOM SUITE ONCE: 616 tests / 8,215 assertions; 26 failures + 1 error are foreign to this atom (all Atom B focused homes green), recorded and passed by without repair or rerun.
- BOARD DEBT: ingest-watchers exhausted inotify (15 failures); dogfood-space timing (1); stale image-citizenship family-count (1); face-transcription golden plus existing shard-3/4/5 baseline debt (2f / 2f+1e / 5f); registered dogfood-llm probe recovered on attempt 2.
- TRACE: AMD Radeon RX 7900 XTX / Vulkan, `target/region3d-seam-gizmo-trace.json`; 15 animation frames / 2134.644 ms, 16 event dispatches / 199.488 ms, 35 GPU tasks / 21.397 ms — slow main-thread windows observed, function cause still unmeasured; kill-probe = sampling CPU profile, no repair in B.
- DONE-CONDITION CHECK: the composed fixture demonstrates drawing/design/3D/connectivity without losing client identity, provenance, or editability; durable coexistence history remains staged, so campaign closure still needs Sid's felt acceptance + custody ruling.
- FELT VERDICT: PENDING — Sid has not yet supplied acceptance words for THE SEAM DEMO.
- CUSTODY QUESTION TO SID: after the cold drive, authorize a separate durable-custody cutover contract for region slot + relation-kernel region-object endpoint + T2 materials, or leave custody staged?
- SELF-AUDIT: implementation followed the one-pass contract; three live falsification defects were fixed in-session; no genuine fork, no perf side quest, no protected-file edit, no commit/push.
- NEXT: Sid drives the six-step demo and gives the accept/reject + custody answer; only then may the board say SID-ACCEPTED / package closed.

## 2026-08-07 — FALSIFICATION ROUND INGESTED; contract repaired; Codex next
- Codex returned 10 decision-changing + 3 soft findings; all 13 adjudicated
  against fresh source (three Opus gatherers, zero line drift) and repaired
  in the contract as ONE batched pass. No recut; every §1 obligation intact.
- Load-bearing repairs: islands formula restored VERBATIM (double-pxRange +
  `uv_texels` killed) · ref resolution got the occurrence-OWNER rule +
  legacy-color adapter · mirror keyed by `:layout/id` (preedit-proof) · ONE
  ink-cache cell, path-system the owner · both grammar migrations made
  executable (region retags 1→2; connector bumps at the descriptor) ·
  `:local` REQUIRED · region door re-homed to `derive-route-set` with a
  value-tuple key + region-before-connector frame order · atlas handles
  per-frame from text-sys, Slug backend = declared `:no-msdf-atlas` refusal
  · S4 provenance reads planted family-shaped rows · S1–S5 each gained the
  anti-gaming leg its named wrong build required (new `:scene-derives`).
- NEXT: paste the contract-tail implementer prompt into ONE fresh Codex
  session; acceptance stays Sid's word at the demo; courtroom §9 at close.

## 2026-08-07 — CONTRACT CUT; round pending at Sid's hand
- Cut from primary reads (A-contract whole · W1 O/G/M/T/C/D2/GROUND ·
  ENGINE §0/§7/§8/§10 · W0-C §3.3/§4/§5.1 · decisions render-seam +
  substrate + engine-lands · T2 + connector scoped · islands receipt) + two
  schema'd Opus breadth indexes (region3d namespaces · 2D lanes), roads
  verified at the bytes in-session.
- Shape: NO new tape family/pass/plan/lease machinery — grammar v2 at two
  families (placed `:text`/`:ink` object kinds; connector `:region-object`
  binding), two new pure/GPU placement namespaces, one store ref-resolution
  step, the carried-layout mirror, the anchor resolver injection, the demo.
- Author pass (four lenses) ran in-cut; three post-Write repairs landed as
  one batched pass (shadow-pass exclusion pinned · live-mirror cache key ·
  S5's clipped-card leg).
- NEXT: Sid pastes the falsification-round prompt (session-close artifact)
  into ONE fresh Codex session; findings → Fable repair session; then the
  contract-tail Codex implementation prompt.
