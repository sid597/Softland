# image-atom — recut ledger R2 → R3 candidate

Maps every `IMAGE-ATOM-CONTRACT-VALIDATION-R2.md` finding to its consumption
in the second in-place recut (2026-08-05). R3 validates consumption in
EXECUTABLE SUBSTANCE plus the W1-conformance gap R2 honestly declared.

## R2 FAILs → consumption

- **N1 (G8's JVM tier over `.cljs` scene_runtime — receipt unexecutable)** →
  §7 op-lane bullet: the pure payload derivation (ordered slots → payload +
  count/order maps) is EXTRACTED to a `.cljc` home (scene_store.cljc or new
  pure ns, both allowlisted), scene_runtime keeps the thin Missionary
  wrapper; behavior-identical, covered by existing suites + G2. G8 recut to
  assert the extracted fn on JVM; the renderer twin-check with image
  entries is explicitly a BROWSER receipt. G5 recut: O1 = JVM tape tier,
  O5 pixels = verifier tier, neither claims the other.
- **N2 (candidate presentation encode has no in-allowlist home)** → §5
  candidate bullet: road FIXED per R2's own trace — the candidate FIXTURE's
  render/capture target declared `*-srgb` (hardware transfer, W1 §8.2.5),
  implementer-owned verifier/renderer fixture path; no shader blit, no
  product present-path change, no MUST-NOT file.
- **N6 (per-binding batch splits inexpressible via one pool-info/count
  key)** → §6 bind-group bullet: the image PRODUCER derives order-contiguous
  per-binding sub-runs from stamped ops itself (producers are arbitrary
  registered fns; `store-pool-entries` is precedent, not mechanism), one
  entry per (vi, binding-run), unique ids, per-run offsets; day-one lever =
  single atlas page + few dedicated textures.
- **N3 (golden assertion "by the run itself" has no code referent;
  imagePass short-circuited)** → §8 bank-append bullet: CASCADE REPAIR is a
  named deliverable — existing-golden mismatch gets its own classification +
  non-zero exit evaluated BEFORE parity; golden + image-parity printed in
  stdout and receipt file; MSDF-only failure still yields exactly
  `candidate-pick-parity-failure` exit 1. G2 references the repair.
- **N5 (modes-vs-cases fork; reading (ii) blocks the append road via
  updateAuthorized)** → §8: image cases are their OWN case family with
  their OWN receipt fields; they NEVER join sentinel/divergence/parity/
  updateAuthorized accounting; pinned invariants (7/7 · 14/21 · 47/regime)
  untouched by construction.
- **N4 (probe reads channel 0; colored quad satisfies floors while
  comparing garbage)** → G4: geometry probe renders the quad with OPAQUE
  REFERENCE PAINT saturating the compared channel (Cg isolation, W1 §4.3);
  texture correctness is G3's job. Floors retained.

## R2 executed receipt → consumption

- **iCCP road** (plain `ImageIO.write` silently omits iCCP; corrupt-chunk
  trap; `getData()` call-order dependence) → §8 fixture-provenance bullet:
  road pinned to explicit `IIOMetadata` + `mergeTree` with PRE-DEFLATED
  bytes; generator asserts well-formedness by re-read + inflate/parse
  (never chunk presence) + two-run byte determinism.

## Declared-gap → R3 duty (not a contract edit)

R2 did not read `W1.md`/`ENGINE.md`/`W2-B-T1.md`. R3 must check the recut
§5/§6/§7 laws against W1 directly: descriptor vs §4.1/§4.9 · two-regime
color vs Contract C §8.1–§8.3 (esp. §8.2.5) · producer sub-runs vs Contract
O §3.2 batching law · product-pick route vs Contracts G/O.

## R2 MINORs/advisories

Thirteen MINOR + five advisory travel to the phase artifact as
implementation notes unless R3 finds one to be §-level (R2 artifact
preserved unmodified — the log is primary).
