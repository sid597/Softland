# CONNECTOR-ATOM — NOW

STANDING (frozen at cut, 2026-08-06): Package 2 wave 3 — the reference/connector
atom: durable reference edges READ from the relation kernel (R1
`relations-for-targets`, read-only, no kinds minted) + angled segments
(straight/elbow/waypoints) + arrowheads + post-T1 labels through the one layout
seam + attachment (boundary anchoring, cross-container via effective
transforms), `:render.family/connector` through the W2-B tape; the `?live-atoms=1`
join grows the durable-edge projection — drag a real block, its edges follow.
Contract: `CONNECTOR-ATOM-CONTRACT.md` (one-pass law; two Sid-touches: cut
2026-08-06 · SID-ACCEPTED 2026-08-06; the cut closes through the armed A/B falsification
round — Codex + fresh-Claude, identical brief, findings pooled, author repairs
in-session, role-vs-model datum recorded here). Implementer: Codex, one session,
whole atom. Genuine forks and strongest-default notes land HERE as one-liners.
Focused suite: the three connector JVM test namespaces (two pure +
`connector_join_test` in isolation-exceptions) + `npm run verify:render-engine`.
This baton never outranks the contract or decisions.md — flag drift, keep moving.

---
(NOW entries below, ≤15 lines each, newest first; close entry includes the
process-vs-code line self-audit.)

**2026-08-06 — WHOLE ATOM BUILT; SID-ACCEPTED.** Durable R1 reads are
read-only through `read-relations-for-targets`; visible-address filtering,
relation-id dedupe, D6 occurrence expansion, boundary anchors, cross-container
transport, straight/`:elbow/v1`/waypoint routes, heads, provenance dress,
post-T1 labels, tape/store/pick/GPU execution, and the flagged live join are in.
Frozen tripwires: material/route 15 tests · 92 assertions; isolated durable join
1 · 9; all GREEN. Frozen GPU cases: straight+label, waypoint elbow+both heads,
and exact human/LLM overlap; goldens/determinism 3/3, zoom parity 7/7, all
connector receipts true. `verify:render-engine` is RED only on the preserved
`candidate-pick-parity-failure`; connector assertion failures = none.
Forks: none. Strongest default: one representative addressed rect per visible
slot (largest bounds), while every cross-slot occurrence pair remains distinct.
Sid's felt pass found round-cap bleed; cap-radius compensation repaired it.
Self-audit: code atom is Sid-accepted and landed in `10dd8e5`; docs land separately.
Residue/LATER: label/path order + painted-contour anchors; optional dev release met the existing Electric login gate.

**2026-08-06 — A/B falsification round RAN; all repairs landed (accept
pending).** Arms: fresh Claude session (6 findings) + Codex session (10),
identical brief, both at Sid's hand. Nine defect groups, repaired in the
contract same day: pick re-ruled (predicate reads the live route cache;
container-spanning bounds; per-edge bbox → LATER) · label road made real (geo
cloned off the content system; family via `text-system-family`; T1-fence
owners gain `layout-label`) · anchor space = op-container == slot-container
(identity join container) · D6 occurrence expansion · composite revision
`[row-stamp dress-revision]` · proportionality receipt-proofed (transform
value-diff + bound-edges index + `:route-resolutions`) · IPC tripwire →
`connector_join_test` (isolation-exceptions) + MUST-NOT carve-out ·
provenance = `:asserter-type` classifier sets + `:actor-id` · `:elbow/v1`
pinned. **Role-vs-model:** Codex the stronger falsifier HERE — 10 findings, 4
unique groups, both internal contradictions; fresh Claude caught 2 groups
Codex missed (cljc/cljs test-lane physics · family hardcode). Keep both arms:
diversity, not redundancy. Expiry NOT fired (≫1). Sid: no more rounds — next
touch = the implementation prompt.
