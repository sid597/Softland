# Decision — the product-architecture span is signed

**Act.** Sid signs the converged product-architecture span: the twelve
clauses of `provenance/exchange-04-meta-syntheses.md` (synthesis 2) as
amended by `provenance/exchange-05-falsification.md` (amendments A1–A12 +
register additions). His word, verbatim, 2026-08-15, in the meta session
(https://claude.ai/code/session_0166nBBfmfToLbUigW1DKN5w): "yeah not needed
and i sign it now" — the first clause declining a further Codex confirmation
round over the amendments, the second signing the span.

**What the signature covers.**

- The amended span as the product architecture to build toward — design
  truth, not build truth: every existence status (exists / embryo /
  missing) and every build-gap receipt in exchange-05 remains honest and
  standing.
- The courts demotion (universal verb tier {escape, ask, go-to/reveal} plus
  the address-set noun; point and the custody courts as kernel machinery
  under the floor) — knowingly, non-kin-confirmed in exchange-05.
- The lens-gate position taken in exchange-05 §6: summon proceeds; a
  deposit through a failed lens lands dark-priced or is refused —
  availability and honesty composed, not traded.
- No further Codex round before this signature (Sid's word above).

**A12 placement.** Recorded as the meta session's default, Sid's veto
standing: A12 (the structural-guarantee meta-law — no clause calls a
guarantee structural without naming producer, transition owner, obligated
consumer, clearing event) takes its own constitution file as the thirteenth
entry, marked meta-law — following the essays' precedent of "no exits" as a
named meta-clause.

**What this decision does not do.** It does not touch the fenced plan docs
(reconciliation is a later act, where this decision also gets recorded in
settled ground); it does not set build priority; it does not close the
stamp-grade vs semantic-grade drawing fork.

Inputs: `provenance/exchange-00` … `exchange-05`; the two destination
essays. Next: the breakup session writes `constitution/` + `architecture/`
as the reading surface of this signed span.
