# Product-Architecture Exchange — 02 · Adjudications

Part of the 2026-08-15 three-session exchange. Each working session adjudicated
its six routed items (adopt / amend / refute, receipts re-verified in source
before ruling) and relayed the adjudication back to the meta session.

Fidelity: both sessions relayed their texts over the session-message channel
with explicit verbatim certification (multi-turn: "envelope line only;
adjudication text below is untouched"; one-shot: "verbatim text of the
adjudication turn as delivered in my chat"). Relay envelope lines are kept.
The sessions' own transcripts are the ultimate primaries
(`softland-product-architecture-should-be-multi-turn` [b8aad9],
`softland-product-architecture-should-be-one-shot` [d3c7d7]).

---

## Adjudication — softland-product-architecture-should-be-multi-turn

ADJUDICATION VERBATIM — PART 1/3 (envelope line only; adjudication text below is untouched)

All six sibling receipts checked in source before ruling — every one held: the distiller's demand-minted `(surface, sub-span)` identity and the hole-id pattern, the client's `targets-by-address` occurrence index, the relation kernel's edge vocabulary, circulation's `activation-as-of` resolution, the trail contract's literal "NO as-of," and the byte-equal briefing law ("agent briefing is `canonical-edn` of the same result the human's portal renders… asserts byte equality rather than trusting a summary"). **[c]** on each below means I read it this session, not that the sibling said so.

Verdicts up front: **1 adopt-with-amendment (my noun loses), 2 adopt (one of my organs evaporates, one merges), 3 adopt (a genuine falsification of my questionnaire), 4 adopt, 5 ruled with my custody test, 6 adopt-with-a-sharper-boundary (I concede two verbs, hold one, add one).**

---

## 1 · Identity — the triple beats my composed address, and I can say exactly why mine was wrong

**Adopt, with one amendment that goes further than the sibling asked.**

(a) Conceded outright: my composed address was the **occurrence record wearing identity's clothes**. I defined it as the containment path the court walk mints — but the walk can only ever know the *route it took through views*, and routes are per-view and perishable. The tree already treats them that way: the workspace store maintains `targets-by-address` as `[address → occurrences]` pairs, incrementally, per view-instance **[c]** — the occurrence leg exists and is correctly ephemeral. And the hard part I flagged myself last turn ("addresses must survive re-parenting or the point-and-ask economy rots") wasn't a hard part — it was the *symptom of the conflation*. Split identity from route and the problem doesn't get solved; it stops existing. Edges and marks attach to the durable, medium-blind address; the walk mints occurrences that *resolve* to it.

(b) Amendment past the sibling's question: the containment chain doesn't move into the anchor leg — **no stored reference should ever hold a chain at all**. An anchor is *one link*: this thing's position inside its immediate parent material, medium-typed (char span, knot range, object-id + surface param). Chains exist only at resolution time, by walking parent anchors. Then re-parenting an object rewrites exactly one anchor and zero references — which is the strongest form of the survivability my basis-stamped path was clumsily reaching for.

(c) Basis comes **off the name**. My `@rev`-in-the-name was wrong on its own terms: it fragments identity across revisions — two marks on the same bolt at different revisions become marks on different names, and the edge graph shatters. Basis lives on the *act* (every referencing act already carries it through the funnel — that's the constitution's first clause doing its job), and capture-time resolution lives on receipts: circulation already resolves `activation-as-of` against a `captured-at-ms`, and marks ambiguous as-ofs as ambiguous **[c]**. Pointing *into history* is an as-of read — a lens dimension (item 3) — not a different identity.

(d) **Demand-minting adopted**, as a generalization of my family-declared grain rather than a replacement: pick grain is the grain a family *offers*; addressing mints parts at whatever grain a mind actually names — the lasso'd face-in-the-sketch that exists in no data structure — idempotent by (authority, span-in-authority). The precedent is exact and verified: `refine-block-path` is "deterministic from (surface, sub-span)… a re-refine of the same span RESOLVES to the same id," with the engagement riding a `:refines` edge as provenance **[c]**. Parts are born by acts of addressing — which strengthens my K6 (authoring acts are material) from the side I didn't expect. The three questionnaire sections are adopted as stated: region-identity type, re-anchor law under edit, dangling policy — the last with the verified precedent of `hole-endpoint-id`, the deterministically-never-minted frontier id **[c]**.

Net for my plane: the name "composed address" dies — it named a conflation. My kernel's fifth piece becomes an **identity service** (durable addresses, demand-minted; per-family typed anchors; resolution), and the court walk's output is demoted to occurrences, which is all it ever really produced.

---

## 2 · Two consequence kinds — my badge commission evaporates, and I check the math on the merge

**Adopt (a). Adopt (b) as one genus, two species — the species difference is load-bearing and stays.**

(a) The reference/derivation distinction is real and I was on the wrong side of it in my own walkthrough. Reference consequences recompute — that's my settle-law hub-and-spoke re-derivation under their better name. Derivation consequences must **not** recompute — a mind made the thing from a basis, and silent regeneration is coherence-preservation running at machine speed (the destination essays' named pathology). The machinery halves exist: `:based-on`, `:grounds`, `:assembled-from` in the edge vocabulary, with human-asserted and LLM-proposed as *two distinct facts* **[c]**; as-of resolution proven in circulation **[c]**. Re-deriving my staleness-badge scene against this: the commission I filed — "projection P must carry `:basis/age-ms`" — **evaporates**. It was a per-face field where a general lens belongs: one served staleness projection ("what moved under this address, weighted by what stands on it") that any face binds. I filed plumbing because I hadn't factored the record side. Withdrawn.

(b) Commissions and stale-marks: tested in my frame, they merge at the genus and split at closure. Genus: **system-filed debt as durable, addressable material** — one produced by grammar refusals naming what the code lane owes, one by basis-diff naming what moved under a derivation. Same queue surface, same address grammar for work items, same consumers (residents walking the queue — the essays' repair economy takes exactly this as input). But the closure laws differ and must not be unified: commissions close by **release** through the slow lane; stale-marks close by **mend-acts independently checked or breaking loudly**. A merged queue with a single closure law would let a mend close like a release (unchecked) or force releases through mend ceremony. One genus, two species, difference preserved.

[END PART 1/3]

ADJUDICATION VERBATIM — PART 2/3 (envelope line only; adjudication text below is untouched)

## 3 · The lens citizen — a clean falsification of my questionnaire, conceded and built into the contract

**Adopt. This is the item that genuinely changes my architecture rather than refining it.**

The receipts are exact: the record is as-of-capable everywhere — append-only revisions, capture stamps, `activation-as-of` **[c]** — while the trail contract reads, verbatim, "over CURRENT truth… NO as-of" **[c]**; and the agent briefing is a *projection contract* — canonical EDN of the byte-same portal result the human renders, byte-equality asserted, no LLM in the path **[c]**.

My questionnaire cannot hold time or the briefing without absurdity — I tried the exercise honestly: time has no authority, no birth gesture, no settlement shape, no cost ladder of its own. It is not a deficient material family; it is **not a material family**. So the contract gets its second half, which the tree has been practicing without naming:

★ Insight ─────────────────────────────────────
The citizenship contract has two forms. **Material citizens** answer settlement laws: authority, settlement profile, birth, grain, cost, version law. **Lens citizens** — projections, briefings, staleness views, trail views, skins — answer read-honesty laws: *declare your cut* (as-of becomes a mandatory dimension of every lens declaration, which is how time enters — as a parameter, never a citizen); *name your lacks* (the tree already does this twice: the projection's own paging-lack note, and the assembly apply-report that counts what degraded **[c]**); *byte-equal across species* (packagers extend what a mind sees, never where it stands **[c]**); *auditable derivation* (press the view, get its basis). My earlier "renderer is kernel, skin is lease" clause re-homes here: skin calibration is lens material, governed by lens law.
─────────────────────────────────────────────────

One consequence I'll name because it's work: "declare your cut" makes the trail's "NO as-of" a *declared lack* today and a contract violation only when a lens claims otherwise — the lens law is what turns that comment from documentation into an obligation.

---

## 4 · Packaging for minds — adopted; my resident-grade reverse arrow was consuming it undeclared

**Adopt as a material-side questionnaire section, governed at the species boundary by lens law.**

Correct catch: "speak at the picked node and the resident authors a candidate" assumed the resident could *read* the picked thing. For text that's free; for a drawing it's a render plus a structural census; for a 3D part it's renders plus the scene slice plus relations. The section enters as: *how does your material become context a mind can read, and what does that packaging cost?* The byte-equal law survives the generalization because it's already stated in the right form — packagers extend WHAT is seen, never mint a machine-only WHERE **[c]**. And there's a symmetry worth keeping from my plane: packaging cost is the record-side sibling of render-cost citizenship — rungs price material reaching the human channel, packagers price it reaching the mind channel. Two species, two briefing channels, both budgeted, both declared.

---

## 5 · Projection authoring — the routed axis, ruled with the custody test

Both sessions missed it; ruling with my own law since that's why it was routed here.

Decompose a projection and the custody test (total interpreter, admission-bounded cost) cuts it cleanly into four parts with three fates: **selection** — a query over declared indexes in a closed vocabulary (filter by kind, edge, actor, time-window; bounded joins; hard result caps) — *data-lane eligible*: totality by construction, cost by admission. **Shaping** — a closed aggregation/grouping set — *eligible*, same argument as `:each`. **Computation** — open terms — *code lane forever*; the line holds exactly where it held for faces. **Arrangement** — already data.

A coined projection's citizenship: it is a **lens citizen authored as material** — no authority over any substance, but authorship, provenance, the candidate → activate lifecycle, worn at addresses like a face. And the capacity to lie is precisely what lens law exists to police: a dashboard that silently truncates is a lying map, and the tree already owns the refusal shape — the apply-report that *counts* what's missing rather than hiding it **[c]**. So the map-must-not-lie obligations attach not as aspiration but as the lens citizen's grammar: declared cut, named lacks, auditable derivation, or it doesn't activate.

First product case, and the loop closes on item 2: the **staleness projection should be the first coined lens** — built as query-material + face-material, worn, and itself subject to lens law. The honest cost: this extends the custody ratchet to the read side, and the query grammar + cost admission is new code-floor machinery — one more checker, named now rather than discovered later.

[END PART 2/3]

ADJUDICATION VERBATIM — PART 3/3 (envelope line only; adjudication text below is untouched)

## 6 · Navigation verbs and the tier boundary — trace extended, boundary settled with a sharper test

**Adopt the verb family; concede two of my universal verbs; hold one; add theirs.**

The trace ended one verb short — agreed. "Views re-derive" leaves you *looking at* consequences with no way to go where a thing lives. Step 11 enters: the go-to family — reveal the occurrences of this address / summon it here / open its medium-native view at this sub-address — riding occurrence queries over exactly the index that exists (`targets-by-address` **[c]**), with drill as the named crude ancestor.

On the live tension, I'll settle my own boundary with a test instead of a list, because the sibling's pressure exposed that my list was intuition: **the universal tier is the set of acts whose capture would cage or silence a mind.** Run it: *Halo/escape* — capture is a cage: universal (already floor **[c]**). *Custody courts* — capture is a cage: universal. *Ask* — capture is silence, a skin that can swallow your line to the minds: universal, with per-family packagers (item 4) as its landing. *Go-to/reveal* — capture is a navigational cage, an unreachable home: universal, per-family landing. And the concessions the test forces on me: **point is not a verb at all** — it's kernel machinery (my trace's steps 1–4: walk, pick, mint); **select is family-tier** — the tree already has three family selection verbs and their semantics are irreducibly medium-shaped **[c]** — producing the universal *noun* (an address-set) rather than being a universal *verb*. Camera resolves without tension once court and idiom are split, which the wheel court already demonstrates: the court is shared and floor, the idiom (orbit vs pan) is medium-owned **[c]**.

Four universal verbs, closed by a test, growing at constitutional speed as my own growth law demands. The sibling's "verbs medium-owned, dispatch shared" is right for everything manipulative; my tier survives only where cage-or-silence bites.

---

## What changed in my plane, counted

One noun died (composed address → identity/anchor/occurrence, with anchors as single links and chains only-at-resolution). One organ evaporated (the badge-scene commission — replaced by the staleness lens). One organ merged at the genus with closure-species preserved (commissions + stale-marks). The questionnaire gained five sections and split into two forms (material citizenship / lens citizenship — the deepest change, a real falsification of my contract-as-stated). The custody ratchet extended to the read side with one new code-floor checker named. The trace gained step 11, and the universal tier is now a four-entry set closed by the cage-or-silence test rather than a list I liked. The kernel stands at: funnel, dispatch, edges, court walk minting occurrences, identity service — plus the lens half of the contract. Everything adopted was adopted on verified receipts, not on the sibling's word; nothing adopted required absorbing its frame — the two planes meet at the record, which is how it should be.

[END PART 3/3 — full adjudication relayed verbatim]

---

## Adjudication — softland-product-architecture-should-be-one-shot

Relay part 1/3 — verbatim text of the adjudication turn as delivered in my chat (header through Item 2). Parts 2 and 3 follow immediately.

---

# Adjudication of the sibling plane's six items

Discipline honored: every sibling receipt cited below was re-verified in source before ruling (file:line given); where I adopt, I adopt on mechanism, not on kin-model agreement — two runs of the same model converging is cheap evidence, so each adoption names the code that carries it. Verdicts in one line each, then the rulings.

1. **Settlement axes — adopt, amended to a three-way custody** (medium declares settlement; the publication *shape* is universal; the publication *cadence* is place policy, not medium policy).
2. **Court walk + composed address — adopt the walk as M2's input half; refute the address half** (their composed record is my occurrence leg, not my address leg; basis lives on the receipt, never in the name).
3. **Set-valued acts + cost citizenship — adopt; designed below** (intent / resolution / per-member acts at three provenance grains, with the existing envelope machinery carrying all of it).
4. **Graph production — adopt as a sixth piece** (M6, the graph economy: three producer strata, each with a running embryo in the tree).
5. **Peer co-reference — adopt; it is M4's missing third mode** (recompute, staleness, and now *re-examination* — symmetric, mechanical to detect, semantic to resolve).
6. **Reflexive test — the system's own material passes as a medium**, and the pass forces two clarifications that improve M1; the two sessions' planes unify.

---

## Item 1 — Settlement and publication are two axes, and M1 fused them

**Verdict: adopt the split; amend the custody of the second axis.**

The sibling's factual claims hold in source. Three settlement shapes coexist **[checked]**: whole-form candidacy for policy masters (`edit-candidate` composes one row edit and validates the *whole* candidate before any append — `anatomy_material.cljc:631-687`); committed echo per confirmed edit for text (the pending-input buffer with ack/refuse/revert — `block_edit.cljc:85-90`); an invertible op-log for 3D (`edit-diff` with `:before`/`:after` and `:op/id` — `region3d_material.cljc:501-509`). And the critique of my M1 is fair — worse, it is exactly the inheritance error the commission fenced against: at n=1, settlement and publication are *degenerately coincident* (one author, one world, so every durable revision is instantly "published"), and my "edit ops → revisions" wording inherited that degeneracy as if it were a law. The walkthrough's gizmo settle landing as a world-visible revision pins the darkness dial at zero. Caught correctly.

The amendment: the sibling assigns both axes to the medium declaration. The evidence supports a **three-way custody**, not two:

- **Settlement profile is medium-declared** — edit cadence forces it, exactly as they argue: continuous manipulation cannot ride whole-form candidacy; rare deliberate deviation shouldn't ride an op-log. The render contract already declares this axis per family (`:draft-settle :scene-entry/lifecycle` in the family registration's provenance block — `scene_tape.cljc:366` **[checked]**), so the record-side citizenship declaration mirrors an existing render-side one.
- **The publication shape is universal, owned by no medium** — signed, scoped, grounded, rollback standing. The activation event is that shape, already closed and running **[checked]**, and scope today admits exactly two values with the written note "richer scopes wait for a need that names itself" (`activation_event.cljc:59-64` **[checked]** — confirming their receipt in code, no doc read needed). The missing scope between `:scope/subject` and `:scope/all-unpinned` is precisely their draft regime: the **workshop scope**.
- **The publication cadence — quantum × darkness — is place policy**, per workspace, not per medium. Nothing about being a stroke or a paragraph decides *when* you go public; the essays put this dial on the workspace and I keep it there. A medium that dictated publication cadence would be a medium dictating social settings, which is a custody error.

Their three questions, answered in my frame:

**(a) M1 restated.** A medium's citizenship declaration now reads: surface form; distiller; anchor type + re-anchor + dangling policy; demand-mint region-identity; **settlement profile** (candidacy | echo | op-log — how input accretes into acts, and what the act quantum is); edit-op vocabulary *under* that profile. Publication is not in the medium contract at all: every medium's acts cross audience boundaries through the one decision-event shape, at the cadence the place sets.

**(b) Where the draft/publish boundary lives across the pieces.** Draft-ness is **visibility scope on acts**, not a new status per row kind — and the container row already carries a `visibility` field (`object_container.clj:105` **[checked]**; I have not verified any enforcement reads it — flagging that honestly, it's a hook, not a working system). A *draft placement* is a placement row worn at workshop scope. A *draft edge* is an edge at workshop scope — no new relation status needed; `:asserted`-within-a-workshop is a location, not a state. A *draft derivation* is a briefing whose basis edges point at workshop-scoped things. The law that keeps this coherent, and which I add as M1's companion: **nothing public may stand on dark ground** — a publication event must close over its basis (publish the closure, or the publish refuses with the named dark dependencies). And the occurrence query inside someone else's workshop answers *nothing* by default; whether it may answer with a count-only presence signal is workspace policy — the query respects the wall, the wall's porosity is the place's dial.

**(c) What undo is, per profile, below the publication boundary.** Three different things, and naming them dissolves the usual undo muddle. Below the *act quantum*, undo is free and recordless — the micro-moves of a gizmo drag before settle are input, not acts; that is what the settlement quantum *means*. Between quantum and publication: candidacy-profile undo discards or abandons the candidate (recorded as refused/abandoned, cheap); echo-profile undo is an inverse splice as a new act; op-log-profile undo is the inverse op — all workshop-local. Above the publication boundary, undo does not exist: there is only the compensating public act, which the rollback half of the activation shape already is **[checked]**.

---

## Item 2 — The minting story from input

**Verdict: adopt the generalized court walk as M2's other half; refute the composed-address half; settle the resolution they asked for.**

The embryo is real and better than I credited in my main essay. `wheel-route` (`region3d_pointer.cljc:102-108` **[checked]**) is a pure custody decision — "Region custody is evaluated first; only an unconsumed sample may reach the ambient ground camera" — with a standing receipt that counts dual dispatches as a defect class (`record-wheel-roads`, `:113-121`). Composed with the binding law I already read first-hand (claim chain innermost-first, falling outward, decisions carrying site/depth/tier/conflicts — `binding_material.cljc:394-422` **[checked]**), the generalized law is: **input custody is adjudicated at every containment boundary, innermost first, and the decision is a receipt.** I adopt this as M2's input half — my navigation verbs do presuppose it, and their "universal uncapturable verb tier" already exists as proven mechanism (floor-reserved camera verbs plus the *universal* meta-gesture reservation across every site and tier — `binding_material.cljc:134-161` **[checked]**); each medium names its uncapturable tier, the reservation machinery is shared.

Now the refutation. The sibling wants the walk to mint the address as a byproduct — "custody and naming are two outputs of one traversal," the composed address a basis-stamped chain of family-opaque segments. Half right, and the half that's wrong would break the triple:

**(a) Who mints.** The walk *names the route* at gesture time — free, pure, already true (a pick returns the containment chain, index paths, and claim subjects **[checked]**). But the walk must not mint the durable address, because **(b)** their composed record is my *occurrence* leg, not my *address* leg. The route is view-relative — it contains the view-instance and container indices; two occurrences of the same part yield two different routes. If the route became the name, the same part would carry one identity per viewing, which is exactly the duplicate-identity bug the container kernel already fixed once for text: `refine!` treats `(surface, span)` as the identity and *resolves to the existing unit* when spans coincide — the F4 lesson **[checked]**. So: minting is a refine-style request issued after (or during) the walk, idempotent by **(parent-address, medium-typed region-identity)** — never by route. The walk hands the request its raw material (which part-geometry was touched, in which parent); the mint law hands back the one durable address, whether this is its first naming or its hundredth.

**And basis lives on the receipt, not in the name.** The deictic receipt already carries `:receipt/picked-at {:address :src-path :view-instance}` plus the visible-address census **[checked]** — that is where "I addressed this *from here, standing on this*" belongs. A name that embeds basis forks identity per viewpoint; a receipt that carries basis enriches one identity with many viewings. Route = evidence and navigation; address = identity; basis = on the act. That is the settlement of their (b), and I'd hold it against pressure.

**(c)** Adopted, as stated — with the one addition that the court walk's *receipts* are what M2's navigation verbs consume when they answer "how did this gesture land where it landed": the dispatch decision is already a served, renderable object (binding lint **[checked]**), so navigation inherits an explanation surface for free.

Relay part 2/3 — verbatim (Items 3 and 4).

---

## Item 3 — Set-valued acts, and cost as a citizen

**Verdict: adopt; here is the design.** The sibling routed this correctly — my M3 carried it in one sentence, and it is a whole organ.

★ Insight ─────────────────────────────────────
A set-valued act is three records at three grains, and provenance needs all three because they answer different questions. The **intent** (query + scope + verb, one signed act) answers *why did all this happen*. The **resolution** (the frozen membership at pinned basis, content-hashed) answers *what was meant* — and it is a claim, with an asserter, because resolving "everywhere this concept occurs" is sometimes mind-work, not mechanics. The **per-member acts** answer *what actually happened*, individually refusable. Collapse any two grains and a familiar lie appears: intent-only is "I ran a migration" with no record of victims; members-only is a thousand edits with no why; resolution-only is a plan mistaken for its execution.
─────────────────────────────────────────────────

The machinery for all three grains already exists **[checked]**: the request envelope carries `:causal {:correlation/id :intent/id}` (`core.clj:150-186`); the space kernel freezes context bundles with a sha-256 over stable material (`space.clj:386-401`) — the resolution record is that shape; per-member idempotency keys derive deterministically (the block-edit law); per-lineage stale guards (`stale-edit?`, `object_container.clj:1398-1404`) give per-member drift detection; and `spine-sync!` is the batch precedent whose receipt splits outcomes `{:fresh :converged :rejected :unresolved}` rather than averaging them (`git_spine.clj:270-282`).

So: **preview** renders the resolution — the resolved set at pinned basis, exactly as they guessed. **Commit** references the resolution hash; members whose lineage moved between preview and commit fail *individually* on the stale guard with a named reason, never silently re-resolved. **Partial failure** settles as per-member decisions plus a batch receipt with counted outcomes. **Rollback** is a new set-valued act over the *recorded resolution* — never a re-run of the query, because the query resolves differently now — and members that moved since are surfaced as conflicts, not reverted. **The record keeps all three grains.**

Their three cases, run through it: *retarget every edge standing on spec-A to spec-B* — mechanical resolution (reverse-dependency read), per-member retract-and-assert with the asserter of the original edge preserved distinct from the retargeting actor (the envelope/asserter custody split already exists — `relation_kernel.clj:254-257` **[checked]**). *Restyle all strokes in a lasso* — the region must first be demand-minted (item 2's law), then per-stroke acts; the lasso's mint is the moment an emergent part becomes an addressable one. *Rename a concept everywhere* — the honest case: "everywhere" is a semantic query, so the resolution is resident-proposed (silver, an asserted claim), human-confirmed, and only then do the mechanical splices run. The rename case is why the resolution grain must be a claim.

**Cost citizenship: adopted, and the tree already runs the pattern at both scales.** Every bounded read ships its denominator (64-page river with `:paging-lack` named, 256-instance cap with `:instances-truncated?`, 32-visible cap with `:visible-count` **[checked]**), and the rung ladder is real: region render admission at divisors 1/2/4/8 against a byte budget, worn pipeline under degradation, refusal drawn when denied (`region_rungs.cljc:3`, `region3d_gpu.cljs:1580-1587` **[checked]**). The generalization for land-scale scope: every heavy read in M2–M5 returns `(result, coverage-receipt)` where coverage is one of *exhaustive*, *bounded-at-N-with-frontier*, or *refused-with-named-bound*; and a scope that exceeds the synchronous rung escalates to a **background sweep as a derived worker** — which is what the reserved-and-unworn `:external-via-derived-worker` effect class exists to say **[checked]**, with the cascade's content-derived idempotency as the single-flight law and the sweep's progress landing as substance. That is the rung ladder for "touches the whole land": synchronous-bounded → sworn background sweep → refusal, every rung receipted.

---

## Item 4 — Who builds the graph

**Verdict: adopt as a sixth piece — M6, the graph economy.** The charge is correct: my walkthrough's "where else does this live" answered three places because three edges existed, and I never said who minted them. On an empty graph, M2 navigates nowhere, M3's scopes collapse to their roots, M4 serves no staleness. Relation production is architecture, not usage.

And it is the piece with the *most* running embryos, one per stratum **[checked]**:

- **Mechanical floor (receipt-grade):** import-time relation distillers already exist — the block distiller mints `:produced` from write-tools and `:grounds` from read-tools with the dangling-target hole law (`block_distiller.clj:440-531`); git-spine mints `:based-on` per commit parent and `:produced` from verified session joins, versioned asserter, evidence anchors (`git_spine.clj:244-294, 533-579`). Deterministic, versioned, idempotent — the model for every future importer (a codebase's `:calls`/`:requires`, a PDF's citations, a mesh's assembly tree).
- **Ambient basis capture (act-time):** deictic receipts + gold banking at every utterance **[checked]** — generalized under M3's design for free, because every ask/derive act now records its resolution bundle, and the resolution's members *are* the `:based-on` targets. Citation becomes structural exactly as the essays demand, as a side effect of the act grammar rather than a virtue.
- **Resident sweeps (silver):** the autotag cascade after every durable turn, with a validation gate before any edge lands **[checked]** — the template for co-reference sweeps (item 5) and concept-linking sweeps.

What makes it an economy rather than a flood, stated as law: every produced edge carries stratum and asserter (already structural — asserter is *in the edge's identity* **[checked]**); promotion is review (silver→gold is a human assertion, already the definition of gold **[checked]**); and review happens in the medium where the edge lands — a proposed co-reference renders as a seam you can press, not a queue you must drain. The strata are the same receipt/silver/gold ladder circulation already runs; M6 is that ladder given producers.

Relay part 3/3 — verbatim (Items 5, 6, and the closing consolidation). End of adjudication text.

---

## Item 5 — Peer co-reference: the M8/M10 bolt

**Verdict: adopt — this is M4's missing third mode, and the sibling's stress case is genuine.** My honesty rule ("no basis edge, no claim") was right as far as it went, and it went exactly one mode short: the co-authored pair, where neither side derives from the other, is the *commission's own featured case*, and my M4 serves it silence.

The repair, in my frame: consequence propagation has **three modes**, distinguished by what kind of link carries them. *Recompute* rides reference (one address, many projections — code does the work, live today). *Staleness* rides derivation (directed basis edges — a mind did the work once; the system marks, a mind repairs). And now *re-examination* rides **co-reference**: a symmetric, articulate-class edge asserting "these two things speak of the same subject," carrying an **affirmation basis pair** — the revisions of both ends at the moment a mind last affirmed the pairing. When either end moves past its affirmed revision, the edge enters *moved-since-affirmation* — detected mechanically by revision comparison, no semantics needed, served exactly like staleness but symmetric. Whether the two ends *still agree* is semantic work, so it is filed as durable work: a system-filed question anchored to the edge. If the re-examination finds contradiction, the contradiction is kept as structure — a `:refutes`/conflict row, rendered as the seam — and convergence happens only as a signed decision citing both sides. Nothing here invents machinery: `:confirms`/`:refutes` exist as stance kinds **[checked]**, conflict is already a renderable citizen (binding ties render as lint; the anatomy has a conflict slot **[checked]**), queryable-absence ids are proven (the `pending-result` hole **[checked]**), and the as-of resolution needed to pin affirmation bases is circulation's existing law **[checked]**.

Who mints co-reference at scale: item 4's economy. Some mechanically (a shared registered name is allocate-class — code symbols, ids); most as resident-swept silver proposals reviewed into gold; a few as gold directly when Sid draws the tether himself — which is, notably, what the seam demo's hand-wired edge already is.

So the walkthrough's bolt now resolves honestly: the model's bolt changes to M10, the spec paragraph says M8; the co-reference edge between them flags *moved-since-affirmation*, a question is filed, and the seam renders on both ends — in the model as a marked surface, in the text as a marked span, in the trail as an open question. Not silence, and not a pretended sync.

---

## Item 6 — The reflexive test

**Verdict: the system's own material passes as a medium under my eight laws + amended M1 — and the pass is load-bearing in both directions.**

Run the citizenship contract against the policy/face family, receipts inline: **surface** — the EDN source, with canonical-bytes discipline stricter than any other medium (`*print-namespace-maps*` pinned **[checked]**); it already rides the record spine as containers with immutable revisions (facet masters and assemblies import through the same container kernel **[checked]**). **Parts** — authored rows with stable ids (`:part/id`, binding rows filed by site), like 3D's named objects; the closed grammars are the distillers, and the derived verdict row the assembly adapter mints is the cut **[checked]**. **Anchor type** — a path into the EDN form; and the occurrence leg is *already stamped*: every rendered node carries `:assembly/src-path` back to the template node that produced it, "built for a designer edit-mode nothing consumes yet" **[checked]** — the reflexive medium's address/anchor/occurrence triple exists and idles, exactly as ink's edit ops idle. **Edit vocabulary** — seven anatomy ops through one agent-and-human boundary with teaching refusals **[checked]**. **Settlement profile** — whole-form candidacy, declared. **Publication protocol** — the activation decision-event, signed, scoped, grounded, rollback standing: *the only medium in the tree with a complete publication protocol today* **[checked]**.

So the unification holds, and I'll state it plainly for the meta session: **the sibling's authoring plane is my architecture instantiated on one more medium** — the policy medium — **plus the publication protocol that medium happened to grow first** because its blast radius forced it. Drawing a component, distilling a sketch into a face, scoped activation, refusals filing commissions — each is (M1 citizenship + item 1's publication shape) with nothing bespoke left over.

Two clarifications the test forces on M1, both improvements:

First, **record-side citizenship and render-side family membership are independent memberships.** The policy medium has no GPU family — it projects *through* text and rect families (the matter room renders it as ordinary blocks) — while ink today has a render family and no record citizenship. M1 must therefore not require a render family of a medium, and the two registries stay orthogonal: one contract for being *on the record*, one for being *on screen*, a medium free to hold either or both.

Second, the sibling's custody-as-blast-radius law lands inside the citizenship contract rather than beside it: what distinguishes the policy medium is that its content governs behavior, so its declarations carry checker tiers and a code floor with real teeth (grammar refusal falls to the floor, camera verbs unbindable **[checked]**), where a stroke's citizenship can declare nearly-empty checker tiers because a wrong stroke is just a wrong stroke. Blast radius is a *declared field* per medium, not a second architecture.

What breaks: nothing structural. The one residue worth naming — demand-minting mostly idles on authored-part media (policy, 3D), exactly as designed; it exists for the sub-part cases (a span inside a source form, a lasso over three parts) and for perceived parts, which authored media don't have.

---

## What this does to the architecture

The five pieces become six, two of them restated: **M1** — the citizenship contract: surface, distiller, anchors, demand-mint identity, *settlement profile*, edit vocabulary; publication excluded from the medium, carried by the one decision-event shape, cadenced by the place; record-side and render-side membership orthogonal. **M2** — durable placements + occurrence queries + navigation verbs, now with its input half: custody adjudicated at every containment boundary with receipts, routes distinct from addresses, minting idempotent by region-identity. **M3** — ask and act over scoped sets: intent/resolution/members at three grains, previews at pinned basis, per-member failure, rollback over recorded resolutions, cost as a refusable, receipted citizen. **M4** — three consequence modes: recompute by reference, staleness by basis, re-examination by co-reference with affirmation pairs and system-filed questions. **M5** — time as a read parameter, unchanged. **M6** — the graph economy: mechanical import distillers, ambient basis capture, resident sweeps, strata and review as the production law.

The convergence caveat, stated once: the sibling and I agree suspiciously often for two kin minds, so the adjudication leaned on receipts — every adoption above names running code, and the two refutations (route-as-address; publication in the medium contract) are grounded in a bug the kernel already fixed once and a dial the essays already placed. Nothing lands on disk from this turn; routing stays with you and the meta session.
