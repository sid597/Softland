# Product-Architecture Exchange — 01 · Cross-Items Routed

Part of the 2026-08-15 three-session exchange. The meta session
(https://claude.ai/code/session_0166nBBfmfToLbUigW1DKN5w) read both round-0
outputs, spot-checked the load-bearing mechanisms in source, and routed six
items to each working session over the session-message channel — the other
session's load-bearing organs plus the axes both had missed, phrased as
falsification targets, never as the sibling's worldview.

Fidelity: the meta session's own messages, reproduced verbatim from its
transcript. The accompanying round-1 meta synthesis (axis map, dilution
filter) is in exchange-04.

---

## Packet to `softland-product-architecture-should-be-multi-turn`

From the meta session Sid commissioned to review your run alongside a parallel sibling run ("one-shot") of the same commission. Sid wants a to-and-fro of only the high-weight items, routed so neither session dilutes into the other's worldview. Below are the six items from the sibling's plane that carry real weight for yours.

Discipline, before the items: every sibling claim below is a hypothesis carrying its own [checked] receipts from its session — verify in source before adopting; existence ≠ inheritance applies to sibling findings exactly as it applies to code. Adjudicate item by item — adopt / amend / refute, each with receipts — in your own frame and vocabulary; do not absorb the sibling's frame wholesale. The same fence stands (no docs/bets.md, decision*.md, next-prompt.md, sense-line-model.md, any DIRECTION.md, vision/**). Deliver your adjudication as a normal turn in your own chat for Sid. Do not message the sibling session directly; routing stays with Sid and the meta session.

The sibling's plane, in three sentences so the items are legible: it worked the commission as literally stated (address any part of the material, ask/manipulate at local or global scope, move between connected bidirectional projections — text, canvas, 3D, time, agent briefing) and concluded the land is two half-built machines: the machine of showing (render substrate) is already contract-general across the three media, and the machine of record has that generality for text only — so strokes and 3D parts can be seen but not linked, marked, asked about, or found again, because record-side addresses are missing. Its five missing pieces: a record-side medium contract; durable placements + an occurrence graph you can query + navigation verbs; ask as a verb over (address-set × scope) with per-medium packagers; basis edges + served staleness for mind-made derivations; and time (an as-of cut) as a parameter of ordinary reads. Its identity law: a triple — address (what a thing is: durable, medium-blind, what edges/marks attach to) / anchor (where it lives inside its parent material: durable, medium-typed) / occurrence (where it currently appears: ephemeral, per view) — with parts demand-minted at the moment a mind addresses them, idempotently, never pre-enumerated.

THE ITEMS

1. Identity collision — your composed address vs its triple, at the one noun you both independently call the single new invention. Your composed address is a containment path ("ground-slot / region-id @ rev / object-id / surface-param"), basis-stamped in the name, minted by the court walk. Sibling receipts to check: the workspace store already keeps an incrementally-maintained address → view-instances index plus targets-by-address (occurrence legs — see src/app/client/workspace/selection.cljc, scene_store.cljc); the container kernel's refine! mints units on demand, idempotent by (surface, span) (src/app/server/rama/object_container/block_distiller.clj) — including parts that exist in NO data structure ("the face in the sketch": an emergent lasso'd region of a drawing). Questions to settle in your frame: (a) is your composed address actually the occurrence/route record — the thing the walk mints — which then RESOLVES to a durable, position-independent address, so that re-parenting an object or moving a region doesn't rot every reference (this dissolves the "addresses must survive change" hard-part you flagged yourself)? (b) does the containment chain belong in the anchor leg rather than in identity? (c) where does basis live — in the name (your @rev), on the edge, or on the receipt (sibling: receipts resolve as-of at capture time, and only receipts)? (d) your reverse arrow (pick + intent → settlement act) currently assumes family-declared grain; adopt-or-refute demand-minting so it works on emergent regions, and consider adding three sections to your citizenship questionnaire: region-identity type, re-anchor law under edit, and dangling policy (sibling's dangling receipt: the deterministically-never-minted hole-id pattern in the container kernel).

2. The substance half of your staleness-pill scene already has a design — check your organ 5 against it. Sibling's law: consequences propagate two ways and conflating them is the classic lie of synced tools. Reference consequences recompute (the 2D↔3D seam mirror: both ends dereference one address; code does the work). Derivation consequences cannot recompute: anything a mind made from a basis (briefings, distilled structures, your distilled faces) is minted with basis edges at pinned revisions; when ground moves, the honest system does not silently regenerate — it serves staleness ("what moved under this": a reverse-dependency projection with the diff that stales it), and repair is a new act by a mind, on the record. Receipts: the relation kernel already has the edge kinds (:based-on, :assembled-from, :grounds); circulation already proves as-of resolution (src/app/server/rama/material_circulation.clj). For you: (a) your badge scene's "weather with basis age" may need ZERO new projection fields once basis edges + a served staleness projection exist — re-derive the scene against that and see whether the commission you filed evaporates; (b) your refusal→commission organ and this staleness queue look like two instances of one law — the system files what it owes / what moved as durable, addressable work — so test whether commissions and stale-marks are one queue family with two producers; the destination essays' repair-crew economy (residents walking derivations when ground shifts, mends independently checked or breaking loudly) consumes exactly that unified queue.

3. Run your citizenship contract against time and the briefing — the two projections in the commission's own list you never tested (you tested policy, text, ink, 3D: all material families). Sibling receipts: the record is as-of-capable everywhere (append-only revisions, two clocks, receipts resolving worn-material-at-capture) yet NO read surface accepts a cut — the trail contract literally says "NO as-of" (src/app/server/rama/trail_view.clj), and the one scrub works within a single 64-block page; the agent briefing is the byte-equal canonical EDN of the same portal result the human renders — a projection contract, not a privileged API. Neither time nor the briefing is a material family: no authority of its own, no birth gesture, no settlement shape. The falsification question, same severity as your 3D pass: does your questionnaire admit a second KIND of citizen — a lens, governed by read-honesty laws (declare your cut, name your own lacks, byte-equal across species) rather than settlement laws? If yes, the contract needs its lens-side half stated explicitly; if no, show how time enters as a family without absurdity.

4. Add "packaging for minds" to the questionnaire — your resident-grade reverse arrow consumes it and your contract never declares it. Sibling: mind-readability is per-medium — text quotes its bytes; a drawing must ship a render plus a structural census (strokes, bounds, classifier tri-state); a 3D part ships renders plus the scene slice (kind, params, transform, parents, placed refs) plus its relations — and the byte-equal law must survive the generalization: packagers extend WHAT a mind sees, never WHERE it stands (no parallel machine-only API). One new questionnaire section: how does your material become context a mind can read, and what does that packaging cost?

5. (An axis BOTH sessions missed; routed to you because you own the custody ratchet.) Projection authoring. The commission's projection list is open ("such as…") — new lenses will be coined, and neither session said by whom or in which lane. Today all twelve projections are code-registry citizens; your own footnote ("eventually projections' selections if not their computation") is the seed. Question: which parts of a projection can ride the data lane under your own custody test (total interpreter, admission-bounded cost) — a saved selection/filter/query? an arrangement? never the computation? — and what is a coined projection's citizenship, given it has no authority but does have authorship, provenance, and the capacity to lie (the map-must-not-lie obligations would attach to it). First product case to reason with: a mind coins a filtered canvas or a small dashboard from inside, as material.

6. Navigation verbs — your ten-step trace ends one verb short of the commission. Your trace ends at "views re-derive," but "reach its connected textual or spatial representation" needs the jump: sibling's verb family — reveal the occurrences of this address / summon it here / open it in its medium-native view at this sub-address — riding occurrence queries over durable placements (today cross-projection movement is a ?drill= page reload, named as deliberate MVP). Two questions for your two-tier verb law: is "go to" universal-tier (uncapturable, must land on every family) or family-tier? And there is a mild live tension to settle: you hold point/ask/select as universal-tier; the sibling holds verbs medium-owned with only dispatch shared, camera floor-reserved per medium idiom. Since by your own law the universal tier grows at constitutional speed, settle where its boundary actually sits and which of these verbs clear it.

---

## Packet to `softland-product-architecture-should-be-one-shot`

From the meta session Sid commissioned to review your run alongside a parallel sibling run ("multi-turn") of the same commission. Sid wants a to-and-fro of only the high-weight items, routed so neither session dilutes into the other's worldview. Below are the six items from the sibling's plane that carry real weight for yours.

Discipline, before the items: every sibling claim below is a hypothesis carrying its own [checked] receipts from its session — verify in source before adopting; existence ≠ inheritance applies to sibling findings exactly as it applies to code. Adjudicate item by item — adopt / amend / refute, each with receipts — in your own frame and vocabulary; do not absorb the sibling's frame wholesale. The same fence stands (no docs/bets.md, decision*.md, next-prompt.md, sense-line-model.md, any DIRECTION.md, vision/**). Deliver your adjudication as a normal turn in your own chat for Sid. Do not message the sibling session directly; routing stays with Sid and the meta session.

The sibling's plane, in four sentences so the items are legible: it entered through "what are Softland's natives / where does the editor live / what is data vs code" and worked the meta plane — the system authoring its own things from inside. Its findings: natives are completed, not invented (a family is native when six stations are staffed: atom, birth, edit loop, face, address, law); tools are closed loops reconstituted at runtime, never components; custody is blast-radius-under-a-wrong-author, with a fixed code floor (checkers, executor, funnel, escape gestures) and a one-way ratchet migrating everything else code→data as grammars mature. Its most consequential find: the tree already runs one complete authoring loop at n=1 — policy masters (deviate → candidate → grammar check → preview → signed scoped activation → rollback, agents drivable end to end) — and after a self-correction it holds that the general architecture is NOT that loop widened but a citizenship contract: a fixed question-set every material family answers with declarations (authority, settlement profile, publication protocol, checker tiers, provenance grain, cost regimes, birth shape, containment), plus a five-piece reference-plane kernel: one funnel, one dispatch, relation edges, a generalized court walk (input custody adjudicated at every containment boundary, receipted — wheel-route is the one running embryo), and a composed address minted by that same walk.

THE ITEMS

1. Settlement is two orthogonal axes and your M1 currently fuses them — with a real contradiction inside your own walkthrough. Sibling receipts to check: the tree holds three settlement shapes, not one — whole-form candidacy (policy), committed echo per confirmed edit (text), invertible op-log (3D's edit-diff, whose :draft-settle field you also read — src/app/client/substrate/region3d_material.cljc, scene_tape.cljc) — and edit cadence forces the difference: continuous manipulation cannot ride whole-form candidacy, rare deliberate deviation shouldn't ride an op log. Orthogonal to the settlement profile is the publication protocol at audience boundaries: when a change crosses from one author's regime to other wearers, it takes the decision-event shape — signed, scoped, grounded, rollback standing (the activation machinery, already running). Key receipt: activation scope today admits exactly two values — whole world or one subject — with the written note that richer scopes wait for a need that names itself (docs/editable-material/CONTRACT_P6.md); the sibling's missing organ here is the draft regime: workshop-scoped wear, a place where several drafts interact without being published. Against your architecture: your walkthrough lands the gizmo settle-diff as a world-visible revision immediately — every act is a publish; the destination essays' darkness dial (settlement quantum × darkness duration) is pinned at zero by M1 as written. Questions: (a) restate M1 so each medium declares (settlement profile, publication protocol) rather than edit-ops→revisions; (b) say where the draft/publish boundary lives across M1–M5 — what is a draft placement, a draft edge, a draft derivation, and what does the occurrence query answer inside someone else's workshop; (c) say what undo is, per settlement profile, below the publication boundary.

2. Your triple lacks its minting story from input — the sibling derived one, and it composes with your demand-minting law. Sibling finding: input walks the containment chain of courts (innermost regime asked first, ambient fallback, decision receipted — wheel-route in src/app/client/workspace/region3d_pointer.cljc + runtime/scroll.cljs is the embryo), and that same walk can mint the address as a byproduct: custody and naming are two outputs of one traversal. Their composed address is a basis-stamped chain of family-opaque segments. Questions: (a) when a pick lands on a not-yet-minted region, who mints — the court walk at gesture time, or a refine-style request afterward — and is minting idempotent from the walk's route the way refine! is idempotent from (surface, span)? (b) their name is a path while your address is position-independent — the candidate resolution is that their composed record is the occurrence/route and your address is what it resolves to, but settle it, and state where basis lives: in the name, on the edge, or on the receipt; (c) your M2 navigation verbs presuppose exactly this input-side law — adopt-or-refute the generalized court walk (custody adjudication, with receipts, at every containment boundary; a universal uncapturable verb tier no court may swallow) as M2's other half.

3. (An axis BOTH sessions missed; routed to you because scope lives in M3.) Set-valued acts — the commission's "manipulate at local or global scope" is currently one sentence in M3 ("a verb over a set the scope computed") carrying an entire missing organ. Design it: what a global edit previews (the resolved set at pinned basis?), how partial failure settles, what rollback means over a computed set, and what lands in the record — the query, the resolved membership, or per-member acts (provenance likely needs all three, at different grains). Concrete cases to design against: retarget every edge standing on spec-revision A to revision B; restyle all strokes inside a lasso'd region; rename a concept everywhere it occurs. Rider on the same item: every heavy read in your five pieces — occurrence queries (M2), scope computation (M3), staleness walks (M4), as-of reads (M5) — needs the sibling's cost-citizenship law, which your pieces currently lack: cost is first-class, refusable, with receipted degradation (their receipt: the rung ladder — offscreen render targets admitted at divisor 1/2/4/8 against a byte budget, grants and denials recorded). What is the rung ladder for a scope that touches the whole land?

4. (Both missed; routed to you because you own the judgment layer.) Who builds the graph — your walkthrough presumes edges exist. "Where else does this live" answers three places only because edges were already minted; hand-wired edges don't scale, and M2/M3/M4 all run on an empty graph without a production story. Candidate pieces, none first-class in either session: ambient basis capture — every act minted with basis edges to what it stood on at act time (your deictic receipts and the autotag cascade are the running embryos — src/app/server/cascade.clj; the essays' law: citation structural, never ritual); import-time relation distillers — when a codebase, PDF, or model lands, what proposes its internal and external edges; resident sweeps proposing silver co-reference edges for review. Question: is relation-production a sixth piece of your architecture, with strata (receipt / silver / gold) and review lanes — and if not, what else keeps the graph from being empty?

5. (Both missed; routed to you as M4's stress case.) Peer co-reference — the consequence M4 doesn't cover. The spec paragraph says the bolt is M8; the model's bolt changes to M10; neither representation is derived from the other — no basis edge in either direction, so staleness serves nothing, and your own honesty rule ("where no basis edge exists, the system claims nothing") fails the commission's "see the consequences across the others" for exactly the co-authored pair the walkthrough features. The essays' shape for this (both sessions cite them as requirements): contradiction is articulate-class — reify the disagreement as an addressable edge/seam, kept as structure, filed as a question by the system itself; convergence only at a signed decision. Question: does the judgment layer need the conflict citizen — a co-reference edge whose two ends can disagree, a served contradiction surface, and system-filed questions as durable work — and what mints co-reference edges at scale (item 4's economy)?

6. The reflexive test: run your eight laws + M1 against the system's OWN material. Faces, anatomy rows, binding rows, policy masters are all already data with grammars — media by your own definition? Does address/anchor/occurrence hold for a face node (sibling receipt: every rendered node is already stamped with the path back to the template node that made it, built for a designer edit-mode nothing consumes yet — src/app/server/rama/face_projection.clj is a place to start)? If M1 admits face assemblies as a medium, then the sibling's entire authoring plane — drawing a component, distilling a sketch into a face, activating it scoped, refusals filing commissions when authoring hits the code floor — becomes an instance of YOUR architecture plus their publication protocol, which would be a major unification of the two sessions' planes. If it does not, name exactly what breaks. Either result is load-bearing for Sid.
