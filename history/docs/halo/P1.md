# halo v0 — P1 implementation receipt

2026-07-29 · Codex implementer context

**STATUS: P1 IMPLEMENTER COMPLETE · G1–G4 GREEN · UNSTAGED,
UNCOMMITTED, AND UNPUSHED.** This is the implementer receipt, not Fable's
independent gate. Sid still owns the code/docs commit ruling. Sid's headed,
unscaffolded first open is C2's instrument and is deliberately not a build
gate under CONTRACT §8.

## Baseline and ruling

- Branch: `docs/current-mental-model-local`
- HEAD before and after implementation:
  `785841d82b1c64e64cd1b329019b6e286fa00d26`
- Starting index and tracked worktree: clean.
- Starting foreign untracked state, preserved exactly:

  ```text
  ?? probe-block-anatomy.html
  ?? probe-block-anatomy.png
  ```

- CONTRACT, NOW STANDING, decisions, VALIDATION, `/rama`, and
  `/rama-retro-lens` were loaded before the Rama composition was touched.
- No genuine policy conflict was found. The validated Jetty locator drift
  was relocated to the actual `src/app/server_jetty.clj` and logged here;
  no stop ruling was needed.
- Starting and final zero-diff pins:

  ```text
  e1f1836f6c092bda250930ccf544b47b15e1b9894635e7170eaf024f44af4179  src/app/server/episode.clj
  ab283b47ae273aa9a0a42b2e14a690a3804c054a7370ef3fb06ee910a2772ca2  src/app/server/rama/relation_kernel.clj
  ```

## What landed

### H1–H2 — one gesture and its inviolable floor

- `events.cljs` observes `contextmenu` on the one
  `#webgpu-canvas`, synchronously tests `ground/ground-active?`, and only
  then prevents default and emits coordinates. Button 2 is excluded from
  both ordinary mouse down and up before the pointer machine can see it.
- `runtime.cljs` joins the meta flow beside the existing mouse flow.
- `binding_material.cljc` visibly widens the grammar with
  `[:pointer/meta :complete]`, adds the universal
  `meta-gesture-reserved?` material fence, and owns the one idempotent
  four-row floor augmenter. The rows are exactly user hit area, machine
  hit area, fold header, and space ground, all reaching only
  `:halo/condense`.
- The same augmenter feeds client dispatch/table tiers in `ground.cljs`
  and the served interaction table in `face_projection.clj`. The four
  meta drill probes and the served table therefore report the same floor
  truth. `space_material.cljc` applies the reservation beside the camera
  reservation in its existing validation lane.

### H3–H4 — derived condensation and exactly four honest handles

- `ground.cljs` performs one pick for a meta gesture, resolves through the
  existing claim chain, and invokes the resolver-chosen subject without
  re-reading mutable focus in the effect.
- The Halo is a real GPU scene citizen. A block census derives exactly the
  six real non-space wears
  (`provenance/attention/foldable/positioned/threaded/text-body`); empty
  ground derives only the `space` wear. Master identity comes from
  `facet-masters/by-facet`, and worn revision/tier or
  `code-owned · floored` is rendered honestly.
- The four and only four handles are `ask`, `enter`, `preview`, and `say`.
  Ask unfolds the existing 17-question portal card in place. Enter uses
  the existing room navigation. Preview uses the existing client membrane
  and restores by identity. Say uses the one new disclosed endpoint.
  Deviate, activate, and rollback have no Halo descriptor.

### H5 — one non-spawning say composition

- `matter_room.cljc/say-request` is a pure, registered-master, one-line
  builder. It defaults the local human actor only when actor is omitted;
  explicit nil and malformed/nonhuman actors remain honest.
- `face_wiring.cljs` mints one immutable client body before fetch and
  exposes `__portal.say` with optional exact replay identity.
- `server_jetty.clj` preflights both runtimes, derives the registered room
  object-key, enriches only the import-envelope capabilities, preserves
  honest user/assistant provenance, calls the existing episode utterance
  request builder, imports + awaits + queries the root, then calls the
  existing circulation artery and awaits/query-checks the reference.
  No spawning functions, new request family, adapter, depot, topology, or
  PState were added.
- The fresh falsifier found that the shared OC fingerprint excludes
  projection hints and that an exact duplicate depot request can return
  the prior accepted decision before topology comparison. The smallest
  in-fence repair strengthens only the composed Halo request fingerprint
  with the existing episode fingerprint plus the immutable act body, and
  applies the owner's existing durable-decision conflict predicate before
  either append. `episode.clj` remains byte-untouched.
- `material_circulation.clj/bank-reference!` wraps the existing private
  asserted-edge composer with the exact `circulation:halo-say:` prefix and
  `:halo/say` note. Its generic experience projection exposes
  `:source-unit-id` and `:mark/type`; only legacy wish rows retain
  `:wish-unit-id`. Human custody is gold; a supplied nonhuman actor remains
  asserted and non-gold.
- The first live say exposed a second concrete boundary bug: the JVM
  result printed an internal `ActorRow` tag that the browser EDN reader
  cannot decode. The repair erases record constructors only in the say
  HTTP response while preserving every field. The durable result and
  owners remain unchanged; a regression fixture now requires the response
  to round-trip through data-only EDN.

### H6–H8 — room boundary and registry honesty

- The durable endpoint set is now exactly
  `deviate/activate/rollback/say`; preview remains endpoint-free.
- `:halo/condense` is declared `:pure-projection` and floor-reserved.
  `:matter/say` is `:durable-via-request`, requires
  `#{:master-id :subject-uid}`, and is inert in kernel dispatch like its
  durable siblings. The ground registration vocabulary remains exactly
  equal to the registry.
- The material portal's existing briefing sentence names all five matter
  verbs and both effect classes. The portal question/serve shape remains
  unchanged.

## G1 — suite and compile

Final current-tree receipts:

```text
binding-dispatch + space-material + material-circulation +
face-projection + material-truth:
  62 tests · 912 assertions · 0 failures · 0 errors · exit 0

material-portal:
  34 tests · 604 assertions · 0 failures · 0 errors · exit 0

episode compatibility:
  18 tests · 110 assertions · 0 failures · 0 errors · exit 0

TOTAL:
  114 tests · 1,626 assertions · 0 failures · 0 errors

CLJS full dev compile:
  272 files · 0 warnings · exit 0
```

The exact pins are green: three floor-reserved verbs; the durable effect
set includes only the named say widening; five matter verbs with exact
args/effects; registry equals ground registrations; four durable matter
endpoints and no preview endpoint; 17 questions; seven facet masters;
four served meta rows; and exactly the original eight import owners.

A production-alias probe was not counted as a gate receipt: the existing
`src-prod/prod.cljc` requires the old `hyperfiddle.electric` namespace
while the checked dependency exposes Electric 3. No production file was
changed or admitted to the fence. The required full development client
compile above is green.

## G2 — isolated live drive

Rig: `LAND_CLUSTER=0 LAND_PINNED=1`, isolated current-tree server
`http://127.0.0.1:8097`, fresh headless Chrome profile, remote-debug port
`9231`. Existing server/Chrome/Rama processes were neither stopped nor
modified. The rig processes are down.

The first browser evaluation attested:

```text
navigator.gpu=true · adapter=true · device=true
vendor=google · architecture=swiftshader
description="SwiftShader Device (Subzero)"
isFallbackAdapter=true
```

Real pointer/portal observations:

- With a real user block focused, button-2 contextmenu left focus,
  confirmed text/caret/sequence, and pointer phase byte-equal/idle.
  Condensation named the picked subject and exactly:
  `fm:provenance`, `fm:attention`, `fm:foldable`, `fm:positioned`,
  `fm:threaded`, `fm:text-body`.
- A real click on ask unfolded exactly 17 questions in place
  (`What is this?` through
  `What does a resident summoned in here know?`).
- A real enter click navigated through the shipped lane to
  `?drill=a983e774-33f1-384f-aa7f-3cf319fd7c75`.
- A real attention-preview click wore
  `preview:fm:attention:1927160654`; endPreview restored
  `code-floor:fm:attention:v3`. The authoritative master projection was
  byte-identical before, during, and after.
- After the response-boundary repair, a real say click completed. Reopening
  the attention master returned exactly one `:halo/say` mark with
  `:source-unit-id` and no wish identity. A final fresh rig then reopened
  the actual attention room and served that exact source unit in the room
  block set:

  ```text
  room object-key:
    chat:569e3833ec1884883fc35d4c81bf268d70d49d30ddc1f58c6b6e0f74d230bb11
  source unit:
    du:chat:569e3833ec1884883fc35d4c81bf268d70d49d30ddc1f58c6b6e0f74d230bb11:episode-native-v0:ep:c0300149:000000
  served-in-room=true · room block count=4
  ```

- An exact-body live replay returned the same root; a new say id returned
  a different root; divergent target reuse returned
  `:idempotency/material-fingerprint-conflict` with
  `relation-appended? false`.
- After closing the prior card, real right-click on empty ground condensed
  exactly `fm:space`, with the pointer idle.
- A console-injected material-tier meta row was refused live with
  `status=refused`, `error=meta-gesture-reserved`.
- After a full `?dev` reload left the ground, a real button-2
  `contextmenu` on the same `#webgpu-canvas` produced
  `defaultPrevented=false`; the native menu path survived.

## G3 — G10-machine

The warm sample used real ground edits while the six-master Halo remained
open:

```text
warm: n=62 · p50=15.2ms · p95=29.8ms · p99=94.8ms · max=94.8ms
bar: p95 < 52ms — observed 29.8ms

pre-warm/cold: n=17 · p50=19.9ms · p95=51.6ms · max=51.6ms
```

The single warm 94.8ms outlier is retained honestly; it did not move p95
over the bar. Focus and confirmed edit state were unchanged by the meta
gesture, and the pointer remained idle.

## G4 — one fresh in-phase falsifier

The first fresh run returned **FAIL** on three assertions: divergent reuse
of one say id with a different picked target reached the depot duplicate
response and appeared accepted. The smallest repair is the durable
fingerprint/conflict check described under H5. The exact target-divergence
counterexample re-ran green, then the one bounded falsifier ran across the
whole required attack surface:

```text
HALO_P1_FRESH_FALSIFIER
8 tests · 155 assertions · 0 failures · 0 errors · exit 0
```

Those fixtures jointly attack every site/tier/frozen+strict reservation
bypass; contextmenu scope and right-button order; eight-owner import
census; replay, new identity, divergent identity, and import-only partial
repair; exact endpoint disclosure and heavy-handle absence; four-handle
honesty; block-versus-space citizenship; served meta rows; say-versus-wish
marking; and the two zero-diff hashes.

The live G2 pass then independently found the tagged-record response bug.
Its smallest response-only repair re-ran the exact handle to
`status="say complete"` and completed the room read-back above.

## Fence and ownership audit

Exactly the §6 source/test fence is modified:

```text
src/app/client/workspace/events.cljs
src/app/client/workspace/face_wiring.cljs
src/app/client/workspace/ground.cljs
src/app/client/workspace/runtime.cljs
src/app/server/rama/face_projection.clj
src/app/server/rama/material_circulation.clj
src/app/server_jetty.clj
src/app/shared/binding_material.cljc
src/app/shared/material_portal.cljc
src/app/shared/matter_room.cljc
src/app/shared/space_material.cljc
src/app/shared/verb_registry.cljc
test/app/binding_dispatch_test.clj
test/app/face_projection_test.clj
test/app/material_circulation_test.clj
test/app/material_portal_test.clj
test/app/space_material_test.clj
```

No new namespace was added, so test-runner classification did not widen.
`git diff --check` is clean. The index is empty. `cascade.clj`,
`episode.clj`, and `relation_kernel.clj` are zero-diff. `env.clj` was
never read. The two starting foreign probe artifacts remain untouched.
Nothing was staged, committed, or pushed.

Next action: Fable runs the fresh independent slim gate against this exact
unstaged worktree. A green implementer gate does not close the package or
authorize a commit; Sid rules both code and docs commits afterward.
