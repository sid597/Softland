# Scene + GPU machine — source map (2026-08-21)

## 0. Scope and reading key

| item | mapped fact |
|---|---|
| area | **CHECKED** — 15 current files were mapped: four named workspace scene files; the actual substrate `scene_tape.cljc`; five substrate `frame_*` cores; workspace `frame_runtime.cljs`; and four WebGPU files. |
| path correction | **LOUD — CHECKED:** `src/app/client/workspace/scene_tape.cljc` does not exist; the file imported by `scene_store.cljc` is `src/app/client/substrate/scene_tape.cljc` (`scene_store.cljc:21-22`). **LOUD — CHECKED:** `frame_scheduler.cljc`, `frame_graph.cljc`, `frame_inputs.cljc`, `frame_delta.cljc`, and `frame_semantic_state.cljc` are under `src/app/client/substrate/`; only `frame_runtime.cljs` is under `workspace/`. |
| hard out | **CHECKED** — no `object_container`, `relation_kernel`, `trail_view`, or `face_arsenal` module/runtime body was opened. No mapped component calls those bodies directly in the scoped source windows; any wider server boundary was left unopened. |
| method | **CHECKED** — every component was skeletoned with `rg -n "^\(def"`, then read through scoped windows. Caller claims distinguish source references from runtime execution. |
| markings | **CHECKED** = the named source, history, search, config, or retained receipt was read in this run. **INFERRED** = a bounded conclusion from those patterns; it is not runtime proof. |
| parked state | **CHECKED** — the cleanup task names this scene + GPU machine as parked after the product client deletion (`waist-cleanup-task.md:56-69,84-97`). The retained close receipt records `verify:render-engine` at 152 files / 0 warnings with base, chrome, and Region3D goldens green (`NOW.md:9-12`). |

## 1. Component map

### 1a. Scene values and runtime

| component | what it is, in one line | owns vs borrows | who calls it / what it calls | file-only replacement boundary | retained verifier: guarded vs unguarded | live-client assumptions, naming, and vintage |
|---|---|---|---|---|---|---|
| **Scene store**<br>`workspace/scene_store.cljc` | **CHECKED** — pure store functions maintain slots, address and order indexes, frame payloads, pick results, context bundles, and data action descriptors (`:151-211`, `:321-430`, `:644-726`). | **CHECKED** — owns the `{:slots :index :ordered}` value, slot construction, address fan-out, maintained entry projection, pick/context shapes, and descriptor helpers. Borrows rect-tree layout/flatten/hit, container transforms, substrate scene-tape ordering, Region3D placement resolution, face assembly, and material-inspector stamp extraction (`:21-27`, `:97-127`, `:321-378`, `:406-444`, `:683-684`). | **CHECKED** — `scene_runtime.cljs` is its mutating caller; tests and substrate tests call its pure API. It calls `rect-tree`, `containers`, `scene-tape`, Region3D placement, face assembly, and material inspector. | **CHECKED** — the file is pure, but its slot and entry shapes are consumed by scene runtime, scene tape, rect tree, renderer inputs, and material inspection; a shape change crosses those files. | **CHECKED** — the static scene-tape fence extracts `pick`, requires maintained reverse-pick plus inverse transform, and rejects independent sort/family dispatch (`verify_scene_tape_fence.mjs:42-45,58-59,138-149`). **CHECKED unguarded:** the browser verifier does not import or drive store upsert/remove, face construction, context bundles, or descriptor dispatch. | **CHECKED** — no browser global occurs in this file. **LOUD — CHECKED:** its public `scene-tape` function compiles store entries, while `scene-tape` is also the alias of the substrate namespace (`:283-299`). **CHECKED vintage:** comments retain scene-substrate, W2-A/W2-B, SEAM-STEP1, Region3D, and editable-material markers (`:169-198`, `:255-299`, `:669-684`). |
| **Scene runtime**<br>`workspace/scene_runtime.cljs` | **CHECKED** — CLJS owner of scene/container atoms, delta journal, face-instance lifecycle, picks/actions, Missionary views, and browser dev APIs (`:1-23`, `:34-132`, `:186-469`, `:569-599`). | **CHECKED** — owns `!scene-store`, `!containers-registry`, the container-delta journal, face/container allocation, pick/action state, mutation wrappers, and window API installation. Borrows pure scene-store/container operations, `frame-delta`, `frame-effects`, and Missionary (`:24-28`, `:34-97`). | **CHECKED** — current imports remain in chrome and Region3D runtimes; it calls scene-store/container APIs and mints `frame-delta` container records. **INFERRED** — the scoped exact-symbol census found no caller of its two window installers or its three Missionary views. | **CHECKED** — it is the mutating sibling of the pure store and registry; its public shapes are the atoms/snapshots/delta callbacks consumed by render-family code. | **CHECKED** — the retained browser verifier reaches this namespace only through `frame_runtime` loading; it does not call scene lifecycle/mutation functions. **CHECKED unguarded:** store/registry mutation order, CID reuse, pick memory, action handlers, and window installers are not dynamically driven by the verifier. | **LOUD — CHECKED:** this source still names a live product client: its namespace contract names `render.cljs`, `mouse.cljs`, `face_wiring`, and `agent_flow`, and `spawn-instance!` expects currently worn face/projection atoms (`:8-23`, `:78-89`, `:475-515`). It installs `window.sceneFaces` and `window.sceneContext` (`:569-599`). **CHECKED vintage:** the file identifies itself as scene-substrate P3a/P3b and describes the main worn face as a separate legacy path (`:1-23`). |
| **Rect tree**<br>`workspace/rect_tree.cljc` | **CHECKED** — pure rect-tree construction, layout, family flattening, bounds/family hit testing, and bubbling dispatch (`:19-187`, `:205-697`). | **CHECKED** — owns node/padding/layout shape, family projections for rect/text/shadow/image/region/path/connector/chrome, narrow-hit registry, hit traversal, and dispatch. Borrows path, connector, chrome, and image material predicates/placement helpers. | **CHECKED** — scene store and frame runtime call it; render-family runtimes and tests also import it. It calls family material helpers while flattening and narrow-hit testing. | **CHECKED** — no state is stored here, but the ten-key node shape and the `tree->*` output shapes are consumed by scene store and family producers. | **CHECKED** — the static fence extracts `rt-node` and forbids a per-node `:transform`; W4 verifier fixtures construct rect trees (`verify_scene_tape_fence.mjs:45,59,146-149`; `verifier.cljs:3637-3641`). **CHECKED unguarded:** the retained browser runner does not directly exercise every layout walker, family flatten, hit predicate, or `dispatch-event`. | **CHECKED** — no browser global occurs here. **LOUD — CHECKED:** local `positioned` means layout-resolved child/glyph placement (`:79-131`, `:174-176`), not the run-1 Positioned material facet. **CHECKED vintage:** source/history spans scene-substrate through image/path/connector/chrome and W4 frame-runtime additions. |
| **Containers**<br>`workspace/containers.cljc` | **CHECKED** — pure container hierarchy and affine registry with compact transport slots, effective transforms, point conversion, and transformed bounds (`:46-185`, `:206-295`). | **CHECKED** — owns registry/root shape, slot allocation/reuse, hierarchy mutation, affine composition, effective rows, inverse/forward point conversion, and bounds transforms. Borrows only numeric primitives. | **CHECKED** — scene runtime is the local mutating caller; frame effects, Region3D placement, render families, selection, renderer verifier, and tests consume its registry/effective shapes. | **CHECKED** — the file is pure, but `:affine`, `:flags`, `:layer`, `:stack-path`, and `:transport-slot` are transported into scene picking and GPU buffers; changing those keys crosses the scene and renderer siblings. | **CHECKED** — the browser verifier calls add/effective/transport-slot/inverse-point paths and writes effective rows to production buffers (`verifier.cljs:199-248,2214,2889-3010`). **CHECKED unguarded:** its retained verifier call census contains no `set-transform`, `set-effects`, `remove-container`, or `screen-bounds` call. | **CHECKED** — no browser global occurs here. **LOUD — CHECKED:** semantic container ID and compact `:transport-slot` are different identities; scene runtime adds `!` wrappers with the same `set-transform`/`set-effects` names (`containers.cljc:111-169`; `scene_runtime.cljs:340-359`). **CHECKED vintage:** source/history carries P3b, W2-A affine transport, W2-B ordering, and W4 markers. |
| **Scene tape**<br>`substrate/scene_tape.cljc` | **CHECKED** — pure render-family registry plus one ordered tape contract with forward paint and reverse pick projections (`:14-70`, `:328-584`, `:641-768`). | **CHECKED** — owns family registrations, scene-color seam, entry/order grammar, maintained sorted-map helpers, batch compilation, and paint/pick traversal. Borrows chrome/connector/path/Region3D material declarations; it owns no atom or GPU object (`:1-12`). | **CHECKED** — scene store, frame graph/effect view, renderer/verifier, render-family GPU namespaces, and tests call it. It validates entries/families and calls only injected paint/hit functions at the traversal edge. | **CHECKED** — its entry grammar and registry are shared by store, graph, renderer, verifier, and plug-in families; the maintained and batch APIs are separate functions over the same entry shape. | **CHECKED** — the static fence checks all nine core families, forward paint, reverse pick, color default, and central-dispatch absence (`verify_scene_tape_fence.mjs:67-170`). The browser verifier calls registration, ordered insert, compile, forward paint, and reverse pick. **CHECKED unguarded:** no product scene/runtime lifecycle is part of either guard. | **CHECKED** — no browser global occurs here. **LOUD — CHECKED:** the requested workspace path is absent; this is the actual substrate file. **LOUD — CHECKED:** `ordered-insert/remove` is the maintained path, while `compile-tape` is the independent batch compiler/oracle (`:708-746`). **CHECKED vintage:** `:scene-order/version` is 1; source/history carries W2-B, IMAGE/PATH/CONNECTOR/CHROME, SEAM-STEP1, and later Region3D registrations. |

### 1b. Frame computation

| component | what it is, in one line | owns vs borrows | who calls it / what it calls | file-only replacement boundary | retained verifier: guarded vs unguarded | live-client assumptions, naming, and vintage |
|---|---|---|---|---|---|---|
| **Frame scheduler**<br>`substrate/frame_scheduler.cljc` | **CHECKED** — scheduler state step plus module clock/deadline registries; it decides encode/skip from causes and due deadlines (`:10-68`, `:84-199`). | **CHECKED** — owns scheduler state/receipt, legal causes, deadline validation/registry, replay, and pulse waveform. Borrows caller time, cause sets, plan hashes, and stop predicates. | **CHECKED** — frame runtime registers the selection-pulse deadline; the verifier calls `decide`, `replay`, `pulse-alpha`, `initial-state`, and `receipt`. | **CHECKED** — `decide` is pure; `decide-at!`/`decide!` add the private clock/deadline registry, so callers can use the value step without the module registry (`:128-177`). | **CHECKED** — W4/Region3D verifier branches call pure decision, replay, pulse, and receipt functions (`verifier.cljs:3964-3983,4986-4990`). **CHECKED unguarded:** the retained verifier does not call register/unregister/clear deadlines or replace the clock source. | **CHECKED** — no browser global occurs here. **LOUD — CHECKED:** `:clock` is one invalidation cause, while `!clock-source` is an execution-time adapter; `legal-causes` is duplicated in frame graph (`frame_scheduler.cljc:11-16`; `frame_graph.cljc:19-20`). **CHECKED vintage:** `scheduler-version` is 1 and the file carries the W4 `143497a` generation. |
| **Frame graph**<br>`substrate/frame_graph.cljc` | **CHECKED** — pure frame resource/pass compiler with structural reuse, binding of current entry ranges, validation, oracle comparison, and export-plan compilation (`:89-109`, `:344-515`, `:615-790`). | **CHECKED** — owns plan/resource/pass/schedule/color shapes, plan hash, validation, maintained-plan state, and export plan. Borrows effect spans, regions, capabilities, viewport, arrangements, and scene-tape vocabularies. | **CHECKED** — frame-plan-view, compositor, verifier, and tests call compiler functions. It calls frame-effects range helpers and scene-tape pass/resource vocabularies. | **CHECKED** — plan maps are consumed by plan-view/compositor; `maintain-frame-plan` separates reusable structure from current entry-range binding. **INFERRED** — the bounded exact-symbol census found no current caller of that public function. | **CHECKED** — the static fence checks pass vocabulary; the browser verifier calls `compile-frame-plan` across W4/Region3D cases (`verify_scene_tape_fence.mjs:47,159-166`; `verifier.cljs:3029,3192,3293,4374,4540,4817`). **INFERRED unguarded:** the same census found no caller of `maintain-frame-plan`; its maintained state branch is not directly driven. | **CHECKED** — no browser global occurs here. **LOUD — CHECKED:** its plan embeds `:schedule {:causes ...}` while the scheduler owns a separate cause set. **CHECKED vintage:** `graph-version` is 1; source/history spans W4 `143497a` and the later cleanup-era frame changes. |
| **Frame inputs**<br>`substrate/frame_inputs.cljc` | **CHECKED** — per-family semantic input declarations, equality/change classification, family entry maintenance, region quantization, and a mutable receipt ledger (`:13-177`, `:199-247`). | **CHECKED** — owns family declarations, forbidden raw-camera inputs, quantization doors, system/shape tokens, `family-entry-delta`, and `!ledger`. Borrows renderer-supplied systems, shape-revision atoms, inputs, entries, and producer IDs. | **CHECKED** — renderer and plug-in GPU namespaces call it; tests and the verifier reach its family, quantization, arrangement, twin, and ledger APIs. | **CHECKED** — pure comparison/delta functions share a file with the client-global ledger; renderer depends on both the declaration shape and `{:remove :insert :next-keys}` result. | **CHECKED** — verifier paths call family declarations through renderer, run arrangement deltas/twin equality, and begin/read the ledger (`verifier.cljs:1095-1102,5035-5058`). **CHECKED unguarded:** no retained verifier call publishes the ledger globals or directly drives every shape-revision/region-quantization mutation. | **CHECKED** — `publish-ledger!` writes `__softlandFrameLedger` and `__softlandFrameLastBindingLedger` on `globalThis` (`:238-247`). **LOUD — CHECKED:** `family-entry-delta` is not a `frame-delta` record: it returns remove/insert/next-key collections (`:177-197`). **CHECKED vintage:** quantization doors and constants carry explicit `v1` identities (`:9-23`). |
| **Frame delta**<br>`substrate/frame_delta.cljc` | **CHECKED** — pure constructors/classifiers for semantic and binding delta records, plus container declarations from a registry (`:9-30`, `:77-195`). | **CHECKED** — owns semantic/binding kind sets, entry/container/region/global constructors, topology signatures, and container declaration classification. Borrows old/new entries, registry rows, effects, regions, viewport, and changed-field values. | **CHECKED** — renderer calls entry/global constructors; scene runtime calls container constructors; Region3D bindings and tests call binding constructors. It calls frame-effects validation/classification. | **CHECKED** — this file has no mutable state, but its `:delta/kind` and `:binding/kind` records are split among renderer, semantic-state, scene runtime, and Region3D binding consumers. | **CHECKED** — renderer arrangement tests reach `entry-deltas` indirectly. **CHECKED unguarded:** the retained browser verifier has no direct namespace call and does not drive scene-runtime container journal delivery or the full container/region/global delta set through `frame-semantic-state`. | **CHECKED** — no browser global occurs here. **LOUD — CHECKED:** semantic rows use `:delta/kind`; binding rows use `:binding/kind`; scene runtime adds `:delta/sequence` in a separate journal (`frame_delta.cljc:9-20,148-195`; `scene_runtime.cljs:36-75`). **CHECKED vintage:** no file-level version constant appears; `topology-signature` is its plan-shape projection. |
| **Frame semantic state**<br>`substrate/frame_semantic_state.cljc` | **CHECKED** — one pure reducer applies semantic delta batches to arrangement/effect/plan views and returns a new generation (`:6-65`). | **CHECKED** — owns the aggregate keys `:arrangement`, `:effect-view`, `:plan-view`, `:generation`, `:globals`, and the last-delta receipt. Borrows effect-view and plan-view reducers plus caller-partitioned deltas/registries/regions/globals. | **CHECKED** — renderer owns the atom, calls `apply-deltas`, and reads the effect/plan views (`renderer.cljs:3392,3797-3874`). It calls frame-effect-view and frame-plan-view. | **CHECKED** — the reducer is pure, but its aggregate shape and single-swap publication are welded to renderer plus the two sibling maintained views. | **CHECKED** — no direct verifier reference exists. **CHECKED unguarded:** because the browser verifier does not invoke `draw-frame!`, it does not execute `frame-semantic-state/apply-deltas`; the static fence checks only that `draw-frame!` still contains the maintained/twin/executor seams. | **CHECKED** — no browser global occurs here. **LOUD — CHECKED:** its scalar `:generation` counts semantic-state publications, unlike buffer-pool's per-slot handle generations (`frame_semantic_state.cljc:47-56`; `buffer_pool.cljs:139-199,384-425`). **CHECKED vintage:** bootstrap generation is 1 and each semantic update increments it. |
| **Frame runtime**<br>`workspace/frame_runtime.cljs` | **CHECKED** — URL-flagged W4 browser fixture join: it constructs effect fixtures, registers them into scene runtime, bridges selection to scheduler deadlines, publishes receipts, and exports PNG (`:1-50`, `:172-355`). | **CHECKED** — owns fixture IDs/trees, boot/mount/pulse/registration atoms, receipt shape, selection-pulse deadline identity, export chord, and download behavior. Borrows scene runtime, rect tree, frame effects/scheduler, and renderer-published browser globals. | **CHECKED** — the verifier calls only `flag-enabled-search?` and `felt-fixture-receipt`. **INFERRED** — the bounded exact-symbol census found no current caller of `boot!`, `mount!`, `sync-pulse-deadline!`, or `export-viewport!`. | **CHECKED** — fixture construction is file-local, but activation registers scene instances and scheduler deadlines and reads compositor/chrome globals. | **CHECKED** — W4 verifier guards flag-off parsing and the pure felt-fixture receipt (`verifier.cljs:3637-3641`). **CHECKED unguarded:** live boot, mount, selection pulse, scene registration, compositor receipt, keyboard chord, and PNG export are not invoked. | **LOUD — CHECKED:** it assumes the deleted product client through `live-atoms=1`, `__softlandChromeReceipt`, scene-runtime registration, `__softlandFrameCompositor`, DOM download, and `window` keydown (`:37-50`, `:198-219`, `:261-353`). **LOUD — CHECKED:** “runtime” here means a flag-only fixture/export join; scene runtime means the scene atom owner. **CHECKED vintage:** the file names itself W4 and carries `:material/revision 1`; history roots it at `143497a` (2026-08-07). |

### 1c. WebGPU and retained harness

| component | what it is, in one line | owns vs borrows | who calls it / what it calls | file-only replacement boundary | retained verifier: guarded vs unguarded | live-client assumptions, naming, and vintage |
|---|---|---|---|---|---|---|
| **Renderer**<br>`webgpu/renderer.cljs` | **CHECKED** — WebGPU shader/system construction, buffer/texture updates, family entry production, retained arrangement/semantic state, tape execution, and frame encoding (`:763-860`, `:1139-1432`, `:1449-2621`, `:3297-4000`). | **CHECKED** — owns core rect/image/text/shadow systems, cameras/container upload, render targets, producer/executor registries, retained arrangement/semantic atoms, and `draw-frame!`. Borrows scene-tape/frame modules, buffer pool, GPU budget, text layout, and plug-in family GPU namespaces. | **CHECKED** — verifier directly calls many public constructors/update/executor functions. Renderer directly calls frame inputs, frame delta, and frame semantic state; frame graph reaches it through plan-view. It also calls family GPUs, buffer pool, GPU budget, and WebGPU device/queue/pass APIs. **INFERRED** — the bounded exact-symbol census found no current source caller of `draw-frame!` or `draw-comparison-frame!`. | **CHECKED** — producer registry, retained views, GPU system ownership, and frame encoding are colocated; entry/plan/binding changes cross scene-tape, frame modules, compositor, and plug-in family namespaces. | **CHECKED** — the browser verifier dynamically builds production rect/text/image systems and atom/family branches; the static fence extracts `draw-frame!`, its executor, arrangement, image producer, and twin check and rejects central family/effect branches (`verifier.cljs:5465-5621`; `verify_scene_tape_fence.mjs:50-170`). **LOUD — CHECKED unguarded:** the browser verifier does not dynamically invoke `draw-frame!`; full retained semantic apply, current-texture acquisition, multipass composition, frame globals, and product sink callbacks remain static-fence-only. | **LOUD — CHECKED:** `draw-frame!` assumes a live browser/WebGPU host: `window.devicePixelRatio`, a configured canvas context, `performance`, and multiple `globalThis` receipt/control hooks (`:2637-2655`, `:3470-3588`, `:3667-3678`, `:3879-3975`). **LOUD — CHECKED:** `rect-stride` 116 duplicates buffer-pool `bytes-per-rect` 116; `shadow-stride` 84 matches its 21-word packer (`renderer.cljs:763,1854`; `buffer_pool.cljs:11-12,65-109`). **CHECKED vintage:** source/history carries W0/Q5/Q8, W2, IMAGE/PATH/CONNECTOR/CHROME, W4, retention, Region3D, and the 2026-08-19 frame-binding generation. |
| **Buffer pool**<br>`webgpu/buffer_pool.cljs` | **CHECKED** — mutable GPU buffer pool with slot, keyed, ordered, batch, handle, draw-info, and mount-shaped APIs (`:111-442`). | **CHECKED** — owns pool atom state, GPU buffer/capacity, active/free slots, prior snapshots, per-slot generations, shape revision, packing, and budget metadata. Borrows device, queue, pipeline/bind group, pack function, and GPU-budget calls. | **CHECKED** — renderer image systems call `create-pool` and `batch-update-pool!`. **INFERRED** — the bounded exact-symbol census found no current non-self caller for keyed/ordered diff, handle, or `gpu-mount` APIs. | **CHECKED** — its public pool atom/draw-info shape is narrow, but retained consumers hold the stable pool reference so buffer replacement remains visible (`:364-382`). | **CHECKED** — verifier image paths reach create/batch-update indirectly through renderer. **CHECKED unguarded:** no direct retained guard calls keyed/ordered diff, handle generation validation, or `gpu-mount`. | **CHECKED** — it assumes a WebGPU device/queue but no browser window/DOM global. **LOUD — CHECKED:** the rect layout is simultaneously described as “28 floats + container u32” and 29 words/116 bytes; `create-pool` defaults `floats-per-item` to 29 while its docstring says 28 (`:11-12`, `:111-140`). **CHECKED vintage:** history roots the retained form in the 2026-08-09 frame-retention generation. |
| **GPU budget**<br>`webgpu/gpu_budget.cljs` | **CHECKED** — mutable tracker for GPU buffers/textures, reserved and active bytes, adapter limits, replacement/destruction, bounded events, snapshots, and reports (`:31-333`). | **CHECKED** — owns tracker state, weak object→resource IDs, resource/event rows, limit ratios, the 0.8 warning threshold, and 200-event cap. Borrows adapter limits and resource objects/byte facts from callers. | **CHECKED** — renderer, buffer pool, verifier, compositor, and plug-in family GPU namespaces call it. It uses object identity, console warnings, and caller-supplied resource lifecycle methods. | **CHECKED** — tracker is a separate service-shaped atom, but resource-system callers depend on its registration/replacement/destroy vocabulary. | **CHECKED** — verifier creates trackers and exercises selected registration, replacement, snapshot, and image/chrome lifecycle paths. **CHECKED unguarded:** no direct assertion covers the 0.8 warning threshold, the 200-event retention cap, or completeness of all runtime GPU allocations. | **CHECKED** — it requires a JS `WeakMap` and console, not the deleted product root (`:39-45`, `:101-110`). **LOUD — CHECKED:** “reserved bytes” and “active bytes” are separate fields; adapter buffer-size and texture-dimension ratios are separate calculations (`:73-99`, `:127-139`). **CHECKED vintage:** history spans image-atom, GPU tracking, and the 2026-08-08 Region3D/retention generation. |
| **Verifier**<br>`webgpu/verifier.cljs` | **CHECKED** — standalone browser harness that builds production renderer systems, captures/computes receipts, aggregates atom/family branches, and publishes a structured result (`:1-43`, `:5465-5695`). | **CHECKED** — owns fixture/capture/case helpers, full and Region3D-only exported runners, prerequisite handling, result schema, and browser completion/result globals. Borrows production scene/frame/renderer/family/font APIs and the adapter/device. | **CHECKED** — the `render-verifier` shadow module initializes `start!`; `start!` selects full or Region3D-only runner by query flag (`shadow-cljs.edn:4-16`; `verifier.cljs:5667-5695`). The full runner calls baseline cases, shader digests, Q5/Q8, image/path/connector/chrome, W4, and Region3D (`:5577-5621`). | **CHECKED** — it is isolated to the verifier build; shadow config says no product server or shipped bundle participates (`shadow-cljs.edn:5-16`). | **CHECKED** — guards secure WebGPU/font prerequisites, renderer construction/update paths, scene-tape/containers/graph/scheduler/input slices, nine async result branches, and result publication (`:5465-5621`). **CHECKED unguarded:** it does not establish a product-client caller, product-server integration, dynamic `draw-frame!`, scene-runtime lifecycle, frame-runtime lifecycle, full semantic reducer path, or every buffer-pool/GPU-budget branch. | **CHECKED** — it assumes a secure browser, `navigator.gpu`, DOM canvas/blob support, font assets, URL query, and `window` result globals (`:5465-5695`). **LOUD — CHECKED:** full result identifies as `softland-render-engine-w0-a`; the query-only branch identifies as `softland-region3d-floor`; both use schema version 2 (`:5594-5621`, `:5651-5665`). **CHECKED vintage:** latest history is cleanup verifier repair `adc30c9` (2026-08-20), after W0 through Region3D generations. |

## 2. Loud collision ledger — inside the machine and across the material-row boundary

| collision | exact fact |
|---|---|
| **ONE REQUESTED PATH, A DIFFERENT FILE** | **LOUD — CHECKED:** `workspace/scene_tape.cljc` is absent. `scene_store.cljc` imports `app.client.substrate.scene-tape`, implemented at `substrate/scene_tape.cljc`. The five pure frame cores are likewise substrate files; only the browser join is `workspace/frame_runtime.cljs`. |
| **`scene-tape` IS A FUNCTION AND A NAMESPACE** | **LOUD — CHECKED:** `scene-store/scene-tape` turns slots into scene entries and calls `scene-tape/compile-tape`; the aliased namespace owns validation/order/traversal (`scene_store.cljc:283-299`; `scene_tape.cljc:653-768`). |
| **ONE ORDER, TWO MAINTENANCE GENERATIONS** | **LOUD — CHECKED:** store/renderer live paths maintain sorted maps through `ordered-insert/remove`; `compile-tape` independently validates and batch-sorts the same entry shape as oracle/fixture path (`scene_store.cljc:169-211,314-317`; `scene_tape.cljc:708-746`; `renderer.cljs:3506-3588`). |
| **`material` COVERS TWO SHAPES AT THIS BOUNDARY** | **LOUD — CHECKED:** every scene entry requires only `:material/id` + `:material/revision` as render identity (`scene_tape.cljc:653-656`); run 1 maps material as revisioned facet forms, wear envelopes, and contribution stamps (`material-rows-map.md:16-33`). The store separately reports actual worn material as `:receipt/worn-materials`, extracted from the picked render path, not copied bytes (`scene_store.cljc:669-684`). |
| **TWO KINDS OF `stamp` IN ONE STORE** | **LOUD — CHECKED:** `stamp-ops-container` stamps a GPU/container slot onto flattened draw ops (`scene_store.cljc:62-95`); `material-inspector/contribution-stamps` extracts seven-key material contribution stamps for the context receipt (`scene_store.cljc:683-684`; `material-rows-map.md:32`). |
| **`positioned` / `placement` SPANS POLICY, LAYOUT, AND PICK COORDINATES** | **LOUD — CHECKED:** run-1 Positioned material owns fallback/pre-cell policy and explicitly not settled geometry (`material-rows-map.md:20`); rect tree calls layout-resolved children `positioned` (`rect_tree.cljc:79-131`); renderer calls glyph results `positioned` (`renderer.cljs:2213-2352`); scene context `:receipt/placement` is picked world/local/camera coordinates (`scene_store.cljc:679-682`). |
| **`space` IS ENTITY/POLICY THERE, COORDINATE FRAME HERE** | **LOUD — CHECKED:** run 1's Space facet uses subject string `"space"` and facet keyword `:space` for zoom/input policy (`material-rows-map.md:24,44`); this machine uses local/world/screen spaces as transform and geometry vocabularies (`containers.cljc:1-16,195-295`; `scene_tape.cljc:171-226`). |
| **THREE DELTA SHAPES** | **LOUD — CHECKED:** `frame-inputs/family-entry-delta` returns `{:remove :insert :next-keys}`; `frame-delta` emits semantic `:delta/kind` and binding `:binding/kind` records; scene runtime adds ordered `:delta/sequence` rows to a private container journal (`frame_inputs.cljc:177-197`; `frame_delta.cljc:9-30,148-195`; `scene_runtime.cljs:36-75`). |
| **THREE GENERATION COUNTERS** | **LOUD — CHECKED:** semantic-state `:generation` counts aggregate publications; buffer-pool `:generations` is a per-slot handle invalidation vector; frame plans carry their own `:plan/generation` in the renderer receipt (`frame_semantic_state.cljc:47-56`; `buffer_pool.cljs:139-199,384-425`; `renderer.cljs:3879-3886`). |
| **`runtime` NAMES DIFFERENT OWNERS** | **LOUD — CHECKED:** scene runtime owns the scene/container/action atoms; frame runtime owns flag-only fixtures, selection deadline, export, and receipts; renderer owns retained frame/GPU state (`scene_runtime.cljs:30-97`; `frame_runtime.cljs:1-35`; `renderer.cljs:3297-3392`). |
| **`canonical` HAS THREE BYTE/SHAPE FENCES** | **LOUD — CHECKED:** run 1 already records two material canonicalizers with different JVM byte fences (`material-rows-map.md:41`); this area separately calls one affine shape canonical (`containers.cljc:46-64,263-268`), recursively canonicalizes graph inputs for a stable hash (`frame_graph.cljc:42-60`), and calls the scene order receipt a canonical preimage rather than a runtime hash (`scene_tape.cljc:721-746`). |
| **DUPLICATED CAUSE VOCABULARY** | **LOUD — CHECKED:** scheduler and graph each declare the same seven cause keywords; scheduler consumes causes to decide encode/skip, while graph embeds them in plan schedule metadata (`frame_scheduler.cljc:10-13,128-177`; `frame_graph.cljc:16-22,668-690`). |
| **DUPLICATED GPU STRIDES** | **LOUD — CHECKED:** renderer declares rect/shadow strides; buffer pool independently declares/implements matching word layouts. Rect is 116 bytes in both; shadow is 84 bytes versus 21 packed words (`renderer.cljs:763,1854`; `buffer_pool.cljs:11-12,65-109`). |

## 3. One system or several generations?

| question | factual answer |
|---|---|
| one system, or generations side by side? | **CHECKED — several generations sit side by side inside one connected machine.** Shared entry/container/plan shapes connect them, while batch and maintained roads, pure cores and browser joins, and package-specific verifier branches remain separately visible. |

| evidence class | checked evidence |
|---|---|
| shared value journey | **CHECKED** — rect-tree nodes are resolved/flattened into scene slots; slots mint scene entries; container effective rows supply order/pick/GPU transforms; frame inputs produce family entries; maintained deltas update arrangement/effect/plan state; renderer projects the retained arrangement into GPU commands (`rect_tree.cljc:19-187,205-697`; `scene_store.cljc:97-127,256-378`; `renderer.cljs:3423-3546,3678-4000`). |
| divergent state owners | **CHECKED** — `scene_store`, `containers`, scene tape, frame graph, delta, and semantic reducer expose pure values/functions; scene runtime owns product-era scene atoms; scheduler/input modules add small registries/ledgers; renderer owns retained GPU/frame atoms; verifier owns only harness state/results. |
| batch beside maintained | **CHECKED** — scene store and renderer maintain ordered maps incrementally, while `scene-tape/compile-tape` remains an independent batch compiler; graph exposes compile/oracle/maintain forms; renderer's flag-on twin check compares maintained versus batch plus effect/plan oracles (`scene_tape.cljc:708-746`; `frame_graph.cljc:668-777`; `renderer.cljs:3506-3588`). |
| divergent entry and delta shapes | **CHECKED** — scene entries carry material/instance/family/order/paint/pick/visibility; input maintenance returns remove/insert/key collections; semantic and binding deltas use different discriminators; semantic state stores an aggregate view generation (`scene_tape.cljc:653-686`; `frame_inputs.cljc:177-197`; `frame_delta.cljc:9-30`; `frame_semantic_state.cljc:6-56`). |
| retained package vintages | **CHECKED history/source** — scene substrate and P3 markers precede W2-A/W2-B tape/affine work; image/path/connector/chrome registrations follow; W4 frame runtime is `143497a` (2026-08-07); buffer retention history includes `79c8839` (2026-08-09); verifier repair is `adc30c9` (2026-08-20). The verifier result still names W0-A and separately returns Q5/Q8, four atom families, W4, and Region3D rows (`verifier.cljs:5577-5621`). |
| source-disconnected branches | **INFERRED** — the bounded exact-symbol census found no current caller of scene-runtime window installers/Missionary views, frame-runtime boot/mount/pulse/export, renderer `draw-frame!`/comparison draw, frame-graph `maintain-frame-plan`, or buffer-pool keyed/ordered/handle/mount APIs. This is source reachability only; it does not assert removal safety or runtime impossibility. |
| retained guards are a separate door | **CHECKED** — `verify:render-engine` runs text-layout, a static source fence, a dedicated Shadow browser build, and a Node driver (`package.json:8-9`). Shadow config states that build is infrastructure, not a shipped bundle, and uses no product server/surface (`shadow-cljs.edn:4-16`). |

## 4. Every remaining live-client assumption found in the area

| component / place | exact assumption |
|---|---|
| `scene_runtime.cljs` namespace and dev APIs | **CHECKED** — names deleted product paths (`render.cljs`, `mouse.cljs`, `face_wiring`, `agent_flow`), expects currently worn face/projection/viewport atoms, and installs `window.sceneFaces` plus `window.sceneContext` (`:8-23`, `:78-89`, `:475-515`, `:569-599`). |
| `frame_inputs.cljc` ledger publication | **CHECKED** — writes two receipts to `globalThis` under `__softlandFrameLedger` and `__softlandFrameLastBindingLedger` (`:238-247`). |
| `frame_runtime.cljs` whole activation road | **CHECKED** — reads `live-atoms=1`, chrome selection globals, registers fixtures into scene runtime, reads compositor/plan globals, installs a window key listener, and uses DOM blob/download APIs (`:37-50`, `:198-219`, `:261-353`). |
| `renderer.cljs` comparison and main frame edges | **CHECKED** — reads browser DPR/performance, acquires the canvas current texture, and publishes family/twin/draw/plan/compositor/Region3D/connector globals (`:2637-2655`, `:3470-3588`, `:3667-3678`, `:3879-3975`). **INFERRED** — the bounded exact-symbol census found no current source caller of `draw-frame!`. |
| `verifier.cljs` retained door | **CHECKED** — requires secure `window`, `navigator.gpu`, DOM canvas/blob support, font assets, location query, and window completion/result globals; it explicitly records `:product-server-used? false` (`:5465-5695`). This is a live browser harness assumption, not a product-client caller. |
| `buffer_pool.cljs` and `gpu_budget.cljs` | **CHECKED** — assume JS/WebGPU objects (`device`, `queue`, `WeakMap`, console) but contain no `window`, DOM, product-root, or URL-flag dependency in the scoped search. |
| pure scene/frame cores | **CHECKED** — no browser-global/product-lifecycle reference was found in `scene_store.cljc`, `rect_tree.cljc`, `containers.cljc`, `scene_tape.cljc`, `frame_scheduler.cljc`, `frame_graph.cljc`, `frame_delta.cljc`, or `frame_semantic_state.cljc`. Their client dependency is supplied values and callbacks, not a product boot door. |
