# Slice A — the starter for landing the executor, the compositor and the path kind's pickup brush (2026-09-07)

**What this slice is.** DESIGN-1 §9's slice A, landed by a Codex session at high in a fresh context from the design as repaired against REFUTE-1: the executor of §3 without its pending branch, `engine/surface.cljc`, the path kind's table in `construction.cljc` with `construct` over `run`, the pickup record with its five receipts, the push edge with its tests and re-measurement, the deletions, the docs in the same commits, a handoff. Slice B (the 3D brush, the pending branch) follows from `3d/STARTER-production.md` §2 on the tree this slice leaves. The definer and the judges are as `path-kind/STARTER-production.md` §3 and §4 say; nothing there changes for this slice.

## 1. The landing session (Codex, a fresh context, in the main checkout)

`````
Fence: you sit in the main checkout `/mnt/data/projects/Softland` on `main`, one branch. Code under `src/app/client/` and, in scope for you, the tests and goldens under `test/`; never `src/app/server/env.clj`. Read anything under `docs/below-the-waist/`. Commit on `main` by exact path, one commit per milestone, each big enough to read as one change; push is mine. The work-package skill and its contract-and-gate process are not used for this work; where an older handoff points at them, this supersedes it.

What I am building is every tool as data above a waist of code that exists once, for the 2D kinds and the 3D kind alike, so that humans and agents create, edit and reuse tools as records. The path kind's first production slice is on `main` (`e8dcd37`; its handoff is `path-kind/production/HANDOFF-1.md`). This slice lands the two pieces both kinds share, designed as data in `production/DESIGN-1.md` by one Fable session and refuted by a Codex session with executed receipts in `production/REFUTE-1.md`, with the findings folded back into the design: one executor that runs a record's program over a vocabulary of capabilities, keeps nothing between runs, suspends into a continuation of bytes and resumes to the same bytes; and one compositor with two operations, paint and sample, as pure functions over a surface value, the CPU runner being the reference. The path kind's pickup brush is their first consumer, a record in the client's grammar whose numbers the definer pinned on bench 9, and the push edge replaces the renderer's per-frame walk with a caller that names what changed. The design says everything you must do: the values exchanged, each level's inputs, the record, every affected caller by name, the deletion scope, the tests and their receipts. Where the design is wrong or short, you bring the better construction back to it, in your handoff and in one sentence to me, rather than filling the gap quietly; where it takes a position it says what supports it and when it stops holding, and you may argue with that.

I ruled three things. Caching is never an option; it means the underlying thing is as it should be and then still needs more performance, therefore caching goes on top: every level is a pure function whose inputs are explicit as data, a key is a level's full input as a value, never what a run happened to read and never a hash, and no memo, bucket or atlas is added, and none that exists is credited, without a trace that shows the need; the design's §1 carries this in words, and the executor's `:fields`, its recipe rule and its subject are that rule applied. The repo's own verification surface is the acceptance surface, necessary and not sufficient, and a red check is reported as red before anything else is claimed. Every claim in a README or a handoff points at the test that shows it or is marked untested. Our aim is not to copy anyone; the best in the field is a datapoint; nothing is carried forward because it exists. The definer's records are what the implementation is measured by, and I carry them to you.

Terrain, with sizes so you can plan the read. `production/DESIGN-1.md` (101 KB) is the whole design and you read all of it; its §3, §4, §5, §8 and §10 are your work, its §6, §7 and §9 tell you what slice B will need of the interfaces you leave. `production/REFUTE-1.md` (58 KB) is the refutation the design was repaired against; read a finding only where the design cites it (F1 to F22) and you want the receipt behind the sentence. `path-kind/production/HANDOFF-1.md` (13.5 KB) is the tree you start from, in its builder's words. `two-chairs.md` (25 KB), the production section only, is the ruling behind the chairs. The code by seam, skeleton first: `src/app/client/engine/executor.cljc` (6 KB; you keep its expression side and replace the rest), `src/app/client/path/construction.cljc` (3 KB; the boundary and the table), `src/app/client/path/renderer.cljs` (19 KB; the walk the push edge replaces), `src/app/client/engine/README.md` and `src/app/client/path/README.md` (the folder maps you extend). The definer's numbers are in `path-kind/bench-9/HANDOVER.md` and re-run from `path-kind/bench-9/node-route/` (`regress.js`, `wire.js`, `extract.js`) with Node from the repo root; the pure suite is `clj -M:test -i test/app/client/path/run_pure.clj` and the build and verifier commands are in `path-kind/judge-claude.md` section 1. The client's reactive laws are the `electric-docs` skill, Missionary only; nothing in this slice wires Missionary. Your handoff lands as `docs/below-the-waist/path-kind/production/HANDOFF-2.md`.

Work the hardest case through what you build before you call the slice done: the pickup's checkpoint at dab 12 with the four edits against it, through bytes and back, and one placement pushed among 1,600 with the row write counted; say where a named piece hid work you had to invent; end on the exact open question you leave. Where you departed from the design, say what supports the departure, when it stops holding, and what else changes with it.

Load turn: read what is named above, reply in one line with what you hold and anything you need to start that is not there, and wait; I set the effort and say when to begin.
`````

## 2. Sid's card

1. New Codex session in `/mnt/data/projects/Softland`. Set the effort for the load; paste section 1.
2. It replies in one line. If it names something missing, answer it. Set the effort for the build and say `Begin.`
3. It lands, commits per milestone, reports red first, writes `path-kind/production/HANDOFF-2.md`.
4. When it says done: the definer against the landing, then one judge per family (`path-kind/STARTER-production.md` §3 and §4, unchanged). Then slice B from `3d/STARTER-production.md` §2, with DESIGN-1 §9's added sentence at its terrain.

**Effort** (a position, not a ruling). The load turn at medium, so the read enters before any judgment forms. The build at high, which is the chair two-chairs names for a landing behind a complete design; xhigh for a turn in which the session reports the design short and brings its construction, then back down. The design was written so that high is enough: every choice a literal landing would otherwise have to make is named in it, and the refutation checked that against the tree.

## 3. After this slice

The design session (Fable at max) stays available for a design problem the landing brings back; a second refutation round against the repaired design is Sid's call and is not scheduled. Slice B's paste gains one sentence at its terrain (DESIGN-1 §9) and one more if Sid rules on Position 9's five slots (DESIGN-1 §9, last paragraph).
