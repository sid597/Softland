# The refutation of DESIGN-1 (a Codex session, fresh context; written 2026-09-07 at Sid's word: "can you write also a prompt for refuting and checking by codex on this")

The design session's file is `DESIGN-1.md`, one Fable session at max from the whole tree and both benches. Before a landing builds from it, one fresh session of the other family reads it literally against the sources and the landed tree and returns findings with receipts. It is one bounded round: findings carry no verdict and change nothing themselves; Sid carries the file back to the design session, which repairs in place, once, and commits. The shape follows `../two-chairs.md`, "How to write the prompts": fence, essence, terrain, the two riders, nothing else.

## 1. The checker (Codex, a fresh context, in the main checkout)

`````
Fence: you sit in the main checkout `/mnt/data/projects/Softland` on `main`. Read anything under `docs/below-the-waist/`, `src/app/client/`, `test/` and the git history; never `src/app/server/env.clj`. Run what you need: the pure suite (`clj -M:test -i test/app/client/path/run_pure.clj`), a single script the same way, the Node probes under both benches' `node-route/` directories from the repository root; a probe of your own runs in a detached scratch worktree, never in this checkout, and is not committed. You change no code and you do not edit `DESIGN-1.md`. What you produce is one file, `docs/below-the-waist/production/REFUTE-1.md`, committed on `main` by exact path; push is mine.

What I am building is every tool, 2D and 3D, as data above a waist of code that exists once: an engine and a few primitives below the waist, everything above it as records, so that humans and agents create, edit and reuse tools; one model for 2D and 3D in one window. The pieces both kinds share, the executor and the compositor's two operations, were designed as data by a Fable session at max in `DESIGN-1.md`, with two brushes as their first consumers: the path kind's pickup brush from bench 9 and the 3D kind's coating-reading brush on the sphere from the seam bench, each to run as a record through one executor and one compositor with the definer's numbers reproduced; the file also says what of the 3D tree moves, designs the push edge, and cuts the work into two slices. The two families have two reflexes, and I have seen both: Fable fills a gap with its own judgment, sometimes wrongly, and Codex stops where the words stop. This round is the second reflex used as a virtue: you read the design literally and hold every sentence against what the sources say and what the landed code does, before a Codex landing does what the file says and nothing it does not say.

What I want to end up holding is every place the design is wrong or short, each with its receipt: a claim about a bench, a record, a number or the client that a source line or an executed example shows false; a number the design says a test will pin that the sources do not carry, or carry differently; a named piece whose landing would have to invent work the design does not say (a field, a convention, an order of operations, a function that does not exist where the design points); a place where the design and the path slice as landed on `main` contradict each other without the design saying so; an invariant the design asserts (a transition consumes only the item, the state before, the program and the resolved reads; eager, delayed and cold reach the same bytes; an answer commits only when it belongs by value) that a walk through the bench's own code or a run of its probes breaks; a case the design's grammar cannot say that one of the definer's records needs. My rulings the design runs under are given and not re-argued (the executor, the compositor as one piece with paint and sample, Position 9, the caching rule, no trial); a finding that the design breaks one of them is a finding. What is not a finding: taste; a preference for the executor your own family landed in the slice, unless the design's form fails a record or a receipt where the landed form passes it; restated law; a verdict on the design as a whole. Every finding is the design's sentence, the source it fails against, and the receipt, and a refutation of something the design says is itself a claim that needs its taken path: "no evidence found" is not "false".

Terrain, with sizes. `docs/below-the-waist/production/DESIGN-1.md` (73 KB) is the file under check; `docs/below-the-waist/two-chairs.md` (25 KB), the production section, is the ruling behind the round and the chairs. The path kind, under `docs/below-the-waist/path-kind/`: `production/HANDOFF-1.md` (14 KB) is the landing session's account of the slice on `main` (`e8dcd37`), and the tree it left is the code the design must agree with, `src/app/client/engine/executor.cljc`, `src/app/client/path/construction.cljc` and `src/app/client/path/README.md` with `src/app/client/engine/README.md` first among them; `path-kind.md` (105 KB) holds the contract block and my rulings with the time, read at those places; `bench-9/HANDOVER.md` (43 KB) is the prototype's account with the definer's numbers, and `bench-9/node-route/` runs the pure declarations in Node (`extract.js` pulls `bench.js` from the bench's html; `regress.js` prints the pickup's texel and its dab packets; `wire.js` the checkpoint's four edits; `harness.js` the strict host); `findings-for-production.md` (6 KB) is the trace the push edge answers; `judge-claude.md` and `judge-codex.md` (42 KB each) are the comparison's receipts. The 3D kind, under `docs/below-the-waist/3d/`: `bench-2/node-route/README.md` (16 KB) is the record contract, `executor.js` (32 KB) the executor the design translates, `records.js` (8 KB) the four definer records, `sweep-and-sphere.js` (24 KB) the sphere, the charts, the bindings and `gLocate`, `probe-4.mjs` (27 KB) and `probe-2-3.mjs` (20 KB) the numbers, `cold/COLD-ACCOUNT.md` the thirteen things a cold author could not say; `3d-kind-2.md` (149 KB) is the page, read at Position 9, Position 12 and the Surface region noun; `HANDOFF-5.md` (11 KB) the last chair's account and what is owed, `HANDOFF-4.md` §6.7 the list of what moves in the tree at my word. The client: `src/app/client/engine/compositor.cljs` and `coverage.cljs`, `src/app/client/path/` and `src/app/client/region3d/` with their READMEs, the harnesses under `src/app/client/harness/`, the tests under `test/app/client/`. The client's reactive laws are the `electric-docs` skill, Missionary only; Electric is not a dependency. The design session stays open; I carry your file to it and its repairs come back on `main`.

Work the hardest case through the design as it is written, not as you would write it: the coating-reading brush on the sphere with its read withheld under budget 0 and then granted, eager against delayed against cold, an answer delivered into a reversed order and into a changed pickup, the provisional read; and the same executor on the pickup brush's checkpoint at dab 12 with its four edits; say at each step where a named piece hides work a landing would have to invent, and end on the exact open question the design cannot answer from the sources it cites. Where the design takes a position (its §11 lists ten) and you would take another, say what supports yours, when it becomes only a default, and what else changes with it.

Load turn: read what is named above, reply in one line with what you hold and anything you need to start that is not there, and wait; I set the effort and say when to begin.
`````

## 2. Sid's card

0. Before the checker starts, paste into the design session: `The path slice is on main; path-kind/production/HANDOFF-1.md is its account.` It amends `DESIGN-1.md` once against the landed tree and commits, so the checker reads the amended file. Skipping this step is fine; the checker then reports the amend's places as findings, which costs a round.
1. New Codex session in `/mnt/data/projects/Softland`. Set the effort for the load; paste section 1.
2. It replies in one line. If it names something missing, answer it. Set the effort for the check and say `Begin.`
3. It writes `production/REFUTE-1.md` and commits it on `main`.
4. Paste into the design session: `production/REFUTE-1.md is on main.` It repairs `DESIGN-1.md` in one batch, says which findings it took, which it refused and why, and commits. Then the landing pastes: the path pickup's, written from the design in `../path-kind/STARTER-production.md`'s shape, and the 3D kind's, `../3d/STARTER-production.md` §2.

**Effort** (a position, not a ruling). The load at medium, so the design enters before a judgment forms; the check at high, since a refutation's work is reading and running, not designing; xhigh only if it reports that a probe it built disagrees with a bench's own probe, then back down.
