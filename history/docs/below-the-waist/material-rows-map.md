# Material rows and kin — source map (2026-08-21)

## 0. Scope and reading key

| item | mapped fact |
|---|---|
| area | **CHECKED** — 18 current files: eleven `*_material.cljc` files plus seven kin: `facet_masters.cljc`, `verb_registry.cljc`, `activation_event.cljc`, `reply_to_block.cljc`, `matter_room.cljc`, `material_inspector.cljc`, and `material_portal.cljc`. |
| hard out | **CHECKED** — no `object_container`, `relation_kernel`, `trail_view`, or `face_arsenal` module/runtime body was opened. Where searches reached one of those boundaries, this map records the shared-file reference or external call-site anchor and stops. |
| method | **CHECKED** — each component was skeletoned with `rg -n "^\(def"`, then read through scoped source windows. Caller claims below distinguish a traced source call from namespace/test presence. |
| markings | **CHECKED** = the named source/history/search was read in this run. **INFERRED** = a bounded conclusion from a source-reference pattern; it is not runtime proof. |

## 1. Component map

| component | what it is, in one line | owns vs borrows | who calls it / what it calls | file-only replacement boundary | naming, shape, and vintage markers |
|---|---|---|---|---|---|
| **Facet material core**<br>`facet_material.cljc` | **CHECKED** — the generic compiler, floor fallback, shared/instance wear resolver, stamp maker, deviation projection, and contribution composer used by facet masters (`:1-5`, `:58-205`, `:370-535`). | **CHECKED** — owns the three envelope keys, exact material-key validation, `~i~` instance identity, wear result shape, stamp shape, and append/priority collision shape (`:9-12`, `:235-298`, `:495-535`). Borrows only CLJ/CLJS EDN readers (`:6-7`). | **CHECKED** — called by all nine registered facet specs and by server material call sites in `facet_master.clj:260-573`, `material_truth.clj:115`, `material_portal.clj:217-763`, `face_projection.clj:900-1236`, and `server_jetty.clj:412`. | **CHECKED** — not an isolated leaf: nine shared specs delegate their compile/floor/wear/stamp API to it, and the named server callers consume the same public shapes. | **LOUD — CHECKED:** `resolved-wear` resolves one shared served value; `wear-for-subject` resolves shared plus instance served values under the same “wear” idea (`:175-192`, `:370-400`). **CHECKED vintage:** added with the P3 family at `13f1dac` (2026-07-25); its source records a prior two-label floor-master divergence at `:362-366`. |
| **Facet-master registry**<br>`facet_masters.cljc` | **CHECKED** — the ordered registry and lookup indexes for nine independently revisioned facet specs (`:1-4`, `:16-64`). | **CHECKED** — owns `specs` ordering, ID/facet indexes, and facet-to-floor-label map; borrows all nine specs and `facet-material/floor-master-id` (`:5-27`, `:29-64`). | **CHECKED** — called by `matter_room.cljc:57-94`, `material_truth.clj:86-424`, `material_circulation.clj:864-876`, `material_portal.clj:191-860`, `face_projection.clj:729-1335`, `cluster.clj:364`, and `server_jetty.clj:929-1493`. | **CHECKED** — aggregate boundary: each entry is spec-shaped, but this file requires every concrete namespace and derives all indexes from one vector. | **LOUD — CHECKED:** the map label “facet masters material” does not match an actual `facet_masters_material.cljc` file. `specs` is the ordered collection; `spec` is the singular ID lookup (`:16-32`, `:45-47`). **CHECKED vintage:** the registry/core plus five new P3 specs landed at `13f1dac` around the earlier provenance spec; space, anatomy, and invocation were appended later (`:16-27`). |
| **Binding material**<br>`binding_material.cljc` | **CHECKED** — a closed gesture/site/tier row grammar plus a pure resolver, drills, and served interaction-table projections (`:1-45`, `:163-205`, `:394-425`, `:589-695`). | **CHECKED** — owns legal gestures, sites, site-supplied args, row keys, tier order, reservations, resolution, and table rows; borrows verb identity/effect/arg facts from `verb_registry.cljc` (`:52-205`). | **CHECKED** — called by attention, positioned, foldable, and space specs; `face_projection.clj:1072-1131` builds served rows; `material_portal.clj:312` reports conflicts; `verb_release.clj:144` validates a release binding. It calls `verb-registry/bindable?`, `known?`, `required-args`, and `effect-class` (`:178-198`, `:361-380`). | **CHECKED** — welded at two surfaces: four facet grammars depend on its validators/reservations, while server projections depend on its table functions. **INFERRED** — `resolve-binding` itself is source-disconnected from current production callers: the current census found only its own `drill-report` and tests (`:394`, `:598`; `test/app/binding_dispatch_test.clj:70`). | **LOUD — CHECKED:** one namespace name covers two shapes: revisioned binding rows and the pure dispatch/table algorithm over those rows. **CHECKED vintage:** P5 addition `faab5f9` (2026-07-25), with later Halo/P6/P8 vocabulary markers at `:64-67` and `attention_material.cljc:19-28`. |
| **Attention facet**<br>`attention_material.cljc` | **CHECKED** — revisioned block hit-area appearance plus the bindings that give user/machine hit areas meaning (`:1-10`, `:99-177`). | **CHECKED** — owns padding, border, background, and attention-site binding rows; borrows binding validation and all generic facet operations (`:11-12`, `:115-195`). | **CHECKED** — registered at `facet_masters.cljc:18`; bootstrap/migration references are at `cluster.clj:386-417`; `verb_release.clj:81,140` checks its master ID. Calls `binding-material` validators and `facet-material` wrappers. | **CHECKED** — spec-shaped leaf, but not path-transparent: the registry and bootstrap name the namespace and exact migration sources. | **LOUD — CHECKED:** “attention” covers both appearance values and gesture meaning in one facet (`:2-9`). **CHECKED vintage:** v0 appearance, v1 bindings, v2 strict validation, v3 reply binding (`:14-28`); file added at `13f1dac`. |
| **Positioned facet**<br>`positioned_material.cljc` | **CHECKED** — defaults for derived placement/reply birth plus the shared drag binding (`:1-10`, `:25-64`). | **CHECKED** — owns reply gap, fallback position, anchor order, persist-derived-birth flag, and one drag row copied to two hit sites; explicitly does not own settled geometry cells (`:2-4`, `:38-53`, `:87-105`). Borrows binding and facet cores. | **CHECKED** — registered at `facet_masters.cljc:20` and named in bootstrap migrations at `cluster.clj:390-421`. | **CHECKED** — same spec leaf boundary as attention; registry and bootstrap sources are explicit siblings. | **LOUD — CHECKED:** “positioned” names policy used before settled cells exist, while settled placement truth is stated as external (`:2-4`). **CHECKED vintage:** v0 defaults, v1 drag row, v2 strict validation (`:14-23`, `:45-64`); added at `13f1dac`. |
| **Foldable facet**<br>`foldable_material.cljc` | **CHECKED** — revisioned fold defaults/header copy, fold gesture rows, and later paste-clamp projection policy (`:1-8`, `:62-140`). | **CHECKED** — owns shared defaults, visible copy, binding, and paste-clamp form; per-appearance toggles remain outside it (`:2-4`, `:71-117`). Borrows binding and facet cores. | **CHECKED** — registered at `facet_masters.cljc:19`; bootstrap/migration sources at `cluster.clj:388-450`. | **CHECKED** — spec-shaped leaf with explicit registry and three bootstrap migration sources. | **LOUD — CHECKED:** one facet name covers folding behavior and outside-paste collapsed projection policy (`:71-79`). **CHECKED vintage:** v0→v3 in one file; v3 is marked smalltalk-ui-vm P2 (`:12-25`); file added at `13f1dac`. |
| **Text-body facet**<br>`text_body_material.cljc` | **CHECKED** — the two positive-integer wrap-column policies used for machine replies (`:1-35`). | **CHECKED** — owns only `wrap-floor-columns` and `wrap-fallback-columns`; raw text and source relation are external durable truth (`:2-4`). Borrows the facet core (`:5`, `:44-61`). | **CHECKED** — registered at `facet_masters.cljc:23`; no direct production source call outside registry aggregation was found in the scoped census. | **CHECKED** — thin spec leaf behind the registry; its wrapper API is identical in shape to threaded/provenance. | **LOUD — CHECKED:** the broad name “text-body” covers a narrow wrap-policy slice, not text storage (`:2-4`). **CHECKED vintage:** one grammar v0; added at `13f1dac`. |
| **Threaded facet**<br>`threaded_material.cljc` | **CHECKED** — thread-adoption distance plus later edge-rail/indent presentation policy (`:1-4`, `:13-45`). | **CHECKED** — owns adoption reach and edge presentation; thread identity and turn truth remain external (`:2-4`). Borrows the facet core (`:5`, `:69-86`). | **CHECKED** — registered at `facet_masters.cljc:22`; bootstrap advances its v1 source at `cluster.clj:439-440`. | **CHECKED** — spec leaf with one registry slot and one explicit bootstrap migration source. | **LOUD — CHECKED:** “threaded” covers shared adoption/render policy, not thread identity (`:2-4`). **CHECKED vintage:** frozen v0 plus explicit v1 edge grammar (`:7-11`, `:43-45`); added at `13f1dac`. |
| **Space facet**<br>`space_material.cljc` | **CHECKED** — revisioned zoom bounds plus the bindable place/marquee subset of space input (`:1-7`, `:18-45`). | **CHECKED** — owns zoom and two material-bindable meanings; borrows the two rows from `binding-material/space-floor-bindings` and refuses camera/meta captures (`:25-36`, `:63-86`). | **CHECKED** — registered at `facet_masters.cljc:21`; its binding predicates call `binding_material.cljc`. No direct production source call outside registry aggregation was found in the scoped census. | **CHECKED** — not independent of binding material: its default form is derived from binding floor rows and its grammar calls two reservation predicates. | **LOUD — CHECKED:** the durable subject is string `"space"` while dispatch/facet ownership is keyword `:space` (`:11-15`, `:88-90`). **CHECKED vintage:** v0 zoom, v1 bindings; added later at `f7945fd` (2026-07-26). |
| **Provenance facet**<br>`provenance_material.cljc` | **CHECKED** — revisioned tint plus append/priority composition policy for provenance contributions (`:4-40`). | **CHECKED** — owns tint, merge, and priority only; borrows the facet core (`:1-2`, `:18-28`, `:63-80`). | **CHECKED** — registered at `facet_masters.cljc:17`; explicit bootstrap and serve call sites are `cluster.clj:343-457` and `face_projection.clj:1589-1645`. | **CHECKED** — spec leaf, but bootstrap and face projection name its spec/master directly in addition to registry aggregation. | **LOUD — CHECKED:** “provenance” here is a render/composition facet, not the full provenance record system; its owned keys are exactly tint/merge/priority (`:10-22`, `:30-40`). **CHECKED vintage:** first of the 18, added at `83aa5dd` (2026-07-24); v0 remains accepted while v1 is floor (`:35-39`). |
| **Invocation facet**<br>`invocation_material.cljc` | **CHECKED** — revisioned model, effort, and bounded `thread+N` precontext policy (`:1-7`, `:16-55`). | **CHECKED** — owns those three values and invocation-specific teaching errors; persistence/transport are explicitly external (`:4-7`, `:74-111`). Borrows EDN and facet core. | **CHECKED** — registered at `facet_masters.cljc:27`; `server_jetty.clj:404-551` resolves its shared/instance wear; `reply_to_block.cljc:20,90` reads precontext depth/vocabulary. | **CHECKED** — spec leaf with two direct consumers beyond the registry; reply narrowing is coupled to its precontext vocabulary. | **CHECKED vintage:** one v0 grammar, added last among the facet specs at `685f2fc` (2026-07-31). |
| **Anatomy facet**<br>`anatomy_material.cljc` | **CHECKED** — a flat, closed block-anatomy grammar that can edit rows, compile them to assembly data, and build apply data (`:1-8`, `:613-789`, `:795-965`). | **CHECKED** — owns part/view/presence vocabularies, candidate-edit grammar, assembly conversion, and apply-data composition; borrows the facet core (`:9-11`, `:725-789`, `:945-956`). | **CHECKED** — registered at `facet_masters.cljc:26`; current production source calls from `material_portal.cljc:403-407` use `form-for-wear`, `expand-parts`, and `invocation-candidate-form`. **INFERRED** — `assembly-for`, `apply-data`, and edit functions are source-disconnected from production at current HEAD: the census found test callers only (`test/app/anatomy_test.clj:66-74,194-226`). | **CHECKED** — welded to the facet envelope and portal composition; its assembly/apply sub-surface currently has only test callers in the source census. | **LOUD — CHECKED:** one `:part/stamp` field accepts either a single stamp row or a nonempty map of named stamp rows (`:173-186`); compilation emits singular `:stamp` or plural `:stamps` accordingly (`:830-840`). **CHECKED vintage:** later smalltalk-ui-vm addition `9250e13` (2026-07-30); invocation parts remain candidate-only (`:543-611`). |
| **Verb registry**<br>`verb_registry.cljc` | **CHECKED** — the closed declaration registry for verb versions, effect classes, continuations, required args, reservations, releases, and descriptive source (`:1-31`, `:34-415`). | **CHECKED** — owns declarations and their deterministic table rows; borrows only string normalization (`:32`, `:317-415`). It does not own implementations: binding material names declarations, not code (`:5-9`). | **CHECKED** — called by binding validation/resolution, `verb_release.clj:137`, `material_portal.clj:361-364`, and `face_projection.clj:1125`. | **CHECKED** — welded to binding grammar by version/name/required-arg checks; release identity is also duplicated by value in reply-to-block. | **LOUD — CHECKED:** the concept label “verb registry material” has no `verb_registry_material.cljc` file. The reply verb’s release-ref bytes exist here and separately in `reply_to_block.cljc:13-15`. **CHECKED vintage:** P5 addition `faab5f9`; comments retain a v1 “accept-then-no-op” historical behavior at `:296-315`. |
| **Activation event grammar**<br>`activation_event.cljc` | **CHECKED** — a closed activation/rollback/pin/unpin event form, deterministic serializer, legacy parser, and causal-trail helpers (`:1-43`, `:52-213`, `:268-314`). | **CHECKED** — owns event/scope/ground vocabularies, exact validation, v0 compatibility labels, and clock-regression rows; borrows EDN/string readers only (`:44-46`). | **CHECKED** — called by `matter_room.cljc:145-203`, `material_truth.clj:193-370`, `material_circulation.clj:258,841`, `material_portal.clj:740`, and face/facet call sites (`face_projection.clj:1159-1319`; `facet_master.clj:242-730`, boundary only). | **CHECKED** — standalone value grammar at the namespace boundary, but exact serialized bytes are consumed by multiple server writers/readers. | **LOUD — CHECKED:** “activation event” is one read shape for two source generations: legacy bare revision strings are parsed and marked `:activation/v0?`; current sources are six-key closed forms (`:31-37`, `:74-80`, `:205-213`). **CHECKED vintage:** P6 addition `be247c5` (2026-07-25). |
| **Reply-to-block seam**<br>`reply_to_block.cljc` | **CHECKED** — pure addressed reply request/narrowing/prompt composition for the resident reply verb (`:1-15`, `:35-93`). | **CHECKED** — owns addressed request shape and narrowed portal payload; borrows invocation precontext depth/vocabulary (`:10`, `:17-50`, `:90`). Owns no effects (`:5`). | **CHECKED** — `server_jetty.clj:432,613` calls narrowing/prompt composition; `verb_release.clj:23,74-75` consumes identity constants. **INFERRED** — public `request` is source-disconnected from production at current HEAD: the census found test calls only (`test/app/reply_to_block_test.clj:73,91`). | **CHECKED** — narrowing is coupled to invocation material; verb identity is coupled by duplicated values to the registry/release chain. | **LOUD — CHECKED:** no `reply_to_block_material.cljc` file exists. A missing addressed wearer makes private `bounded-wearers` return `[]` (`:21-33`), which public narrowing carries as empty `:wearers`/`:master-ids` under `:narrowed? true` (`:35-50`). **CHECKED vintage:** P8 addition `15497df` (2026-07-26). |
| **Matter room**<br>`matter_room.cljc` | **CHECKED** — pure room addressing, matter-act normalization, and deterministic resident projection (`:1-49`, `:57-285`, `:416-534`). | **CHECKED** — owns master↔room tables, act cards, resident identity/text/order/placement; borrows registered master IDs and activation validation (`:50-59`, `:124-285`). | **CHECKED** — `server_jetty.clj:430,755-1024` calls room narrowing, residents, and act normalizers; server portal/face call sites use room IDs and residents (`material_portal.clj:697-699`; `face_projection.clj:730,1650`). Calls `facet-masters` and `activation-event`. | **CHECKED** — welded to registry inventory, activation grammar, portal-result keys, and Jetty’s birth/edit driver shape. | **LOUD — CHECKED:** no `matter_room_material.cljc` exists. `:resident/time-ms` is explicitly a composition ordinal, not a time (`:42-49`). `residents` returns `[]` when `:portal/master-id` is absent or non-string (`:496-500`). `room-id` derives on JVM and throws on CLJS (`:61-72`). **CHECKED vintage:** P1–P4 markers in source; added at `7a3127c` (2026-07-28). |
| **Material inspector**<br>`material_inspector.cljc` | **CHECKED** — deterministic extraction/normalization of material contribution stamps and wearer snapshots, plus canonical EDN helpers (`:1-6`, `:8-184`). | **CHECKED** — owns seven-key stamp recognition, current-scene wearer projection, and canonicalization; borrows string helpers and the producer shapes of scene-store slots/stamps (`:8-27`, `:125-157`). | **CHECKED** — `scene_store.cljc:684` calls `contribution-stamps`; `face_projection.clj:1431-1489` calls normalization/canonicalization; `material_portal.clj:706` normalizes wearers. **INFERRED** — `wearers-from-scene-store` has no production source caller in the current census; scene store calls only stamp extraction. | **CHECKED** — dependency-light namespace, but its accepted input shape is welded to facet stamps and `scene_store`’s `:slots/:ground-block` keys. | **LOUD — CHECKED:** it defines public `canonicalize`/`canonical-edn` independently of material portal (`:159-184`). **CHECKED vintage:** P2 addition `c6bb5d8` (2026-07-24), before the generic facet family. |
| **Material portal (pure half)**<br>`material_portal.cljc` | **CHECKED** — the question data, anchor helpers, canonical bytes, anatomy composition, recipe, render model, briefing, and question rows for a portal projection (`:1-30`, `:38-120`, `:150-372`, `:394-480`, `:881-1010`). | **CHECKED** — owns question/card order, anchor sentinels, portal-local canonicalization, rendering/briefing models; borrows anatomy row expansion for composition and string helpers (`:29-30`, `:394-447`). | **CHECKED** — server organ `material_portal.clj:644-1098` calls its helpers/render/briefing; `face_projection.clj:1809-1834` canonicalizes and transports it. It calls anatomy `form-for-wear`, `expand-parts`, and `invocation-candidate-form` (`:403-407`). | **CHECKED** — pure projection boundary, but anatomy shape is explicit in composition and two server callers consume its exact result/byte/card shapes. | **LOUD — CHECKED:** same public `canonicalize`/`canonical-edn` idea as inspector, but portal’s JVM `canonical-edn` additionally pins `*print-namespace-maps* false` (`:332-366` vs inspector `:159-184`). `unanswered` returning `[]` means every question path is present (`:310-326`). **CHECKED vintage:** P7 addition `04ae2df` (2026-07-25), later widened for matter-room/anatomy. |

## 2. Loud collision ledger

| collision | exact fact |
|---|---|
| **FILE FAMILY NAME ≠ FILE ROLE** | **CHECKED** — only eleven files match `*_material.cljc`. Seven “kin” files do not. Five concept labels in the older map can be mechanically misread as nonexistent filenames: `facet_masters_material`, `verb_registry_material`, `matter_room_material`, `activation_event_material`, and `reply_to_block_material`. |
| **THE SAME `[]` MEANS THREE DIFFERENT THINGS** | **CHECKED** — `reply_to_block/bounded-wearers`: no addressed wearer in the supplied snapshot (`reply_to_block.cljc:21-33`); `matter_room/residents`: no string `:portal/master-id` (`matter_room.cljc:496-500`); `material_portal/unanswered`: no unanswered question (`material_portal.cljc:310-326`). The first two are absent input/identity; the third is complete coverage. |
| **TWO CANONICALIZERS, SAME PUBLIC NAMES, DIFFERENT BYTE FENCE** | **CHECKED** — inspector and portal both define `canonicalize` and `canonical-edn` with the same recursive map/set/collection shape. Portal additionally binds `*print-namespace-maps* false` on JVM; inspector does not (`material_inspector.cljc:159-184`; `material_portal.cljc:332-366`). |
| **ONE STAMP FIELD, TWO INPUT SHAPES, TWO OUTPUT KEYS** | **CHECKED** — anatomy `:part/stamp` accepts one row or a nonempty keyword-keyed map of rows; assembly props become `:stamp` or `:stamps` (`anatomy_material.cljc:173-186,830-840`). |
| **`time-ms` THAT IS NOT TIME** | **CHECKED** — matter-room `:resident/time-ms` is a composition ordinal for deterministic room order, explicitly not a moment (`matter_room.cljc:42-49,490-493`). Matter act `:time-ms` remains an integer request time (`:133-157`). |
| **SPACE STRING VS SPACE KEYWORD** | **CHECKED** — instance lanes use subject string `"space"`; facet/dispatch ownership uses keyword `:space` (`space_material.cljc:11-15,88-90`). |
| **ONE REPLY VERB IDENTITY DECLARED TWICE** | **CHECKED** — `reply_to_block.cljc:13-15` owns the verb name/version/release-ref constants; `verb_registry.cljc` carries matching declaration values; `verb_release.clj:23,74-75` re-exports/consumes the reply constants. |
| **BROAD FACET NOUNS, NARROW OWNERSHIP** | **CHECKED** — “positioned” owns pre-cell defaults, not settled cells (`positioned_material.cljc:1-4`); “text-body” owns wrap columns, not text (`text_body_material.cljc:1-4`); “threaded” owns adoption/edge policy, not thread identity (`threaded_material.cljc:1-4`); “provenance” owns tint/merge/priority in this file (`provenance_material.cljc:10-22`). |

## 3. One system or several generations?

| question | factual answer |
|---|---|
| one system, or generations side by side? | **CHECKED — several generations and component kinds sit side by side inside one connected current subsystem.** The 18 files are not one uniform “row” shape: nine are `facet-material` specs; the other nine are a generic core, a binding grammar/resolver, two registries, an event grammar, a reply seam, a room composer, and two projection helpers. Explicit imports/call sites connect those kinds. |

| evidence class | checked evidence |
|---|---|
| shared shapes | **CHECKED** — nine specs are aggregated at `facet_masters.cljc:16-27`. Each has `fm:…` identity, grammar map, floor form/revision, and generic compile/floor/wear surface; `facet_material.cljc:58-205` supplies their common result and stamp shapes. |
| divergent shapes | **CHECKED** — binding rows have five keys (`binding_material.cljc:12-20,167-172`); activation events have six different keys (`activation_event.cljc:9-16,74-80`); reply requests are addressed turn payloads (`reply_to_block.cljc:57-80`); room residents are ordered projection rows (`matter_room.cljc:486-534`); inspector/portal consume projections rather than declare facet specs. |
| connected call graph | **CHECKED** — facet specs converge through `facet_masters` into material truth/serve/portal call sites; binding calls verb registry; matter room calls registry + activation; reply calls invocation; portal calls anatomy; inspector consumes facet stamps. The external call-site anchors are recorded per component above. |
| source-disconnected branches, static only | **INFERRED** — current source references leave `binding-material/resolve-binding` behind its drill/tests, anatomy assembly/apply/edit behind tests, `reply-to-block/request` behind tests, and `material-inspector/wearers-from-scene-store` without a production caller. This is a source-reachability pattern, not runtime proof and not a removal claim. |
| vintage sequence | **CHECKED** — additions span eight days and named build generations: provenance P1 `83aa5dd` (07-24) → inspector P2 `c6bb5d8` (07-24) → generic facet family P3 `13f1dac` (07-25) → binding/verb P5 `faab5f9` → activation P6 `be247c5` → portal P7 `04ae2df` → reply P8 `15497df` (07-26) → space `f7945fd` (07-26) → matter-room `7a3127c` (07-28) → anatomy `9250e13` (07-30) → invocation `685f2fc` (07-31). |
