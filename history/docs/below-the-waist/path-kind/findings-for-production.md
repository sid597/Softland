# Findings for the production buildout — the path kind

Sid, 2026-09-06, on a fix the builder had started mid-judging: "no this is a finding note it down so when we start doing the production buildout work it gets there". The comparison branches are the lab; findings are recorded here with their receipts and the fix's shape, and the production buildout picks them up. Each finding says what was measured, where the receipt is, what the fix is, and whether anything landed. Nothing here is a ruling. On `main` since `cd4aecf` with the measurement page and the two trace receipts it cites. Read it in the order of Sid's rule (`../two-chairs.md`, the production section): the underlying-thing parts first (finding 1's push edge, finding 2's pan fraction, finding 3's key by content, finding 4's representation, each pinned by the judges' counterexamples), and the layers on top (the item cache, the bucket band and the spread, progressive opening, the undo LRU) only after a trace shows the need.

| # | Finding | Receipt | The fix, as designed | State |
|---|---|---|---|---|
| 1 | **A one-record edit pays for every unchanged item.** The frame loop rebuilds and compares every item's rows and rereads every construction's reads on any changed frame: about 22 µs per item. A colour edit of one record costs 8 ms at 400 items and 35 ms at 1,600, with no run and no pack. | `receipts-trace-client-claude.json`, colour-edit rows; `measurements-client-claude.md` | Cache each item's entry and rows under its key (`frame/item-key`: identity, revision, group slot, bucket, and scale or pan only when read). Reuse both as the same objects while the key and the atlas's epoch stand, so the pool's equality check is an identity check; the atlas gains an epoch that advances only on a compaction (a regrow keeps slot positions). Prune the run and pack caches only when the set of item ids moved. The revision becomes the caller's word that same-revision means same-content, which the frame-level early-out already assumed. Expected: an edit costs what changed plus the keys, whose floor is 1.2 ms at 1,600. A push edge (the caller naming changed records) removes the walk entirely; this is where the reactive question lands. | Designed and drafted on the Claude branch, built clean, reverted unmeasured at Sid's word. Not landed. |
| 2 | **Records that read the view rerun on every pan.** A snapped border with a device-unit width reruns its construction per pan or zoom at about 0.3 ms; 266 of them cost 79 ms per pan. | Same receipt, pan rows | Two parts. (a) Snapping depends only on the fractional part of the pan in device pixels: round(x·s + p) − p with p = n + f gives round(x·s + f) − f. Expose `view.pan-fraction` in the scope and bind the snap step to it; an integer-pixel pan then reruns nothing. (b) A device-unit width reads the scale and must rerun per zoom; rerun only the visible ones, or carry the device width into the placement. | Not landed. |
| 3 | **A zoom across a power-of-two bucket repacked every region.** 350 ms at 400 records, 1,394 ms at 1,600. The pack key carried the bucket for every region, though a region without cubics (every stroke's skin) lowers identically at every tolerance. | `receipts-trace-before-pack-fix.json` versus `receipts-trace-client-claude.json`, zoom-across rows | Packs keyed by the region's content and rule with the buckets inside the entry; a cubic-free region packs once under `:all`. After: 46 ms and 184 ms, the remainder being cubic fills and the borders' reruns. Further: pack cubic regions for a band of three buckets at the band's finest tolerance (quads grow by the cube root, about 1.6× for a 4× range), and spread repacks over frames. | The first step landed on the Claude branch (0c57fdd) with the harness's rates check updated. The band and the spread are not landed. |
| 4 | **The curve count per stroke is the CPU's cost.** The envelope flattens the centerline at a fixed tolerance, so the draw tool's skin is 421 curves at every zoom; 1,600 records put 566,239 curves in the atlas. Packing costs 0.5 ms per region; the GPU absorbs the count at 2 to 8 ms a frame. | Same receipts, the curves column and packing-alone times | The offset stroker: keep curves as curves, offset the cubics, so a stroke's skin is tens of curves. This is the one fix that changes the bench's numbers; the nib's meaning (the external tangent of the end discs) is pinned by tests. | Not landed. On the definer's list since attack 1. |
| 5 | **The GPU has room.** On the AMD RDNA 3 adapter, 2 to 8 ms a frame with half a million curves; 18 ms on the frame that uploads them. No finding moves work to the GPU. | Same receipts, GPU column | None needed now. Re-measure after 4, when curve counts fall, and on the shipped target sizes. | |
| 6 | **The first frame is the whole scene's geometry plus packing.** 3.1 s at 1,600 records: 1.05 s geometry, 0.98 s packing, the rest rows and uploads. | Same receipts, first rows | A document opens progressively: run and pack across frames by priority (visible first), draw what is ready. Also cut by 4. | Not landed. |
| 7 | **The run cache holds one result per material id**, so undoing an edit reruns. | `HANDOFF-client-claude.md` judgment calls; the rates receipt's restore rows | An LRU per revision if undo thrash shows in use. | Not landed. |
| 8 | **Cover-cell thresholds are guesses.** Cells switch on when the box exceeds 256² device pixels and 2,048 px per curve. | `path/renderer.cljs` `cover-options` | Measure on a long thin stroke at high zoom with fragment counts. | Not measured. |

How to reproduce any number here: `measurements-client-claude.md` carries the commands (`timing.clj` on the JVM; `RENDER_VERIFIER_HARDWARE=1 node test/render_engine/dump_result.mjs <out>` for the trace on the real adapter).
