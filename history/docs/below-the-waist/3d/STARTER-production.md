# 3D kind — the starters for its first production slice, and the design session both kinds share (2026-09-07, written ahead)

Written at Sid's word, 2026-09-07 after midnight IST: "since we are here i think we should also write the production prompt for 3d i will not start but it seems the right place to write up a new prompt for it given all the context that we have here". The chairs and the order are his rulings in `../two-chairs.md`, the production section; the pastes point at it and restate only his rulings, in his first person.

**May start while the path slice lands** (Sid, 2026-09-07: "should i not even start the claude one that will not change any code"). The design depends on that slice in three named places and nowhere else: the executor's landed form, which is the Codex worktree's `engine/executor.cljc` today; the edit boundary as Codex lands it, on which the push edge sits; and the names of the callers and files the slice changes. The first is readable now, the other two arrive with the slice's handoff, so the session designs now and amends its file once when Sid pastes that `../path-kind/production/HANDOFF-1.md` exists. Its one file is under `docs/below-the-waist/production/`, in nobody's way.

**Why one design session for both kinds.** The executor and the compositor exist once; both benches converged on them (the path ledger's rulings of 2026-09-06 11:27 and 11:51, carried on the 3D ledger; the seam bench's executor is bench 9's shape). Two designs, one per kind, would be two waists. So the design session designs the shared pieces in the client with both kinds' brushes as the first consumers, says what of the 3D tree must move for the sphere brush to run there and no more, and says whether the two brushes land as one slice or two. Its output is one file, `docs/below-the-waist/production/DESIGN-1.md`, on `main`. Section 2 is the 3D kind's landing from it; the path kind's pickup landing, if the design cuts it apart, is written from the design when it is on disk, in the same shape.

## 1. The design session (Fable at max; the load turn at low)

`````
Fence: you sit in the main checkout `/mnt/data/projects/Softland` on `main`; read anything under `docs/below-the-waist/`, `src/app/client/`, `test/` and the git history; never `src/app/server/env.clj`. You commit no code. What you produce is one file, `docs/below-the-waist/production/DESIGN-1.md`, plain commits on `main` by exact path; push is mine. A Codex session is landing the path kind's first production slice on `main` while you work; it changes `src/app/client/engine/executor.cljc` (and deletes `engine/expression.cljc`), `src/app/client/path/` (`component.cljc`, `records.cljc`, `stroke.cljc`, `frame.cljc`, `renderer.cljs`), `src/app/client/harness/path.cljs`, the path renderer's callers under `src/app/client/region3d/` (`on_plane.cljc`, `on_plane_renderer.cljs`) and the tests and goldens under `test/`; nothing else. The landing sessions that build from it are Codex sessions in fresh contexts; a landing does what your file says and nothing it does not say, so your file says everything. You stay open after the file is on disk: a landing's design question comes to you through me.

What I am building is every tool, 2D and 3D, as data above a waist of code that exists once: an engine and a few primitives below the waist, everything above it as records, so that humans and agents create, edit and reuse tools; one model for 2D and 3D in one window, fine-grained reactivity over data and mouse actions; click in, "you hit this" out; the store above the kind owing it stable identity and fine-grained diffs. Two kinds have been explored to the close of their phase B on their benches, and the definer's records pass on both. The path kind's first production slice is landing on `main` as you work: the base consolidated, the capsule nib, the path value as the renderer's input with the recipe run at the edit boundary, one expression language in the shared executor. When I paste that it is on `main` with its handoff, re-read what it changed and amend your file; the file is done when it names the tree as it stands. The 3D kind's bench runs a coating-reading brush on a sphere through an executor over records: a read may return pending, the run suspends into a continuation of bytes with no next state, resumes under a grant to the same bytes, and refuses an answer that does not belong to its request. I ruled the pieces both kinds share. Behaviour as data runs on the executor, records over a vocabulary of capabilities, an ordering rule and compiled expressions. The compositor is one piece with two operations, paint and sample, owning clips, blends, layers, the screen and the surface a brush reads, painting and vector sharing it. And for the 3D kind, Position 9: the kind takes the recipe as its input and keeps it with the result; a recipe is a record over a vocabulary of capabilities with formulas in the leaves, one executor and several runners, tools never in git, capabilities code with receipts, solvers as capabilities; nothing in a record is a program, and foreign code enters only as a value in a slot with a declared output. The exits are on the page and are not re-asked.

What I want to end up holding is the design as data, as the production section of two-chairs describes the design session's output, for the pieces both kinds share in the client, the executor and the compositor, with the two brushes as their first consumers: the path kind's pickup brush from bench 9 and the 3D kind's coating-reading brush on the sphere from the seam bench, each running as a record through one executor and one compositor with the definer's numbers reproduced; what of the 3D tree must move for the sphere brush to run there, and no more; the push edge where a caller names what changed, which the path slice left to you; and whether the two brushes land as one slice or two, with the reason. Not a contract and not a test suite before the code.

I ruled three things after the comparison. Caching is never an option; it means the underlying thing is as it should be and then still needs more performance, therefore caching goes on top: first each level of the data flow is a pure function whose inputs are explicit as data, a key is the level's full input as a value and never what a run happened to read and never a hash, and every layer on top waits for a trace that shows the need. There is no trial; this is production, judged on its delivery. The two families have two reflexes, Fable fills a gap with its own judgment and Codex stops where the words stop, so you design and Codex lands, and your file carries my caching rule in words. Our aim is not to copy anyone; the best in the field is a datapoint; nothing existing is carried forward because it exists, and that includes the 3D tree as it stands.

Terrain. `docs/below-the-waist/two-chairs.md`, the production section, is the ruling: the chairs, the order, the design session's output, the fence sentence, one rule per reflex. The path kind, under `docs/below-the-waist/path-kind/`: `STARTER-production.md` section 1 is the paste the landing session holds and says what the slice lands, and `production/HANDOFF-1.md` will be its account when I say it exists; the three pieces it ports are in the worktree `../Softland-codex` (`waist/path-codex` at `07ea014`) at `src/app/client/engine/executor.cljc`, `src/app/client/path/geometry.cljc` and `src/app/client/path/component.cljc` with `records.cljc`, and the base it ports them into is `../Softland-claude` (`waist/path-claude` at `e8cb946`) until the merge lands; `src/app/client/path/README.md` and `src/app/client/engine/README.md` are the tree's own maps; `bench-9/HANDOVER.md` and `bench-9/node-route/` are the prototype's record vocabulary with the definer's records and numbers, the pickup brush among them (the texel (0.013, 0, 0.987, 1), red after blue); `path-kind.md` holds the picture, the contract block (what a record declares, what the kind takes and returns, the compositor's two operations) and my rulings with the time; `judge-claude.md` and `judge-codex.md` name what neither lane built, the executor loop with carried state and the compositor's paint and sample. The 3D kind, under `docs/below-the-waist/3d/`: `HANDOFF-5.md`, with `HANDOFF-4.md` and `HANDOFF-3.md` §3 for the standing positions, §6 for what is owed, and `HANDOFF-4.md` §6.7 for the composer's list of what must move in the tree, at my word only; `3d-kind-2.md`, the page (live as `3d-kind-2.html` at https://claude.ai/code/artifact/400dcba1-c53c-4eb2-bba8-7978dee838d7 ), with Position 9's three sentences and exits, Position 12's receipt and obligation, the Surface region noun and the closing section; `bench-2/seam-bench.html` (live at https://claude.ai/code/artifact/32169816-ae1c-4bbb-ba07-a9171eea419a ) with `bench-2/HANDOVER.md`; `bench-2/node-route/README.md` is the record contract, `executor.js` and `records.js` the executor over the bench's records as the bench embeds them, `probe-4.mjs` and `probe-2-3.mjs` the definer's numbers, `cold/COLD-ACCOUNT.md` what a cold author could not say from the contract, three of them kept for production (a region as a clip on a read and region algebra; a painting that covers a region; a painting named mid-event); `fact-base-2.md` Part 16 is the last fold's receipts; `attack-1.md` to `attack-4.md` are the definer's. The client: `src/app/client/region3d/` with its README, and `HANDOFF-3.md` §3 item 12 for the code facts (the region row a portal with the world inline, pick meshes only, a static edit a full rebuild, the placement ray meeting local z = 0 only, the mesh kind dispatch closed); `src/app/client/engine/compositor.cljs` as the compositor stands today; `src/app/client/engine/executor.cljc` as the path slice left it; `src/app/client/harness/region.cljs` as the 3D harness. The client's reactive laws are the `electric-docs` skill, Missionary only; Electric is not a dependency. Memory holds the proven roads for driving the app and for a WebGL2 bench headless.

Work the hardest case through whatever you design: the coating-reading brush on the sphere, its read pending under a withheld binding and then granted, its state the same whether the grant came before or after the read was demanded, and the same executor and compositor carrying the 2D pickup brush; say where a named piece hides work the landing session would have to invent; end on the exact open question the slice leaves. Where you take a position, say what supports it, when it becomes only a default, and what else changes with it.

Load turn: read what is named above, reply in one line with what you hold, and wait; I set the effort for the design turn.
`````

## 2. The landing session for the 3D kind — slice B (Codex, a fresh context; after slice A and `path-kind/production/HANDOFF-2.md` are committed)

`````
Fence: you sit in the main checkout `/mnt/data/projects/Softland` on `main`, one branch. Code under `src/app/client/` and, in scope for you, the tests and goldens under `test/`; never `src/app/server/env.clj`. Read anything under `docs/below-the-waist/`. Commit on `main` by exact path, one commit per milestone, each big enough to read as one change; push is mine. The work-package skill and its contract-and-gate process are not used for this work; where an older handoff points at them, this supersedes it.

What I am building is every tool, 2D and 3D, as data above a waist of code that exists once, so that humans and agents can create, edit and reuse tools as records. The design for this slice is `docs/below-the-waist/production/DESIGN-1.md`, written by a Fable session at max from the whole tree, both benches and both rounds' records; you own the complete implementation of the 3D kind's part of it as the design cuts it: the algorithms, every caller it names, the tests built with the code, the hierarchy of documentation from the READMEs down to the docstrings changed in the same commits, the repairs, and the handoff. Where the design is wrong or silent you say so before you build around it, in the commit and in the handoff, and you keep the right to bring the better construction or the simpler boundary; the design session stays open for a design question, and I carry it.

I ruled three things. Caching is never an option; it means the underlying thing is as it should be and then still needs more performance, therefore caching goes on top: a key is a level's full input as a value, never what a run happened to read and never a hash, and no memo, bucket or atlas is added, and none that exists is credited, without a trace that shows the need. The repo's own verification surface is the acceptance surface, necessary and not sufficient, and a red check is reported as red before anything else is claimed; a green flag that reads no pixel is not a picture. Every claim in a README or a handoff points at the test that shows it or is marked untested. Our aim is not to copy anyone; the best in the field is a datapoint; nothing is carried forward because it exists. The definer's records are what the implementation is measured by; the definer works new constructions through what you land as it lands, and I carry them to you.

Terrain. `docs/below-the-waist/two-chairs.md`, the production section, is the ruling behind this slice, and `docs/below-the-waist/production/DESIGN-1.md` is the design. Under `docs/below-the-waist/3d/`: `bench-2/node-route/README.md` is the record contract the bench's executor runs; `records.js` and `probe-4.mjs` carry the definer's brush and its numbers, the coating-reading brush's bytes and carry, the reach region's saturation at πR, the contact event; `bench-2/HANDOVER.md` is the bench's account and `bench-2/seam-bench.html` the bench; `3d-kind-2.md` is the page with my rulings on its ledger; `HANDOFF-5.md` is the last chair's account with what is owed. Under `docs/below-the-waist/path-kind/`: `production/HANDOFF-2.md` is the account of slice A immediately before yours: the shared executor and CPU compositor, the pickup record, the push edge, executed receipts, design departures and remaining limits; read it before the design's §3. `production/HANDOFF-1.md` is background for the earlier path-value slice. The executor's pending-read branch is your first milestone, as DESIGN-1 §9 specifies; keep slice A's pickup and push receipts passing as you extend it. `src/app/client/path/README.md` and `src/app/client/engine/README.md` map the shared pieces as they stand. `src/app/client/region3d/README.md` is the 3D tree's map. The harness runs and dumps as the READMEs say; the numbers and pictures are files it writes. The client's reactive laws are the `electric-docs` skill, Missionary only; Electric is not a dependency. Your handoff lands as `docs/below-the-waist/3d/production/HANDOFF-1.md`.

Work the hardest case the design names through what you build before you call the slice done, and say where the design hid work you had to invent; end on the exact open question you leave. Where you departed from the design, say what supports the departure, when it stops holding, and what else changes with it.

Load turn: read what is named above, reply in one line with what you hold and anything you need to start that is not there, and wait; I set the effort and say when to begin.
`````

## 3. Sid's card

1. Any time, the path slice's Codex session running or not. New Claude session in the checkout, `/effort low`, paste section 1.
2. It replies in one line. `/effort max`, say `Begin.` It writes `production/DESIGN-1.md`, commits, and says so; leave the session open.
3. When the path slice is on `main` and `path-kind/production/HANDOFF-1.md` exists, paste into the design session: `The path slice is on main; path-kind/production/HANDOFF-1.md is its account.` It amends its file and commits; if it names a design problem in the slice, that goes to the path definer.
4. New Codex session in the checkout. Set the effort for the load; paste section 2. It replies in one line; if it names something missing, answer it or carry it to the design session. Set the effort for the build and say `Begin.`
5. It lands, commits per milestone, reports red first, writes `3d/production/HANDOFF-1.md`. Its design questions go to the open design session; its answers come back as a paste.
6. When it says done: the definer against the landing (section 4), then one judge per family (section 5).

**Effort** (a position, not a ruling; nothing on this project has measured it). The design session: low for the load, max for the design turn, the split the boot memory describes. The landing: medium for the load; xhigh for the build, for the same reason as the path slice's card, since the sphere brush's landing moves parts of a 3D tree its family did not write; max only for a turn where it reports a design problem, then back.

## 4. The definer, against the landing

The definer's paste is the second block of `STARTER-5.md`, with its first sentence read as: the production slice of the 3D kind is being landed on `main` from `production/DESIGN-1.md`, under `../two-chairs.md`'s production section; work your records and new constructions through what lands as it lands, every finding a record with a number, in this directory as before. Phase B's guards stand: your programs first, as shipped, then the landed client.

## 5. The judges, at the slice's end

One fresh session per family, the same paste, `../path-kind/STARTER-judge.md`'s shape with this kind's terrain in place of the path kind's: the card in `two-chairs.md`, a receipt behind every mark, a difference settled on an executed example. Written when the slice closes, with the hash it froze at.
