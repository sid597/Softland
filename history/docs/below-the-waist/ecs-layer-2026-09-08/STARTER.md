# STARTER — the ECS layer's builder (Sid's own words, spliced 2026-09-08)

One complete prompt, ready to paste as the first message of a fresh Codex
session. Its worktree exists: `/mnt/data/projects/Softland-ecs-layer`, branch
`codex/ecs-layer`, off main at the commit that carries `PLAN.md`. The shape is
the path builder starter's (`docs/below-the-waist/path-kind/STARTER-client.md`):
preflight, fence, the job, what I am building, how to read the file, how I
work, terrain as names. A Fable lane, if Sid wants one, takes the same prompt
with `fable, effort max` and its own worktree.

## Codex builder — paste whole

```
preflight: permission mode and toggles set before this first prompt, nothing mutates mid session. codex, effort high.

Fence: you are in your own git worktree /mnt/data/projects/Softland-ecs-layer on branch codex/ecs-layer, off main at the commit that carries the plan. Code under src/ and test/ is yours to read and change, and read whatever you need to; never src/app/server/env.clj. The server keeps its shape through C6; C7 stops for my word before any server change. Commit on your branch with exact paths, one commit per component; push and merge to main are mine. Attached: docs/below-the-waist/ecs-layer-2026-09-08/PLAN.md.

The job: build the eight components in section 4 of that file, in order, starting at C0. A component is done when I can open it in a browser and see and do what its row says; its receipt PNG and trace file are written under target/place/ by the drive script; its tests are green under the repo's own lanes (the pure lane in test/app/test_runner.clj, and the drive script); the READMEs and docstrings of the namespaces it touches say what is implemented and how, and every claim in them points at the test that shows it or is marked untested; and the checkpoint table at the end of the file carries the commit and one line on what changed in shape. A red check is reported as red before anything else is claimed. When a component has to change shape, change the file in the same commit as the code. Where the file is silent and the choice is small, decide, and say in the README that you did; where it is large, or you have tried everything and cannot build forward, write docs/below-the-waist/ecs-layer-2026-09-08/ASK-N.md with what you were going to build, what you tried, where you are, and the one missing piece that needs my approval, and stop there.

What I am building is every tool, 2D and 3D, as data above a waist of code that exists once, so humans and agents can create, edit and reuse the tools themselves, in one window where I think through different representations of the same subject. The first subject is Softland itself: its code, how the architecture is implemented, how a change travels through it, how the agents see it. There is value in all three, text, 2D and 3D, to see the flow of the architecture live and analyse it. The executor, the compositor and the path and 3D tools as records are built and tested. What is missing is the ECS layer that makes a client, and this round builds it as a loop over records: four systems in code, the wire, the pointer, the clock and the deliverer, and a few record kinds, where I stand, event, identity, trace, stroke. The bet: with only these, I point at anything on screen and it says what it is, where it is and who made it, at the grain of the record; and I change a tool record from where I stand and it takes hold live, with no engine code touched to make it so. The last client died because the anatomy of the block was code and I had nowhere to stand to change it: typing slowed, click and cursor disagreed, a paste took half the space, ctrl-enter hid the model and the effort, I typed kinds by hand. This is the client I will work in, not a bench, and I expect it to be high quality.

How to read the file: section 1 is the bet and where it fails. Section 2 is fact, read from the tree this week with file and line; verify what you rely on, the tree may have moved. Sections 3 and 5 are adopted design, not implemented behaviour: the record shapes, the tick, the style tokens; build to them, and where the tree refuses a shape, the tree wins and the file changes in the same commit. Section 4 says what I see at each component, not how to build it. Section 7 is where you stop and ask. Section 8 is the next round, not this one. Whatever I say is an approximation; fill the rest with your judgment and say where you did.

How I work: the prototype is the spec, no contracts. Build the tests as you build. Documentation lives where the code lives, READMEs to docstrings to code, changed in the same commit. Functions follow the functional programming rule: transformations pure and composable with explicit inputs and outputs; mutable storage, asynchronous work, GPU effects and the store with explicit owners and lifetimes. Work out early where the data layers and the data flow are and where reactive programming belongs, if anywhere. Ceilings are built through, never hacked. Caching is never an option in place of a computation that is right: each level's inputs explicit as data first, with a test that a change to one input reruns exactly its level; caching on top only after a trace shows the need. Data is saved as it is. The best in the field is a datapoint and nothing is carried forward for existing. Say "works" only about what ran.

Terrain: the executor is src/app/client/engine/executor.cljc with its tests under test/app/client/engine/; the tool records the loop runs are src/app/client/region3d/records.cljc and src/app/client/path/records.cljc, with their capability tables beside them; the pure painter is src/app/client/engine/surface.cljc and the GPU side is src/app/client/engine/compositor.cljs; the renderers are region3d/renderer.cljs, path/renderer.cljs and text/renderer.cljs; text layout and its hit test are src/app/client/text/layout.cljc; the harness pages under src/app/client/harness/ are benches and stay benches, and test/render_engine/run_verifier.mjs is how a page is driven and read back today. shadow-cljs.edn holds the builds; resources/public holds the fonts. The server is src/app/server/: routes in door/server_jetty.clj, the boot in door/cluster.clj, the store in rama/. docs/below-the-waist/two-chairs.md is the workflow.
```
