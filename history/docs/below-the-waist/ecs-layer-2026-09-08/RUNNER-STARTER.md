# Runner starter — the ECS read, continued

Paste whole into a fresh Claude session in `/mnt/data/projects/Softland`.
Preflight is yours (fable, effort max). The predecessor session ran the
round from the morning-context rebuild through the top merge and the
audio; this is its chair, handed on.

```
you are taking over the chair that ran the ECS read today. hold its view, take my feedback as it comes, and don't re-open what is settled. the terrain is in this message; read on disk only what it names.

what the round is. three builds of the ECS-layer hypothesis, read back from their write ups and a baseline of the current code, never the code. the loop and its levels are docs/meta/README.md and docs/meta/LEDGER.md; the round's prompts, predictions and the merge structure are docs/below-the-waist/ecs-layer-2026-09-08/ASKS.md. the reads are in reads/ beside it: six reads (gaps, repetition, boundary × claude, codex), three referee files (merge-*.md), two top merges (top-claude.md, top-codex.md), and episode-1.md, a six-chapter spoken script of the whole round written for listening: baseline, the three builds, the one thing, what stayed in code and the three seams, the two chairs agreeing, the two chairs parting. all of reads/ is uncommitted; commit it as one commit when i say land.

where it stands. the two chairs agree on what the layer is: the mounted tool, built three times in its middle, zero times at its floor and edges. they part on the first move: the claude chair says merge the trees, one envelope and one wire, read what a program step can say, then build the mount once and re-host the three clients over it; the codex chair says build one record-authored inspector in vantage now through its existing durable editing path, let it name the missing operations, merge after. that fork is mine to rule and i have not ruled. i am listening to the round rather than reading it, on the go. a notebooklm deep dive over the five docs (episode-1.md, the two tops, repetition-claude.md, BASELINE.md) is in flight; it speaks in its own words, so nothing is ruled from it. the faithful version is a local render of episode-1.md, chaptered, and it has not been rendered yet.

the render. breeze tts 2 runs on my two 7900 xtx under rocm: venv /mnt/data/projects/tts-lab/.venv-bz, code /mnt/data/projects/tts-lab/breeze-tts (run infer.py from that dir), weights /mnt/data/projects/tts-lab/breeze-tts-2, voice design by --instruction with --cfg-scale 4, about real time per gpu once loaded, so keep the model loaded and feed it paragraphs, one gpu per chapter. three designed voices: a narrator for chapters 1 to 4, one voice for the claude chair and one for the codex chair in 5 and 6. output: one mp3 per chapter plus one m4b with chapter marks, sent to me with SendUserFile so it reaches my phone. the mp3s stay out of git. /mnt/data was full earlier; check df before writing anything big. edge tts (uvx edge-tts) is the fallback if breeze breaks.

what comes after i rule. the merge debt first: the gaps read found the executor changed two ways in two trees and region3d three ways, neither tree's docs knowing, so the foundation reconciles before any next build sits on it. then the meta prompt, its own session, evidence is the chats plus the referees' held/broke verdicts, never in the same room as the instance. then the next build prompt, job first, codex as the builder, the way docs/meta says.

three things i corrected today that you carry. the read is docs only and trusts the docs; silences are findings, they go back to the builder session that made them, never to the code. the predictions about which model family finds what are process, not instance; they belong to the meta session and nowhere near a prompt about the code. and when i ask for something holistic, chapter by chapter, baseline first, never cut the baseline to fit a tool's length setting; the listener has to know what we have before anyone argues about what to build.
```
