# Server material organs — source map (2026-08-21)

## 0. Scope and reading key

| item | mapped fact |
|---|---|
| area | **CHECKED** — eight server organs plus the material-facing entries in `face_projection.clj`: facet master, material truth, material circulation, machine cut, verb release, code atoms, cascade, and material portal. The requested facet-master file currently lives at `src/app/server/rama/object_container/facet_master.clj`; cascade currently lives at `src/app/server/cascade.clj`. |
| hard out | **CHECKED** — no `object_container.clj`, `relation_kernel.clj`, `trail_view.clj`, `face_arsenal.clj`, or runtime body was opened. Their calls are named at the allowed caller and stop there. `src/app/server/env.clj` was not opened. |
| method | **CHECKED** — every area file was skeletoned with `rg -n "^\(def"`; only scoped definition and call-site windows were then read. Caller claims distinguish a traced production source call, a registry/test reference, and runtime proof. |
| markings | **CHECKED** = the named source/history/search was read in this run. **INFERRED** = a bounded source-reachability or replacement-boundary pattern; it is not runtime proof. |

## 1. Component map

| component | what it is, in one line | owns vs borrows | who calls it / what it calls | file-only replacement boundary | naming, shape, and vintage markers |
|---|---|---|---|---|---|
| **Facet master adapter**<br>`rama/object_container/facet_master.clj` | **CHECKED** — a plain-Clojure adapter that turns any registered facet spec into Object Container candidate imports, revisioned active-pointer edits, instance-master revisions, and causal activation history (`:1-13`, `:69-343`, `:446-780`). | **CHECKED** — owns facet-master IDs derived from specs, `imp:fm:` import keys, request/materialization maps, active-pointer event writes, instance subject digests, and the `activation-trail`/`worn-at` result shapes (`:15-50`, `:69-205`, `:252-343`, `:446-516`, `:693-780`). Borrows the shared facet compiler, binding rules, activation-event grammar, core ID/time helpers, and Object Container constructors/runtime calls; the excluded bodies were not followed (`:6-13`). | **CHECKED** — cluster bootstrap/migrations call `ensure-master!` and `ensure-active-source!` (`cluster.clj:343-447`); `material_truth.clj:54-416` calls the instance writes and trails; `face_projection.clj:923-1645` reads masters/pointer history; `server_jetty.clj:403,950,975,1010,1503-1505` compiles, imports, activates, ensures, and drills; circulation and portal read the pointer/trail (`material_circulation.clj:773-777`; `material_portal.clj:173-192`). Calls into excluded Object Container APIs at the adapter call sites and stops there. | **CHECKED** — callers bind directly to this namespace's functions and returned keys; no protocol boundary sits between them. **INFERRED** — the file can be replaced without a new durable owner only if its existing IDs, request bytes, pointer content, and public result shapes remain the same, because current callers and stored rows name them directly. | **LOUD — CHECKED:** the requested path omits the current `object_container/` directory, while the file is an adapter in the requested area, not the excluded module body. **LOUD — CHECKED:** “master” covers a shared registered master and a synthesized per-subject instance master; both use the same adapter but different IDs/specs (`:446-516`). **CHECKED vintage:** added as the provenance P1 adapter at `83aa5dd` (2026-07-24), then widened by the later facet/activation/instance generations. |
| **Material truth**<br>`material_truth.clj` | **CHECKED** — a derived composition layer over facet-master writes and reads: instance registry upkeep, served instance tiers, blast radius, announcements, case reports, and standable history (`:1-30`, `:32-430`). | **CHECKED** — owns only the derived result shapes and the best-effort registry update wrapper; it declares no module, depot, PState, or topology (`:1-30`, `:32-102`). Borrows facet-master state/trails, the episode instance registry, shared facet/activation grammars, and one excluded runtime revision read (`:104-235`). | **CHECKED** — `face_projection.clj:988,1190-1210` consumes served instances, blast, announcements, reports, and history; `server_jetty.clj:410,943` consumes `served-instance` and `deviate!`. It calls the facet-master adapter and `episode/register-instance-masters!`/`read-instance-registry`; its one excluded runtime point read is named at `:109-116` and not followed. | **CHECKED** — the durable writes remain in facet master even if this file is absent; current callers consume this file's public result maps directly. **INFERRED** — its file boundary is the derived API and key shapes, not a storage boundary. | **LOUD — CHECKED:** the namespace name says “truth,” while its own source states every value here is derived and it owns no truth (`:1-18`). **LOUD — CHECKED:** `served-instances` returns `{facet-keyword -> {subject -> instance}}`, not a master-ID map (`:148-176`). **CHECKED vintage:** added with the P6 truth-loop generation at `be247c5` (2026-07-25); `change-kinds` includes `:change/preview` while the source states this server never mints preview (`:236-247`). |
| **Material circulation**<br>`material_circulation.clj` | **CHECKED** — a multi-owner foreign-client driver that shapes receipts, asserts semantic edges, stores calibrated machine records, runs ambient autotag, resolves captured wears as-of, and composes experience (`:1-30`, `:83-1090`). | **CHECKED** — owns receipt/record/request formats, gold/silver classification, starter predicates, terminal-escape analysis, autotag IDs/output validation, as-of analysis, and experience result maps (`:34-80`, `:83-169`, `:288-449`, `:450-772`, `:773-1090`). Borrows Object Container for projection-hint records, Relation Kernel for edges, the LLM runtime for runs, facet-master pointer identity, and shared activation/spec vocabularies; excluded/out-of-area bodies were not followed (`:13-28`). | **CHECKED** — episode births/turns call `receipt-from-context` (`episode.clj:519,670`); Jetty calls autotag, receipt selection/banking, and `bank-reference!` (`server_jetty.clj:375,530-568,1134`); face entries call record/as-of/experience/escape functions (`face_projection.clj:637-782,1592`); cluster calls starter culture and escape reporting (`cluster.clj:557,574`). It calls the three borrowed runtime families at the named sites and stops. | **CHECKED** — this one file crosses three runtime families and several pure projections; no single sibling interface contains the whole component. **CHECKED** — `experience-around-many` is a pure-composition boundary plus one batched relation read, while autotag and banking are write drivers (`:615-772`, `:927-1090`). | **LOUD — CHECKED:** “gold” and “silver” classify custody, not relation kind; a human `:felt-at` edge is not gold (`:83-96`). **LOUD — CHECKED:** `read-circulation-records` accepts an object key and internally derives `oc:chat-conversation:<object-key>` (`:366-377`); the lower reader named in the prior waist map accepts the full conversation-container ID. **CHECKED vintage:** P4 circulation arrived at `2642ad6` (2026-07-25); P6 as-of/escape and later Halo banking sit in the same file. |
| **Machine cut**<br>`machine_cut.clj` | **CHECKED** — a conversation-pair annotator with a pure input/validation/reconcile core and an app-JVM LLM/WAL/relation-write shell (`:1-44`, `:107-526`, `:527-1020`). | **CHECKED** — owns annotator identity/version, hashes and run IDs, strict JSON validation, pair-to-edge specs, reconcile plans, the local `data/machine-cut-log.ednl` format, and replay order (`:46-92`, `:107-526`, `:830-1020`). Borrows the served river-page shape, transcript identity, LLM lifecycle calls, Relation Kernel edge calls, and ingest epoch (`:32-42`, `:527-950`). | **CHECKED** — cluster boot calls only `replay-wal!` (`cluster.clj:531-536`); face projection imports only `machine-cut-actor-id` for read filtering (`face_projection.clj:320-326`). **INFERRED** — current production source has no caller of `annotate-conversation!`; its callers found in this census are tests (`test/app/machine_cut_test.clj:294-576`). Runtime target bodies were not opened. | **CHECKED** — the source explicitly separates pure core from shell, but the shell and cluster replay share the WAL schema, actor ID, relation IDs, and transition request keys. **INFERRED** — the pure functions are individually callable; the current boot seam is welded to the WAL/replay surface rather than the live annotator entry. | **LOUD — CHECKED:** `reconcile-plan` puts transition IDs under `:request-id` for asserts and `:mc/request-id` for retract rows (`:488-526`). **LOUD — CHECKED:** the durable annotation recovery record is a local EDN WAL, while circulation's machine record is an Object Container projection row. **CHECKED vintage:** added at `ceb84da` (2026-07-12), before the facet-material sequence. |
| **Verb release**<br>`verb_release.clj` | **CHECKED** — a fixed eight-node, source-address-only release chain for the resident reply verb, with validation, import, and readback helpers (`:1-22`, `:29-215`). | **CHECKED** — owns the ordered release kinds, manifest shape, release validation, EDN serialization, and import wrapper (`:23-177`). Borrows code-address derivation from code atoms, binding/attention/reply identity from shared material rows, and Object Container adapter/runtime calls (`:10-20`, `:29-50`, `:162-215`). | **CHECKED** — the production face path calls only `read-release` through the interaction table (`face_projection.clj:1113-1121`). **INFERRED** — `manifest`, `code-address`, and `append-release!` have test callers but no current production source caller (`test/app/reply_to_block_test.clj:23-212`). Excluded Object Container calls stop at `:178-215`. | **CHECKED** — the face consumes the fixed release-ref and eight-node readback shape; the write helpers are not on the current production source path. The code-address half directly depends on code-atoms ID math. | **LOUD — CHECKED:** `release-ref` aliases `reply-to-block/release-ref`, whose bytes also appear in the shared verb registry; the server release is the eight-node bundle over that identity (`:23`, `:51-91`). **LOUD — CHECKED:** `verb-release` means publishing/reading this chain; `facet-master/release-deviation!` means activating an inherited instance form, not publishing a verb. **CHECKED vintage:** added with P8 at `15497df` (2026-07-26). |
| **Code atoms**<br>`code_atoms.clj` | **CHECKED** — a git-backed code ingestion/lineage driver plus a later HEAD analyzer that derives `:requires` and `:calls` relations (`:1-40`, `:102-476`, `:477-1050`). | **CHECKED** — owns git subprocess/parsing, named-form cuts, blob/continuant ID joins, lineage plans, HEAD materialization, clj-kondo analysis, desired-edge derivation, transition reconciliation, and a caller-owned in-memory analyzer basis (`:55-340`, `:477-1050`). Borrows the Clojure adapter, Object Container runtime, Relation Kernel, git object store, and clj-kondo (`:32-39`). | **CHECKED** — production `verb-release/code-address` calls `head-blob-sha`, `blob-text`, and `cut-named-units` (`verb_release.clj:34-39`). **INFERRED** — `code-sync!` and `analyzer-sync!` have test callers but no current production source caller (`test/app/server/rama/code_atoms_test.clj:92-629`). Calls into excluded durable owners stop at this file's call sites. | **CHECKED** — P2 ingest/lineage and P3 analyzer share git/ID helpers but have separate actors, edge kinds, reconciliation rules, and entry functions (`:266-476`, `:477-1050`). **INFERRED** — the production dependency visible now is the pure code-address substrate, not either sync entry. | **LOUD — CHECKED:** “code atom” here is a named top-level Clojure form cut from a git blob; “block as atom” in the prior waist map is the durable editing unit. **LOUD — CHECKED:** one file contains two derivation generations: commit lineage emits `:supersedes`, while HEAD analysis emits `:requires`/`:calls`; it also keeps an in-memory basis that the git/OC/relation state does not supply (`:477-528`, `:742-1050`). **CHECKED vintage:** both P2/P3 lanes landed together at `9a2445b` (2026-07-09), with later reconciliation repairs at `f06f511`. |
| **Cascade**<br>`src/app/server/cascade.clj` | **CHECKED** — one in-process declaration table plus a best-effort trigger dispatcher (`:1-47`). | **CHECKED** — owns one literal `:episode/turn-durable` row, enumeration order, future-per-row dispatch, and immediate dispatch receipts (`:7-47`). Borrows only logging and the dynamically resolved handler symbol `app.server-jetty/run-ambient-autotag!` (`:2`, `:10`, `:35-43`). | **CHECKED** — Jetty calls `react!` after the durable turn path (`server_jetty.clj:589`); face projection calls `rows` for the served cascade table (`face_projection.clj:1500-1535`). The resolved handler target is named at `cascade.clj:10,35` and not followed. | **CHECKED** — two public functions are the boundary: `rows` for enumeration and `react!` for dispatch. The face projector also has an injected row-source arity, so its enumeration does not require invoking a handler (`face_projection.clj:1514-1535`). | **LOUD — CHECKED:** these `rows` are code-owned, non-durable in-process declarations, not revisioned material rows (`cascade.clj:4-14`; `face_projection.clj:1500-1535`). **LOUD — CHECKED:** `:dispatched? true` is returned immediately after a future is launched; handler completion is not part of that receipt (`cascade.clj:22-47`). **CHECKED vintage:** added at `34bf7c0` (2026-07-27). |
| **Material portal, server half**<br>`material_portal.clj` | **CHECKED** — a clock-free server-side join that opens one entity/master world by recursively calling the face registry, then derives the portal's cards, query plan, rendering model, and briefing (`:1-73`, `:76-140`, `:141-1098`). | **CHECKED** — owns section error boundaries, master/placement/binding/deviation/history/recovery/lint/chrome/why shaping, the assembled `:portal/*` result, and its declared read plan (`:76-669`, `:672-1082`). Borrows the face `serve` function, Object Container point reads, episode geometry/registry reads, material truth, shared facet/binding/anatomy/portal/inspector grammars, and matter-room identity (`:54-70`). | **CHECKED** — face projection is the direct caller at `face_projection.clj:1807-1833,2027-2028`; `open` sub-serves `:facet-materials`, `:material-inspector`, `:interaction-table`, `:material-truth`, `:material-experience`, and in master-anchor mode `:cascade-rows` (`material_portal.clj:743-836`). Excluded runtime calls stop at their local sites. | **CHECKED** — `serve-fn` is explicitly injected, avoiding a namespace cycle, but the join names exact face keys and consumes their exact envelopes. **INFERRED** — it is replaceable behind `open`/`render`/`briefing` only as a whole join contract; individual sub-projections remain sibling dependencies. | **LOUD — CHECKED:** “material portal” spans the shared pure grammar/render helper (`shared/material_portal.cljc`), this server join, and the face transport entry (`face_projection.clj:1794-1836`). **LOUD — CHECKED:** a failed sub-read receives an empty-shaped fallback plus `:section/error`; an honestly empty successful section can carry the same collection shape without that error (`:90-120`, `:743-1082`). **CHECKED vintage:** added with P7 at `04ae2df` (2026-07-25), then widened for matter-room/cascade. |
| **Face projection registry and entries**<br>`face_projection.clj` | **CHECKED** — the read-only registry/dispatch hub that converts runtime reads and the server organs above into twelve face data-contexts (`:1-70`, `:1837-1952`). | **CHECKED** — owns projection envelopes, error totality, the registry map, face-to-projection resolution, the twelve entry functions, and the recursive portal/seed composition (`:78-2045`). Borrows served block/episode reads, the organs above, shared material grammars, and excluded Object Container/Relation Kernel/Face Arsenal read APIs; each excluded call stops at this file. | **CHECKED** — Jetty calls `serve` for `:material-portal` at the matter-room open and rollback paths (`server_jetty.clj:823,999`), calls `episode-seed` and `portal-briefing` (`:556,607`), and the portal recursively calls `serve` for its subfaces (`face_projection.clj:1807,2028`; `material_portal.clj:743-836`). | **CHECKED** — all entries share one literal registry and one total `serve`; there is no namespace boundary per entry. Individual functions are direct map values, while portal and interaction/truth entries call siblings in the same file. | **LOUD — CHECKED:** the registry's “truth” entries are unlike shapes: `:block-truth` is keyed unit text, while `:material-truth` is facet diffs/blast/history (`:1142-1216`, `:1703-1747`). **LOUD — CHECKED:** totality is not uniform: some entries return named error envelopes, while machine-cut reads collapse several conditions to `[]` (`:450-494`, `:808-893`, `:1837-1952`). **CHECKED vintage:** the registry began with conversation/assembly at `c84ebfa` (2026-07-11); material entries accumulated P1–P8 on 2026-07-24–26, cascade/gauge on 2026-07-28. |

## 2. Face entry map

| registry entry | what it serves and borrows | current source reachability |
|---|---|---|
| `:conversation` | **CHECKED** — shapes river blocks into turns, geometry, camera, turn records, circulation experience, and machine-cut pair structure; it borrows the served river-page call, episode reads, circulation, and the machine-cut actor-filtered relation read (`face_projection.clj:450-718`). | **CHECKED** — `episode-seed` calls this function directly (`:1986-2004`), and Jetty calls `episode-seed` for fresh sessions (`server_jetty.clj:607`). |
| `:assembly` | **CHECKED** — resolves one assembly name to a current revision and an arsenal-index status; calls excluded Face Arsenal and Object Container reads at `face_projection.clj:808-857` and stops. | **INFERRED** — present in the registry and tests; no current production source caller outside generic `serve` resolution was found. |
| `:face-list` | **CHECKED** — returns arsenal roster rows plus wear counts; calls excluded Face Arsenal reads at `face_projection.clj:858-893` and stops. | **INFERRED** — present in the registry and tests; no current production source caller outside generic `serve` resolution was found. |
| `:facet-materials` | **CHECKED** — serves every registered shared master under `:facet-materials/by-id` plus the instance tier under `:facet-materials/instances` (`face_projection.clj:894-1095`). | **CHECKED** — material portal sub-serves it on every open (`material_portal.clj:743-766`); interaction-table and material-truth also call it directly (`face_projection.clj:1105,1169`). |
| `:material-inspector` | **CHECKED** — joins a picked entity/current-scene wearer snapshot to candidate and activation trails, then returns canonical result/EDN envelopes (`face_projection.clj:1217-1499`). | **CHECKED** — material portal sub-serves it on every open (`material_portal.clj:775-781`). |
| `:material-experience` | **CHECKED** — batches material IDs, optional room residents, receipt records, machine records, and relation experience (`face_projection.clj:719-807`). | **CHECKED** — material portal sub-serves it on every open (`material_portal.clj:795-816`); conversation projection also composes circulation experience directly (`face_projection.clj:625-690`). |
| `:cascade-rows` | **CHECKED** — wraps `cascade/rows` as a code-owned, in-process, read-only declaration table (`face_projection.clj:1500-1535`). | **CHECKED** — master-anchor portal opens sub-serve it; the current Jetty matter-room paths open master anchors (`material_portal.clj:817-836`; `server_jetty.clj:823,999`). |
| `:escape-gauge` | **CHECKED** — performs an on-demand, single-flight git-commit plus provenance-pointer-history scan and caches the last report in process (`face_projection.clj:1536-1702`). | **CHECKED** — the material portal emits only a link and does not invoke it (`material_portal.clj:817-836,1003-1010`). **INFERRED** — the current production source census found no other caller; registry/tests remain. |
| `:interaction-table` | **CHECKED** — combines served master bindings, code-floor bindings, verb declarations, conflicts, and the fixed verb release read (`face_projection.clj:1036-1141`). | **CHECKED** — material portal sub-serves it on every open (`material_portal.clj:782-789`). |
| `:material-truth` | **CHECKED** — combines instance diffs, per-master blast radius, announcements, case reports, and world-at history (`face_projection.clj:1142-1216`). | **CHECKED** — material portal sub-serves it on every open (`material_portal.clj:790-801`). |
| `:material-portal` | **CHECKED** — transports the server portal result, canonical EDN, render model, briefing, unanswered list, token, and clock (`face_projection.clj:1748-1836`). | **CHECKED** — directly served by current Jetty matter-room open and rollback paths (`server_jetty.clj:823,999`); `portal-briefing` also opens the same server join (`face_projection.clj:2016-2045`). |
| `:block-truth` | **CHECKED** — reads a request map of unit IDs to nonces and returns found/text/nonce rows (`face_projection.clj:1703-1747`); the excluded unit reader call is named there and not followed. | **INFERRED** — present in the registry and tests; no current production source caller outside generic `serve` resolution was found after the client read/write wire deletion. |

## 3. Loud cross-boundary collision ledger

| collision | exact fact |
|---|---|
| **ONE ENVELOPE, TWO KEY DOMAINS** | **CHECKED** — `:facet-materials/by-id` is keyed by string master ID such as `"fm:attention"`; `:facet-materials/instances` is keyed first by facet keyword such as `:attention`, then subject (`face_projection.clj:974-1028`; `material_truth.clj:148-176`). `material_portal.clj:213-218` reads the first by master ID and the second by facet. A lookup in the other domain returns nil rather than a key error. |
| **OBJECT KEY ≠ CONVERSATION-CONTAINER ID** | **CHECKED** — the prior waist map records that `read-transcript-conversation-projection` is keyed by `oc:chat-conversation:<address>` and a bare address returns `[]`. This area's `read-circulation-records` accepts the bare object key and adds that prefix itself (`material_circulation.clj:366-377`); `conversation-projection` passes the bare address (`face_projection.clj:642-647`). The wrapper parameter and the lower reader parameter name different shapes of the same conversation identity. |
| **THE SAME `[]` COVERS MORE STATES HERE** | **CHECKED** — prior material rows already use `[]` for absent wearer, absent room master, and complete questions. Server-side additions: machine-cut edge read returns `[]` for nil runtime, nil address, read failure, or no matching edges (`face_projection.clj:464-481`); room-resident IDs return `[]` for invalid master/object mismatch/no runtime or no residents (`:719-740`); face-list returns `:faces []` for unavailable/read-failed and for a genuinely empty roster, distinguished only by sibling error keys (`:858-893`); missing verb release returns empty nodes/anchors plus `:release/found? false` (`verb_release.clj:186-215`). |
| **THREE ACTIVATION-HISTORY SHAPES** | **CHECKED** — `facet-master/activation-trail` returns newest-first causal chain plus regressions (`facet_master.clj:693-750`); `face_projection` has a private same-named `activation-trail` returning oldest-first classified pointer entries plus off-chain orphans, later mixed with candidate revisions for display (`face_projection.clj:1248-1424`); circulation has raw `activation-history` plus `analyze-activation-history` for time eligibility/ambiguity (`material_circulation.clj:773-858`). All consume the shared activation-event grammar, but their result shapes and ordering roles differ. |
| **“PORTAL” IS THREE COMPONENTS** | **CHECKED** — `shared/material_portal.cljc` owns pure questions/canonicalization/render/briefing; `server/rama/material_portal.clj` owns the multi-face server join; `face_projection/material-portal-projection` owns transport envelope/token/clock (`material_portal.clj:1-73`; `face_projection.clj:1794-1836`). |
| **TWO CANONICAL EDN FENCES STAY SEPARATE** | **CHECKED** — the prior material-row map records separate public `canonicalize`/`canonical-edn` functions in inspector and portal, with portal additionally binding namespace-map printing off on JVM. The server preserves the separation: inspector envelope calls `material-inspector/canonical-edn` (`face_projection.clj:1463-1499`); portal envelope calls `portal/canonical-edn` (`:1807-1833`). |
| **“TRUTH” COVERS DURABLE OWNER, DERIVED WORLD, AND UNIT READ** | **CHECKED** — `material_truth.clj` states it owns no durable truth and derives facet state (`:1-18`); `:material-truth` serves those derived maps (`face_projection.clj:1142-1216`); `:block-truth` serves current text for requested units (`:1703-1747`). The excluded modules remain the durable owners named by their call sites. |
| **“RELEASE” COVERS TWO DIFFERENT ACTS** | **CHECKED** — `verb-release` publishes/reads a fixed eight-node code-addressed chain (`verb_release.clj:51-215`); `facet-master/release-deviation!` writes a new instance revision whose event kind is rollback and whose material matches inherited state (`facet_master.clj:652-663`). |
| **“PROVENANCE” IS RENDER POLICY AND CUSTODY METADATA** | **CHECKED** — the prior row map's provenance facet owns tint/merge/priority only. In this area, facet-master imports carry request provenance such as `{:source/type :facet-master}` (`facet_master.clj:154-205`), circulation receipts carry master/revision/site identity (`material_circulation.clj:50-80,99-148`), and machine-cut edges carry evidence source plus LLM asserter (`machine_cut.clj:402-455`). These are different shapes under the provenance idea. |
| **“ROWS” ARE NOT ONE STORAGE CLASS** | **CHECKED** — shared binding/facet rows are revisioned material forms; `cascade/rows` are a private literal vector with in-process lifetime (`cascade.clj:4-16`); face projection turns them into a read envelope without making them durable (`face_projection.clj:1500-1535`). |
| **“ATOM” CROSSES THE WAIST WITH TWO REFERENTS** | **CHECKED** — the prior waist map uses block-as-atom for the durable editing unit; `code_atoms.clj` uses atom/unit for a named top-level Clojure form cut from a git blob (`code_atoms.clj:174-203,477-495`). |
| **DISPATCH RECEIPT ≠ HANDLER OUTCOME** | **CHECKED** — cascade returns `{:dispatched? true}` after creating the future, before the dynamically resolved handler finishes; handler failures are only logged inside the future (`cascade.clj:22-47`). |

## 4. One system or several generations?

| question | factual answer |
|---|---|
| one system, or generations side by side? | **CHECKED — several generations sit side by side inside one connected current app-JVM subsystem.** The current source graph connects them through Jetty, episode, cluster, face `serve`, shared facet/activation shapes, and excluded runtime handles; their owned state, entry shapes, and lifetimes are not one generation. |

| evidence class | checked evidence |
|---|---|
| connected current graph | **CHECKED** — Jetty uses facet writes, circulation writes, cascade dispatch, episode seed, portal briefing, and `serve`; portal `serve` recursively reaches facet materials, inspector, interaction table, material truth, experience, and cascade rows (`server_jetty.clj:375-607,823-1010,1134,1503-1505`; `material_portal.clj:743-836`). |
| shared shapes | **CHECKED** — the facet path shares registered specs, `fm:` identities, immutable revisions, activation events, material contribution stamps, and one face registry. Material portal and face projection consume the same `:facet-materials/*`, `:truth/*`, `:experience/*`, and `:interaction-table/*` envelopes (`facet_master.clj:69-343`; `face_projection.clj:894-1216`; `material_portal.clj:743-1082`). |
| divergent state/lifetime | **CHECKED** — facet master and verb release write Object Container requests; circulation writes Object Container records plus relation edges plus LLM lifecycle; machine cut uses relation edges plus a local EDN WAL; code atoms uses git blobs plus Object Container plus relation edges plus an in-memory analyzer basis; cascade is process-memory only; material truth/portal/face entries own derived values only. |
| divergent totality shapes | **CHECKED** — face entries use error envelopes, floor substitutions, nil, or collapsed empty collections depending on generation: facet materials name errors/floors (`face_projection.clj:894-1035`), portal sections attach `:section/error` (`material_portal.clj:90-120`), machine-cut edge read returns `[]` on failure (`face_projection.clj:464-481`), and `serve` converts thrown projections to one generic error context (`:1896-1952`). |
| static-only or one-sided branches | **INFERRED** — current production source reaches machine-cut WAL replay but not live annotation; code-atoms pure address helpers but not either sync; verb-release readback but not append; and the face registry contains `:assembly`, `:face-list`, `:escape-gauge`, and `:block-truth` without a current production caller found outside generic dispatch. Registry/source presence is not runtime execution proof. |
| explicit version/generation markers | **CHECKED** — activation pointers accept legacy bare revision strings and current closed event maps (`facet_master.clj:227-250`); inspector labels v0 events separately (`face_projection.clj:1248-1332`); machine cut fixes its WAL/actor at v1 (`machine_cut.clj:46-92,830-950`); circulation records/autotag are versioned independently (`material_circulation.clj:42-45,615-772`); cascade and face envelopes use their own version 0 values (`cascade.clj:7-14`; `face_projection.clj:1500-1535`). |
| source vintage | **CHECKED** — code atoms arrived 2026-07-09 (`9a2445b`), face projection 07-11 (`c84ebfa`), machine cut 07-12 (`ceb84da`), facet master 07-24 (`83aa5dd`), circulation/truth/portal 07-25 (`2642ad6`, `be247c5`, `04ae2df`), verb release 07-26 (`15497df`), and cascade 07-27 (`34bf7c0`). Material face entries accumulated across that sequence rather than landing as one uniform registry generation. |
