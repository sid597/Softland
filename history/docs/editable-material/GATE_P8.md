# GATE P8 — one verb born from inside: PASS (slim gate)

Gate session 2026-07-26 (fresh Fable context), gate object `15497df` +
`86c4255`; HEAD's code tree verified byte-identical to the gate object.
First gate under the gate-sizing rule (decisions.md, Sid 07-26): drive the
seam live, falsify the new claims, rule — build receipts (suite
454t/6,274a/0/0 · cljs 0 warnings, both carried in the release's own
receipt nodes) trusted, not re-run.

## The four probes

1. **LIVE DRIVE — HOLDS.** Real DOM Ctrl+Enter on a real block
   (drill lane `a87287ac…`, headed-less Chrome, real WebGPU device): tap →
   type → Ctrl+Enter rode the material dispatch. Captured POST: the deictic
   chain intact — `:source-unit-id` == focused block == `:portal-open
   :entity-id` == every wearer row's subject; `:narrowed? true`;
   `:master-ids [fm:attention fm:threaded]`. Server `:episode-durable` (P8
   keys live): same subject, `:portal-briefing-bytes 80,878` (~12× under
   P7's un-narrowed 955KB sitting). A real claude-CLI resident ran
   (run-start → thinking → text-delta → run-done) and **the machine reply
   block landed beneath the subject (y 360 → 437), same lane — durable on a
   fresh reload**. `__portal.release()` through the artery: found/complete/
   worn true, eight kinds in order, worn-row tier `:master` at the activated
   revision, namespaced keys preserved. **Read-only byte-proof**: canonical
   portal sha identical before/after all console reads (`73c276fb…`,
   117,726 bytes ×2); the probe-4 JVM reads had their own identical bracket.
2. **WIDENING FALSIFIER — HOLDS.** Live through the real endpoint, all
   named vectors at once (second entity + foreign wearers + injected
   `:master-ids` + `:narrowed? false` + foreign conversation):
   `:portal-master-ids []`, briefing 54,680 B (the subject-less base,
   SMALLER than honest 80,878), durable subject forced to the real block —
   and the empty master set stayed EMPTY instead of widening to the
   registry. JVM half (the endpoint's exact functions): direct master-ids
   discarded, second entity overridden, foreign wearers dropped, narrowed?
   forced true, empty stays empty. Residual (recorded, not a widening of
   authority): a client lying about its own appearance snapshot can claim
   the subject wears an unknown master name — it prices one honest
   not-found card in its OWN resident's briefing (56,527 B, still under
   honest). Wearers are constitutionally client truth (P7 portal law);
   single inhabitant; the briefing is read-only projection the client can
   already read whole via `__portal.open()`. Owner: when a second/foreign
   wearer arrives, the server derives wearers from its own derived-
   attachment truth (uniform today, so already derivable).
3. **SINGLE-ARTERY — HOLDS.** `:eval` routes ONLY to `dispatch-key-eval!` →
   binding resolver → the one `invoke-verb!` site → registry
   `:resident/reply-to-block` → `reply-to-block!` → the ONE
   `/api/episode/utterance` post site. `submit-turn!` survives as a shim
   with ZERO callers; events.cljs has one Ctrl+Enter producer; the other
   `stream-agent-run!` sites hit `/api/agent/stream` (old jit lane). Live
   corroboration: the drive's keystroke produced the dispatch decision and
   the POST. The table's second `:key/eval` row is the code-floor twin
   (`code-floor:fm:attention:v3`) — P1 totality, not a parallel artery.
4. **FRESH-JVM RELEASE REPLAY — HOLDS.** `read-release` on the durable
   cluster: found, complete, 0 errors, eight nodes in order, 9 anchors,
   6,991 bytes total. **All nine addresses** (7 implementation + test +
   build receipts) resolve — blob in git, anchor cuts, unit-id AND
   source-anchor-id recomputed and matched. Wish address: LOG.md blob
   `f9a2112f` lines 727–736 ARE the 07-18 reply wish, verbatim; the wish
   text appears nowhere in the release bytes (nothing copied — address
   nodes carry exactly the six address keys). **Worn == activated**: worn
   revision == activation revision == the LIVE active pointer
   (`rev:fm:attention:57c9d6b2…`); binding node grammar 3. **Exactly ONE
   binding row**: one `:key/eval` row in the v3 form, one `:master`-tier
   row in the live table, `:release/worn?` true through the portal with
   `:release/query-path` declared.

**Guard — HOLDS.** `terminal-escape-report` live:
`:ambiguous-activation-history`, count nil. Honest; not massaged.

## Rulings

- **(a) The campaign build-out is COMPLETE.** P1–P8 all gates PASS; the
  board flips to the RETURN. The first full breath — pressure felt →
  repaired → felt again without leaving — is Sid's act and the campaign's
  true gate (CAMPAIGN §Return).
- **(b) F4 escape detector → LATER item** (ends three gate records of
  carrying): scope the linearity/clock-regression check to the measurement
  window (post-latest-activation) so the v0-era provenance clock regression
  stops poisoning the denominator; the durable cluster then reads
  `:measured`. Nothing blocks on it; the `:ambiguous` reading is true.

## Findings + ops (owners named)

- **Stale dev server (ops divergence, corrected):** the :8080 dev-app JVM
  booted 07-25 23:27 — BEFORE the P8 commits — so its server half was
  P7-era (first drive: durable event without the P8 keys, prompt without
  briefing). The gate booted a HEAD jetty on **:8091** (shadow cache — and
  the Electric token — untouched; the old process's watch had already
  compiled the P8 client). The :8080 process must be restarted at HEAD
  before the return wear, or wear at :8091.
- **Virgin non-UUID lane cannot spawn the resident (pre-P8, drill-only):**
  `decide-episode` uses the lane-id as the CLI `--session-id`; a
  thread-less `?drill=<string>` lane fails "Invalid session ID". Genesis is
  a UUID by design and thread ids are UUIDs — the return wear is safe.
  Convention: UUID-shaped drill ids when a drill resident is wanted.
  Owner: friction-driven.
- Gate writes on the land: drill turns on `p8-gate-live-01`/`-02` (stale
  server; -02 durable, resident spawn refused) and `a87287ac…` (the drive +
  two widening attacks, three resident turns). All drill-lane, all
  recorded here.
- R1/R2/R3 from GATE_P7 carry unchanged; R2's first consumer (the visual
  face) now rides the return wear.

## Verdict

**PASS — the react-bits seam is closed and the campaign build-out is
COMPLETE.** The banked wish travels wish → code addresses → receipts →
versioned verb → one binding row → activation → worn, replayable from a
fresh JVM and readable from inside the land; Ctrl+Enter rides the material
artery with the narrowed briefing; the client cannot widen what the server
re-derives; the gauge stays honest. The board flips to the RETURN.
