# matter-room — P4 receipt: citizens and gauges

2026-07-28 · implementer receipt, delivered via session handoff and
transcribed unmodified by the orchestrating session (the implementer worked
in a clean worktree and wrote no docs there — this transcription is the
phase artifact; provenance: the handoff message is the source).

**STATUS: P4 IMPLEMENTER COMPLETE · UNCOMMITTED AND UNSTAGED IN A CLEAN
WORKTREE.** This is not the independent Fable gate. Both commit decisions
remain Sid's. Nothing was staged, committed, or pushed.

Worktree: `/mnt/data/projects/Softland-matter-room-p4` · branch
`codex/matter-room-p4` · base HEAD
`620c021e134ec362452fcc4d452641bf58abb65c` (the P3 code commit).

## What landed

- `src/app/server/rama/face_projection.clj` (~:1488)
  - Registered `:cascade-rows` and `:escape-gauge`.
  - Production `:cascade-rows` reads exactly `app.server.cascade/rows` and
    reports: `:cascade/labels [:code-owned :in-process]`,
    `:cascade/ownership :code-owned`, `:cascade/lifetime :in-process`,
    `:cascade/read-only? true`.
  - Its three-arity test seam accepts an injected row source without
    touching `cascade.clj`.
  - After the fresh falsifier found a metadata-honesty hole, injected
    sources now identify as `:fixture-injected` rather than laundering
    arbitrary rows as `:code-owned`.
  - `:escape-gauge` is on-demand only. Its git root is derived locally from
    `System/getProperty("user.dir")`. It reads activation history exactly
    once from `(facet-master/active-pointer-container-id
    provenance-material/spec)`, cursor `""`, limit `100000`. It passes that
    single provenance history and `default-material-policy-paths` directly
    to the existing `terminal-escape-report`; no alternate inference path
    was added.
  - Timeout ownership is at the face: callers receive a poisoned-but-total
    report on expiry. One process-wide single-flight protects the
    git-backed read; concurrent/post-expiry callers receive the last cached
    report while the abandoned worker completes naturally.

- `src/app/server/rama/material_portal.clj` (~:789)
  - Anchor mode serves the cheap `:cascade-rows` face. Adds
    `:portal/cascade`. Links `:escape-gauge` with `:on-demand? true` and
    `:embedded? false`. Adds `:cascade` and `:escape-gauge` truncation
    entries. Does not put `:escape-gauge` in the standard portal query
    plan. Preserves the 17-question card floor.

- `src/app/shared/matter_room.cljc` (~:371)
  - Adds the stable `gauge` resident. Initial standard room birth is a
    non-refreshable "not measured" placeholder. An on-demand gauge response
    carries the same resident identity with the last computed report. The
    test routes that resident through the existing P2
    `matter-room-refresh!` edit lane. Subsequent standard portal opens
    cannot overwrite/downgrade the computed report with the placeholder.

- `test/app/material_portal_test.clj` (~:1010)
  - Fixture-injected dark row remains inert; its handler is never called.
  - Production rows exactly equal `cascade/rows`. Injected and production
    source/ownership labels are independently pinned. Standard portal open
    succeeds while `git-spine/read-commits` is replaced by a throwing spy,
    with exactly zero calls. Clean provenance history, ambiguous two-tip
    history, resident refresh, timeout totality, cache behavior, and
    single-flight are asserted. Ambiguous history retains
    `:ambiguous-activation-history`, `nil` count, and `nil` escapes; no
    inferred zero is rendered.

- `test/app/face_projection_test.clj` (~:171)
  - Registry enumeration now pins both new faces and their callable
    projections.

## Gate receipts (implementer-side)

Environment attestation was the first browser evaluation in the definitive
final-source G10 run: isolated current-tree server `http://127.0.0.1:8095`,
`LAND_CLUSTER=0 LAND_PINNED=1`, headless Chrome `150.0.0.0`, WebGPU
available, adapter vendor `google`, architecture `swiftshader`,
`isFallbackAdapter true`. Existing port `8080`, Chrome session, and the
Rama cluster were not restarted or modified.

Before sampling, the real server dispatcher returned:
`:cascade/row-count 1` · `:cascade/source app.server.cascade/rows` ·
`:cascade/labels [:code-owned :in-process]` · `:cascade/read-only? true` ·
`:escape-gauge/on-demand? true` · gauge report status `:measured` · gauge
computation completed with no in-flight worker.

The final live portal returned: one cascade row with the exact production
labels; `:escape-gauge` linked as on-demand and non-embedded; no
`:portal/escape-gauge` report; both `cascade` and `escape-gauge` truncation
entries; exactly 17 questions; query plan includes `cascade-rows` but
excludes `escape-gauge`; four room residents: `head`, `bindings`, `gauge`,
`trail`.

Final-source G10-machine echo distribution:

```text
n=43 · p50=14.2ms · p95=16.4ms · p99=65.1ms · max=65.1ms · misses=[]
bar: p95 < 52ms — observed 16.4ms
```

The one 65.1ms cold echo remains honestly visible in the slow-echo report.

Final post-repair JVM suite: `62 tests · 1,171 assertions · 0 failures ·
0 errors` (exit 0). Targeted P4 run after the falsifier repair: `5 tests ·
58 assertions · 0/0`. (An earlier targeted invocation used an invalid
custom `atom` for clojure.test counters and did not complete; the corrected
`ref`-based invocation is the actual receipt.)

CLJS compile, before the final server-only metadata repair: `272 files ·
271 compiled · 0 warnings`. The repair touched only server `.clj` and JVM
test code; shared/client sources covered by that compile did not
subsequently change.

## One fresh falsifier

Exactly one fresh-context falsifier ran after the initial gates. First
verdict **FAIL** on one genuine issue: the injectable cascade seam could
return an arbitrary/durable-looking row while claiming
`app.server.cascade/rows` and `:code-owned`. Smallest in-fence repair
separated the envelopes — production two-arity: `app.server.cascade/rows`,
`[:code-owned :in-process]`; injectable three-arity: `:fixture-injected`,
`[:fixture-injected :in-process]`. The same falsifier re-ran its exact
counterexample and returned **PASS** with `:post-repair-pass? true`. Its
gauge-honesty checks were already PASS: no inferred linear result under
ambiguous activation history; no alternate provenance truth source; no
standard-portal invocation of the git-backed face; no `escape-gauge` entry
in the standard portal query plan; timeout remains poisoned-total and
single-flight.

## Fence and ownership audit (implementer-side)

Exactly five modified files in the code worktree:

```text
src/app/server/rama/face_projection.clj
src/app/server/rama/material_portal.clj
src/app/shared/matter_room.cljc
test/app/face_projection_test.clj
test/app/material_portal_test.clj
```

`5 files changed, 578 insertions, 23 deletions` · `git diff --check` clean
· index empty · `src/app/server/cascade.clj` zero diff · `cluster.clj`,
`material_circulation.clj`, `facet_master.clj`, shared
`material_portal.cljc`, `binding_material.cljc` zero diff · no new test
namespace (test-runner classification not widened) · ignored `env.clj` and
`node_modules` harness symlinks removed without reading `env.clj` ·
isolated ports `8095`/`9229` down · no staging/commit/push · no docs edited
in the code worktree · GATE_P3 findings 1–3 untouched.

Pinned-enumeration path scans ran for every edited file before final suite
selection: `face_projection.clj` is scanned by `face_arsenal_test`,
`provenance_material_test`, and `face_projection_test` (the existing exact
registry pin was updated); `matter_room.cljc` by `material_portal_test`;
no exact-path enumeration scanner exists for server `material_portal.clj`
or either edited test file. The final selected suite covered the two edited
test namespaces plus the existing face-arsenal, provenance-material,
material-circulation, and cascade-table pins.

The shared docs tree remained at `604e6a8` with the parked foreign
write-set unchanged and never absorbed.

Next action: fresh independent slim gate runs G8/G9/G10-machine under
CONTRACT §8 against the exact uncommitted worktree. If it passes, Sid
issues the source/docs commit ruling. The package's phases end at P4.
