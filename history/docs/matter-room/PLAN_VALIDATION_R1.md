# matter-room — PLAN VALIDATION · ROUND 1 — FAIL (preserved verbatim)

2026-07-27 · fresh-context default-fail validator (Opus subagent, staging
session orchestrating) · inputs: CONTRACT.md (as amended 2026-07-27 at plan
staging) + PLAN.md (first cut) + the manifest source files on disk.
Never overwritten (skill law); the R2 record and the final verdict live in
`PLAN_VALIDATION.md`.

## VERDICT: fail

## FINDINGS (validator's text, verbatim)

1. **[HIGH][S3/S9] No server-callable machine-actor birth path exists; P2's stated approach requires editing a file that is on neither the allowlist nor P2's file list.** Every request builder in `episode.clj` hard-codes the actor: `utterance-actor` returns `{:actor/id "sid" :actor/type :human}` (`episode.clj:124-130`), and `utterance-rows` hard-codes `:production/actor "sid"`, `:production/actor-role "user"`, and cuts text as `{:part-type :human-message}` (`episode.clj:149-156`). There is no actor/kind parameter anywhere (`grep actor episode.clj` → :310, :419, :547, :676 all `(utterance-actor)`). So G5's amended text — "residents ride the existing episode import path **with a machine actor**" — is not achievable: (a) editing `episode.clj` violates §11 (not listed) and P2's own file list, a scheduled false stop; (b) calling `utterance-import-request` unmodified brands every resident as Sid's human utterance and injects them into `read-receipt-records`' `:birth` receipt stream (`episode.clj:590-613`), poisoning the L6 record; (c) composing a bespoke request in `matter_room.cljc` IS a second import artery in substance while passing G5's executable form verbatim (that form only greps for `extract-object-key` changes and new adapter namespaces). **Fix:** P2 must pin one of these before implementation — either amend §11 + the plan to name `episode.clj` and add an actor/unit-kind parameter to `utterance-import-request`/`utterance-rows` (the smallest honest change), or re-cut the resident-birth law. Also re-cut G5's executable form to grep for *new action-request builders targeting `:object-container/import-material`*, not just `extract-object-key`.

2. **[HIGH][S8/G7] `:matter/preview` has no existing server artery and cannot "end in an accepted event"; the P6 preview is client-only, by explicit design law.** `material_truth.clj:236-240`: "`:preview` is here so the vocabulary can SAY it, **because the server never mints one, because a preview is by definition something that did not happen**." The real preview is `window.__bindings.preview` / `.endPreview` in `ground.cljs:3243-3249` ("the active face never moves and nothing durable is written"). The plan defers this to "the exact lane re-pinned from GATE_P6 receipts at implementation — verification duty" (PLAN.md:120-121); the primary source already answers it. As written, G7 ("The four verbs end in accepted events through the EXISTING artery") is unsatisfiable, and `ground.cljs` — where preview actually lives — is absent from P3's file list. **Fix:** declare `:matter/preview` `:pure-projection` (the honest class), amend G7 to "the three durable verbs end in accepted events; preview is proven to write nothing (byte-identical served material before/during/after, the GATE_P6 G3 shape)", and name `ground.cljs` in P3.

3. **[HIGH][S6/S9] `portal-briefing` structurally drops `:master-id`, and P3's file list omits the one file that must change — so the room briefing silently degrades to entity mode and G6 cannot detect it.** `face_projection.clj:1689-1705` destructures `{:keys [entity-id wearers conversation-id master-ids narrowed?]}` and builds an **explicit** five-key map for `material-portal/open` — an unknown `:master-id` is discarded. `reply-to-block/narrowed-portal-open` likewise returns a fixed five-key map (`reply_to_block.cljc:33-37`), so the jetty call at `server_jetty.clj:960-967` cannot carry the anchor either. P3's Files line (PLAN.md:104-107) lists verb_registry / matter_room / server_jetty / face_wiring / tests — no `face_projection.clj`. G6 asserts human/resident **byte identity**, which both sides would satisfy while both being the wrong (entity-mode, nil-entity) projection. **Fix:** name `face_projection.clj` in P3's files, plumb `:master-id` through `portal-briefing`, and strengthen G6 with a positive assertion (the briefing bytes at a room turn contain the anchor's master-identity section) — byte-equality alone is a blind gate here.

4. **[HIGH][S1/G2] At the master anchor, blast radius reports a confident zero — the exact lie the portal's own code comment says is "worse than none".** With no `:entity-id` and no `:wearers`, `subjects` collapses to `[]` (`material_portal.clj:696-699`, `(vec (remove nil? [nil]))`), `blast-radius` receives `candidate-wearers []` and returns `:blast/will-move []`, `:blast/counted-over 0` (`material_truth.clj:199-229`), and `:blast/candidate-wearers` is `[]` (`material_portal.clj:787-789`). The comment at `material_portal.clj:690-695` documents precisely this class ("the portal then reported `0 will move` for an activation that was about to reach every block on screen"). `:portal/wearers` has the same hole: `:wearers/count 0` with basis `:current-client-scene` (`material_portal.clj:842-851`), i.e. the room's own "who wears this type" question answers EMPTY at the address built to answer it. PLAN.md:47-52 says these sections are "exactly today's shapes priced to the anchor" and adds nothing. **Fix:** P1 must override both — blast declares `:blast/basis :no-wearer-snapshot-at-anchor` with `:blast/counted-over nil` (never `0`) when the anchor open carries no snapshot, and `:wearers/basis` names the absence; G2's text must require *blast and wearers* to declare basis, not only "sections touched by the anchor".

5. **[HIGH][S3/T2] The resident-id scheme is self-contradictory: turn-id-keyed unit ids plus content-hashed request-ids means a changed resident either duplicates or is permanently rejected.** Through the episode path, unit ids derive from `(object-key, turn-id, index)` only (`episode.clj:102-109`) and the import/request keys from `turn-id` only (`episode.clj:90-92, :120-122`). PLAN.md:83-85 asks for deterministic unit ids `mr:<master-id>:<section>[:<revision-id>]` with "request-ids derived from (unit-id, content-hash) so replays converge". Both branches break: content-hashed ids mint a NEW turn-id whenever a resident's text changes → a NEW `du:` unit alongside the stale one (T2's duplicate class, at the second open after any activation); turn-id-only ids re-submit the same `imp:` key with a different payload → `import-material-fingerprint-conflict-error` (`object_container.clj:559-565`), a durable rejection that never heals. **Fix:** pin the resident lifecycle explicitly — either residents are content-addressed and a content change is *intended* to mint a new block while the old one is tombstoned (name the tombstone mechanism; `episode.clj:280-284` has `:deleted?` for geometry cells only), or resident bodies are edited through the existing block-edit lane rather than re-imported. The G3 test as planned ("N opens → one row set") passes in both broken branches because it never changes content between opens; add "activate a new revision, re-open, assert exactly one head resident".

6. **[MED][S5] The grammar's refusal of arg-starved matter rows is grammar-VERSION-dependent, so the plan's "structural" claim is false for the facets still on v1.** `valid-bindings?` (v1) calls `bindings-ok?` with `site-check? false` and never runs `site-can-feed?`; only `valid-bindings-strict?` (v2) does (`binding_material.cljc:229-246`). `fm:space`'s grammars map carries only v0 + v1 with a local `valid-master-bindings?` and **no strict entry** (`space_material.cljc:94-104`). So a future `fm:space` revision (or any v1 revision of attention/foldable/positioned, all of which keep their v1 entry frozen by the per-version grammar law) binding `:matter/activate` at `:space/ground` or `:block/user-hit-area` is ACCEPTED and lands in the served table as a live-looking row that no-ops — the exact P5-gate class T10 was written to kill. **Fix:** P3 must state the refusal is v2+-scoped and add a gate assertion for it; if the contract's §4 refusal is meant to be absolute, that requires a v2 grammar entry on `fm:space`, which is a facet-material edit outside §11 — escalate rather than assume.

7. **[MED][S8/G1] G1's headline clause is a tautology that passes today, before any code is written.** `unanswered` walks `:question/answers-at` (`material_portal.cljc:211-223`) and `open`'s `base` map assocs all seventeen paths unconditionally (`material_portal.clj:886-933`) — every one is `sect`-wrapped, so even a section that throws returns a present fallback (`material_portal.clj:103-116`). `(open ctx serve {:master-id "fm:attention"})` returns `unanswered = []` on the current HEAD. **Fix:** G1's executable form needs teeth — assert `:portal/errors` is empty at the anchor, `:portal/identity :entity/found?` is true for a registered master and false for an unknown one, and the anchor's `:portal/masters` contains exactly the anchored master.

8. **[MED][S8/G10] The partition sum-checks, but G10 loses the carve-out its authorizing section carries.** Contract §8 G10 reads "(P2 **+ every serve-widening phase**, machine half + Sid's headed half)". PLAN.md:98 assigns G10 to P2 alone; P1 widens the portal serve, P3 adds act endpoints + a briefing branch, P4 adds two serve faces — all serve-widening, none carrying G10. (P1{G1,G2} ∪ P2{G3,G4,G5,G10} ∪ P3{G6,G7} ∪ P4{G8,G9} = G1..G10 ✓ — the sum-check itself holds.) **Fix:** add "G10 machine half" to P1, P3, and P4's gate lists.

9. **[MED][S7/G10] The escape-gauge serve face spawns a `git log --all` subprocess per call and reads its entire output synchronously — inside a path the portal opens.** `git-log-bytes` builds a `ProcessBuilder`, `.readAllBytes` the whole stdout and `.waitFor`s with **no timeout** (`git_spine.clj:75-88`); `read-commits` parses every commit reachable from every ref (`git_spine.clj:140-149`). PLAN.md:136-140 puts this behind a `:escape-gauge` serve face and PLAN.md:141-143 lands it as an anchor-mode portal section. Against G10's standing 52ms bar this is not survivable, and a hung `git` blocks the serve thread forever. **Fix:** the gauge serve must read a cached/periodically-refreshed report (or be an explicitly on-demand face the portal links to rather than embeds), and the plan must say which; add a subprocess timeout.

10. **[MED][S8/G8] G8's two clauses contradict each other, and the plan restates them without resolving.** G8 demands "a dark row (test-only, never-emitted trigger) present in the projection and inert" AND "zero changes under `src/app/server/cascade.clj`". `cascade/rows` returns the private `declared-rows` literal — one row, no injection seam (`cascade.clj:7-19`). A dark row cannot reach the projection without editing the forbidden file. **Fix:** P4's `:cascade-rows` face must take its row source as an injectable argument (defaulting to `cascade/rows`), and G8's dark-row clause must say the row is injected in the test fixture, never present in the real serve.

11. **[MED][S10] The four matter verbs are grammar-unbindable by construction, so the registry entry is decoration and the real artery is a new jetty invocation lane — a source-specific branch dressed as "the reply-to-block pattern".** The cited precedent is bound and dispatched through the grammar: `reply-binding-row` binds `:resident/reply-to-block` at `:block/user-hit-area` on `[:key/eval :complete]` under the strict v3 validator (`attention_material.cljc:98-110, :169-176`), and the verb declares `:verb/required-args #{}` (`verb_registry.cljc:197-212`). PLAN.md:114-117 deliberately gives the matter verbs `#{:master-id}` so **no** site can feed them — which makes the binding kernel structurally unable to dispatch them, leaving jetty endpoints + console verbs as the only path. G7's falsifier greps for a new *write* path and would not see a new *invocation* path. **Fix:** name this explicitly in the plan and the verb docstrings ("registry-declared, grammar-unbindable, invoked through the named act lane; the registry entry is the effect-class + release-ref home"), and extend G7's grep to new HTTP act endpoints so the lane is disclosed rather than discovered. Also note `:matter/deviate`'s instance branch needs `subject-uid` (`material_truth.clj:51-55`), which `#{:master-id}` does not name.

12. **[MED][S4] T6's durable, discoverable master→room mapping row is silently dropped by the plan.** Contract T7/T6 ruling: "deterministic UUID-SHAPED id … **+ a durable, discoverable master→room mapping row**". PLAN.md:76-79 specifies only the derivation and says "Clients never derive it; they read it served" — no mapping row, and P2's gates (G3/G4/G5/G10) do not test one. The derivation is one-way (MD5), so nothing can invert a room id without a table. A finite in-code reverse table over `facet-masters/master-ids` (7 entries, `facet_masters.cljc:14-27`) would satisfy discoverability with zero durable state — but dropping a contract clause without naming the substitute is a stop-clause-shaped move. **Fix:** state the derived reverse table as an explicit contract amendment, or add the row.

13. **[MED][S1] At the anchor, every `*-here*` key and `:deviations/here` describes a subject that does not exist, and reads as a positive fact.** `masters-of` is called with `subject` = nil, so `(get-in instances [facet nil])` → nil → `:master/pinned-here? false`, `:master/tier-here :master`, `:master/floored-here? …`, `:master/worn-here-revision-id …` (`material_portal.clj:207-270`); `deviations-of` returns `:deviations/here {}` / `:deviations/here-count 0` (`material_portal.clj:355-376`). "Nothing deviates here" is not the same answer as "there is no here". PLAN.md:47-52 overrides `placement` (correctly) but leaves these. **Fix:** anchor mode must either drop the `-here` keys or set them to an explicit `:not-applicable-at-anchor` sentinel; fold into G2.

14. **[MED][S2] The experience face routes the master-id through `extract-object-key`, producing a bogus conversation address and missing the room's own receipts.** `material-experience-projection` computes `object-key` as `(or (:conversation-address params) (some-> (first ids) oc/extract-object-key))` (`face_projection.clj:706-709`). `"fm:attention"` matches no prefix in `extract-object-key` and falls to `:else s` (`object_container.clj:287-390`), so `read-receipt-records` runs against `oc:chat-conversation:fm:attention` — a wasted read that always returns nothing. PLAN.md:53-55 passes only `:material-ids [master-id]`. Contract L6 also wants the room's residents fed in. **Fix:** P1/P2 must pass `:conversation-address (episode/episode-object-key room-id)` explicitly and include the room's resident ids in `:material-ids`. *(The `:relation-roundtrips 1` half is fine — see CLEARED.)*

15. **[MED][S9] The "minimal client entry" is unnamed, and the lane it rides is the machinery-drill escape hatch, not a product navigation lane.** §11 delegates client-file naming to the plan ("named per-phase from the plan"); PLAN.md:72-73 says "`face_wiring.cljs` + minimal client entry" without naming the second file, making the allowlist check unfalsifiable. The "existing UUID conversation lane" is `?drill=<conv-id>` — `drill-conversation-id` in `ground.cljs:67-71`, documented as "machinery drills run on their OWN episode; the genesis stays virgin". Entering a room therefore means a URL change + page reload, not an in-land gesture. **Fix:** name `ground.cljs` (and any other file) in P2, and state the drill-seam entry as the deliberate MVP with its felt cost, so L3's "every first-light verb works in the room UNCHANGED" is not read as "you get there without leaving the land".

16. **[MED][S8/G1] There is no cross-JVM byte harness; the plan cites "the P7 harness pattern" that does not exist.** The shipped determinism test opens twice **in one JVM** and re-checks under `*print-namespace-maps* true` (`test/app/material_portal_test.clj:128-152`). PLAN.md:56-57, 60-61 and G1's third clause assume a two-JVM comparison. **Fix:** either build it (a second process printing `canonical-edn`'s sha, compared) or amend G1 to the existing in-JVM + print-flag form with the sha pinned in the phase artifact.

17. **[LOW][S1] `:portal/identity :entity/id` is unpinned at the anchor, and it is load-bearing for the render title and the briefing's first sentence.** `render-model` reads `[:portal/identity :entity/id]` → "portal · (no entity)" and `briefing` produces "You are working INSIDE the material portal for (no entity)" when it is not a string (`material_portal.cljc:709-712, :750-754`). PLAN.md:43-45 names `found?`/kind/facet/floor-master-id but not `:entity/id`. **Fix:** pin `:entity/id` = the master-id at anchor mode.

18. **[LOW][S9/S8] Two small allowlist/naming gaps.** `repo-root` is private in `cluster.clj:267` (not on the allowlist) — P4 must re-derive `(System/getProperty "user.dir")` locally rather than reach for it. And PLAN.md:137-139 says "the activation trail" without naming it: the only existing activation-event read for this report is `read-revision-history` over `(facet-master/active-pointer-container-id provenance-material/spec)` (`cluster.clj:533-539`) — pin it, since which master's trail is read is exactly what F4's `:ambiguous` residue turns on.

## CLEARED (validator's text, verbatim)

- **S7 (gauge inputs — fully cleared):** `terminal-escape-report` consumes only log-derived rows and activation events with zero filesystem inference, and `read-commits` rows carry `:files` in exactly the shape `commit-touches-policy?` reads (including the `" -> "` rename form) — `git_spine.clj:138` against `material_circulation.clj:404-412`. *(Latency is finding 9; the input composition itself is sound.)*
- **S2 partial:** `:relation-roundtrips 1` and `:batched? true` ARE still reported for a target with no relations — the count keys off `(and rk-rt (seq ids))`, never off result size (`material_circulation.clj:1015-1021`).
- **S4 partial:** `nameUUIDFromBytes` is JDK-specified (MD5, v3), byte-stable across JVMs; no in-code UUID validator exists — the CLI (`summon-argv` `--session-id`, `episode.clj:836-849`; jsonl filename `:827-834`) is the external validator. The stop clause's "record the exact constraint" is satisfied by this record.
- **S6 partial:** the authority half holds — a server-side reverse map over the seven registered masters means a forged conversation-id can only name a different room, never widen past one master.
- **S8 partial:** the partition sum-checks exactly as claimed; no gate unassigned, none double-owned.
- **S10 partial:** the package's own edits cannot self-trip the pre-registered falsifier — `default-material-policy-paths` excludes `verb_registry.cljc`/`binding_material.cljc` and none of the phase files (`material_circulation.clj:60-78`). `app.server.cascade` + `face-projection` are required into the jetty JVM (`server_jetty.clj:10, :13`) — the `:code-owned :in-process` label is honest.
