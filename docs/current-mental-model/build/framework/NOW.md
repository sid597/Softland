# framework — thread file (faces-as-assemblies)

## STANDING (v1, re-frozen at CONTRACT v1 landing 2026-07-11)

- **Binding:** `docs/current-mental-model/decisions.md` + `build/framework/CONTRACT.md` (v1, 2026-07-11). `ROAD.md` v2 is direction-grade input, NOT binding.
- **Precedence rule:** this file is a baton, not a source of truth; if it contradicts CONTRACT.md or decisions.md, those win — flag the discrepancy in NOW, don't pause.
- **Process:** the `/work-package` skill governs (one phase per fresh CONTEXT; validation default-fail; never overwrite a FAIL artifact; ≤15-line NOW entries, FAIL findings verbatim exempt). Wave QC = the 2026-07-05 delivery-mode shape: gates green in-lane, ONE serial suite + ONE batched falsification-by-class + ONE Fable gate at wave end (CONTRACT §11 G16).
- **Scope guard:** CONTRACT §2 fences govern — new files only; `rect_tree.cljc` / `ui_primitives.cljs` / `trail_face/*` READ-ONLY; `electric_flow.cljc` + `file_viewer.cljc` owned solely by lane W1-C; shared runtime files touched only at W1-INT by the orchestrating session; probe code stays UNCOMMITTED (evidence, not product).
- **Hard rules:** never read `src/app/server/env.clj`; code and docs in separate commits; docs only on the local docs branch (never pushed/merged); relation-kind additions require explicit authorization (closed enum, D-004 — block-kernel precedent; `pairs-with` is the pre-named candidate, CONTRACT §8).
- **NOT without Sid:** wave dispatch (spend); countersign of the two decision-log drafts (CONTRACT §15: middle regime; self-hosting test formulation); relation-kind additions; Wave 2 dispatch.
- **Prompts:** probe → DONE (`build/framework/PROBE.md`; slots finalized in CONTRACT §10) · W1 orchestrator → `docs/sessions/framework-w1-orchestrator-opening-prompt-2026-07-11.md` (**the one to paste in a fresh Fable session**; wave word given 2026-07-11) · W1 lanes → `framework-w1-lane-prompts-2026-07-11.md` (PINNED to CONTRACT §§4–11; consumed by the orchestrator's subagents).

## NOW (≤15 lines per entry; newest last)

- 2026-07-11 · Fable (`3-way-design-framework-converge`) · **package STAGED.** ROAD v2 committed (`6febdb1`) after ingesting two independent source-reviews — mechanism re-grounded (pure-fn primitives over rect-tree; Electric = bridge only; U4/U5 off the critical path). Execution map drawn: Fable line = contract · probe-brief · wave gates · log-entry drafts; two waves × three parallel lanes; machine-cut timing rides W2 (D-005), live tailer waits on its pre-registered form-break (2026-07-06 gate-raised default). All opening prompts written (see STANDING). Next: dispatch probe ∥ Fable writes CONTRACT; W1 dispatches only after CONTRACT v1 pins the lane prompts. No code exists; nothing on main.
- 2026-07-11 · Fable (contract session) · **CONTRACT v1 WRITTEN** — `build/framework/CONTRACT.md`: grammar v0 (closed key set; the guard as a mechanical no-lists/no-symbols walk; error cards registry-independent), two-stage interpreter with Δ1 `(view-instance,address)` carry + apply-report, registry (runtime map / persisted keyword+address), artery (one generic pull; projection registry server-side; INV-19 epoch re-pull), §8 kernel-object schema (W2-implemented; **zero enum edits needed** — `supersedes` already registered, verified `relation_kernel.clj:58-72`; `pairs-with` pre-named authorization), 13-trap ledger, G1–G16 gates (adopts the Trunk-5 mechanical fixture-fidelity gate as G7 source-form diff + G14 two-instance falsifier), §10 PROBE slots with defaults (wave does NOT block on probe), §15 drafts of the two log entries (D-011 middle regime · D-012 self-hosting test) for Sid's countersign. Every source claim re-verified at file:line this session; key correction vs the staging prompt: G14's sidebar falsifier is split (JVM trail-face-slice equality in the suite + live sidebar check at INT) because `build-sidebar-tree` is cljs-only — grounds in §11. Lane prompts PINNED (`framework-w1-lane-prompts-2026-07-11.md` → §§4–11); STANDING re-frozen v1. PROBE.md had not landed. Next: Sid's wave word (spend) → dispatch the three W1 lanes; probe still dispatchable ∥.
- 2026-07-11 · probe (Opus subagent) · **Step-0 PROBE DONE — walker real on the real path, 25/25 green JVM-side.** Findings → `build/framework/PROBE.md` (reconcile against CONTRACT §10 PROBE-slot defaults; flag-don't-pause). Numbers (Ryzen 9 9900X / JDK21 / shape-proxy, browser rides Step 1): apply-assembly **5.2 / 12.3 / 24.6 ms** @ 201/501/1001 nodes on a data change (~linear; resolve-layout <0.3 ms — the walk dominates); wrap-line 15.6 µs/block, 7.8 ms/500 blocks. **"Sub-millisecond" is optimistic → low single-digit ms @ conv scale, ~linear to ~12–25 ms @ 1000 nodes, data-change not per-frame.** Three rt-node gaps (did NOT extend rect_tree): (1) `:text-layout` is not an rt-node param & NO shipped code sets it — `resolve-text-layout` is a dead hook; riding it DOUBLES wrap cost (one-wrap measured ≈49% cheaper) → Step-1 text-run must wrap once & emit positioned ops (`build-empty-state` pattern); (2) `ui_primitives.cljs` is cljs-only → extraction is cljs→cljc PORTING, not reference; (3) "measure in primitive, arrange in engine" is law (nested `:auto-height?` mis-measures). Scroll (check b): assembly pane rides the camera (trail-face convention) + declares content-h in root `:data`, stays scroll-independent (never bake scroll-y into the tree). Dev-hook mount SKIPPED (>10-line multi-file wiring past the fence); substituted a JVM flatten through the real `tree->rects`/`tree->text-ops`/`tree->shadows` (correct `:id`-carrying, wrapped output). Probe code UNCOMMITTED (`src/app/client/workspace/face_probe.cljc` + test). This session committed nothing (code or docs).
- 2026-07-11 · Fable (contract session, close) · **WAVE WORD GIVEN by Sid ("counter signed now for the wave" — scoped to the wave; D-011/D-012 drafts remain pending his hand). CONTRACT finalized v1.1** — §10 slots resolved against PROBE.md (all three on their defaults, no activation fired), the one-wrap `:text-run` law applied at §6 + G8 (dead `:text-layout` hook, ≈2× cost — PROBE Finding 1), pane scroll contract pinned in §5 (`:assembly/content-h` + scroll-independence); complementary edits reconciled with the probe-close session's §10 outcomes pass (no conflicts). Orchestrator prompt written: `docs/sessions/framework-w1-orchestrator-opening-prompt-2026-07-11.md` — ONE fresh Fable session dispatches lanes A/B/C as parallel Opus 4.8 subagents, runs W1-INT + G16 itself. Board flipped to wave OPEN. Next: Sid pastes the orchestrator prompt in a new session.
- 2026-07-11 · W1-B vocabulary (Opus subagent) · **LANE W1-B DONE — G7–G9 green in-context (7 tests / 135 assertions, 0 fail/0 err).** Built `src/app/client/workspace/face_primitives.cljc` + `test/app/face_primitives_test.clj`. `registry` = plain map, 16 prims: 11 verbatim `ui_primitives.cljs` copies (`:panel*` `:list-item` `:card` `:badge` `:divider` `:empty-state` `:scrollbar`) wrapped into the §6 `(ctx props children)→rt-node` interface + 2 trail_face proto-primitives (`:omissions` `:hole-card`, D-005 affordances) + `:stack` + the two new: **`:text-run`** (wraps ONCE via `wrap-line`, emits own positioned ops, h=lines×line-height; NO `:text-layout`/`resolve-text-layout` anywhere — dead hook, T7/PROBE-F1; measured 5 lines→h100, 5 ops) and **`:indent-rail`** (layout-skipped gutter rail; golden h=136, children stack non-overlapping y=[0,48,116] indented x=16). **G7** = source-form DIFF (read cljs/cljc as DATA, JVM; not call-and-compare) pins 29 copied forms (all 14 named builders) against `ui_primitives.cljs` + `trail_face/cards.cljc`, allowlist empty, catches drift (verified). Measure inside primitive, arrange in engine; width default `(:content-w geom)`, max-chars ÷ `(:char-advance geom)`. Copies verbatim/originals untouched; fence honored (new files only). One interface note for W1-INT: 3 legacy builders (`panel-group`/`list-item`/`scrollbar`) derive child ids via `(name id)` → wrappers fold the vector-path id to a name-able keyword (`[:root 1]→:root.1`, still travels on reorder, T6); copies NOT edited. feed-entry-card NOT harvested (heavy private-helper chain, band-specific measure — extension point). No conflict escalated. Artifact: `build/framework/W1-B-vocabulary.md`. Committed nothing (orchestrator owns commits).
- 2026-07-11 · W1-A interpreter (Opus subagent) · **LANE W1-A DONE — G1–G6 green in-context (6 tests / 49 assertions, 0 fail/0 err).** Built `src/app/client/workspace/face_assembly.cljc` (`compile-assembly` (registry,assembly)→compiled · `apply-assembly` (compiled,data,view-ctx)→rt-tree) + `test/app/face_assembly_test.clj` + `test/app/fixtures/faces/{outline.edn, outline-conversation.edn, outline.golden.edn}`. Two-stage, PURE, TOTAL (never throws): compile validates V1–V7 into a pre-resolved data PLAN (builders closed over once, T3); **V4 guard** = mechanical `scan-guard` (first list/symbol/set/char rejects w/ path, T2); **V3 = fn? check** (not mere presence) → error path is registry-independent (T4); apply is post-order (children built before parents per §6 `(ctx props children)→rt-node`), then ONE `resolve-layout` (measure-in-primitive/arrange-in-engine, T7), then stamps root `:data`: **Δ1 `(view-instance,address)` carry** (T10), **apply-report** `{:items-without-id :binds-missing}` (counted, never lies), **`:assembly/content-h`** (measured bottom-up, §5 scroll contract). Ids deterministic: `[vi name]` + structural segs; each-item seg = item `:id` (travels across reorder, T6) else index (counted); `:props {:id}` overrides. `error-card-node` built-in/registry-independent, renderable, name+addr+≤5 errors. Tested vs 3 local stubs (`:stack :card :text-run`) — interface contract-fixed, goldens stay valid vs lane B. **G1** golden-equal committed snapshot; **G2** 5 guard classes each→error card; **G3** malformed+unknown-prim+**corrupted-registry** all→valid error card; **G4** purity+Δ1; **G5** each id discipline; **G6** bind discipline. Probe **DISCARDED** (different builder iface, no guard/Δ1; architecture already in CONTRACT §5/§6 via PROBE.md) — both probe files DELETED. One flag for INT: width flows thread-unchanged (interpreter stays generic; child content-w narrowing for real indentation is a lane-B/`:indent-rail` question the Outline face doesn't hit — padding-offset indentation suffices). Artifact: `build/framework/W1-A-interpreter.md`. Committed nothing (orchestrator owns commits).
- 2026-07-11 · W1-C projection+artery (Opus subagent) · **LANE W1-C DONE — G10–G13 green in-context (7 tests / 73 assertions, 0 fail/0 err, over the REAL `7c80ce2a` corpus).** Booted `/rama`+`/electric-docs` first. Built `src/app/server/rama/face_projection.clj` (projection registry `{:conversation …}` + server-side face dispatch `serve`, READ-ONLY over `river-page`+`read-source`; NO depots/topologies/kernel edits — T12/G12) · `file_viewer.cljc` `FacePull` e/defn + first-light distiller runtime (trail-view-runtime precedent) · `electric_flow.cljc` `!face-request`/`!face-data` + GENERIC no-`case` pull loop (T8) · `face_wiring.cljs` (INV-19 epoch debounce, S1/S2). **Empirically de-risked before coding** (probed real corpus): (1) 247 river EVENTS (not blocks; =`:river`=read-plan total), river-page HARD-CAPS 64 blocks / NO cursor → projection serves ONE bounded page + signals `:conversation/truncated?`+`:paging-lack` (full paging = block-kernel §10 scale ext, D-001-gated; v0 face wears ~first 52 events — first D-005 data-work item); (2) `:until-ms` time source — units DON'T graduate (read-unit `:graduation nil`, DerivedUnitRow has no time) so NO block-material API carries time; used per-part `SourceArtifactRow.created-at-ms` (=msg production time, verified `block_distiller.clj:790`) via `ocr/read-source`, ≤ page-surfaces ≤ limit, **verified monotone → prefix-consistent** (G11). **Request** `{:face :address :params{:limit :until-ms} :epoch}`; **data-context** top-level `:turns [{:id :speaker :order :blocks[{:id :kind :text :order :time-ms}]}]` + `:conversation/{address,limit,until-ms,river-events-total,blocks-returned,truncated?,page-complete?,debris-excluded?,paging-lack,read-plan}` + `:face/rendered-at-ms`. **G10** 247 total honest + flatten==river-page(64) bijection/order + truncation + debris-excluded(399); **G11** `:until-ms` prefix-consistent + seek-bound=1+4·limit page-bounded; **G12** mechanical read-only source scan; **G13** one atom/one pull/one data + stub-registry dispatch + unknown-face→error(no throw). Both `.cljc` compile clean clj-side. FLAG for INT/gate: `read-source` is the 1 read outside the strict 3 named APIs (no API carries time) — bounded F3-class filter read, no kernel edit; confirm framing. Did NOT mount into runtime loop (W1-INT's shared-file job). Artifact: `build/framework/W1-C-projection-artery.md`. Committed nothing (orchestrator owns commits).
- 2026-07-11 · Fable (W1 orchestrator) · **WAVE 1 CLOSED — G16 PASSED; Step-2 exit reached.** Lanes A/B/C parallel-green (entries above); W1-INT in-session: `:face-assembly` mode mounted (trail-face flow shape), worn Outline authored vs the REAL vocabulary (`resources/public/faces/outline.edn`; lane A's stub fixture stays the interpreter golden), G14a PASS in suite (holes-column equality; trailing-gap finding pinned exactly), G14b honest negative (sidebar PRE-DATES the vocabulary — `:sidebar-entry` prim = named gap), G15 GREEN: real `7c80ce2a` worn (35 turns/64 blocks; scrub prefix-visible at a 10:33:30Z cut; epoch-bump lifecycle fix found-by-wearing, verified fresh-JVM; CPU-raster screenshots — GPU capture = env lack). SLOT-A browser 2.6/6.0/11.8 ms @ ~200/500/1000 nodes. G16: 5-class falsification → 10 real findings FIXED (wear-race publish guard · drag-select hidden-doc leak · scrub scroll strand · non-prefix dirty-time cuts · serve totality · first-light pending/failed honesty · hash-id parity · root-`:each` compile reject · named 0.56 fallback · agent overlay gates) + recorded minors — verdicts in `W1-GATE.md`. Suite 22t/281a green (+root-each regression). Code commit `c84ebfa`; artifacts `W1-INT.md`/`W1-GATE.md`/screenshots/`W1-INT-g14b-verbatim.json`. Deviations recorded: render.cljs pass-throughs · agent_flow command site · G12 read-source amendment (`fadd067`, lane C's framing CONFIRMED). NOT started: Wave 2 (needs Sid's dispatch); D-011/D-012 countersign still pending Sid.
- 2026-07-11 · Fable (W2 orchestrator, this session) · **WAVE 2 OPENED on Sid's dispatch word.** CONTRACT v2: §§16–21 appended (W2 = ROAD Steps 3–4; §8 realized; arsenal placement rulings — `face_arsenal` intent-only micro-kernel for wear log + face index (§8 T11 carve-out added in place), `/assert`-style WAL durability, `RecordFaceWear` = first write e/defn, per-root classify-fn on the watcher, `imp:asm:` = the one authorized kernel edit; traps T14–T19; gates G17–G26). **D-011 + D-012 COUNTERSIGNED by Sid in-session (asked explicitly) → entered in decisions.md CLOSED; §15 flipped to lineage; MEMORY/board swept.** Design-round correction: real candidates = BlockExplorer's six modes (ROAD's Margin/Arcs were working names); files pulled verbatim via DesignSync → `build/framework/design-round/` (63k+49k, untruncated; DesignSync unavailable to subagents — probed). **Sid's picks: 1e Boxes + 1f Minimap/Reader.** Lane prompts pinned (`framework-w2-lane-prompts-2026-07-11.md`); lanes D (arsenal) ∥ E (plurality) dispatched as background Opus subagents. Next: lanes land → W2-INT (client arsenal + flip) → G26 in this session. Docs commits: `4aadbf2`, `a63d797`.
- 2026-07-11 · W2-E plurality (Opus subagent) · **LANE W2-E DONE — G22–G23 green in-context (lane suite 9t/234a; full face regression 32t/529a, 0 fail/0 err).** Sid's picks transcribed over the REAL vocabulary: `resources/public/faces/boxes-face.edn` (1e — russian-doll turn frames, 6-line clip w/ honest ▸ stub, page-end paging answer) + `minimap-reader-face.edn` (1f — fixed 152px density strip h∝√words + one-turn full-fidelity reader). Four gap-fill prims in `face_primitives.cljc`, each cited to its design element: **`:box`** (containment frames; `:card` can't stack), **`:header-band`** (frame headers, right-aligned meta = the flex-spacer, in-prim word/count computation per INTENT), **`:text-clip`** (clip-at-~6 + "▸ N more lines", honest `:data`), **`:sliver`** (density bar, the :795 formula verbatim, kind palette). **The named interpreter extension TAKEN** (W1-INT Lack 2, §16): `:child-w`/`:child-inset` props narrow the subtree's content-w in `face_assembly.cljc` — engine-side arithmetic, grammar untouched, regression in suite; W1 behavior byte-identical without the props. Goldens committed over `plurality-conversation.edn`; apply-reports zero/zero. **Headline lack (G25 evidence): both faces WANT turn⊃user|response pair structure** — v0 single-speaker turns flatten the doll; + `:reader-turn` focus selection (minimap reader honest-gaps over the real projection until a `:params` focus key lands), `:actions` verbs, event ranges, noise classes, conditional display, no `birthed-by` (provenance lack — design conversation not in OC). `:pad-after` NOT needed. Artifact: `build/framework/W2-E-plurality.md`. Committed nothing (orchestrator owns commits).
- 2026-07-11 · W2-D arsenal (Opus subagent) · **LANE W2-D DONE — G17–G21 green in-context (lane suite 6t/233a, 0 fail/0 err; receipt re-run over the real 3-face dir after lane E landed).** Booted `/rama`+`/rama-pitfalls` first. Built `assembly_adapter.clj` (§17 identity `asm:<name>`; `imp:asm:<object-key>:<sha>` literal; T18 ONE-compiler verdicts; R1 malformed-still-ingests; closed provenance→kind map — unregistered kind unconstructible, G19) · `face_arsenal.clj` (intent-only kernel #6: one depot, wear journal/events/counts atomic per face event, pointer roster — T14/T16; `record-wear!` WAL-first + server stamp; idempotent boot replay) · fenced edits: watcher `:classify-fn` + `faces-classify` + `import-assembly!` (T17 dual-append converges on every accepted decision; T19 `.edn` only via the faces classifier) · the ONE kernel branch (`extract-object-key` `imp:asm:`, two-segment — a literal `imp:clj:` mirror would truncate at "asm" and fail G18) · projections `:assembly`+`:face-list` (read-only, total, named-API surface — G21 scan asserts the exact call sets). **Design finding:** OC's native-identity claim rejects content change on a stable container → the face container materializes as IDENTITY ANCHOR (nil inline content; bytes ride the revision chain via `read-current-revision`) — resolved in-seam, no kernel edit. **Routing law worked in full:** colon-carrying `asm:` keys misroute through `src:/du:/rev:/evt:` families → product reads use decision seam + completion (new branch) + `oc:doc:` + `$$source-latest-by-ref` only; misrouting APIs = named lack (INT flag 3). **INT flags:** W1 `g13-serve-dispatch` pins registry to `[:conversation]` — superseded by §16, one-line fix, out of fence (the suite's single red); KERNEL-SHAPE `:examples` entry pending; W2-INT wiring notes in the artifact. No stop clauses. Artifact: `build/framework/W2-D-arsenal.md`. Committed nothing (orchestrator owns commits).
- 2026-07-11 · Fable (W2 orchestrator, close) · **WAVE 2 CLOSED — G26 PASSED; Step-3+4 exits reached.** Lanes D/E parallel-green (entries above); W2-INT in-session: arsenal boot chain (launch → WAL replay → sweep → watcher, async per trail precedent), wear-from-Rama (W1 HTTP fetch RETIRED), first write e/defn (`RecordFaceWear` + outbox), sidebar FACES section from Rama, kernel #6 registered. **The flip is live**: real `7c80ce2a` (35t/64b) worn through Outline + Boxes + Minimap/Reader, flipped by command, wear counts [1,1,1]; scrub appends nothing (T15 live); **Step-7A live twice** (edit .edn → watcher → epoch → recompile, no re-wear). G26: 5-class falsification (~503k subagent tokens) → 14 findings FIXED in-session (incl. 3 CONFIRMED HIGH: guard poison-event wedge · §17 contract-text error on edge idempotency (amended, dated) · rename-ghost roster entries; + the live-found projection-routing gap → roster-based resolve) + 10 recorded minors — verdicts in `W2-GATE.md`. Suite **46t/804a/0f/0e** (+8 biting gate regressions). Live evidence + real screenshots in `W2-INT.md` (+3 PNGs, results JSON). Lacks ordered (D-005): pair structure = THE machine-cut evidence item (double-confirmed); reader focus control (:actions-era); paging carried. Dev app left RUNNING for Sid (`/face boxes-face` etc.). NOT done: package-close retro + adversarial recheck (Sid's call — more subagent spend); machine-cut package opening (his call on the G25 evidence).
