# space-as-entity — P2 (rung 2) receipts

2026-07-26 · Codex implementation context · **PASS**. Contract deliverables
6–10 and RULING R1 deliverable 6b landed. Rung 3, the G10 lift, reaction
declarations, zoom bands, nested-space cameras, and type recipes were not
started.

## Deliverables

6. `space_material.cljc` now declares `fm:space`: zoom form
   `0.1 <= zoom <= 8.0`, per-key finite bounds inside `[0.01, 1000]`, the
   cross-form `min < max` invariant, and grammar-v1 material containing only
   the bindable tap-anchor and Shift-marquee rows. The camera-inclusive four
   space rows remain the code floor in `binding_material.cljc`.
6b. `facet_material.cljc` has the optional `:form-validators` seam. Both
   `compile-form` and `valid-material?` pass the material-keys projection to
   it. Declarations without the entry retain their prior result shape.
7. One `camera-gesture-reserved?` Var owns the reservation set. Its three
   product read sites are:
   `space_material.cljc` (`fm:space` grammar validator), `ground.cljs`
   `set-instance-bindings!` (console), and `ground.cljs`
   `served-instance-binding-rows` (durable instance lane).
8. `:camera/zoom-at-pointer` reads the resolved `:space` wear from
   `current-material-wears`; `0.1` and `8.0` are literal fallback values. No
   atom or second serve/read path was added.
9. `fm:space` is registered. The space floor label is
   `code-floor:fm:space:v0`, sourced beside the special floor rows and derived
   through the registered spec by the server and client label maps.
10. `space_material.cljc` is a member of
    `default-material-policy-paths`.

## Gate receipts

- **G4 PASS** — suite candidate binding naked
  `[:pointer/press :threshold #{}]` to marquee was rejected with
  `:facet-master/bindings-invalid`; a synthetic hostile served value floored
  the whole facet and pan/zoom remained claimed at `:floor`. In the live
  browser, `__bindings.preview("fm:space", ...)` returned
  `candidate-invalid`, created no preview state, and the 12-probe drill stayed
  `pass`; pan and both wheel probes remained `floor`. The console instance
  seam returned `camera-gesture-reserved` for naked drag, while the Shift
  counterexample crossed that fence and reached the unchanged
  `instance-site-refused` boundary.
- **G5 PASS** — source gate found one predicate definition and exactly three
  product call sites: space grammar 1, console 1, served-instance lane 1.
  Independent falsification checked the four refused combinations
  (naked/`:any` drag and wheel), plus Shift and non-ground counterexamples.
- **G6 PASS** — live-cluster server receipt activated revision
  `rev:fm:space:df1e3a0b68c603be2b9d96b11fda9be75b5d779b074eced569ed0af144671fda:ec986bc027dc13c0744df5e135faf636e53ff2c2fa3b92bf7c1b887143565d8a`
  with `[zoom-min zoom-max] = [0.1 2.0]`. A trusted browser wheel drove zoom
  `1.0 -> 2.0` exactly. Tap-empty and Shift-marquee flipped to `master`;
  pan, wheel-on-ground, and wheel-at-block stayed `floor`.
  Rollback repointed to base revision
  `rev:fm:space:df1e3a0b68c603be2b9d96b11fda9be75b5d779b074eced569ed0af144671fda:dab5f109f04f564adfccef4eed23392fcf52d1e971226c4d76e1343623953327`;
  after reload, a trusted wheel reached `8.0`. Malformed revision
  `rev:fm:space:df1e3a0b68c603be2b9d96b11fda9be75b5d779b074eced569ed0af144671fda:ebeff1719fdb4019794ab84a6a52df441f18c5725c08922ca1cdde9edc3723a0`
  (`min 5.0`, `max 4.0`) was retained as an inactive candidate, activation
  was refused with `:space/zoom-clamp-invalid`, and the base remains active.
- **G8 PASS** — direct membership assertion for
  `src/app/shared/space_material.cljc`.
- **G9 PASS** — live `__bindings.table()` and the server's
  `__bindings.served()` emitted identical space rows and the same floor
  master/revision label, `code-floor:fm:space:v0`. The stale literal
  `code-floor:space` remains only in the negative sweep assertion.
- **G11 PASS** — arbitrary-garbage candidate produced an error card; a
  synthetic garbage served value floored the whole facet. All 12 probes
  remained claimed at the floor in the suite; the live preview was refused
  without creating preview state and its drill remained `pass`.
- **G12 PASS** — all six pre-space specs (every grammar declaration) have no
  `:form-validators` entry; an entry-less probe retained the exact previous
  compile result and wear validity. `min >= max` refuses at `compile-form`
  with only `:space/zoom-clamp-invalid`; the same synthetic served material
  falls through `valid-material?` to `[0.1 8.0]` floor wear. Valid instance
  forms carrying the three extra material keys compile, and malformed
  instance forms inherit the same clamp refusal.

## T8, regression, and build receipts

- Updated pins/enumerations: `binding_dispatch_test.clj` (registered
  `fm:space`, special four-row floor, label), `material_truth_test.clj`
  (G14 label), `provenance_material_test.clj` (registered family),
  `material_circulation_test.clj` (escape membership), and
  `test_runner.clj` (fail-closed pure-tier classification). The dynamic
  `face_projection_test.clj` and `material_portal_test.clj` lanes were rerun.
- Focused final G4/G5/G6/G9/G11/G12 suite:
  6 tests / 52 assertions, green.
- Affected seven-namespace sweep before the final three test-only G12
  assertions: 77 tests / 1,208 assertions, green.
- Final `clj -X:test fast`: 25 namespaces / 189 tests / 1,777 assertions,
  green; shared-cluster count 1; flake registry unchanged.
- `clj -M:dev -m shadow.cljs.devtools.cli compile dev`: 272 files,
  0 compiled, 0 warnings. The live watch build that first loaded the P2
  client compiled 21 files with 0 warnings.
- Fresh-context falsification found no concrete code defect after adversarial
  checks of the fence, both T9 sites, instance inheritance, P1 chain append,
  label/escape/clamp integration, and both stop boundaries.
- `git diff --check` is clean. `verb_registry.cljc`,
  `instance-legal-sites`, and frozen binding grammar v1/v2 are untouched. No
  new state atom exists in the diff.

No commit or push was made. The live cluster is left with the 8.0 base active
and the malformed candidate inactive and queryable. No rung-3 work was begun.
This is the implementer receipt; the role-specific Fable gate verdict and its
`decisions.md` record were not fabricated in the Codex implementation context.
