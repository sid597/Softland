# space-as-entity — P1 (rung 1) receipts

2026-07-26 · Codex implementation context · **PASS**. Contract deliverables
1–5 landed. P2, rung 3, reaction declarations, space material, and every
other later cut were not started.

## Pre-code manifest verification

The contract manifest was checked against disk before the first product edit.
The operative anchors were present and semantically matched:

- `ground.cljs`: `wears-for` 271; floor rows 2201; served instance rows 2260;
  claim chain 2330; binding tables 2411; zoom verb 2649; decision/dispatch
  2666–2735; key/eval 2719; wheel 2888.
- `binding_material.cljc`: sites 77; G10 fence 102–119; validators 136–234;
  space floor/claim 384–421; drill 432 onward; interaction table 532 onward.
  `space-claim` was confirmed to be a one-element vector before applying T1.
- `verb_registry.cljc`: floor reservation 171–195 and `bindable?` 234–240
  were read and left untouched.
- The attention form/bindings/validator pattern, facet registry at 14–20 and
  42–53, and circulation policy set at 60–79 all matched the manifest.

## Deliverables

1. `claim-chain` now always performs
   `(into claims binding-material/space-claim)`; miss/stale claims therefore
   produce the space-only chain.
2. `handle-wheel!` sends `(pick-at x y)` instead of fabricating `:hit nil`.
3. `decide` is the one route through the one runtime `claim-chain`; pointer
   and key/eval inputs both cross it.
4. The shared floor drill has probe 12, wheel at a real block site, and emits
   decision site + depth.
5. T8 pins were swept and the moved enumerations were updated in
   `binding_dispatch_test.clj`.

Changed code/test files: `ground.cljs`, `binding_material.cljc`,
`binding_dispatch_test.clj`, and the stable live server receipt helper
`space_as_entity_receipt.clj`. `verb_registry.cljc`,
`instance-legal-sites`, `facet_masters.cljc`, `material_circulation.clj`,
and `space_material.cljc` were not changed.

## Gate receipts

- **G1 PASS** — baseline banked before code at 17 tests / 317 assertions.
  Final suite: 17 / 329, zero failures/errors. The first 11
  verb/tier/facet/outcome tuples remain exactly:
  focus/place-caret-floor-attention; focus/enter-block-floor-attention;
  placement/drag-group-floor-positioned; selection/text-begin-floor-attention;
  focus/release-floor-attention; placement/drag-group-floor-positioned;
  fold/toggle-section-floor-foldable; anchor/place-floor-space;
  camera/pan-floor-space; selection/marquee-begin-floor-space;
  camera/zoom-at-pointer-floor-space — all `:claimed`.
  Live `__bindings.drillAll()` passed all 12 probes for `fm:attention`,
  `fm:foldable`, and `fm:positioned`, with empty deviations. Probe 12 was
  `zoom-at-pointer / floor / space / claimed`, decision site `ground`,
  depth 1, for all three.
- **G2 PASS** — stable command:
  `clj -M:test -m app.space-as-entity-receipt g2`. Final live-cluster,
  server-read enumeration: 6 masters, 29 revisions, 22 binding rows,
  complete history; active served wheel-at-block rows 0; active history
  rows 0; flagged non-active residue 0. The stop clause did not fire.
- **G3 PASS** — Chrome 150 drove the live dev app over the real served block
  ending `ep:5c962bf4:000000`. Its click focused that exact unit; wheel at
  the same point moved zoom 1 → 1.1273954827805408. A temporary console
  wrapper around the real handler captured that actual event's decision:
  `claimed`, subject `space`, site `ground`, facet `space`, tier `floor`,
  depth 1, verb `zoom-at-pointer`, conflicts empty. Empty-ground wheel moved
  1 → 1.0941005032024715; empty tap changed editing/focused → anchor/empty;
  empty drag moved the camera by [30, 20]. Reciprocal drives restored the
  initial camera (within floating-point epsilon).
- **G10g PASS** — grep: one `claim-chain` definition, one runtime
  `binding-material/space-claim` reference, and one shared
  `:claims (claim-chain hit claims)` route. The key/eval test proves its
  pre-rung and appended-space decisions are exactly equal.

## T8 and regression receipts

The test-tree sweep found five source-pin namespaces:
`binding-dispatch-test`, `material-truth-test`, `provenance-material-test`,
`material-circulation-test`, and `reply-to-block-test`.

- Updated: drill count 11 → 12, exact prior-11 decision fields, probe-12
  depth receipt, and one-builder source scan in `binding-dispatch-test`.
- Confirmed unchanged: material truth's 21 interaction rows and G10
  space-instance refusal; reply-to-block's key/eval seam; provenance's
  no-parallel-artery scan; circulation's unrelated ground-file fixture.
  The dynamic face-projection table checks also require no count change.
- Focused binding suite: 17 tests / 329 assertions, green.
- Three remaining pin suites: 26 / 423, green.
- `clj -X:test fast`: 183 / 1,724, green; flake registry unchanged.
- Shadow dev compile: 312 files analyzed, 16 compiled, zero warnings.
- `git diff --check`: clean.

No commit or push was made. The live cluster and dev build remain running for
the next wearing/phase context.
