# FIRST LIGHT — R-2 over the real corpus (2026-07-06, Sid's boot; stamped Trunk-5)

Evidence: `vision/images/2026-07-06-r2-first-light.png` (Sid's screenshot,
pasted in-session with the countersign blanket "from my side everything is
approved just fucking get to it").

## What the screenshot shows working

- **Threads render over the real corpus** — the endpoint gate-fix did its
  job: commit cards thread under a conversation lineage (lane spines
  visible), not the all-band face the pre-fix path would have shown.
- **Two clocks live end to end**: `claimed 2024-01-18 · arrived 07-06` on
  commit cards — the committer clock riding seam 1 through the additive
  field to the band-2 reading line; cross-year claimed prints its year as
  designed.
- **The expanded card (2232fbb)** carries sha/parents/author/authored-at/
  committed-at/attested + kraft edge lines (`<- based-on … by
  import:git-spine`, `-> based-on …`, `<- produced oc:chat-conversation:…`)
  — typed relations over real material, the H1-family render.
- **Move chips fire** (`★ joined thread · produced …`) with the incident
  edge as the reason.

## Observations for the design track (true-but-loud class)

1. **Chip noise**: every commit card in the visible region wears the same
   long `joined thread · produced <conversation-id>` chip — TRUE (each
   commit genuinely joined the thread when extract landed its edge this
   pull) but visually loud at corpus scale. R-3 design question: chip decay
   (age out after N pulls?), dedupe per thread, or reason compression.
2. **Conversation ids are raw** in chip reasons (S6 honest fallback —
   conversations carry no `:display-name`). A conversation display-name
   enrichment (first-message snippet or session date) would shorten the
   loudest string on screen. Candidate small item, not improvised now.
3. Band region and `N unthreaded` line not visible in this crop —
   unverified at first light; check at the face-2 G-FL sitting.

## Standing

R-2 CLOSES on this evidence (gate PASS + first light). The face-2 wave
opens on the same message's countersign.
