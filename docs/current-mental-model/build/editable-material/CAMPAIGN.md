# editable-material CAMPAIGN — the full build-out, then Sid returns

Sid's order (2026-07-24): build the WHOLE layer — all packages, however many
— then he resumes living in the land. He is blocked on usage until it stands.
DIRECTION.md (settled 07-24) is the law; this file is the execution ladder.
Codex prompts for P2–P8 (send ONE per gate PASS): `PROMPTS.md` beside this
file — P1's proven skeleton, each package's fences + pre-registered stops.
Work-package mechanics per `.claude/skills/work-package/SKILL.md` for every
package: contract → phases (cheaper models where sensible) → batched
falsification → gate → NOW.md entry + board flip. Parallel sessions are
normal; two lanes legal where marked ∥. Echo bar (52ms) gates every package
that touches the client hot path.

**Honesty up front:** gates close mechanically on BANKED pressure (Tasks
1–11, the feedback zones — real, recorded; the no-fabrication clause is
honored because nothing is staged). The layer's first full BREATH — felt →
… → felt again in live inhabitation — lands at Sid's return, by design.
Built and alive are different states; the campaign builds; the return proves.

## The ladder

**P1 — anatomy: the provenance master (Gate 1).** Scope = DIRECTION §Probe
FINAL, verbatim. Generalize the assembly serve/revision/epoch precedent —
never fork it. Floor drill included (trace kept for Gate 4). ~1–2 sessions.

**P2 — the material inspector (before any second facet).** One deterministic
projection: pick → entity → attachments → master → active revision →
contribution sites → wearers → revision trail. Console-grade first, face
second. This is the noun-scatter repair landing BEFORE the scatter. ~1
session.

**P3 — the facet campaign (the worn five).** Extract in this default order,
each its own mini-phase: attention (border/box policy) → foldable (header
text + fold defaults) → positioned (reply-gap, placement defaults, birth
rules; settle cells already ARE its instance data) → threaded (column
adoption reach) → text-body policy slice (wrap floor/fallback). At facet 2:
the **second-wearer reuse test** (DIRECTION FAIL-tells) + the reasoning-cost
falsifier — both pre-registered STOPS; on FAIL, re-cut while cheap. The
composition protocol (roles, merge laws, conflict lint) emerges HERE from
the first real collision. Render byte-identical at current values at every
step. ~2–4 sessions.

**P4 ∥ — circulation: receipts + the record intake (Gate 2).** Runs parallel
to P3 (different organs: server/record vs client/render). Context RECEIPT on
birth/utterance/mark (extend the existing pick/context-bundle machinery);
relation-kernel kinds by reviewed line (instance-of-shaped, about-shaped);
ambient silver proposals via the autotag lane (llm-module — machine mouth
calibrated per DIRECTION); retroactive kinds over the existing
#TASK/#Feedback corpus (predicates; starter culture); "everything around
this material" served; as-of resolution against activation history;
pre-material records associated, never rewritten. **Fulfills first-light P3
under its CONTRACT terms (point→say→wish = the gold path)** — one contract,
no fork; flag any discrepancy inline. **First-light flag A (wish-edge
target kind) is PRE-RULED by Fable at the P3 facet-2-halt session; the P4
prompt ships with the ruling, never the ask** (Sid's division ruling
07-24: contract forks are Fable's before the package opens, not the
implementer's mid-package). Gate 2 closes here. ~2–3 sessions.

**P5 — bindings + dispatch.** Binding rows as material; the one rule
replaces the pointer cond ladders strangler-style, gesture family by gesture
family; verb registry with effect classes; instance-level bindings (locality
tiers); interaction table served; conflicts-as-lint; dispatch branch counts
frozen after migration. Space-as-outermost-entity and camera bindings LAST,
on the code floor. Needs P3's facets to carry claims. ~2–3 sessions.

**P6 — the truth loop whole (Gate 3 + Gate 4 close).** Instance deviations
(scoped, visible, diffable) · pins · candidate revisions + preview membrane
(fulfills first-light P4) · scoped activation events + blast-radius view ·
announce at three scales (breath / trace / material-weather) · derived case
reports with declared grounds (fulfills first-light P5's explain) ·
standable history (as-of portal). The material-truth owner condenses HERE if
a homeless truth demands it (platform-check against OC + relation kernel
first; one Rama module only if needed — /rama + rama-pitfalls before any
touch). **P6 opens with a Fable-authored CONTRACT.md** — the
owner-condensation decision is pre-adjudicated there (Fable runs the
platform-check; traps ledger + numbered gates per the work-package skill)
and Codex implements against it, never rules it (Sid's division ruling,
07-24). The P6 prompt in PROMPTS.md is the implementation half only. **Gate 3:** one BANKED friction from Sid's own corpus (candidates:
reply width, muted-gray legibility — both recorded in his notes) travels
pressure → record → candidate → preview → Sid's one-line activation word →
announced change → zero terminal anywhere. **Gate 4 formal close:** the P1
wound-trace lands as the record's first immune memory; drill re-run because
the kernel changed. ~3–4 sessions.

**P7 — the portal proper.** The material/type world as an inhabitable
projection: recipe view (the block recipe gets NAMED here if recurrence has
earned it), why-this-pixel, reverse navigation to origin instances,
blast-radius before activation, historical cuts. Portal self-editing only
AFTER Gates 3+4 — then its own material enters the layer (the strange loop
closes on the dull floor). ~2–3 sessions.

**P8 — react-bits seam: one verb born from inside.** A BANKED behavioral
wish ("reply to just this block" — LOG 07-18) travels: wish → resident
authors implementation in the code lane → build/test receipts as material →
versioned verb activated → a material binding uses it. The two-strata story
closed end-to-end. ~1–2 sessions. (May overlap Sid's return; the wish is
already banked so nothing waits on new friction.)

**Return.** Sid opens the land. The first full breath — a live pressure felt
→ repaired → felt again without leaving — is HIS act and the campaign's true
gate. first-light A closes with it; B still waits for genuine recurrence.

## Sizing (intent, not law)

~14–22 build sessions total; two lanes (P3 ∥ P4) compress calendar. Every
package reversible; strangler-only; no bulk rewrite anywhere.

## Sid's total touchpoints (everything else runs without him)

1. Package-gate words — one line each (batchable; ~8 total).
2. The Gate-3 activation word — one line.
3. Stop-clause escalations only if a falsifier FIRES (second-wearer FAIL,
   reasoning-cost FAIL, echo-bar regression) — his redline, one read.
4. The return wear.
Code commits ride his standing 07-24 word (veto anytime). Docs on the docs
branch, never pushed. No spend anywhere.

## Standing stops (pre-registered, never waived by momentum)

Second-wearer reuse test at facet 2 · reasoning-cost check at facets 2–3
(compositions harder to reason about than the monolith ⇒ reverse + re-cut) ·
52ms echo bar per extraction · byte-identical render at current values per
extraction · terminal-escape detector armed from P4 (a code-lane commit
changing material-owned policy with no activation event = one escape).
