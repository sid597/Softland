# shaping-correction — P1 implementation phase

**Date:** 2026-08-05
**Implementer:** Codex
**Terminal:** **PACKAGE BLOCKED — {S4}**
**Code state:** no code opened or edited; Act 0 fired the prompt's mandatory
pre-code S4 stop.

## 1. Opening custody

- Repository: `/mnt/data/projects/Softland`
- Branch: `docs/current-mental-model-local`
- Opening HEAD: `f2d65a1c5907de279477e04f92437164ef0ee363`
- Contract SHA-256:
  `3699f3fb7db413d4b8b07936e4192950f486171aae02a90b4a408028e128e19b`
  — equal to the R4 pin in the opening prompt.
- The baton's clean-tree statement had one custody drift at open:
  `docs/current-mental-model/build/smalltalk-ui-vm/PODCAST_SCRIPT.md` was an
  untracked foreign file. It is outside this package and remained untouched.
  No tracked diff existed before this phase artifact and baton entry.
- `src/app/server/env.clj` was not read. No commit, push, merge, staging,
  golden update, or verifier edit occurred.

## 2. Act 0 — §11 manifest sweep

The sweep ran before any code edit. Every named §11 symbol/form was located;
no manifest substance was missing, so S3 did not fire.

| Surface | Receipt |
|---|---|
| `text_layout.cljc` | 797 lines; `shaped-layout` 328, `union-bounds` 320, `shaped-segments` 283, `layout` 506, `line-paint-ops` 527, `clip-result` 660, `hit-test-result` 766, `caret-result` 590, `selection-result` 638. Hint-exact. |
| `face_primitives.cljc` | 1,324 lines; `text-op` 550, `wrap-lines` 559, `block-render-lines` 572, `ground-block-layout` 587, `block-root-prim` 618, all named auxiliary primitives 755–846. Hint-exact. |
| `renderer.cljs` | 2,386 lines; `destroy-text-system!` 850, `clone-text-system` 1100, `position-text-op` 1472, packers 1646/1675, `update-text-data` 1719. Named substance intact. |
| `runtime/render.cljs` | 792 lines; `reconcile-slot-text-geos!` 25, `<combined-text-ops` 87, `<store-frame` 130, `store-frame-changed?` 241, `text-by-vi` 248. Named substance intact. |
| `ground.cljs` | 4,141 lines; contracted post-SEAM hint drift confirmed. Named substance relocated: `!refs` 83, `metrics` 175, `run-view` 1186, `machine-visual-lines` 1219, `block-anatomy-view-model` 1249, `current-block-render-inputs` 1402, `rebuild-block!` 1496, `context-blocks` 1741, `reconcile!` 1790, `pointer-move!` 3429, `install-ground!` 3954. |
| Read-only context | `paste-projection`, `block-view`, `!truth-overlay`, `truth-text*`, `!active-font`/`:layout-provider`, provider enrichment, foldable defaults/header-copy, verifier fields, dev build, and Jetty's current default port were all present. Jetty's 8080 default relocated to line 2405. |

The known line-count shift and locator movement were hints only. The five
allowlisted production files retained the named forms and semantics.

## 3. Act 0 — G10 baseline replay

Command, run unmodified:

```text
npm run verify:render-engine
```

Process exit was `1`. The console structured close was:

```json
{
  "pass": false,
  "classification": "candidate-pick-parity-failure",
  "receipt": "target/render-verifier/receipt.json",
  "images": 21,
  "deterministic": "21/21",
  "q8AffineTransport": true,
  "q5AffineRasterBoundary": true,
  "candidateParity": "14/21",
  "productBoundsDivergenceSentinels": "7/7",
  "updateRequested": false,
  "updateAuthorized": true,
  "environmentFingerprint": "9213da9a74738c3397e99ef62b08c3833cbcaab9b6124d8255c1a4616e58a03c"
}
```

The receipt surface recorded:

- determinism 21/21, pass;
- golden comparison 21/21, byte-identical/pass;
- candidate parity 14/21, with SDF 7/7 and Slug 7/7 green and MSDF 0/7;
- product-bounds divergence sentinels 7/7, pass;
- Q8 affine transport and Q5 affine raster boundary, pass;
- `sourceMatch=false` and `environmentMatch=false` in the generated receipt;
- no golden update requested or made.

This is not the baseline required by `CONTRACT.md:1113-1135`: the contract
requires `updateAuthorized=false` and environment fingerprint
`e79490f8882cd785f32b5bb82cadd425dc90f2d7616cc9f0debf8a0f1c476282`.
The current verifier deterministically emits `updateAuthorized=true` from its
four green authorization predicates (`run_verifier.mjs:337-342`), the same
value banked by `SEAM-STEP1-P1.md:49-68`.

### Adapter-feature ordering receipt

A structural diff of the banked and current environment values, excluding
their stored fingerprint, found exactly one difference:

```diff
-        "timestamp-query",
         "clip-distances",
+        "timestamp-query",
```

Sorting only `browser.adapter.features` on both values makes the full
environment objects equal (`equalAfterFeatureSort=true`). Thus the fingerprint
mismatch is caused by unordered adapter-feature iteration order alone. This is
exactly the queued SEAM finding: make the adapter-feature fingerprint
order-insensitive before shaping-correction opens
(`SEAM-STEP1-P1.md:331-339`).

## 4. Stop ruling

The opening prompt is explicit: if the fingerprint mismatch is caused by
adapter-feature ordering alone, **STOP and escalate S4; never edit the verifier
yourself** (`NOW.md:141-152`). Contract S4 requires escalation rather than a
silent choice when the package contract and Contract T/render-seam ground
conflict (`CONTRACT.md:1322-1324`).

Therefore:

- fired stop set `B = {S4}`;
- exact terminal: **PACKAGE BLOCKED — {S4}**;
- G10 has no phase PASS/FAIL ruling while the package is blocked;
- the CLJS compile warning baseline was not run: the S4 instruction fired
  first and stopped Act 0 before the compile-baseline step;
- no implementation, harness, new fence, G1 namespace, live probe, or
  in-phase falsifier was started.

## 5. Ruling required for re-entry

The smallest evidence-preserving correction is to settle the already-queued
SEAM finding outside this package's §12 edit surface: canonicalize the
adapter-feature set before hashing, preserve the existing golden/parity
semantics, and separately reconcile G10's stale `updateAuthorized=false` pin
with the verifier's current declared authorization predicate and the banked
SEAM receipt. Then run a wholly fresh Act 0 baseline and require every pinned
console and receipt field before reopening implementation.

No such correction is authorized here: the verifier and existing fence scripts
are MUST-NOT surfaces. Fable/Sid owns the S4 ruling/recut.

## 6. Diff-derived custody sum-check

Package-owned paths at this blocked handoff:

1. `docs/current-mental-model/build/shaping-correction/P1.md` — new, allowed
   package phase artifact.
2. `docs/current-mental-model/build/shaping-correction/NOW.md` — existing
   package baton, allowed terminal entry.

Production-code paths changed: **0**. Test/harness/fence paths changed: **0**.
The unrelated untracked `smalltalk-ui-vm/PODCAST_SCRIPT.md` remains foreign and
untouched. The package write set is entirely inside the §12 package-doc surface.

## 7. Sid ruling and direct-fix re-entry

Sid ruled in the same implementation context that direct, receipt-proven misses
must be fixed by the implementer and accumulated for one final Fable broad
review; they are not each routed to a new verification session. Fable's final
question is whether the collected decisions became local patches, narrowed the
systemic fix, or prematurely pulled the package toward an MVP.

Applied direct corrections:

1. `run_verifier.mjs` now canonicalizes the WebGPU adapter feature set before
   it enters the serialized environment or its fingerprint. Ordered arrays
   remain order-sensitive. The environment metadata bank was migrated without
   changing any pixel/golden image.
2. W1 and G10 now pin canonical environment fingerprint
   `5ced2482f3d0b8e9a14465bba06808a04d3a5a9b7d386fcf71f40a30f557343e`.
3. G10's `updateAuthorized` expectation is `true`, matching the verifier's
   documented four-predicate authorization semantics and the banked SEAM
   receipt. `updateRequested` remains `false`; no update was requested.

Fresh verifier replay after the correction: exit 1,
`candidate-pick-parity-failure`; deterministic 21/21; golden comparison 21/21;
candidate parity 14/21; divergence sentinels 7/7; Q8/Q5 true;
`environmentMatch=true`; canonical fingerprint exact; no image update.

The unresolved S4 set is now empty. The earlier blocked terminal is preserved
above as the exact pre-ruling state; the phase resumes under Sid's ruling.

## 8. Direct-fix pass — 2026-08-05, Sid ruling (no revalidation rounds)

Fable's broad review of the implemented phase produced three findings
(storyboard geometrically unexecutable · probe/positive able to overclaim ·
G1 fixtures narrowed). Sid ruled directly: fix everything now, no contract
recut round, no fresh validation session. Contract amendments land in place,
stamped `AMENDED 2026-08-05 — Sid ruling (direct, no revalidation round)`.
Four Opus implementation passes executed the ruling.

### Applied fixes

1. **G1 restored to the contract's letter**
   (`test/app/client/workspace/shaping_correction_test.clj`): contracted
   pathological fixture (1 source line, 4,400 units, C=4,291, R=333, G=4,181;
   work 17,613 ≤ 35,220) · shuffled twin covering the non-monotonic
   exact-index path (`text_layout.cljc:548–572`, previously zero coverage;
   8 visited vs span width 22) · growth row (4×=17,613 ≤ 5×1×=22,065,
   ratio 3.99; 4× fixture asserted = four literal 1× repetitions) ·
   negatives (a)/(b) reimplemented as instrumented legacy strategies through
   the real counter vocabulary (158,509 measured glyph-visits rejected even
   with the sort allowance; 36 vs 8 span visits rejected). Fabricated-literal
   receipts deleted. Receipt: 10 tests / 159 assertions / 0 fail / exit 0.
2. **§8 hook surface closed**: audit found 13/14 drive hooks and the dynamic
   pack-cause fix already present in the worktree (the review's Finding-2
   evidence was stale against the tree); the one missing named road,
   `capacityAppend`, was added (`ground.cljs:4325` `profile-capacity-append!`,
   exposed :4714, same `profile-truth-edit!` truth road). Harness row (f) now
   drives the named hook; fence allowlist extended and a token guard added.
   Compile: 0 warnings before and after.
3. **§9 storyboard recut to executable form**
   (`profile_shaping_correction.mjs` + CONTRACT.md §9/G3/G9): reposition
   segments between measured acts — park on pick-proven empty ground →
   over-wheel into the product 0.1 zoom clamp → chunked empty-ground pan
   drags → prove target picks — all UNMEASURED, literal input only (ground
   wheel zooms; there is no scroll rail). Measured counts re-derived
   13 → **7** (each measured act crosses exactly one non-nil hover side;
   the 13 presupposed co-visibility, impossible by the hover geometry
   receipt: 8,142.95 world gap, zoom floor 0.1, 601px viewport). Exact `===`
   assertions; reposition telemetry reported, never asserted.
4. **probe/positive made fail-honest**: hardcoded completeness flags replaced
   by a 20-row `contractedRowRegistry`; any unimplemented contracted row
   emits a named `contractedRowUnimplemented:<row>` red forcing exit 3.
5. **The last three row definitions added** to the optional two-isolated-trial
   profiler:
   `g4-b-hover-flip` (recut to a run-time-discovered adjacent co-visible
   pair — pinned-pair flip impossible per the same geometry; pair identity,
   rects, and rejected candidates recorded in the receipt) ·
   `g4-h-vanished-slot` (executed row via the halo overlay slot road:
   right-click open → Escape close → `close-instance!`; the contracted fold
   candidate is structurally unperformable — a fold reprojects, V≡0) ·
   `g4-l-oracle-positive` (replays the recut (b) with oracle enabled;
   checks +2, mismatches 0). Registry inventory after: contracted 20 /
   definitions present 20 / missing [] / stray []. This was structural
   inventory, not 20-row execution PASS; the retained live run later stopped at
   the backend-flip drive and the matrix is now retired as non-blocking.
6. **Test-lane registration**: `app.client.workspace.shaping-correction-test`
   added to `test_runner.clj` `pure-namespaces`; `assert-inventory!` passes
   (`:INVENTORY-OK {:discovered 57, :pure 22, :shared 8, :isolated 27}`).

### Verification receipts

`node --check` clean on both mjs files · shaping-correction, text-layout, and
scene-tape fences all exit 0 (`dynamicPaintCause`, profile-row inventory
present) · JVM namespace 10/159/0 exit 0 · shadow compile 0 warnings. Live run
attempted on a fresh dev server (port 8099; the squatting `agentsview` app on
8080 untouched): app boots but the Rama conductor is down (`[CLUSTER] manager
unavailable`), corpus serves 0 blocks, so both modes exit 1
`unclassified-environment` — the probe receipt proves only the static registry
inventory (20 definitions present, empty registry-red map, truth table 7/7),
not execution or PASS of the 20 rows. All four banked receipts restored
byte-identical after the runs (sha256s in the fix-pass session record); the
collateral overwrite of the hover geometry receipt earlier in the pass was
reconstructed byte-identical (5,196 bytes).

### Open at handoff (named, none silent)

- **Historical open item, now retired as a blocker:** the old recut requested
  live execution of every matrix count. Sid's cadence ruling removes that next
  action; no hover/probe/cold profile command is run for settlement.
- **Optional matrix incomplete at backend flip.** The positive drive later
  stopped in row (e) after observing zero slot-text writes. Under Sid's cadence
  ruling this timeout/zero-write observation stays visible historical residue;
  the row is not repaired, the matrix is not rerun, and no 20-row pass is
  claimed.
- **`:monotonic?` dead pass** (`text_layout.cljc:542–572`): computed, stored,
  never read; its per-glyph comparisons ride the single `g` increment
  (two G-passes, one count). No verdict changes at current bounds; G1
  sum-check reviewer should rule count-or-delete.
- Full `clj -X:test` lane and `--case=oracle-negative` not run (cluster
  suites / corpus blocker).

### Custody

Write set of this pass: the five fix files above + CONTRACT.md in-place
amendments + this record. No commit, push, staging, or golden/image change;
pre-existing uncommitted Codex-phase diffs left untouched for their owning
session.

## 9. Feedback pass — 2026-08-05, Codex-verified corrections (Sid: apply)

Codex verified the fix pass against the files and returned two corrections;
Sid ruled apply-directly. Same amendment stamp.

1. **Correction of record — hover is attention, not text paint.** The static
   inference that every hover rebuild reaches the text write site was wrong.
   The text runtime keys reuse on the layout token plus the glyph paint token;
   hover changes the interpreted anatomy's attention rectangle while the glyph
   paint token stays equal, so the write site is never entered. The banked live
   diagnostic observed seven attention transitions, seven RAF/store-frame
   executions, and zero layouts, misses, text repacks, GPU text writes,
   dirty-text marks, or text-geometry lifecycle changes. CONTRACT §9/G3 and
   optional-profiler assertions now state that measured law. Backend flip
   remains a separate incomplete row and retains no verdict.
2. **Timing remains unclassified.** SwiftShader is the current adapter.
   Physical-GPU timing has not been measured and cannot produce a pass/fail
   verdict. Sid retired the full matrix/storyboard/cold loop as a blocking
   path; the harness and its receipts remain historical and non-blocking.

The earlier slice-eval receipts remain historical. They are not 20-row live
completion and are not settlement gates.

## 10. Settlement pass — 2026-08-05, Sid cadence + custody ruling

- Multiline correction: the durable 48-line note retained 2,506 characters;
  body-line glyph spans and queries now share absolute document source space,
  while header spans remain local to each header domain. The focused regression
  covers a nonzero later-line offset, a wrapped later line, and a header.
- Corpus identity: 174 blocks, 36 machine blocks, 586,927 served chars, 174
  block slots, 174 text slots, zero non-block slots. The old 172/177 capture was
  172 block slots plus five transient Studio/Workshop slots; those five were
  archived and two durable blank Studio drafts were subsequently born.
- Hover receipt: attention transitions 7; RAF/store-frame executions 7; text
  layouts, misses, repacks, GPU text writes, dirty-text marks, and text-geometry
  lifecycle changes all 0. Text paint is invariant.
- Optional profiler: **INCOMPLETE, NON-BLOCKING**. The positive matrix stopped
  at backend flip after zero slot-text writes. No repair, rerun, or 20-row PASS
  claim. SwiftShader timing remains unclassified.
- Lived receipt is reserved to Sid's two manual answers: later-line text visible
  in the 48-line `#Feedback` note; hover/attention outline follows the pointer.
- Authorized settlement checks after the one production coherence edit:
  focused namespace PASS (11 tests / 177 assertions / 0 failures/errors);
  shaping fence PASS (`retiredProfileInventoryPresent=true`, no production
  failures); exact `npx shadow-cljs compile dev` exit 1 before project compile
  because the standalone launcher lacked the Electric hook and `src-dev/dev`
  namespace on its classpath. The repo's classpath-backed build wiring is
  `clj -M:dev ...`, but no substitute command was run because it was outside
  Sid's literal settlement allowlist. No profile command or unrelated test ran.
