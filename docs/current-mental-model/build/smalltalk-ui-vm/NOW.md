# smalltalk-ui-vm — thread

## STANDING (frozen at package open, 2026-07-30)

Binding docs: `CONTRACT.md` (this directory) + `DIRECTION.md` (settled
2026-07-30) + `decisions.md`. This file is a baton, not a source of
truth; if it contradicts CONTRACT.md or decisions.md, those win — flag
the discrepancy here, do not pause. Two phases (P1 engine cutover FULL
tier · P2 Workshop slim tier), each: fresh implementer context builds
whole → fresh in-phase falsifier → fresh gate → Sid's commit ruling.
Before P1: ONE fresh default-fail validation round over CONTRACT.md
(V1–V6 + coherence). Allowlists per CONTRACT §6; stop clauses §9 —
never improvise policy; manifest binds on SUBSTANCE. Hard rules: commit
decisions are Sid's · code/docs separate commits · docs branch never
pushed · never Co-Authored-By · `env.clj` NEVER read. Does not start
without Sid: nothing (standing 07-24 word covers campaign-class code
commits; veto anytime).

## NOW (newest first; ~15-line budget per entry)

**2026-07-30 · Fable (independent FULL gate, two fresh contexts) ·
GATE_P1 VERDICT: PASS — zero code defects; artifact `GATE_P1.md`.** Gate
session A (fresh, no implementer context) re-verified the fence (HEAD
`c9a18ef`, six pins byte-exact, change set allowlist-exact), read the full
diff against W1–W9/T1–T11, re-ran G1 (200t/2,030a 0/0 + the 82t/1,192a
pinned batch + CLJS 273/0), and drove its OWN isolated :8097 rig: full G3
lifecycle incl. restart persistence + 2s rollback, G4 echo n=50 p95 17.5ms
(0 over 52), W8 preview-survives-typing, G5 live replay of both orders
with no rank-marker leak; its independently seeded baseline revision id
matched the banked seed bytes exactly. Interrupted pre-verdict; the
finisher session spot-verified the banked drive logs, re-ran the
anatomy+assembly slice (14t/134a 0/0), re-read the three riskiest
surfaces, and RATIFIED all four judgment calls (:focused-or-hover ·
multi-stamp root · threaded floor→v1 · sibling-rank under "additive,
disclosed"). Residue banked with falsifiers (anatomy-view JVM gap ·
swiftshader-only echo · entity-mode pin growth · teaching-card char-splat
· rig-dir leftover, Sid removes). No commit; next act = Sid's commit
ruling; P2 stays closed.

**2026-07-30 · Codex (P1 implementer context #2) · whole P1 built,
falsified, repaired, and handed to the independent FULL gate.** Implementer
G1–G5 PASS; full receipt is `P1.md`. Every block now wears revisioned
`fm:anatomy` through the existing assembly interpreter; `block-tree` is
deleted; threaded grammar 1 supplies candidate rail/indent pixels through the
existing OC revision/pointer path. Fifteen browser-generated legacy goldens
are exact and the named live corpus was 1/1. Isolated
`LAND_CLUSTER=0 LAND_PINNED=1` drive completed deviate → preview → activate →
client restart → rollback → client restart with focus/caret continuity.
Attested warm preview echo: pre-falsifier n=50 p95=18.0 ms; post-repair p95
18.2 ms (52 ms bar). Fresh G5 found the omitted normal-priority
machine+hover order: static anatomy reversed rail/box while the tie golden
needed the opposite. Smallest repair: generic optional apply-time sibling
rank in `face_assembly`, populated from actual composition; both cases are now
fixtures/live-green and no rank marker leaks. Disclosures: closed presence adds
`:focused-or-hover`; root uses a multi-stamp map; Electric value-equal serves
require preview-base rebasing; IPC “restart” means page/Electric-client
restart. Fast lane 200/2,030 green; CLJS 273 files, 0 warnings; six pins
zero-diff. No commit/push; index empty; independent gate + Sid ruling pending;
P2 NOT OPEN.

**2026-07-30 · Fable (P1 implementer context #1) · terrain read + design
banked, handed off pre-code at ~270k context (Sid's call).** NO source or
test file changed; tree clean but for `P1_IMPLEMENTER_NOTES.md` + this
entry. All binding docs + the full render/material terrain read against
disk: block-tree whole, engine whole, rect-tree whole, wear/preview/watch
machinery, pointer phases, cluster ingest pattern, test pins. Every design
decision for the cutover is settled and banked in
`P1_IMPLEMENTER_NOTES.md` §B (presence-through-:each, root-part-IS-the-
char-grid, apply-data with compose reproduction, the G2 comparator incl.
:address-not-stripped + JVM numeric normalization, compile-cache keying,
W8 wiring points, threaded grammar-2 keys, census/pin re-cut list).
Judgment call for the gate, pre-disclosed: presence vocab gains
`:focused-or-hover` (the attention box's own condition; the contract's
14-key list can't express it — F-class precision, argued in notes §B.6).
Not reached: face_wiring hands, halo rig recipe, fixture-wear floors
(notes §D). Next act: FRESH implementer context builds P1 whole from
CONTRACT + VALIDATION + the notes; starter (with Sid's context-budget
directive, binding for this phase) banked at notes §C.

**2026-07-30 · Fable (fresh session) · validation round → MINOR-FAIL,
fixes applied, no re-run.** Artifact: `VALIDATION.md` (walk + receipts +
the P1 starter). Code tree verified byte-identical to the manifest cut
(`git diff db8e21d..HEAD -- src/ test/ bin/` empty). Findings, verbatim
headlines: **F1** W1 had no instance-data bind form — `[:view key]`
added with a closed view-key vocabulary (data-resolution law); **F2** G2
data-exactness was unsatisfiable through the machinery — equality
relation named, interpreter-provenance keys normalized, ids exact;
**F3** non-generic facet surfaces are FOUR, not three —
`block-wear-census` ground.cljs:347 gains `:anatomy`; **F4** render-sig
is 16-element; **F5** corpus surface named (`__ground.blocks()`
post-merge + committed EDN goldens, JVM harness); **F6** `worn-five`
recipe set disturbed — P2 rules it; **F7** V6 receipt: CLI 2.1.220 has
`--model` AND `--effort low..max` — stop-clause (c) positive; **F8**
"grammar-v2" = second version key. Judgment call flagged for the gate:
F1/F2 sat at the minor/substantive line — ruled minor because no file
set, gate substance, phase, or §0 ruling moved. V1 walk found NO §9(a)
convention blocker (layout-children passes unpositioned nodes through —
the block's explicit-geometry pattern survives the engine). Next: fresh
implementer builds P1 whole; starter at VALIDATION.md tail.

**2026-07-30 · Fable (orchestrating session) · contract cut.**
DIRECTION settled + committed `25ce803` earlier this session; CONTRACT
authored after four fresh terrain sweeps (render path · material
machinery · write lanes/halo · test surface), all pins machine-verified
at HEAD `25ce803`. Terrain rulings banked in CONTRACT §0: the assembly
machinery (compile/apply/registry) IS the interpreter — the block
becomes its next wearer; anatomy rides OC as `fm:anatomy` (no new
module); the loop needs ZERO new endpoints and ZERO new verbs; the
halo's enter is the Workshop door; type-preview = master-candidate
preview on the membrane. Next act: fresh default-fail validation round
over CONTRACT.md (V1–V6). Starter prompt banked on the board.
