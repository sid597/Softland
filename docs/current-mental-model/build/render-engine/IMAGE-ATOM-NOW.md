# image-atom — thread file (Package 2, wave 1)

## STANDING (frozen at package open — do not edit while active)

- Binding docs: `IMAGE-ATOM-CONTRACT.md` (this package) · `W1.md` Contracts
  O/G/M/C · `ENGINE.md` §0 (FULL-BREADTH LAW + Package 2 + hard joins) + §12
  Q2/Q8 · `decisions.md`. Receipts context: `W2-A-T0.md` · `W2-B-T1.md`.
- Precedence rule, verbatim: this file is a baton, not a source of truth; if
  it contradicts CONTRACT.md or decisions.md, those win — flag the
  discrepancy in NOW.
- Process: ONE phase, one fresh implementer context (Codex lane), no
  PLAN.md; ONE fresh default-fail validation round over the contract before
  the implementer prompt goes out; FULL-tier independent gate after
  implementer green; Sid's commit ruling at gate (implementer NEVER
  commits); code and docs commit separately on
  `docs/current-mental-model-local`; nothing pushed or merged.
- Edit surface = CONTRACT §12 exactly. `src/app/server/env.clj` is never
  read. The studio archive is read-only reference under §3's terms.
- Felt gate: NONE here — staged behind the durable-authoring-artery /
  studio-custody ruling (Sid's; board carries it). Dark machine work only.
- Stop clauses S1–S6 per CONTRACT §13; the implementer never improvises
  policy on binding docs.
- What must NOT start without Sid: any commit; any felt activation; any
  durable-bytes store decision (S1); any scope change.

## NOW (one writer at a time; ≤15 lines per entry; FAIL findings verbatim)

- 2026-08-05 · Fable (package-open session) · **CONTRACT CUT.** Package 2
  opened at Sid's word ("get started on the work for package 2 … lets go").
  Gathering ran on four Opus lanes (W1 obligations · code ground truth ·
  campaign receipts · collision set); manifest facts machine-verified at
  staging (wc/grep receipts in-session). Findings folded into the cut:
  shaping-correction already LANDED + lived-accepted (tree clean, no
  collision); the archived 247-ln `image_material.cljc` draft ruled
  REFERENCE-ONLY under the archive ruling; "HIG reference corpus" has no
  on-disk referent → capability promised, ingest refused; D7
  binary-assets-as-material note discharged as CONTRACT §3; E0 priced zero
  image costs → no perf gate bars, attestation-first measurements instead.
  NEXT: fresh default-fail validation round (R1), then the Codex opening
  prompt lands here.
- 2026-08-05 · Fable (same session) · **VALIDATION R1: FAIL → RECUT IN
  PLACE.** Five recut-class findings (verbatim in
  `IMAGE-ATOM-CONTRACT-VALIDATION-R1.md`): F1 per-instance store entries
  corrupt the scene_runtime payload derivation · F2 slot-entry fan-out
  makes maintained≡oracle impossible · F3 no pick dispatch for
  :image-quad (latent S6) · F4 strict C4 unsatisfiable under
  direct-present (candidate presentation encode absent from tree) · F5 G4
  decisive-pass vacuous at zero boundary pixels. Recut: §7 → pool-lane
  pattern (msdf/slug precedent), §5 pick → declared product route +
  refused central branch, §5/G6 → two declared color regimes with the
  candidate encode made code-real default-OFF, G4/§8 → decisive floors +
  ramped-edge law, §8 → scoped bank-append/capture/fixture-delivery/
  ICC-generator mechanics, T10/T12/§12 → in-fence purity check +
  strengthened self-test. Map: `IMAGE-ATOM-RECUT-LEDGER-R1.md`. NEXT:
  validation R2 (fresh, default-fail) over the recut + ledger.
- 2026-08-05 · Fable (same session) · **VALIDATION R2: FAIL → SECOND RECUT
  IN PLACE.** R1's worst repairs verified consumed (pool-lane composes —
  store-pool-entries offsets per-invocation; F2 dissolved; F3 closed); six
  new/surviving findings (verbatim in `VALIDATION_R2` artifact): N1 G8's
  JVM tier over .cljs scene_runtime · N2 candidate presentation encode had
  no in-allowlist home · N6 per-binding splits inexpressible via pool
  helper · N3 golden assertion had no code referent · N5 accounting fork
  blocks the append road · N4 parity probe channel-0/colored-quad vacuity.
  Plus an EXECUTED iCCP probe: naive ImageIO.write silently omits the
  profile chunk — road pinned to IIOMetadata+deflated bytes+re-read
  assertion. All consumed per `IMAGE-ATOM-RECUT-LEDGER-R2.md`. Sid's
  ruling this session: ONE additional external cycle — Codex validates
  with split authority (narrow fixes in place / §-level escalations, max
  5) AFTER the internal ladder settles. NEXT: R3 (fresh, default-fail) =
  R2-consumption + the W1-conformance gap R2 declared; then the Codex
  cycle; then the opening prompt.
- 2026-08-05 · Codex · **VALIDATION: NEEDS-FABLE.** Fresh binding-doc,
  source, and executable verifier audit completed after re-reading the
  concurrently-landed R2 artifact + ledger. Five NARROW repairs applied in
  `IMAGE-ATOM-CONTRACT.md`: budget replacement road, locator drift,
  red-channel Cg isolation, pre-write/expected-RED assertions, and exact gate
  commands + receipt-test allowlist. Four protected rulings remain: Contract-C
  zero-transfer contradiction · mixed rect/image order representation ·
  product boundary/slop route · unnamed asset-error/device-loss sink.
  Artifact: `IMAGE-ATOM-CONTRACT-VALIDATION-CODEX.md`. NEXT: fresh Fable
  rules those four questions; implementation remains CLOSED.
- 2026-08-05 · Fable (authoring session) · **R3 FAIL ∥ CODEX NEEDS-FABLE →
  MERGED; CONTRACT FROZEN.** R3 (fresh, default-fail, W1-conformance duty)
  returned six §-level findings; it ran CONCURRENTLY with the Codex cycle
  and the two instruments converged on the same forks (color-regime law
  unlawful under W1 §8.2.5 · mixed rect/image order undeclared with a
  pr-str tie hazard · pick-route precondition/owner · error sink unnamed)
  plus R3-only items (twin-check vehicle · helper authorization · buffer
  home). Parallel-authors law applied: both finding sets merged into
  `IMAGE-ATOM-RULING-PACKET.md` — seven deduped questions Q1–Q7, each with
  citations, options, recommendation + grounds, and re-run scope. No
  further contract edits from this session; ledgers R1/R2 are historical
  maps only, git diff is the change map (the contract moved mid-R3 via
  Codex's five narrow fixes — R3's process warning honored). Sid's
  external cycle is SPENT; after the ruling session's recut, NO further
  validation rounds — implementation opens; the FULL gate carries the
  rest. NEXT: fresh Fable ruling session boots the packet, rules Q1–Q7,
  recuts in place, sweeps, re-pins sha, cuts the Codex opening prompt.

## Starter — the fresh Fable ruling session (Sid pastes this)

> You are the ruling session for Package 2's image-atom contract. Boot, in
> order: `docs/current-mental-model/build/render-engine/IMAGE-ATOM-RULING-PACKET.md`
> (the merged Q1–Q7 question set with recommendations), then the artifacts
> it indexes — `IMAGE-ATOM-CONTRACT-VALIDATION-R3.md`,
> `IMAGE-ATOM-CONTRACT-VALIDATION-CODEX.md`, and `IMAGE-ATOM-CONTRACT.md`
> (re-pin its sha256 at boot; git diff is the change map, not the recut
> ledgers). Rule Q1–Q7 — accept, amend, or replace each recommendation,
> grounds required, verifying any load-bearing citation you rely on against
> W1.md and the code. Execute the third recut in place per your rulings,
> then run the ruling-execution sweep (grep every derived enumeration:
> gates G1–G12, the receipt↔M map, §11 manifest, §12 allowlist). Close the
> R3 §Also items (R2 repair (c); MINOR sweep). Update
> `IMAGE-ATOM-RECUT-LEDGER-R2.md`'s successor as `IMAGE-ATOM-RECUT-LEDGER-R3.md`,
> append your NOW entry with the new contract sha, and cut the Codex
> implementer opening prompt into this file (Act 0 = §11 sweep + §8
> boot-verify; template precedent: `build/shaping-correction/NOW.md`).
> Constraints: do NOT reopen settled R1/R2 consumptions, campaign scope
> (ENGINE.md §0), or the Sid-reserved artery/custody decision; the external
> feedback cycle is SPENT — after your recut, NO further validation rounds;
> implementation opens and the FULL gate carries the rest. Docs-only
> commit(s) on `docs/current-mental-model-local` at close; never push;
> never read `src/app/server/env.clj`.
