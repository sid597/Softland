# image-atom — merged ruling packet (R3 ∥ Codex convergence)

**For the fresh Fable ruling session.** Two independent validators ran
concurrently on the recut contract — internal R3
(`IMAGE-ATOM-CONTRACT-VALIDATION-R3.md`, fresh default-fail, W1-conformance
duty) and the single external Codex cycle
(`IMAGE-ATOM-CONTRACT-VALIDATION-CODEX.md`, split authority: five NARROW
fixes already applied in place, four protected escalations). They converged
on the same §-level forks. Per the parallel-authors law, this packet is the
MERGE: one deduped question set, each with citations home in the two
artifacts, options, the authoring session's recommendation + grounds, and
what re-runs. The ruling session rules each (accept / amend / replace),
executes the third recut in place, and runs the ruling-execution sweep
(grep every derived enumeration — gates, receipt↔M map, §12, §11 — after
amending; banner-only amendments are the known failure).

**Freeze state:** the contract moved during R3 (Codex's narrow fixes) —
the ledgers are NOT the change map; git diff is. The ruling session re-pins
the contract sha at boot and works from the frozen bytes.

**Process budget (Sid's ruling, 2026-08-05):** the one external feedback
cycle is SPENT. After the ruling session's recut: NO further validation
rounds — implementation opens directly; the FULL-tier gate carries the
remaining risk.

---

## Q1 — Color regime law (R3 N1 ≡ Codex escalation 1) — the hard one

**Finding:** the "legacy declared zero-transfer passthrough row" is
unlawful under the exact sentence cited as its authority — W1 §8.2.5: "an
`*unorm` target requires the declared shader/presentation transfer. The
plan proves exactly one conversion, never zero or two." Also C4 demands
cross-mode EQUIVALENCE where §5 declares a "recorded known difference."
And there is no code-real declaration site: `validate-family!` hard-throws
unless `:scene` is the linear-premultiplied id (scene_tape.cljc:364-367) —
"both DECLARED" exists only in prose.

**Options:**
(a) R3's repair — ONE regime; image render REFUSED under the legacy seam
    state (named extension point). Strongest law; makes the atom invisible
    in the product's current mode.
(b) RECOMMENDED — one DECLARATION, seam-state orthogonal: the image family
    declares exactly what the five existing families declare (the
    linear-premultiplied scene contract that `validate-family!` already
    requires) and RENDERS under whatever seam state is live, exactly as
    rect/text/slug do today. The legacy mode's color behavior is the
    SEAM's recorded migration-input property (W1 §8.2 closing already
    records it), never a per-family "regime row." Strict C1/C2/C4/C5
    receipts run candidate-enabled (the `*-srgb` fixture-target road —
    R3 confirms that half LAWFUL); C4 equivalence is scoped within
    candidate mode (direct vs intermediate present under the seam), which
    is what W1 §8.3 C4 actually enumerates. Grounds: matches the admitted
    families' precedent exactly; deletes the unlawful prose without
    inventing a refusal the product would feel; keeps every strict receipt
    executable.
**Re-runs under either:** §5 color block + G6 + C6-row language + the
recut-ledger-R2 N5 entry.

## Q2 — Mixed rect/image order representation (R3 N2 ≡ Codex escalation 2)

**Finding:** the image lane's intra-slot order is undeclared; the code's
tie fallback is `pr-str` on the count-key (scene_tape.cljc:393-397), so
`:images` < `:rects` by SPELLING — every image paints under every rect,
silently. W1 §3.1: stable-tie "cannot replace missing sibling order." And
part-rank is the wrong field for a distinct material (it orders ONE
material's subparts); the lawful field, sibling-rank, cannot ride the
one-entry-per-(vi,run) shape — an undeclared capability limit.

**Options:**
(a) RECOMMENDED — DECLARE the intra-slot kind order with explicit
    part-rank values (shadow 0 · rect 1 · images 2 · text 3, or the
    ruling session's ordering) on the pool entries, and DECLARE the
    capability limit as a named extension point: tree-interleaved
    image/rect sibling ordering within one slot is NOT expressible this
    wave (images paint as a kind-layer within the slot, as text already
    does — text unconditionally paints above rects today, same shape);
    the extension seam is per-run sibling-rank carried from ops when a
    consumer demands true interleave. Grounds: explicit declaration
    satisfies W1 §3.1's letter; the limit is honest and matches the
    existing text precedent; zero new mechanism.
(b) Per-run sibling-rank now — true interleave, heavier producer, no
    consumer demands it this wave.
**Re-runs:** §7 producer bullet + G5/G8 order language + a T-trap naming
the pr-str tie hazard.

## Q3 — Product-pick route: precondition, justification, owner
(R3 N3 ≡ Codex escalation 3)

**Finding:** the §5 pick claim is TRUE as geometry (R3 re-derived it) but
(i) rests on an UNSTATED precondition — rt-nodes carry no per-node
transform today; (ii) justifies by importing text's migration debt as
admission precedent, which W1 §1/§2 forbid for a NEW family; (iii) the
declared `:owner` is unreachable — `slot-entry` hardcodes
`:pick {:geometry :rect-tree-bounds :owner vi}` and `deepest-addressed`
can return an ancestor.

**Recommended repair (single option, no fork):** state the precondition as
a pinned manifest fact + receipt (an assertion that image rt-nodes carry no
per-node transform — if that fact ever changes, the receipt fails loudly);
reframe the justification honestly — the family's Contract-G truth is the
quad classification receipted by the verifier probes (candidate authority),
and product pick's bounds route is the W0-A DIVERGENCE-SENTINEL relationship
extended to the image cases (its own receipt field, per the R2 N5
isolation), never "text does it too"; declare `:owner` as the SLOT pick
owner (vi) with the image node's address REQUIRED in the pick result
payload — G4/G8 assert the address reaches the result (executable owner
claim instead of an unreachable one). Boundary/hit-slop: declared on the
quad in the descriptor, receipted by the verifier probe; the product route
inherits the slot's existing slop behavior, recorded as part of the same
sentinel relationship.
**Re-runs:** §5 pick bullet + §11 (new pinned fact) + G4/G8 wording.

## Q4 — G8's browser twin-check has no vehicle (R3 N4)

**Finding:** the verifier bundle never calls `draw-frame!`; the only
caller is the product frame loop behind a flag whose established road is
wearing the real ground — i.e. Sid's hand, contradicting §10's "no gate
needs Sid."

**Recommended repair:** drop the browser twin-check-with-images claim from
this wave's gates. Grounds: in a DARK wave no product surface mints image
ops, so the product frame arrangement cannot contain image entries — the
claim is vacuous until authoring exists. The felt wave (post-artery) owns
the product-frame-loop join receipt; record that as a staged obligation
beside the felt gate. The wave's order-truth coverage stays: JVM tape
receipts (G5 O1) + verifier image cases + the fence's static guarantees.
**Re-runs:** §7 last paragraph + G8 + the staged-obligations list in §0.

## Q5 — Shared-helper extension authorization (R3 N5)

**Finding:** the tape's private `registration`/`regime` helpers cannot
emit the M9 export-refusal field or a 3-band regime partition; extending
them is load-bearing but unauthorized.

**Recommended repair:** one authorization sentence in §12/§5 — the
implementer MAY extend scene_tape.cljc's private descriptor helpers
(they are file-local; the file is MAY EDIT) provided the five existing
family contracts re-validate byte-identically (assertion in G1).
**Re-runs:** §5 + §12 + G1 wording.

## Q6 — Image instance buffer home (R3 N6)

**Finding:** all four `pool-draw-info` sites live in MUST-NOT
`runtime/render.cljs`; `buffer_pool.cljs` is in neither allowlist column;
the buffer's home is unnamed.

**Recommended repair:** adopt R3's own lawful road explicitly — the image
instance buffer is owned by `init-image-system` (renderer.cljs, MAY EDIT)
and written inside `draw-frame!`'s existing encode window; `buffer_pool.cljs`
stays untouched (not added to the allowlist).
**Re-runs:** §6 + §12.

## Q7 — Asset-error / device-loss sink is unnamed (Codex escalation 4)

**Finding:** §6/G9 say "reports through the existing error lane" without
naming a lane; unfalsifiable as written.

**Recommended repair:** name the sink — the renderer's existing
structured-receipt mechanism (the `globalThis` receipt precedent, e.g. the
twin-check's `__softland_frame_tape_twin_receipt` shape): a
`__softland_image_ingress_receipt` carrying per-digest
{ok | refused | unavailable | device-lost, reason, counts}, plus the
deterministic placeholder paint; the verifier fixture drives
asset-unavailable and asserts the receipt + placeholder bytes. No product
error UX this wave (felt-wave material).
**Re-runs:** §6 + G9.

---

## Also before the ruling session closes

- R3's R2-consumption table: R2 repair (c) (stdout/receipt printing leg)
  ruled only partly consumed — fold its completion into the Q1-adjacent §8
  edit. R2 MINORs N7/N13-N19 remain open; three became findings above;
  sweep the rest into the phase artifact's implementation notes or close
  them explicitly.
- Re-pin the contract sha in NOW after the recut; note that ledgers R1/R2
  are historical maps only — git holds the true diff (the mid-round Codex
  edit made this the permanent state).
- Cut the Codex opening prompt into `IMAGE-ATOM-NOW.md` (Act 0 = §11 sweep
  + §8 boot-verify; the prompt template precedent is
  `build/shaping-correction/NOW.md`).
