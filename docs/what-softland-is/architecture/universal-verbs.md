# Universal verbs

**Duty.** Carry the reserved tier — escape, ask, go-to/reveal — plus the
universal noun, the address-set. Each universal verb lands per family: ask
lands via [packagers](packagers.md); go-to/reveal rides occurrence queries —
reveal the occurrences of this address, summon it here, open its
medium-native view at this sub-address; escape is the constitutional way out
of any worn material. Select stays family-tier and produces the noun. The
tier grows at constitutional speed, closed by the cage-or-silence test —
nothing enters it that a family could safely own.

**Seams.** Reservation is enforced by the binding grammar's universal
strength ([dispatch-and-courts](dispatch-and-courts.md)) and the
[code-floor](code-floor.md). Ask consumes packagers; go-to consumes the
occurrence index ([identity-service](identity-service.md)'s per-view leg);
the address-set feeds [set-acts](set-acts.md), whose coordinator row owns
the noun's store question.

**Existence: embryo.** Escape/meta is universally reserved across every site
and tier, camera floor-reserved but site-scoped — court shared, idiom
medium-owned (receipts with [dispatch-and-courts](dispatch-and-courts.md)).
Select's demotion is confirmed in source: three medium-shaped selection
verbs exist — text spans, rendered lines, spatial marquee
(`verb_registry.cljc:118-155`). The go-to family is missing, with drill as
its named crude ancestor, riding an index that already exists. Ask runs
today only at block grain (the conversation-turn loop, round-0 receipt); the
generalized ask over (address-set × scope) is missing, and the ask
reservation itself is design, not yet a row.

**Open.** Nothing owned here beyond the build; the address-set store's
custody lives with [set-acts](set-acts.md).

**Law.** [cage-or-silence](../constitution/cage-or-silence.md) ·
[read-honesty](../constitution/read-honesty.md) (ask's landing is byte-equal
packaging) · [identity-law](../constitution/identity-law.md) (the noun is a
set of addresses).

**Provenance.** [exchange-02](../provenance/exchange-02-adjudications.md),
multi-turn item 6 (the trace's step 11; the tier closed by test);
[exchange-03](../provenance/exchange-03-ratification-round.md) R6 (ratified;
courts demoted to machinery);
[exchange-04](../provenance/exchange-04-meta-syntheses.md) clause 9;
[exchange-00](../provenance/exchange-00-commission-and-round0-outputs.md)
round-0 (build-state testimony: ask at block grain, drill as ancestor).
