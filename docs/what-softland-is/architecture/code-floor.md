# Code floor

**Duty.** The never-migrating floor: the executor and its mathematics, the
grammars and compilers, the funnel, the escape gestures. The floor is what
checks declarations and therefore cannot itself be declared; admission of
citizenship declarations — both forms — is a floor-tier act. It protects the
kernel machinery that verb reservation doesn't: the courts and point. It
holds the code lane: open terms live here forever — a coined lens's
computation is quarantined to code while its selection and shaping ride the
data lane under admission-bounded cost.

**Seams.** Admits declarations for the
[citizenship-contract](citizenship-contract.md); backs
[dispatch-and-courts](dispatch-and-courts.md)' machinery protection; the
checker tiers declared in the contract get their teeth here; the
[funnel](funnel.md) is itself floor.

**Existence: exists in pattern, not as one named organ.** The admission
precedent runs — the registry asserts its own well-formedness before
anything serves (exchange-03 R3(b), checked). The floor's exemption is real
in source: binding and verb-floor files are excluded from activatable policy
(`material_circulation.clj:61`) while one shared human/agent edit grammar
sits above the floor (`anatomy_material.cljc:631`) — the exact pair on which
clause 12's recursion attack failed (Codex verdict 12: HOLDS). Camera verbs
are unbindable; the custody cut's outlier receipt — the escape gauge's
process-spawning computation, code lane forever — was isolated independently
by the cut and by the recon (exchange-04, the escape-gauge note;
exchange-03 R5).

**Open.** The coined-lens admission checker: the selection-query grammar +
cost admission is new code-floor machinery — "one more checker, named now
rather than discovered later" (exchange-02, multi-turn item 5).

**Law.** [custody-floor](../constitution/custody-floor.md) ·
[cage-or-silence](../constitution/cage-or-silence.md) (machinery under the
floor) · [read-honesty](../constitution/read-honesty.md) (computation stays
code) · [checkers-and-depth](../constitution/checkers-and-depth.md).

**Provenance.** [exchange-02](../provenance/exchange-02-adjudications.md),
multi-turn item 5 and one-shot item 6(b);
[exchange-03](../provenance/exchange-03-ratification-round.md) R3(b) and R5;
[exchange-04](../provenance/exchange-04-meta-syntheses.md) clauses 2, 11,
12; [exchange-05](../provenance/exchange-05-falsification.md) §3 verdict 12.
