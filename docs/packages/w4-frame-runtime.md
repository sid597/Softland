**W4-frame-runtime — Package 2's frame-kind package, SID-ACCEPTED
2026-08-07; CODE `143497a`.** The W2-B tape's
executor learns compiled multi-pass plans; seven serial internal cuts, one
owner (pass-graph/intermediates · groups+opacity · masks · blur · scissor
clip road · export seeds · THEN clock/scheduler); plan-level color mode
(effectless = legacy byte-identical; any effect = the whole frame on the
Contract-C linear road); pick fully effect-blind v1, declared; all visible
behavior behind `?live-atoms=1`. Contract (ends with the Codex
implementation prompt): `docs/render-engine/W4-FRAME-RUNTIME-CONTRACT.md`;
baton `W4-FRAME-RUNTIME-NOW.md`. Four-lens author pass closed (12 author +
~17 adjudicated sweep repairs). **ROUND CLOSED 2026-08-07** — one Codex
arm, 3 decision-changers repaired (`9c192af`; gradient law · per-op
scissor granularity · variant-layer resource custody); Sid: no second
arm; the one-arm round shape + token-economy laws landed in the
work-package skill. Codex built and closed all seven serial cuts in one pass:
13/92 focused JVM GREEN; all prior banks GREEN; W4 3/3 deterministic; verifier
RED only on the preserved MSDF counterexample. Sid's first felt pass found the
4K target-pool refusal; the in-session repair replays the real 3840×2160
mask/backdrop frame at five live targets with zero refusals. The accepted
second pass made opacity/frost legible, retained effect-blind semantic selection
bounds, and approved cross-frame reuse of free GPU targets as resource pooling,
never semantic caching. NEXT: T2 input-floor contract (starter delivered),
then the Package-2 seam courtroom over the landed set.
