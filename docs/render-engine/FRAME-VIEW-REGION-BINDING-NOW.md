# FRAME-VIEW / REGION-BINDING — NOW

## STANDING

Frame semantic state (arrangement · effect execution view · keyed plan fragments) maintains itself from explicit deltas under one generation authority; no complete old/new input comparison exists on the live road.
Region3D lease size, composite slot, and background are GPU binding/payload state owned by the region binding owner under `[semantic-generation, device-epoch]` stamps; a lease-rung crossing reconciles one region's physical resources and touches nothing semantic.
The batch effect/plan compilers live on as oracles; proportionality is counted (containers/fragments touched, comparator calls), never assumed.

## NOW

- 2026-08-10 — WHOLE atom built: explicit entry/container/region/global deltas, one semantic reducer/swap, maintained effect events + keyed plan fragments/order, and device-local Region3D bindings/slots/epoch.
- Desktop AMD RX 7900 XTX crossing `768×512→1024×768`: changed/produced/arrangement/comparator/effect/declaration/plan/full-validation all 0; binding update/acquire/retire `1/1/1`; required roles encoded 2; plan 0.1ms; twin 0 divergences.
- Mobile touch route (430×932, DPR 2, 43 entries) crossing `1024×1024→1024×1280`: the same zero semantic counters and `1/1/1` binding receipt; plan 0.1–0.2ms across the pinch; twin 0 divergences.
- Within-quantum motion acquires 0; viewport resize is binding-only and post-resize pick resolves; same-device replacement keeps semantic/plan generation 1 while epoch advances `1→2` and bindings reacquire cleanly.
- S1–S5 pure tripwires + effect/plan oracles: 22 tests / 120 assertions green; broader focused homes: 40 tests / 257 assertions, 0 failures, one preserved typed-plane `#object` replay error.
- Production release: 325 files / 42 compiled / 4 preserved warnings; verifier release: 201 files / 0 warnings; scene-tape fence green.
- Golden verifier remains stopped before Region3D at the recorded foreign T1 browser-layout `cluster-count 0`; Region3D capture now consumes the production binding owner/deltas; golden bytes and `scene_tape.cljc` are unchanged.
- F-d not reached. Self-audit: source/tests carry the implementation; docs are this close entry + standing/board flips only. Code banked `ec8c430`; no push; Sid acceptance remains the sole close gate.
- 2026-08-10 felt-pass repair — fullscreen workspace zoom exposed a shared-pool arbitration hole: a Region3D rung could consume cached mandatory frame targets, then `frame/group-output/42` hard-failed the whole canvas.
- Persistent Region3D admission now preflights the whole lease and preserves the free frame reserve; optional blur may reuse an exact target but cannot reclaim that reserve. Pressure takes the existing Region3D refusal-fill/degraded-blur roads; cap, old-before-new, and async retirement are unchanged.
- W4-only receipt: pool pass true, `region-reserve-preserved? true`; 3.5 MiB lease refused beside a 1 MiB cached frame target with free/destroyed `1/0`, and the optional probe preserved the same bytes. Focused tests 10/74 green; final verifier release 201 files / 0 warnings; foreign T1 stop unchanged.
- Sid fullscreen re-wear: the whole canvas no longer fails black, but maximum workspace zoom reaches the contract's declared Region3D refusal fill (checkerboard + corner glyph), so continuous mesh/text/ink visibility at that extreme is not accepted. A budget-aware current-frame lower-resolution road would change frozen refusal semantics and requires a narrow correction contract; no cap bump or silent/stale fallback. Acceptance remains open.
