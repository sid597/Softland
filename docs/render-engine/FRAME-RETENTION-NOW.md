# FRAME-RETENTION — NOW

## STANDING

FRAME-RETENTION retains semantic frame entries by declared family inputs while every encoded frame still projects the current camera.
Prepare-owned shape revisions reopen only entry-shape work; mutable GPU payload is resolved through stable system/pool references at encode time.
Region3D admits camera into semantic work only through the 256px allocation ladder and the 1.12x geometric encode ladder, with exact pick viewport rebuilt at pick time. *(Supersession pending: `FRAME-VIEW-REGION-BINDING-CONTRACT.md` moves the allocation ladder to the GPU-binding lane — this line flips at that atom's close.)*

## NOW — 2026-08-09 (receipt session; 2026-08-08 entry kept verbatim below as evidence)

- CORRECTION: the 2026-08-08 protected-ground line is FALSE — scene_tape.cljc is `bdb0d1f9…` (one-line image paint-key change, uncommitted). §5d and §6:476/MUST-NOT 1 cannot both hold on one registry; the contract fork is prepared for Sid's ruling (readings in the session close). Same change-set re-blessed the image test tripwire and weakened the fence's `:execute!` pin (now re-strengthened, T7 move).
- Twin caught a REAL retention defect on first runtime exercise: connector-minted label entries ride the text family's `:family/id`, so the family-scoped delta removed them on any text produce and dropped them on any connector produce — connector labels vanished after the first keystroke. Repaired: entries bucket by `:frame/producer` (produce tag in renderer + fallback in family-entry-delta); tripwire added (frame-inputs 9 tests / 23 assertions green).
- Proven post-repair, twin in shipped throwing form, ON from boot, real GPU (Radeon RX 7900 XTX, real space n=261): boot → edit → pan → orbit → same-length swap = 0 divergences; S5 strong form passed (screen changed, zero families produced, twin equal). S1 clean incl. live-selection pan; S3 encode-cross re-encodes without produce; lease-cross mints region-3d alone; S3(d) unreachable (zoom clamp 0.1 = regime boundary); device-loss leg unit-tier only.
- Residual: a twin divergence THROW kills the entire runtime loop (Missionary join cancels) — flag-only path, recorded, not repaired here.
- Run P (trap, zero file edits): NO post-boot arrangement construction — C1a's "rebuilt from scratch" hypothesis dead; the ~1.9k comparator calls on a mint frame are the plan gate's deep `=` over old/new arrangement maps (stack receipt); lifecycle is the lawful 1C road; plan/effects remain population-sized per 1-entry delta → the C1 session's target.
- `?twin=1` boot switch added to index.html (rides mdbg's lifetime) so the mobile twin leg is drivable without a console.

## NOW — 2026-08-08

- Built the pure family ledger/declaration fence, per-frame receipt, family-scoped arrangement delta, lifecycle reset, effects/plan gates, and compare-only twin.
- Re-plumbed renderer, image, pool, path, chrome, connector, Region3D, and verifier executors through stable encode-time payload resolution; retained all existing RAF/DRAW/fams instrumentation.
- Gated subsystem prepares on complete semantic keys; shape revisions are paired with stable system identity, and pool revisions mint only at the zero/nonzero entry-shape boundary.
- Region3D now holds unchanged derivation/placement/order state, separates allocation and encode ladders, counts work inside real bodies, and reconstructs current exact pick viewport.
- Falsification: ledger tripwires S1-S5 classes plus family-delta/declaration/lying-prepare cases pass (8 tests, 19 assertions); `compile dev` passes (320 files, 4 preserved foreign warnings).
- Felt correction: legacy command/settings rect systems expose `:instance-buffer`; encode-time resolution now normalizes it for direct and clipped draws, with the exact status draw `Draw(6,1,0,3)` proving pipeline → bind → scissor → slot-0 buffer → draw in the compiled dev build; verifier tripwire added.
- Headless WebGPU verifier was attempted once: release compiled, then the run stopped at the T1 browser-layout receipt (`cluster-count 0`) before any FRAME-RETENTION scenario; no golden was rewritten.
- Protected ground: `scene_tape.cljc` SHA-256 remains `f47aa9f34279a0ad7fc29838c4a14f328c6370a2e1ee64aa49307725686c045c`; scene tape and GPU-golden diffs are empty.
- Defaults: connector zoom remains a registered regime door because it feeds route derivation; Region3D encode-rung changes re-encode payload without family production, while lease-rung changes mint shape.
- Fork question: none raised; exact-pixel pick dependency is routed through the current viewport at pick time, while prepared scene/BVH state remains retained.
- Status-derived changed files (including preserved pre-existing work): `resources/public/index.html`, `src/app/client/substrate/frame_inputs.cljc`, `src/app/client/substrate/webgpu/{buffer_pool,chrome_gpu,connector_gpu,path_gpu,region3d_gpu,region3d_placement_gpu,renderer,verifier}.cljs`, `src/app/client/workspace/region3d_runtime.cljs`, `src/app/client/workspace/runtime.cljs`, `src/app/client/workspace/runtime/{fonts,render,touch}.cljs`, `src/app/client/workspace/text_shaper.cljs`, `test/app/test_runner.clj`, `test/app/client/substrate/frame_inputs_test.clj`, `.frame-retention-impl.log`, `docs/.obsidian/`, and this file.
