# PATH-ATOM — NOW

STANDING (frozen at cut, 2026-08-06): Package 2 wave 2 — the tessellated/path
atom (ink + shapes as material, D2=A, day-one shapes = open polylines + closed
contours + explicit holes) + the dev-flagged live join (`?live-atoms=1`; Sid's
cut ruling — custody stays OPEN, both atoms render live through the real artery).
Contract: `PATH-ATOM-CONTRACT.md` (one-pass law; two Sid-touches: cut 2026-08-06 ·
accept pending). Implementer: Codex, one session, whole atom. Genuine forks and
strongest-default notes land HERE as one-liners. Focused suite: the two path JVM
namespaces + `npm run verify:render-engine`. This baton never outranks the
contract or decisions.md — flag drift, keep moving.

---
(NOW entries below, ≤15 lines each, newest first; close entry includes the
process-vs-code line self-audit.)
