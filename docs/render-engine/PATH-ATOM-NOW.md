# PATH-ATOM — NOW

STANDING (frozen at cut, 2026-08-06): Package 2 wave 2 — the tessellated/path
atom (ink + shapes as material, D2=A, day-one shapes = open polylines + closed
contours + explicit holes) + the dev-flagged live join (`?live-atoms=1`; Sid's
cut ruling — custody stays OPEN, both atoms render live through the real artery).
Contract: `PATH-ATOM-CONTRACT.md` (one-pass law; two Sid-touches: cut 2026-08-06 ·
SID-ACCEPTED 2026-08-06). Implementer: Codex, one session, whole atom. Genuine forks and
strongest-default notes land HERE as one-liners. Focused suite: the two path JVM
namespaces + `npm run verify:render-engine`. This baton never outranks the
contract or decisions.md — flag drift, keep moving.

---
(NOW entries below, ≤15 lines each, newest first; close entry includes the
process-vs-code line self-audit.)

## 2026-08-06 — IMPLEMENTED; SID ACCEPTED
- Whole atom landed in code commit `10dd8e5`: fail-closed grammar/edits, content-keyed cache, CPU truth, deterministic tessellation.
- Ink expands directly to segment quads + regime round fans; shapes bridge holes
  then ear-clip in pure `.cljc`; coverage is honestly `:aliased-v1`.
- `:render.family/path` is non-nil registered; store/tape/GPU/pick are live; path
  narrow pick dispatches through rect-tree's per-family predicate registry.
- `?live-atoms=1` awaits procedural image ingress, then injects image + two inks +
  holed concave shape through rt-node → store → tape → registered pipelines.
- Five JVM tripwires / 39 assertions GREEN; product `dev` compile GREEN, 0 warnings.
- Verifier: prior goldens 42/42; path 3/3 deterministic; parity 7/7 (0 mismatch);
  color/order/dark/upload gates GREEN; ONLY RED is preserved MSDF 14/21.
- Falsification fixed value-identical mesh-set reuploads. No genuine fork; strongest
  default: shape point edits use stable contour id + index until vector networks.
- Self-audit: process-artifact lines written (this entry + board flip) < code lines.
- SID ACCEPTED from `?live-atoms=1`: aliased baseline stands; AA/exact overlap remain LATER. Code `10dd8e5`.
