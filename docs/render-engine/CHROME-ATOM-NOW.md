# CHROME-ATOM — NOW

STANDING (frozen at cut, 2026-08-06): Package 2 wave 4 — selection /
manipulation / snapping / guides / drawing chrome. One new tape family
`:render.family/chrome` (first STORE-SLOT consumer of the overlay stratum,
ranked under the product-chrome frame ladder) + the
interaction machinery: scene selection ({:vi :address} session truth; the
Task-18 legacy road stays untouched unflagged), arrangement manipulation
(translate through the existing settle artery, snapped; fixture scale via
corner handles), the pure snap engine (8.0px/zoom, anti-gaming law), smart
guides, and the hybrid anchor/metric WGSL law — all behind `?live-atoms=1`,
flag absent byte-identical. Contract: `CHROME-ATOM-CONTRACT.md` (one-pass
law; cutter `claude-fable-5` effort max; two Sid-touches: cut 2026-08-06 ·
accept pending; the cut closes through the armed A/B falsification round —
Codex + fresh Claude, identical brief, both at Sid's hand, findings pooled,
author repairs in-session). Implementer: Codex, one session, whole atom.
Genuine forks and strongest-default notes land HERE as one-liners. Focused
suite: the four pure JVM namespaces (`selection_test` ·
`chrome_material_test` · `snap_test` · `chrome_derive_test`) + `npm run
verify:render-engine` (RED only on the preserved MSDF counterexample). This
baton never outranks the contract or decisions.md — flag drift, keep moving.

---
(NOW entries below, ≤15 lines each, newest first; close entry includes the
process-vs-code line self-audit.)

2026-08-06 — ROUND CLOSED; the contract is the build law. Arm 1 (10
findings, Sid's count) was ingested in the cutter's session — its repairs
are in the contract. Arm 2 (6 findings; its quotes pin it to the
pre-repair draft) adjudicated THIS session against fresh source receipts:
five of six already covered by arm-1's repairs (namespace count →
enumerated SIX · slop → the zoom×container-scale law · overlay population
→ store-slot qualification + ladder pin · shift-tap → wholesale swallow ·
M10 → S4 rows); genuine residue = ruled-default #7 still said "visible"
against clip-blind-v1 — repaired, plus four tightenings (dispatch-action
inside the swallow · slop cross-ref at the forms table · budget-refusal +
asset-N/A named · the :493 docstring rider). A/B datum: arm-2 overlap 5/6
vs connector's 3-of-9 shared — pooling value FELL as the author pass
improved; arm attribution (which arm was Codex?) = Sid's line. Expiry did
NOT fire. Next: paste the contract-tail opening prompt into the implementer.
