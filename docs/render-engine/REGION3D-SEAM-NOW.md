# REGION3D SEAM (Atom B) — NOW

STANDING (frozen at cut, 2026-08-07): Package 3 Atom B — the coexistence
seam + THE SEAM DEMO; the FINAL atom of the render-engine campaign, and its
close carries the ONE seam courtroom over Packages 2+3 (A-contract §10 format
+ the latency trace, contract §9). Contract:
`REGION3D-SEAM-CONTRACT.md` (one-pass law; cutter `claude-fable-5` effort
max; two Sid-touches: the cut + the accept at the demo; the cut closes
through ONE bounded fresh-eyes Codex-class falsification round at Sid's hand
— findings return to a Fable session for in-session repairs, no verdict
authority, no recut). Implementer: Codex, one session, whole atom, after the
round's repairs land. Discharges the eight frozen §9 obligations of the
A-contract, none shrunk. The observed object-interaction slowness stays
observed/unmeasured/unattributed until the courtroom's adapter-pinned frame
trace — no perf work rides it. Focused suite:
`region3d_placement_test` + `region3d_scene_test` + `region3d_material_test`
+ the connector test home + `scene_tape_test` + `frame_graph_test` +
`maintained_view_test`; `npm run verify:render-engine` RED only on the
preserved MSDF counterexample. Genuine forks and strongest-default notes land
HERE as one-liners. This baton never outranks the contract or decisions.md —
flag drift, keep moving.

---
(NOW entries below, ≤15 lines each, newest first; close entry includes the
process-vs-code self-audit line.)

## 2026-08-07 — CONTRACT CUT; round pending at Sid's hand
- Cut from primary reads (A-contract whole · W1 O/G/M/T/C/D2/GROUND ·
  ENGINE §0/§7/§8/§10 · W0-C §3.3/§4/§5.1 · decisions render-seam +
  substrate + engine-lands · T2 + connector scoped · islands receipt) + two
  schema'd Opus breadth indexes (region3d namespaces · 2D lanes), roads
  verified at the bytes in-session.
- Shape: NO new tape family/pass/plan/lease machinery — grammar v2 at two
  families (placed `:text`/`:ink` object kinds; connector `:region-object`
  binding), two new pure/GPU placement namespaces, one store ref-resolution
  step, the carried-layout mirror, the anchor resolver injection, the demo.
- Author pass (four lenses) ran in-cut; three post-Write repairs landed as
  one batched pass (shadow-pass exclusion pinned · live-mirror cache key ·
  S5's clipped-card leg).
- NEXT: Sid pastes the falsification-round prompt (session-close artifact)
  into ONE fresh Codex session; findings → Fable repair session; then the
  contract-tail Codex implementation prompt.
