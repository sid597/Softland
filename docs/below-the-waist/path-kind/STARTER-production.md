# Path kind — the starter for the first production slice (2026-09-07; rewritten at Sid's word after the comparison was judged)

Written at Sid's word, 2026-09-07 after midnight IST: "we do merge first and then the further fixes by codex because as you found out its anal about its boundings and how to do things correct ... i think it would take care of the merge as well so write the full prompt", then "yes please and this is for path only". The chairs, the order and the first slice are his rulings in `../two-chairs.md`, the production section; this paste points at it and restates only his rulings, in his first person. Path kind only; the 3D kind's starter is written when this slice is on `main`.

**What this slice is, and what it is not.** The merge of `waist/path-claude` into `main`, the three pieces ported from the Codex lane, the five defects the judges named, the base's tests re-pinned, the repo's verifier green, the documentation hierarchy in the same commits, a handoff. There is no design session before it: the design as data for these pieces exists already, in the two judge files with their receipts and in the Codex lane's own code, and the pieces are the landing session's own family's work. The push edge, the rest of findings 1 to 4, the offset stroker question, the pickup brush and the compositor are not in it; they are designed next, on the tree this slice leaves, by one Fable session at max for the pieces both kinds share (two-chairs, the production section).

**Why the merge is in the paste.** The house rule keeps merging Sid's alone. The base is not on `main` (the branch is eleven commits ahead, four of them code and one tests; `main` has changed no file under `src/` or `test/` since the fork at `bedb890`; a dry-run merge is clean on code with one docs conflict, checked 2026-09-07), and Sid handed this merge to the landing session at his word. The paste carries that word so a session that reads the law does not stall on it.

## 1. The landing session (Codex, a fresh context, in the main checkout)

`````
Fence: you sit in the main checkout `/mnt/data/projects/Softland` on `main`, one branch. Code under `src/app/client/` and, in scope for you, the tests and goldens under `test/`; never `src/app/server/env.clj`. Read anything under `docs/below-the-waist/`. The worktree `../Softland-codex` (`waist/path-codex` at `07ea014`) is a read-only source to port from; nothing is committed there. Commit on `main` by exact path, one commit per milestone, each big enough to read as one change; push is mine. The work-package skill and its contract-and-gate process are not used for this work; where an older handoff points at them, this supersedes it.

The base is not on `main` yet. `waist/path-claude` (worktree `../Softland-claude`, at `e8cb946`; `4d9e0d6` is the judged delivery, and the code after it is the dump script and the pack fix from the trace) carries the path kind's first client change, and `main` has changed no file under `src/` or `test/` since they forked at `bedb890`. Your first commit is that branch merged into `main`: a merge commit, not a squash or a rebase, so the hashes the judge files cite stay citable. The one conflict is `docs/below-the-waist/path-kind/findings-for-production.md`, where `main`'s copy is the newer one and wins. The house rule keeps merging mine; for this slice I hand it to you at my word (2026-09-07), and this paste is that word.

What I am building is every 2D tool as data above a waist of code that exists once, so that humans and agents can create, edit and reuse tools as records. The exploration of the path kind is done, the prototype is satisfied from both models, and the first implementation round was a comparison: your family's lane and a Claude lane from one starter, judged by a fresh session of each family with a receipt behind every mark. The receipts settled the base and this slice. The Claude lane goes forward because its shortfalls are five one-function fixes and the other lane's are scope; three pieces of your lane are ported into it because they are right where the base is wrong: the capsule union with the radius-error subdivision as the nib's construction; the path value as the renderer's input with the source recipe run at the edit boundary, so that the renderer runs no construction and keeps no run cache, and a record's declared view inputs (the projected scale for a device-unit width, the pan's fraction for a snapped record) are explicit inputs of the geometry level; and one expression language for recipes and width rules in place of the base's two. Then the five defects the judges named, each fixed with a test that fails on its counterexample; the base's tests re-pinned where a number was the tracer's and not the definer's; the repo's verifier green, any golden re-recorded on purpose with the reason named; the documentation hierarchy from the READMEs down to the docstrings changed in the same commits; a handoff. Not in this slice: the push edge where a caller names what changed, the offset stroker question, the pickup brush and the compositor. Those are designed next, on the tree you leave.

I ruled three things. Caching is never an option; it means the underlying thing is as it should be and then still needs more performance, therefore caching goes on top: a key is a level's full input as a value, never what a run happened to read and never a hash, and no memo, bucket or atlas is added, and none that exists is credited, without a trace that shows the need. The repo's own verification surface is the acceptance surface, necessary and not sufficient, and a red check is reported as red before anything else is claimed; a green flag that reads no pixel is not a picture. Every claim in a README or a handoff points at the test that shows it or is marked untested. Our aim is not to copy anyone; the best in the field is a datapoint; nothing is carried forward because it exists. The definer's records are what the implementation is measured by; the definer works new constructions through what you land as it lands, and I carry them to you.

Terrain. `docs/below-the-waist/two-chairs.md`, the production section, is the ruling behind this slice. In `docs/below-the-waist/path-kind/`: `judge-claude.md` and `judge-codex.md` with their receipts directories are the named defects, each with the code and the probe that shows it (the base's A1 to A5; your lane's C1 to C4 sit in code the base does not take unless you bring it), and every probe re-runs with `clj -M:test -i <script>` from a worktree; `findings-for-production.md` is the base builder's trace, sorted by my rule; `HANDOFF-client-claude.md` and `code-map-client-claude.md` are the base's own account of itself, on `main` once merged, and `src/app/client/path/README.md` is its folder map; `bench-9/HANDOVER.md` is the prototype's record vocabulary with the definer's numbers, `bench-9/node-route/` runs the pure declarations in Node, and `path-kind.md` holds the picture, the contract block and my rulings with the time. The path value at the waist is the bench's shape as the base wrote it (`:kind :line`, `:quad`, `:cubic`; `:p`, `:c`, `:c1`, `:c2`; `:closed?`; `:knots`); your lane's segments are named differently (`:type`, `:to`, `:control`). The bench's draw-tool and pen-tool records, fixtures in the base's tests, use dashes, a ribbon, butt caps and open miters, which the base's tracer says and your lane's capsule does not. The three pieces are in `../Softland-codex` at `src/app/client/path/geometry.cljc` (`flatten-stroke`, `capsule`, `swept-nib`, with `curves.cljc` under them and `classify` reading the clip), `src/app/client/path/component.cljc` with `records.cljc` (the path-value input and the recipe at the caller) and `src/app/client/engine/executor.cljc` (the language); the base's counterparts are `stroke.cljc`, `records.cljc` with the source gate and `run` in `component.cljc`, and `engine/executor.cljc` with `engine/expression.cljc`. The base's tests are under `test/app/client/path/` with `run_pure.clj` as the fast lane; its goldens are under `test/app/fixtures/render_engine/gpu-goldens/` with `manifest.json`; the build and verifier commands are in `judge-claude.md` section 1. The client's reactive laws are the `electric-docs` skill, Missionary only; Electric is not a dependency. The harness runs and dumps as the READMEs say; the numbers and pictures are files it writes. Your handoff lands as `docs/below-the-waist/path-kind/production/HANDOFF-1.md`.

Work the hardest case through what you build before you call the slice done: the border record's one device pixel at every zoom and the snapped record, now that nothing in the renderer observes what a construction read; say where a named piece hid work you had to invent; end on the exact open question you leave. Where you departed from a piece as your lane wrote it or from the base as it stands, say what supports the departure, when it stops holding, and what else changes with it.

Load turn: read what is named above, reply in one line with what you hold and anything you need to start that is not there, and wait; I set the effort and say when to begin.
`````

## 2. Sid's card

1. New Codex session in `/mnt/data/projects/Softland`. Set the effort for the load; paste section 1.
2. It replies in one line. If it names something missing, answer it. Set the effort for the build and say `Begin.`
3. It merges, lands, commits per milestone, reports red first, writes `production/HANDOFF-1.md`.
4. When it says done: the definer against the landing (section 3), then one judge per family (section 4). Then the shared design session (two-chairs, the production section).

**Effort** (a position, not a ruling; nothing on this project has measured it). Astra offers medium, high, xhigh and max; the comparison lane ran high throughout. The load turn at medium, the lowest there is, so the read enters before any judgment forms. The build at xhigh: this slice has no design session in front of it, and one of its three ports, the boundary, restructures a renderer this family did not write, which is exactly where a literal execution at high can relax the validation line and leave the run cache standing. Max only for a turn in which it reports a design problem, then back down. High throughout is defensible, since the comparison lane's failures at high were omissions the words now cover rather than reasoning depth; whoever chooses it reads the boundary port first in the handoff.

## 3. The definer, against the landing

The paste is `STARTER-client.md`, "The definer (after the comparison, when the roles are assigned)", with its terrain's first sentence read as: you hold the definer's chair against the first production slice landed on `main` from `STARTER-production.md` and the judge receipts, under `../two-chairs.md`'s production section; the records you work through it land in this directory as before, with numbers.

## 4. The judges, at the slice's end

One fresh session per family, the same paste, `STARTER-judge.md`'s shape (on `main` once the base is merged; it was `5405ebe` on the branch): the card in `two-chairs.md`, a receipt behind every mark, a difference settled on an executed example. Written when the slice closes, with the hash it froze at.

## 5. After this slice

The Fable design session for the pieces both kinds share, the executor and the compositor's paint and sample, with both benches' records as the measure, starts once this slice is on `main` and its handoff exists. Its paste, the 3D kind's landing paste from it and Sid's card are `../3d/STARTER-production.md`, written 2026-09-07 ahead of its start; the path kind's pickup landing, if the design cuts the two brushes apart, is written from the design then, in the same shape.
