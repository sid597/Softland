# Path kind — the starters for the first production slice (2026-09-06, after the comparison was judged)

Written at Sid's word ("lets fix the docs that needed fixes update our thigns and then we will get to the production in future sessions"; then "is there a prompt ??? where???"). The chairs, the order and the first slice are his rulings in `../two-chairs.md`, the production section; these pastes point at it and restate none of it. Two pastes, in the order they run: the design session (Fable at max) whose output is the design as data, then the landing session (Codex at high, a fresh context) that builds from it. The definer's paste is the one already in `STARTER-client.md` with one line of terrain changed, given at the end. The judges' paste is `STARTER-judge.md`'s shape on the Claude branch, written when the slice closes.

The design lands as `production/DESIGN-1.md` in this directory on `main`; the landing session's paste names that path, so it is sent only after the design is committed.

## 1. The design session (Fable at max; turn one at low reads and replies in one line, then the switch)

`````
Fence: you sit in the main checkout `/mnt/data/projects/Softland` on `main`; read anything under `docs/below-the-waist/`, and `src/app/client/`, `test/` and the git history in the main checkout and in the two worktrees `../Softland-claude` (`waist/path-claude`, at `e8cb946`; `4d9e0d6` is the judged delivery, the commits after it are the pack-key fix and the trace) and `../Softland-codex` (`waist/path-codex`, at `07ea014`); never `src/app/server/env.clj`. You commit no code. What you produce is one file, `docs/below-the-waist/path-kind/production/DESIGN-1.md`, plain commits on `main` by exact path; push is mine. The landing session that builds from it is a Codex session at high in a fresh context; it does what your file says and nothing it does not say, so your file says everything.

What I am building is every 2D tool as data above a waist of code that exists once, so that humans and agents can create, edit and reuse tools as records. The exploration of the path kind is done, the prototype is satisfied from both models, and the first implementation round was a comparison: two lanes from one starter, judged by a fresh session of each family with a receipt behind every mark. The receipts settled the base and the first slice. One implementation goes forward, on `main`, from the Claude lane, because its shortfalls are local and the Codex lane's are scope; and three Codex pieces are ported in first because they are right where the base is wrong: the capsule union with the radius-error subdivision as the nib's construction, the path value as the renderer's input with the source recipe run at the edit boundary, and one expression language for recipes and width rules. Then the named defects with tests, the repo's verifier reconciled with the text goldens re-recorded on purpose, and the underlying parts of the findings ledger.

I ruled three things after the judging. Caching is never an option; it means the underlying thing is as it should be and then still needs more performance, therefore caching goes on top: first each level of the data flow is a pure function whose inputs are explicit as data, a key is the level's full input and never what a run happened to read and never a hash, and every layer on top waits for a trace that shows the need. There is no trial; this is production, judged on its delivery. And the two families have two reflexes, Fable fills a gap with its own judgment and Codex stops where the words stop, so you design and Codex lands, and your file carries my caching rule in words so that your own reflex cannot put the memo back. Our aim is not to copy anyone; the best in the field is a datapoint; nothing existing is carried forward because it exists, and that includes the base's own machinery.

What I want to end up holding is the design as data: the values exchanged at each boundary, each level of the data flow with its inputs, the owners of mutable storage, GPU effects and lifetimes, the record examples, every affected caller by name with what changes in it, the deletion scope, tests and goldens in scope for the builder, the definer's records as the measure, the unresolved choices with your position on each, and the repo's own verification surface named as the acceptance, red reported before any claim. Not a contract and not a test suite before the code.

Terrain. `docs/below-the-waist/two-chairs.md`, the production section, is the ruling: the chairs, the order, the first slice, the fence sentence, one rule per reflex. `docs/below-the-waist/path-kind/judge-claude.md` and `judge-codex.md` with their receipts directories are the named defects with the code that shows each (the Claude lane's A1 to A5 and the boundary; the Codex lane's C1 to C4, F3 and F4), and the worker reading behind the roles. `findings-for-production.md` with `measurements-client-claude.md` and the two trace receipts is the builder's ledger of where the time goes, sorted by my rule into the underlying thing and the layers on top. `path-kind.md` is the picture, the contract block and the rulings on its ledger; `bench-9/HANDOVER.md` and `bench-9/waist-bench.html` are the prototype and the record vocabulary, and the definer's records there and in `bench-9/node-route/` are what the implementation is measured by; `from-12-to-client.md` names the five scenarios the first client change owed; `HANDOFF-client-claude.md` and `code-map-client-claude.md` are the base's own account of itself, and the Codex lane's `src/app/client/path/README.md` is the other's. In the Claude worktree start from `src/app/client/path/README.md`; the three pieces to port are in the Codex worktree at `src/app/client/path/geometry.cljc` (`flatten-stroke`, `capsule`, `swept-nib`), `src/app/client/path/component.cljc` with `records.cljc` (the path-value input and the recipe at the caller) and `src/app/client/engine/executor.cljc` (the expression language). Memory holds the proven roads for driving the app and for a WebGL2 bench headless.

Work the hardest case through whatever you design, the pickup brush's per-step read of the surface it paints and the push edge where a caller names what changed, and say where a named piece hides work the landing session would have to invent; end on the exact open question the slice leaves. Where you take a position, say what supports it, when it becomes only a default, and what else changes with it.
`````

## 2. The landing session (Codex at high, a fresh context; sent after `production/DESIGN-1.md` is committed)

`````
Fence: you sit in the main checkout `/mnt/data/projects/Softland` on `main`, one branch; code under `src/app/client/` and, in scope for you, the tests and goldens under `test/`; never `src/app/server/env.clj`. Commit by exact path, one commit per milestone, big enough to read as one change; push is mine. The two comparison worktrees `../Softland-claude` and `../Softland-codex` are read-only sources to port from; nothing is committed there.

What I am building is every 2D tool as data above a waist of code that exists once, so that humans and agents can create, edit and reuse tools as records. The design for this slice is `docs/below-the-waist/path-kind/production/DESIGN-1.md`, written by a Fable session at max from the whole tree and the comparison's receipts; you own the complete implementation of it: the algorithms, every caller it names, the tests built with the code, the hierarchy of documentation from the READMEs down to the docstrings down to the code changed in the same commit, the repairs, and the report. Where the design is wrong or silent you say so in the commit and in the handoff and do the engineering, and you keep the right to bring the better construction or the simpler boundary, since your family's geometry was the better of the two in the comparison. The design session stays open for a design question; I carry it.

I ruled three things. Caching is never an option; it means the underlying thing is as it should be and then still needs more performance, therefore caching goes on top: a key is a level's full input, and no memo, bucket or atlas is added or kept as a credit without a trace that shows the need. The repo's own verification surface is the acceptance surface, necessary and not sufficient, and a red check is reported as red before anything else is claimed; a green flag that reads no pixel is not a picture. And every claim in a README or a handoff points at the test that shows it or is marked untested. Our aim is not to copy anyone; the best in the field is a datapoint; nothing is carried forward because it exists.

Terrain. `docs/below-the-waist/two-chairs.md`, the production section, is the ruling behind this slice. `judge-claude.md` and `judge-codex.md` in `docs/below-the-waist/path-kind/` with their receipts are the named defects, each with the code and the probe that shows it; `findings-for-production.md` is the ledger, sorted by my rule. The definer's records on the bench (`bench-9/HANDOVER.md`, `bench-9/node-route/`) and in the base's tests are what the implementation is measured by; the definer works new constructions through what you land as it lands, and I carry them to you. The base's own account is `HANDOFF-client-claude.md` and `src/app/client/path/README.md` in `../Softland-claude`; the Codex lane's is its `src/app/client/path/README.md`. The harness runs and dumps as the READMEs say; the numbers and pictures are files it writes.

Work the hardest case the design names through what you build before you call the slice done, and say where the design hid work you had to invent; end on the exact open question you leave. Where you departed from the design, say what supports the departure, when it stops holding, and what else changes with it.
`````

## 3. The definer, against the landing

The paste is `STARTER-client.md`, "The definer (after the comparison, when the roles are assigned)", with its terrain's first sentence read as: you hold the definer's chair against the production slice being landed on `main` from `production/DESIGN-1.md`, under `../two-chairs.md`'s production section; the records you work through it land in this directory as before, with numbers.

## 4. The judges, at the slice's end

One fresh session per family, the same paste, `STARTER-judge.md`'s shape (on `waist/path-claude` at `5405ebe`): the card in `two-chairs.md`, a receipt behind every mark, a difference settled on an executed example. Written when the slice closes, with the hash it froze at.
