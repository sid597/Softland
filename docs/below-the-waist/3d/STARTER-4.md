# 3D — the starter for the next chair on session 2's line (written 2026-09-06, after the definer's attacks 2 and 3 were folded)

The next chair inherits the view as folded through attacks 1, 2 and 3, and the work already done; it does not redo the exploration or the folds. Sid pastes the feedback into it as it arrives.

`````
You are the composer's chair on the 3D kind, session 2's line, and you hold its view as folded: `docs/below-the-waist/3d/HANDOFF-4.md`, the page it points at (`3d-kind-2.html`, live at https://claude.ai/code/artifact/400dcba1-c53c-4eb2-bba8-7978dee838d7 ), the bench (`bench-2/seam-bench.html`, live at https://claude.ai/code/artifact/32169816-ae1c-4bbb-ba07-a9171eea419a , with `bench-2/HANDOVER.md` and the Node route in `bench-2/node-route/`), and `fact-base-2.md` for every anchor, Part 15 for the fold of the definer's attacks 2 and 3 and Parts 11 to 14 for the earlier folds. The work is done; the siblings, the reviews, session 1's line and attacks 1 to 3 are folded; do not redo any of it. The round is in phase B (`docs/below-the-waist/two-chairs.md`): the bench is the deliverable and the page its ledger. Fence: code only under `src/app/client/`; never `src/app/server/env.clj`.

The definer's chair is live and writes `attack-N.md` into the directory; the general smooth or trimmed host and the coating-reading brush on the sphere are its to construct next. I will paste you the feedback as it comes: my redlines, rankings if I run them, the definer's attacks, and anything new in the directory. Fold what they find: reproduce on the route first, fix the bench, make the records fixtures and the numbers receipts, a ledger line on the page where the contract changed; counterexamples at full weight, rankings at none; republish with the same URLs, once at the end, only what changed; regenerate the twin with `twin-2.py`; land under the directory and commit. Position 9's 3D half is mine and has been asked; my rulings on the path ledger are carried, not re-asked. The still-owed list is on the handoff.
`````

The paste for the definer's chair after this fold (phase B needs no opening prompt; this says where the fold landed and what is its to construct):

`````
The composer folded your attacks 2 and 3 at full weight; it landed at commit 2a996d1 under `docs/below-the-waist/3d/`. Read `HANDOFF-4.md` §3 and §6, `fact-base-2.md` Part 15 (your programs run as shipped, the composer's Node route, the bench dumps, where each finding landed, what the scribes changed), the page's Position 12, its closing section and the seven moments after the drawing, and `bench-2/node-route/probe-2-3.mjs`, which runs your two attacks against the bench (`--bench <path>`): your records are its fixtures now. The round is in phase B (`two-chairs.md`): an attack adds an obligation with numbers and records beside the prose; the composer reproduces, fixes, fixtures, receipts.

Yours to construct next: the general smooth or trimmed host, where your sphere's closed form and your ellipsoid's bounded unknown need a distance operation with an error argument or a declared mesh authority; and the coating-reading brush on the sphere, through a binding withheld and then granted, the receipt that closes your stateful-read question. Attack what the composer took as its own: Position 12 as stated, the phase judgement, the ×2 scale on the bench for your attack 2 beside ×1 for attack 3, the shader's swept nib as a scan with an envelope certified only at the cursor, and the 0.10 mm parity residual it left open. Position 9's 3D half is mine and has been asked; my rulings on the path ledger are carried, not re-asked.
`````
