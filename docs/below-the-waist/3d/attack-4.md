# Attack 4 — the coating read must finish the same brush step

Definer's closing attack against the composer's fold **2a996d1**, 2026-09-06. The subject is `3d-kind-2`, `HANDOFF-4` §§3 and 6, `fact-base-2` Part 15 and the shipped `bench-2`. Its SHA-256 is `3061db2a056b354c10af2c2dd47bdcae5010f0790660b4d1b535cf585c1abdb9`. [Records](attack-4/records.json), [runner](attack-4/run.mjs), [pending continuation](attack-4/continuation.json) and [receipts](attack-4/receipts.json) accompany this attack. The composer owns the fold; none of its files is changed here. No client or server source is changed or newly used as evidence.

**The closing construction runs, and I hold Position 12's ordinary answer.** A brush reads the composed coating on G through B's withheld binding. Pending commits neither carry nor painting. A grant before demand, a grant after two pending demands, and a grant after restoring the pending transaction in a new process produce identical state, history and painting bytes. Consuming the transparent guess instead changes the painting and every subsequent carry in this fixture. This supplies the exchanged-value receipt for the question; the composer still has to fold this brush into the bench. The three obligations below belong to the existing executor, compositor and surface operations. Phase B holds.

Sid's latest scope takes precedence over the handoff's owed list: **a general smooth or trimmed host is production work, and is not required to close this attack.** I agree. The contract already says distance enclosure, witnesses, work/accuracy limit and unsupported or an explicitly different mesh authority. Another surface solver would exercise that capability; it would not settle this read's state semantics. Position 9's 3D half remains Sid's, already asked. The path ledger's rulings are carried, not re-asked.

**1. A pending read retains the transaction; a provisional input retains a choice.**

Use the bench's R = 200 mm sphere, merged revision G@2, A@0 and B@0, their actual root restrictions and bindings, and order [kA, kB]. The new brush's record reads linear-light premultiplied coating, without lighting or a screen lease. Its own preceding logical painting lies above A and B in that sample. It mixes the result into carry at .25, constructs an intrinsic circular deposit of radius 8.75 mm and paints with source-over opacity .5.

The retained events address kA's great-circle arc at parameters .65, .70, .41 and .65. They are four authored dabs, not samples inferred from pointer timing. The logical painting is a 64 × 32 Float32 RGBA grid over the saved unwrapped S0 patch in `records.json`; reads use the declared nearest logical texel. Deposit membership evaluates sphere distance at each texel centre. This finite painting discretization is part of this reference construction, not a claim of exact continuous coverage. At .41, B's root restriction excludes it even though the tap reaches the location. At the second and fourth dabs the brush reads pigment it previously deposited, so this is a feedback painting, not four repetitions of the first arithmetic example.

The runner executes the **embedded bench** `gLocate`, its binding and mark operations, `over`, `pickupMix` and sphere distance. It adds the read adapter, the transaction and continuation, and the spherical logical-painting operations. Its small interpreter executes the `read-surface → mix → surface-region → paint` nodes in the JSON record. These additions are explicitly this attack's construction; `pickupStep` on the current bench cannot yet run that record.

| Demand / outcome | Receipt |
|---|---|
| First read with B withheld | Support location known; A known; B unresolved; no next painting or carry. Repeating the demand gives the identical pending transaction. |
| What survives the wait | Original event and input-state digest, painting bytes and carry, pinned coating definitions/order/binding revisions and interpretation, missing B dependency, and the three queued authored events. The cold continuation also names the bench implementation it requires. |
| First resolved sample | (0.25, 0, 0.5, 0.75) |
| First carry after that sample | (0.8125, 0, 0.125, 0.9375) |
| First carry if B was guessed transparent | (0.875, 0, 0, 0.875) |
| Four-dab final carry, eager = delayed = cold | (0.6781253814697266, 0, 0.16514968872070312, 0.8432750701904297) |
| Four-dab final carry after the guess, B granted for later dabs | (0.7258682250976562, 0, 0.0880279541015625, 0.8138961791992188) |
| Painting, eager = delayed = cold | `30cb13f2147784bdae44b4aa303d412c8980f00c09435c0983521cd291d75579` |
| Painting after the guess | `c1cf81702b98fb287cbbb1432166d81e7a55750d3306c3934cd09528960d2c3d` |
| Change pickup from .25 to .5 in the record | Different painting: `4df8a532ec4048f5489927daff4e7dd3635853c66c8d6bb09ebe04a18fc5f0ce` |
| Use the first returned painting as the second recipe's starting painting | `a5aba6ed27c234df463b322995e052bba3e3c4fa7871e453d4a0400270cd887a`; the retained first result is unchanged. |

The receipts retain all four actual samples, snapshot identities, event identities and changed texel counts. Reversing the coating order changes the painting. Delivering the old answer to the new order's demand returns `stale` with no next state; replay from the original input under the new order produces its own result. Delivering an already consumed answer again also commits nothing. No saved overlap order yields `needs-policy`, not a transparent sample or an undifferentiated pending status.

The smallest addition to Position 12 is an executor obligation: **the resumed transition consumes the event, previous state, program and read dependencies it suspended with; a changed dependency starts the corresponding new computation, and an old answer cannot commit into it.** “Pending yields no next state” means no next *painting-step* state. The executor may create a waiting continuation and accept more authored events. Neither the scene nor the whole correspondence has to freeze.

I would narrow the exception's sentence, “so its result never varies with a completion race.” Retaining a provisional sample makes a selected history reproducible. It does not make the initial selection independent of scheduling. A rule “use the coating if ready, otherwise consume known A with B transparent” chooses the two different first carries above depending on when the grant arrives, even if both choices are retained perfectly. Either the choice is an explicit retained input, with two possible histories, or the record defines a selection rule that is itself independent of that race. The ordinary brush here waits. No additional permission or policy question for Sid follows from that distinction.

**2. A retained region obeys its declared radius and keeps a truthful subject.**

The ×2 / ×1 adaptation is dimensionally consistent at the tested scale. The [custody receipt](attack-4/custody-receipts.json) runs the shipped functions: attack 2's first distance is 144.54684956268315 mm, divided by two 72.27342478134157; its cap area divided by four is 16858.48556844744 mm². Attack 3's longitude displacement still measures 7.99839984003628 mm on the R = 200 mm host. Pressure, angles and RGBA are unchanged. There is no reason to reopen either scale. For the cold author, say **radius 16 mm** for kA; the bench's record uses `radius: 16`, whereas the pickup pressure function's w = 17.5 mm at p = .5 is a *diameter*. “Nib 16” leaves that avoidable ambiguity in the page's merge moment.

The adaptation did lose a record-level boundary the prose says works. `probe-2-3.mjs` tests the antipode with `reach.distance(q) <= 629`. A consumer of the reusable result calls `reachRegion(...).member(q)`. Those are different computations in the shipped bench.

| Saved reach radius | Correct membership of the antipode, distance πR = 628.3185307179587 mm | Actual `member` | Actual area |
|---:|---|---|---:|
| 628 mm | outside | outside | 502654.50582273136 mm² |
| 629 mm | inside | **outside** | 502653.3656189489 mm² |
| 2πR = 1256.6370614359173 mm | inside; every point of the sphere is included | **outside** | **0 mm²** |

For every radius at least πR, the result is the whole sphere, area **502654.8245743669 mm²**, bounds [−200, 200] on each axis. `cos(radius/R)` is a cap threshold only before saturation. Letting it cycle makes larger reaches shrink and, at 2πR, produces reversed bound endpoints as well. This is not numerical uncertainty near a boundary: the 629 mm antipode is 0.6814692820413 mm inside the declared radius. Saturate the angular radius for this construction and fixture the *returned region's* membership, area and bounds. Negative radii need the operation's stated admission behavior; none is invented by this attack.

A second receipt changes the caller's radius field from 150 to 140 after constructing a result. The result now reports `record.radius = 140`, but its closed-over membership still includes the point at 144.54684956268315 mm. Reconstructing from that reported record excludes it. The cap arithmetic was retained; its reported subject was aliased. Retain an immutable record/revision or own its snapshot so a result's declared subject can reconstruct its meaning. This is the surface-region obligation already adopted in attack 2, not a new piece.

There is also a precise fixture-custody limitation. All **40 non-vector functions** compared with the adjacent Node modules currently match, ignoring whitespace; the scribe's six vector-helper exception is respected. But on an owned temporary copy, changing embedded `gLocate` to return opaque green still passes **55/55** of `probe-2-3.mjs`'s checks. That script subsequently loads the sidecar modules over those definitions. Its `--bench` argument and printed hash are not enough to catch a future divergent embed. The temporary copy is removed. The new [loader](attack-4/bench.mjs) executes embedded declarations directly; the [custody probe](attack-4/custody.mjs) provides the exact mutation receipt. Keep an equality guard or execute the embedded functions in the inherited suite. The current 40-function equality preserves the present fold's numbers; this finding limits what the old suite will catch next.

**3. Numerical quality belongs to the answer actually consumed.**

The [GPU probe](attack-4/gpu.mjs) and [receipt](attack-4/gpu-receipts.json) use Chrome 150.0.7871.46, ANGLE/Vulkan SwiftShader, DPR 1, antialiasing disabled and clean shader/GL logs. Direct function diagnostics use an RGBA32F attachment, avoiding RGB24 depth encoding for those values. These are bounded probes, not a whole-image or hardware-GPU certificate.

For the swept nib, keep the correction: the shader now evaluates the pressure-derived radius, not the endpoint-radius capsule. Its scan is still an approximation. On the first segment translated to c0 = (0,0), c1 = (160,160), p0 = .2, p1 = .8, the saved query is q = (−1.5390163110831097, 6.461674036251386) mm. At t = .0158, pressure is .20948 and radius is 5.658228056 mm. This one source disc supplies an inside witness; no global minimizer assumption is needed.

| Operation at the saved query | Value |
|---|---:|
| Witness `distance(q, c(.0158)) − radius(.0158)` | −0.00010000000000065512 mm |
| Same witness using q rounded to the floats actually uploaded | −0.00009983735787599812 mm |
| Actual `sweepMinGL` return | **+0.000125885009765625 mm** |

This disproves treating the shader's sign as exact membership. It does not establish a visible defect at the current zoom. In exact real arithmetic the 33 samples at spacing 1/32 give a coarse bound: if m is the true minimum and ŝ is the scan/refinement result, **m ≤ ŝ ≤ m + L/64**, where L = √(160²+160²) + 30·.8·.6. Here L/64 = **3.760533905932738 mm**. The golden steps improve witnesses; they cannot invalidate that bound, but their local search does not alone certify a tighter global bound. Floating-point and coordinate error must be added separately. The CPU's cursor enclosure is a certificate about its own objective evaluation, not every fragment's returned value.

The obligation is to attach the achieved envelope to this coverage lane and meet a declared consumer demand, or explicitly report that it does not. The fold need not build a production coverage solver to close the stateful-read question. It must not promote the cursor's ±.0001 mm readout into an image-wide shader guarantee. A geometric membership request and a pixel's filtered coverage are also different outputs; this diagnostic measures the scalar returned by `sweepMinGL`, not anti-aliased coverage.

The field's residual reproduces at the same region pixel (226,127), with the pixel-centre ray used for both readings:

| Field extent a | Eye-origin shader | Shifted-origin shader | Shifted, with three Newton refinements using the unexpanded spatial field |
|---:|---:|---:|---:|
| .2 m | 2.2786185348471335 mm | **0.1009834448512825 mm** | 0.1009834448512825 mm |
| .1 m | .26149875464749284 mm | **0.06295363668402246 mm** | 0.06295363668402246 mm |

The last column is a negative diagnostic on an in-memory copy, not a repair proposal. It falsifies the narrow prediction that this particular spatial root refinement would remove the measured residual. The remaining cause is still unassigned. Keep the residual as a measured failure to establish parity, not a tolerance silently accepted. I do not require an arbitrary-host solver, contact image or full field-image certification as another round-closing construction.

There is a separate exact contact receipt at the same consumer boundary. Let f = (x/.2)⁴ + (y/.1)⁴ + (z/.1)⁴ − 1. The ray from (1,.1,0) toward (−1,0,0) contacts the boundary at **t = 1 m** and has no material interval of positive length. The GPU's `quarticRoots` returns that root at exactly 1; `fieldHit` returns **−1** with either origin setting. The CPU isolator returns a contact at .9999999523162841; `hitField` returns **null**. The axis-ray control from (1,0,0) returns .8 m on the CPU and .800000011920929 on the GPU.

Thus “the isolator finds tangencies” is true; “the hit operation serves those boundary contacts” is not established by it, and fails on this ray. The CPU consumer drops the event when its material-interval list is empty. The GPU also returns no hit in this direct call; this probe does not assign its internal loss to a single cause, including its reciprocal box test on a parallel boundary ray. Preserve contact events for a requested geometric-boundary answer. A visible-coverage policy may exclude zero-area contact, but it must be declared separately. Agreement on discarding the event would not prove the broader geometry claim.

**The fold and the stopping point.**

| What the composer took as its own | This attack's position / smallest fold |
|---|---|
| Position 12 | Keep the ordinary resolved-read barrier. Fold the actual G brush, its retained pending transaction, eager/delayed/cold equality and guess control. Narrow the provisional-history sentence as above. |
| Phase B | Keep it: state transition, retained surface region, composition sample and query quality are existing outputs. This attack adds obligations to their consumers. |
| ×2 beside ×1 | Keep the factors. Keep lengths/areas distinct, say radius versus width, and test the region result at 629 mm rather than a different comparison. |
| Shader scan with a cursor certificate | Keep the corrected objective; make the coverage lane's approximation and achieved envelope explicit. This receipt supplies a query where its sign differs. |
| .10 mm parity residual | Keep it open with the captured numerical tier and demand. Do not turn failed Newton refinement into a causal conclusion. Keep the separate contact-consumer record. |
| Claimed fixture route | Preserve the current matching functions. Add embedded-source custody so a divergent browser implementation fails the suite. |

Reproduce from the repository root:

```sh
node docs/below-the-waist/3d/attack-4/run.mjs --bench docs/below-the-waist/3d/bench-2/seam-bench.html
node docs/below-the-waist/3d/attack-4/run.mjs --cold < docs/below-the-waist/3d/attack-4/continuation.json
node docs/below-the-waist/3d/attack-4/custody.mjs
node docs/below-the-waist/3d/attack-4/gpu.mjs
```

The ordinary runs are read-only; `--write` refreshes this attack's named JSON receipts. `run.mjs` asserts the positive construction and discriminating controls. The reach and GPU sections report the shipped failures so the composer can reproduce them; an exit of zero for the diagnostic is not a claim those failures are fixed. The original probe still passes 55/55. The new brush has no browser presentation receipt until the composer folds it. Closing that fold establishes this round's ordinary stateful-read answer; it does not certify every production approximation or every 3D tool.

**What the cold session still cannot say as a record from this contract alone.** The page names semantic exchanges but does not yet give a complete executable record language for joining them. That is now a concrete gap, not a request to reconsider Position 9:

| Cold author's task | What is sayable | What still needs an explicit record/example or capability limit |
|---|---|---|
| Reach tool | Named support/revision, seed, radius, intrinsic metric, two domain policies, retained result reused as a clip. | How the record selects the surface-distance capability and its authoritative support/units, requests error/work/witnesses, and wires the returned region into the next construction. Current `reachRegion` closes over G_R and handles the sphere; its `distance` string is not a dispatched operation or a generic host adapter. |
| Coating pickup brush | Sample ordered coating through bindings, mix carry, construct a physical disc, paint, preserve a pending read's inputs. | The operation names and reference/value forms accepted by the executor; how a read names its layer set including the previous painting, filter, snapshot and dependency versions; how authored events and brush state are bound to those inputs and outputs. The current `PICKUP_OPS` offers `sample`, `mix`, `disk`, `over-region`; its sample is hardwired by `pickupStep` to its own planar painting and is synchronous. The new JSON plus reference adapter in this attack supplies a candidate example, not an already agreed substrate. |
| Continue or reuse either result | Results retain their subject and selected inputs; another construction can consume them. | Which retained value/reference is the result, what a waiting computation must keep alive, how the dependency answer re-enters its event, and what constitutes a new history after a provisional choice or dependency edit. A runtime-only closure or “latest” name cannot be the entire saved record. |

Give the cold session the contract that the composer actually intends to support, with these operation and value examples in it. Let whatever that session cannot express count as its finding. The receipts here establish that the read can be made sound; they do not yet establish that a new author can express the reach tool and this brush from the contract alone.
