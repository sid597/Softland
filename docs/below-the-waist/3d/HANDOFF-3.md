# Handoff from session 3 to its successor — the 3D kind, the composer's chair

Written 2026-09-06, afternoon, by the chair that folded the definer's attack 1 into session 2's line (the third chair on that line; `HANDOFF-2.md` is its predecessor's file and stays as the record of the first two folds). Read in this order: this file; `3d-kind-2.html` (live at https://claude.ai/code/artifact/400dcba1-c53c-4eb2-bba8-7978dee838d7, or the `.md` twin); `bench-2/HANDOVER.md` and the bench itself (https://claude.ai/code/artifact/32169816-ae1c-4bbb-ba07-a9171eea419a); `fact-base-2.md`, Part 14 first (the fold of attack 1: the receipts, the dumps, where each finding landed), then Parts 11 to 13; `attack-1.md` (the definer's, 429 lines; folded, keep as the source of its constructions); `HANDOFF-2.md` §3 for the view as it stood before this fold. Then the path directory: `path-kind/path-kind.md` at its ledger (Sid's two rulings of 11:27), `HANDOFF-9.md`, `attack-2.md`. The fence is Sid's: code only under `src/app/client/`, never `src/app/server/env.clj`. `docs/below-the-waist/two-chairs.md` is the workflow both chairs run.

## 1. The problem, in Sid's words

From the starter: "What I am trying to build is every tool, 2D and 3D. The way I want to get there is a waist: an engine and a few hardcoded primitives below it, and everything above defined as data, ECS-style … I want the 3D theory first: what a 3D thing is and what it is made of. I want one model for 2D and 3D in one window that is elegant and plays with differential reactive data and mouse actions over data, so reactivity can be fine-grained; click in, 'you hit this' out." And: "The store that merges is above this kind; what the kind owes it is stable identity and fine-grained diffs." The hardest case is the stroke on a face inside a portal on a page, with zoom, orbit, hover, an occluder, a portal in the portal, a screenshot beside it, the face's construction edited under the ink; since this fold also a mark across a curved wall's chart seam, a field with no face as the occluder, two painted supports merged, and a section drawing beside the portal.

## 2. The phase

Exploration, before any contract. Framing and the whole picture first; details are a fix list, never a reframe (Sid, 2026-09-05). Positions are marked and carry exits; the verdicts are Sid's. Things he can feel before documents about them: every number on the page is a bench readout. Sid carries feedback between chairs by pasting it; fold at full weight for counterexamples and definitional sharpenings, at none for rankings. He ruled on nothing in this fold; his two rulings of 11:27 on the path ledger are carried verbatim into Position 9 and not re-asked. Rankings from this chair: none.

## 3. Where the problem-solution space stands (all POSITION unless quoted)

1. **Four nouns.** A space is the entity (planar or spatial, with a unit); things are placements of definitions; a view is a session's camera; a portal is a thing in either kind of space showing a view of another. A portal's surface is a lease, derived, worn visibly.
2. **The seam is a pair of maps, crossed three ways** (portal, attachment, derivation); agreement by a shared camera covers where, not what; what is checked apart on each side. The derivation crossing now has its pointer-down half spelled: a drawing's curve returns its identity, parameter and source relation, and the inverse edit is a saved reaction, because provenance does not select which cause to change (attack 1's section drawing; on the bench as D).
3. **The general 3D object** is a placed shape with a material, stored as its source made it, under one question protocol; each representation declares its answers (a result, candidates, unsupported, needs a policy). The runner for a field is chosen by what the field supplies: a distance bound → sphere tracing; a polynomial along the ray → root isolation with contacts and unresolved intervals; a sampled grid → interpolation and an isovalue (attack 1; the handoff before this one had said "a sphere-trace on the CPU", which was wrong for a field that is not a distance).
4. **Ink on a face is a planar surface** attached by a saved rule; the attachment is a noun of its own: content, its **authored domain (the content's own coordinates, unwrapped on a wrapping host, never the renderer's chart)**, the support and the revision it was defined against, an anchor rule, a clause per lineage (on split: clip | detach; on merge: keep every binding, compose overlaps by a saved order or needs-policy; on reparameterize: compose with the map; on deform: the anchor rule), what it reads as, whose it is.
5. **The hit is an address with two halves**: the durable thing addressed and the location found in this evaluation (representation-specific; candidates where several: a self-crossing, a wrapping host); the location need not be a named part (a field: definition, boundary rule, local point, active leaf). Hover is a value of (pointer, data, views) whose dependency is anything that can enter its ray; pending is an output (an unresolved interval before the nearest surface makes the winner behind it not definite; the bench prints it).
6. **Two cameras compose**; page zoom magnifies with the framing kept; the region's camera is per view; travel re-roots on a declared reaction.
7. **The axis**: per edit · per view · per frame · per time · derived pictures on their own inputs; per scale is the planar case of per view; a support's chart is a per-edit value whose change moves no ink. Changes have several grains: the authored patch is one row, the evaluated effect is whatever it honestly is, published coherently, with the correspondence naming what continues. The shadow's dirty role is set by any scene change and nothing else (CHECKED renderer.cljs:1076-1139).
8. **One waist, two floors of libraries**; three requests: evaluate, render, query. What must be code, said once (session 1's nine) plus attack 1's four: the executor's spatial door (a saved program's boundary events in the same visibility and the same walker), the root isolator, surface location on a periodic, trimmed, curved support with its metric (today's inverse is the affine plane only, CHECKED on_plane.cljc:72-95), the query as an observation service. "A few primitives" is a small calling surface over machinery that may be large and bought.
9. **The evaluator** (Position 9): a node vocabulary as data with an executor, code-as-data as the escape hatch; one language, several runners. Sid ruled the path kind's executor on 2026-09-06 11:27 ("executor yeah", records over a vocabulary of capabilities, an ordering rule and compiled expressions), recorded on the page and not re-asked; the 3D half, constructions as the kind's input and their language, stays his and stays asked once.
10. **A derived part's identity** (Position 10, rewritten this fold): what its construction returns, a name and a **correspondence** (pieces of from/to domain with a valid subset, a relation, forward and backward capabilities, evidence, an error; plus what was removed, what has no predecessor, what is unresolved), of which the lineage word is the summary and the frame the affine case; where the ink belongs is the attachment's rule; a merge keeps every binding. Three relations hid under one word: operation provenance (the kernel's history), correspondence between evaluations (the construction's), attachment preservation (the rule's); "OCAF" supplies the first only. On the bench: the post's wall re-charted (same points, the ink's bytes and world position unchanged, the metric diag(4, 1) keeping the nib round), the post widened (a declared deformation; the rule decides), the top merged with two records (two restrictions, a strip with no predecessor, the overlap composed by order or needs-policy).
11. **A field's hit is its definition and a local point** (Position 11, new): the extent tool's saved binding selects the parameter, not the hit; root isolation by the derivative chain on the CPU and per fragment on the GPU with the field's own depth; a hollow body is a Boolean of two leaves over the same events; a work limit returns unresolved.
12. **Today's code** (unchanged since `4b8e986`; `git log 4b8e986..HEAD -- src/app/client/` is empty): the region row is a portal with the world inline; pick is meshes only; any static edit is a full rebuild; `ray->placement-plane` intersects local z = 0 only; the mesh kind dispatch is closed (component.cljc:386-392).

**The decision that remains:** Position 9's 3D half, Sid's.

**The exact question that remains**, sharpened by attack 1 and held with a position: what must evaluation return before an attachment may continue: (a) a materialised mapping for every preserved domain, complete before publishing, or (b) a revision-bound correspondence value answering demanded forward and inverse locations, with explicit unresolved portions, completed on demand. This chair: (b) is the exchange's shape and (a) is (b) with every portion resolved before publishing; the difference is the executor's scheduling, with pending as an output either way. Closes by the definer's receipt: one authored mark through a chart change and then a merge of two retained records, in one sequence on one support (the bench has each half; not the two together). **The next construction:** the general curved host, a surface-distance region on a doubly curved face; then the pickup brush's replay on the post.

## 4. What is on disk (this chair)

| File | What it is |
|---|---|
| `3d-kind-2.html`, `3d-kind-2.md` | The page and its twin; republish with `url` set to keep the link. Six moments, Position 11, the Correspondence and Location nouns, the exchange, the closing section: all from this fold. |
| `twin-2.py` | `python3 twin-2.py 3d-kind-2.html 3d-kind-2.md "<label>"`. |
| `fact-base-2.md` | Parts 1 to 10 the anchors at `4b8e986`; 11 to 13 the earlier folds; **14 this fold**: the definer's six checks reproduced in Node through the bench's functions, the dumps behind every new number, the landing table, the scribe's deviations. |
| `bench-2/seam-bench.html`, `bench-2/HANDOVER.md` | The Seam Bench with the post, the field, the second record and the drawing; ~144 KB, two programs; the deep links, the measured rows, the fix list. |
| `HANDOFF-3.md`, `STARTER-3.md` | This file, and the starter Sid pastes into the next chair. `HANDOFF-2.md` and `STARTER-2.md` stay as the record. |

Not this chair's, do not edit: `3d-kind.html`, `fact-base-1.md`, `bench-0/`, `HANDOFF-1.md`, `STARTER-1.md`, `crossread-a0f20b1.md` (session 1's line, closed); `attack-1.md` (the definer's); `3d-kind-working-model.md`, `3d-object-space-and-query.md`, `HANDOFF-object-space-and-query.md` (the other lane; untracked in the tree at this writing); `feedback-*.md`, `review-6f62711.md`; anything under `path-kind/`.

## 5. Folded (this chair)

The definer's attack 1, at full weight: its constructions on the bench where its record allowed (the post at ×10, the field in metres, the second record with the definer's composition arithmetic, the drawing), its definitions on the page, its notation translated. Its six arithmetic checks were reproduced in Node through the functions the bench then embedded, before the bench changed (Part 14); two of this chair's first drafts were wrong there (the capsule's closed form; a root at a bounds-clipped endpoint and the work limit at a derivative level) and were fixed before the bench took them. The bench's nine pre-existing deep links print byte-identical addresses after the patch, checked before and after. The page's earlier moments are unchanged.

## 6. Still owed

1. **Whatever Sid pastes next**: redlines, rankings (no weight; their counterexamples full), the definer's attacks (`attack-N.md`). Fold in place; regenerate the twin; republish both artifacts once at the end with `url`; commit exact paths.
2. **The general curved host**: a surface-distance region on a doubly curved face (membership, bounds, chart pieces, unresolved portions); the post is developable and does not reach it. The definer constructs, this chair folds.
3. **The definer's closing receipt in one sequence**: the same authored mark through a chart change and then a merge of two retained records on one support; the bench has each half.
4. **The pickup brush's replay on the post** (the painting variant on a curved host: one logical painting across chart fragments, the carry not resetting at the cut).
5. **Lift bench 9's filler onto the face**: the coverage lane under the projection, replacing the per-pixel distance.
6. **A parity read for the field**: a depth read at the cursor against the walker's t (both run the same isolation today; no receipt that they agree).
7. **Travel**, **query modes** with one translucent thing, **a shadow**, **a portal from L back into S** with the bound measured: the earlier fix list, unchanged.
8. **At Sid's word only, boots on the ground**: split the region row into space, view, portal, lease; move segment counts and required normals out of the value; placed ink in the pointer map; the aspect declared by the portal; one grammar for the question protocol; the executor's spatial door.

## 7. Guards, from this chair (the earlier chairs' guards in `HANDOFF-2.md` §7 all stand)

- The scribe road works for a bench integration of this size when the parent hands it pure functions verbatim plus described placements: one Opus scribe applied ~630 lines with two looks per failure state, and the parent never held the script. The parent read three screenshots itself, one look each, and says so. Cost: the scribe ran about 280K tokens in its own context; the parent's stayed at the patch text.
- Reproduce the definer's arithmetic through the bench's own functions in Node before the bench changes; the first draft of a closed form is wrong often enough that this is where the error is cheapest (two this fold).
- A scale factor on a construction is stated once and carried everywhere: the post is the definer's ×10 in every length and the same in every ratio; the field is in metres and its ratios are the definer's. Never mix two scale factors in one readout.
- A probe that does not exercise the claim is not a receipt: at 15 mm both nibs said inside; the scribe added `pd` and found the flip at 25 mm. Pick the point where membership must change.
- The unresolved case must be an output of the walker, not an aside: as first written the bench returned a definite hit at the bounds box while admitting the interval unresolved; the scribe caught it against the page's stated rule. Write the rule on the page first and let the bench be checked against it.
- Sid's rulings are quoted verbatim with the time from the ledger where they were made; a ruling on one kind is carried into another as recorded, never widened.
- This fold cost 330K in the chair's context and about a third was avoidable: the functions were written twice (a Node module, then inside the bench patch), the page was drafted and then rewritten with the numbers, the dumps passed through the chair four times (the receipt, the fact base, the handover, the page). Next fold: the code as one file on disk the scribe embeds; the page once, after the dumps; the bench scribe writes the fact base's dump table and the handover's measured rows itself and returns only what the page quotes; the shader is the scribe's from the chair's definitions. If the fold is bigger than one construction, split at the zone boundary and land in a second session (`two-chairs.md`, the guards).
