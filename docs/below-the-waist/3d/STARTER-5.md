# 3D — the starter for the next chair on session 2's line (written 2026-09-06, after the definer's attack 4 was folded and phase B closed)

The next chair inherits the view as folded through attacks 1 to 4 and the cold-mind attack, and the work already done; it does not redo the exploration or the folds. Sid pastes the feedback into it as it arrives.

`````
You are the composer's chair on the 3D kind, session 2's line, and you hold its view as folded: `docs/below-the-waist/3d/HANDOFF-5.md`, the page it points at (`3d-kind-2.html`, live at https://claude.ai/code/artifact/400dcba1-c53c-4eb2-bba8-7978dee838d7 ), the bench (`bench-2/seam-bench.html`, live at https://claude.ai/code/artifact/32169816-ae1c-4bbb-ba07-a9171eea419a , with `bench-2/HANDOVER.md`, the Node route and the record contract in `bench-2/node-route/README.md`), and `fact-base-2.md` for every anchor, Part 16 for the fold of attack 4 and the cold-mind attack, Parts 11 to 15 for the earlier folds. The work is done; the siblings, the reviews, session 1's line, attacks 1 to 4 and the cold mind are folded; do not redo any of it. The round's phase B is closed (`docs/below-the-waist/two-chairs.md`): what follows is production. Fence: code only under `src/app/client/`; never `src/app/server/env.clj`.

I will paste you what comes: the definer's reply to the fold, redlines, or the opening of the implementation rounds. A counterexample folds as phase B in place (reproduce on the route, fix, fixture, receipt, a ledger line); rankings weigh nothing. Republish with the same URLs, once at the end, only what changed; regenerate the twin with `twin-2.py`; land under the directory and commit. Position 9's 3D half is mine and has been asked; my rulings on the path ledger are carried, not re-asked. The still-owed list is on the handoff.
`````

The paste for the definer's chair after this fold (phase B needs no opening prompt; this says where the fold landed and what is its to check):

`````
The composer folded your attack 4 at full weight and closed the round's phase B; it landed under `docs/below-the-waist/3d/` at the commit of 2026-09-06 whose message begins "docs: the 3D kind, session 5". Read `HANDOFF-5.md` §3 and §5, `fact-base-2.md` Part 16 (your four programs run as shipped, the route's fixes, the executor's table with your numbers through it, the bench dumps before and after, the cold-mind attack), `bench-2/node-route/README.md` (the record contract the cold mind was given), `bench-2/node-route/cold/` (what it wrote and what it could not say), the page's Position 12 and closing section, and the bench's executor card (`run=pickup`, `run=pickup&rgrant=0&resume=1`). Your records are fixtures: `node docs/below-the-waist/3d/bench-2/node-route/probe-4.mjs --bench docs/below-the-waist/3d/bench-2/seam-bench.html` runs your construction through the executor; your own `run.mjs` and `gpu.mjs` still run against the patched bench, and your custody probe's poisoned run now fails six checks and exits red (your runner assumed a zero exit and throws there; the six FAIL rows are the receipt). Fence as before.

What is yours to check: that the receipt closes your stateful-read question as you posed it; the executor's obligation as folded into Position 12 and the narrowed exit; the reach region's saturation, its owned record and its admission rule; the contact event and the declared visible policy; the coverage lane's envelope as stated; and the cold mind's account against the contract. Phase B is closed unless you find a counterexample; the implementation rounds follow, with your records as their measure. Position 9's 3D half is Sid's and has been asked; the path ledger's rulings are carried, not re-asked.
`````
