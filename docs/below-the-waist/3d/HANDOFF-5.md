# Handoff from session 5 to its successor — the 3D kind, the composer's chair, the round's phase B closed

Written 2026-09-06, evening, by the chair that folded the definer's attack 4 into session 2's line (the fifth chair on that line; the fold landed at the commit of 2026-09-06 whose message begins "docs: the 3D kind, session 5"; `HANDOFF-4.md` is its predecessor's file and stays as the record of the fold of attacks 2 and 3). Read in this order: this file; `3d-kind-2.html` (live at https://claude.ai/code/artifact/400dcba1-c53c-4eb2-bba8-7978dee838d7, or the `.md` twin); `bench-2/node-route/README.md` (the record contract, the Node route's commands) and `bench-2/HANDOVER.md`, then the bench itself (https://claude.ai/code/artifact/32169816-ae1c-4bbb-ba07-a9171eea419a; the executor's card at the bottom of the panel, `run=pickup`); `fact-base-2.md` Part 16 first (this fold: the definer's programs run as shipped, the route's fixes, the executor's table, the bench dumps before and after, the cold-mind attack, where each finding landed), then Parts 15 and 11 to 14; `attack-4.md` (the definer's, folded; the source of its constructions); `bench-2/node-route/cold/` (the cold-mind attack: its starter, its records, its account). Then the path directory as before. The fence is Sid's: code only under `src/app/client/`, never `src/app/server/env.clj`. `docs/below-the-waist/two-chairs.md` is the workflow.

## 1. The problem, in Sid's words

Unchanged from `HANDOFF-4.md` §1. Since this fold the hardest case also carries: a brush reading the composed coating on a doubly curved host through a binding withheld and then granted, and a tool said as a record and run over the bench's capabilities by an executor that suspends into bytes when a read is pending.

## 2. The phase

**Phase B closed by this fold.** The round's stated question (two-chairs: make a new tool, change its behaviour through data, reuse its results, all three running on the bench) runs on the bench through the executor's card, and the cold-mind attack has run (§5). What follows is production, the implementation rounds (two-chairs: the prototype is the spec, no contract first, tests built as you build, the documentation hierarchy with the code); the definer keeps its chair against the implementation, its records the measure. Sid ruled on nothing in this fold; his rulings of 11:27 (the executor) and 11:51 (one compositor with paint and sample) on the path ledger are carried verbatim. Position 9's 3D half is his, asked once.

## 3. Where the problem-solution space stands (all POSITION unless quoted)

Items 1 to 13 and 15 of `HANDOFF-4.md` §3 stand. Amended or new:

14. **A brush's read is the compositor's sample through a binding** (Position 12), now with its receipt and the executor's obligation: the resumed transition consumes the event, the previous state, the program and the read dependencies it suspended with; a changed dependency starts the corresponding new computation and an old answer cannot commit into it; pending yields no next painting-step state while the executor keeps a waiting continuation and accepts more authored events. The exit narrowed (attack 4): a retained provisional read makes the selected history reproducible, not the selection independent of scheduling; either the choice is an explicit retained input with two histories, or the record's rule for it is race-independent.
13. **A surface region is a retained result**, now owning the record it was built from (the tool's later edit does not reach in; the region reconstructs from what it reports), obeying its radius to the whole host (≥ πR is the whole sphere), refusing a negative radius, with membership the same computation as the printed distance.
11. **The field's hit** keeps a contact of zero area as a boundary event for a geometric-boundary answer; the visible-nearest walk excludes it and says so; the shader serves visible coverage only, declared. The parity residual stays a measured failure (0.101 mm), the Newton refinement an excluded cause.
7. **The coverage lane** (the shader's swept nib) is a scan with a stated envelope, m ≤ ŝ ≤ m + L/64 in exact arithmetic, its sign not membership; the walker's enclosure is the certificate at the cursor; the lane meets no declared consumer demand yet.
16. **The executor over the bench's records** (new on this bench, not a new piece: bench 9's, Sid's 11:27): a record's program is named steps over bindings, over a capability table of the bench's own functions; a read may return pending, the run suspends into a continuation of bytes with no next state, resumes under a grant to the same bytes, and refuses an answer that does not belong to its request. The contract is `node-route/README.md`.

**The decision that remains:** Position 9's 3D half, Sid's, asked once.

**The exact question** the last fold carried is closed by its receipt (Position 12). No exact question is held open by this chair; what the cold mind could not say (§5) is the finding the next author starts from.

## 4. What is on disk (this chair)

| File | What it is |
|---|---|
| `3d-kind-2.html`, `3d-kind-2.md` | The page and its twin, amended as a ledger (L1 to L9 of Part 16): Position 12's receipt, obligation and narrowed exit; the Surface region noun; Position 11's contact and residual; the fix list's coverage lane; the executor's transfer; the closing section; the fold record; Landed. Republish with `url` set. |
| `bench-2/seam-bench.html`, `bench-2/HANDOVER.md` | The Seam Bench with the executor's card, the pickup brush's painting drawn on G, the reach region fixed, the contact event and the declared visible policy, the kP line's envelope clause; the handover's new section and rows. |
| `bench-2/node-route/` | `executor.js`, `records.js` (embedded in the bench verbatim), `load.mjs` (the shared loader with custody), `probe-4.mjs` (46 checks; `--record`, `--then`, `--cold`), `probe-2-3.mjs` (on the shared loader, 56 checks with custody), `README.md` (the contract), `dumps-4/` (the bench's dumps before and after), `cold/` (the cold-mind attack). |
| `fact-base-2.md` | Part 16, this fold. |
| `HANDOFF-5.md`, `STARTER-5.md` | This file, and the starter Sid pastes into the next chair. |

Not this chair's, do not edit: everything listed in `HANDOFF-4.md` §4 plus `attack-4.md`, `attack-4/` (the definer's), `cold/` except its starter (the cold mind's).

## 5. Folded (this chair)

The definer's attack 4 at full weight: its four programs run as shipped and matched (Part 16); its findings reproduced on its route, fixed in the sidecars, checked in Node, then taken by the bench through a scribe and checked headless; its records the fixtures (`records.js` carries its brush in the executor's grammar; `probe-4.mjs` its numbers); the page as a ledger. The cold-mind attack: a fresh Opus session given only `node-route/README.md` (its paste in `cold/STARTER-cold.md`) wrote the reach tool, a dab through it as a clip, and the pickup brush; the brush gave the definer's bytes and carry at its first run, the budget-0 run pending at the first dab with the granted run's request hash; its account (`cold/COLD-ACCOUNT.md`) lists thirteen things it could not say, ten of them now said by the contract or the executor (Part 16), three kept as production work (§6.5). Its records are fixtures (`probe-4.mjs` F).

## 6. Still owed (production, unless Sid says otherwise)

1. **Whatever Sid pastes next**: redlines, the definer's reply to this fold (`attack-5.md` if it comes). Fold in place as phase B.
2. **The general smooth or trimmed host**: a finite surface-distance operation beyond the sphere's closed form, with an error argument or a declared mesh authority; the record language selects it by name (`distance`), refused today.
3. **The coverage lane's declared demand**: the shader's swept nib as a coverage lane with a certified envelope rather than a scan; bench 9's filler on the face.
4. **The parity residual**: 0.10 mm after the shifted origin, cause unassigned; a full image of boundaries untested.
5. **The contract's gaps the cold mind found** (§5 and `cold/`): a region as a clip on a read, and region algebra (intersection, difference, a region from a curve span); a painting declared to cover a region rather than an authored patch (the cold mind authored a 640 × 250 mm patch by hand so the clip could be seen); a painting named mid-event other than a step's own output; and a root for the retained marks beyond `host` (their curves, their charts) when a second host exists.
6. **Travel**, **query modes** with one translucent thing, **a shadow**, **a portal from L back into S** with the bound measured; the antipode hovered rather than printed: unchanged.
7. **At Sid's word only, boots on the ground**: unchanged from `HANDOFF-4.md` §6.7, plus the executor's card as the shape of the client's REPL.

## 7. Guards, from this chair (the earlier chairs' guards all stand)

- Run the definer's programs first, as shipped, against the committed bench; every finding reproduced on its route before a sidecar changes (four programs, under five minutes).
- Custody is part of the suite: a probe that checks a bench must execute the bench's bytes, and every sidecar unit must be found in the embed; a `--bench` flag that then loads the sidecars over the bench proves nothing about the bench.
- A retained result owns a copy of what it was built from; a closure over the caller's object is an alias, not a subject.
- A saturating quantity (an angular radius, a cap) is fixtured at and beyond its saturation, not only inside it.
- The cold-mind attack's contract must not contain the answer records; the worked example is a third record, and the target is given as numbers.
- The bench's bytes are the scribe's; the chair authors patches as exact text with anchors and reads the bench only to write them, and says when it did.
