# The cold-mind attack on the record contract (2026-09-06)

The paste given to a fresh session, verbatim; the two-chairs workflow's second attacker (a cold mind given only the contract, asked to write a tool as a record; whatever it cannot say is a finding). The session had no access to the bench, the attacks, the fact base or the executor's source: one file, one command.

`````
Fence: read exactly one file, `docs/below-the-waist/3d/bench-2/node-route/README.md`, from the repository root `/mnt/data/projects/Softland`; open nothing else in the repository (no other file under `docs/`, `src/` or `node-route/`, no git history). You may run one command, in the forms the README gives: `node docs/below-the-waist/3d/bench-2/node-route/probe-4.mjs --record <your file> [--then <your file>] [--budget <n>]`, as often as you like; read its output only. Write your files under `docs/below-the-waist/3d/bench-2/node-route/cold/`.

I am building a kind below the waist where a tool is a record: data above, an executor and a few capabilities below. The README is the contract of that executor on one bench. I want to know whether someone who did not build it can say a tool in it. Write two tools as records, from the contract alone:

1. The reach tool on the sphere: a region of surface distance 150 mm about the seed at longitude 180°, latitude 60°, retained as a result another record can consume as a clip; and, if the contract lets you say it, a second record that paints one dab through that clip.

2. The coating-reading pickup brush: four authored dabs on the arc kA at parameters .65, .70, .41 and .65, in that order, at the merged revision G@2 with the order kA under kB. Each dab reads the composed coating at its point with the brush's own previous painting above the coating, mixes that sample into the carried pigment by .25 (the carry starts as opaque red, (1, 0, 0, 1)), constructs a disc of surface distance of radius 8.75 mm, and paints the carry into its painting at opacity .5. The painting is 64 × 32 texels over u ∈ [588.3185307179587, 668.3185307179587), v ∈ [194, 224) mm on S0, nearest, initially transparent. When every binding is granted, the composer's run of this brush ends with the carry (0.6781253814697266, 0, 0.16514968872070312, 0.8432750701904297) and the painting bytes 30cb13f2147784bdae44b4aa303d412c8980f00c09435c0983521cd291d75579; run yours with no work granted too, and say what it does.

Work the two through the contract as written, run what you write, and end on what you could not say from the contract alone: where you had to guess, where the words in the README did not settle a choice, what a record cannot express that you needed, and what would have settled it. Land the records and that account as files under `cold/`.
`````
