# The Seam Bench (bench 2) — handover, session 2 and its successors, 2026-09-06 (the definer's attack 1 folded in the afternoon)

## Where it is

- Source: `docs/below-the-waist/3d/bench-2/seam-bench.html` (one file, ~144 KB, WebGL2 with two programs + a 2D canvas, no build; only a Google Fonts link, with fallbacks).
- Live: https://claude.ai/code/artifact/32169816-ae1c-4bbb-ba07-a9171eea419a . To republish from another session: `Artifact` action `read` with that URL first, then publish with `url` set and this file's sibling `seam-bench.html` as `file_path`.
- Run locally: open the file in any WebGL2 browser (`file://` is fine). Headless check (proven, SwiftShader): `google-chrome --headless=new --no-sandbox --use-angle=swiftshader --use-gl=angle --enable-unsafe-swiftshader --hide-scrollbars --window-size=1500,1100 --virtual-time-budget=6000 --screenshot=out.png "file://…/seam-bench.html#aim=k"`; `--dump-dom` instead of `--screenshot` prints the address panel (`<pre id="addr">`), the diff (`#diff`), the rate lines (`#rEdit #rView #rScale #rFrame #rHover`), the derived line under the record (`#derived`) and the WebGL status (`#glstat`).
- Deep links (the URL hash): `zoom=<page zoom>` · `yaw=` `pitch=` `dist=` (view V) · `occ=<occluder x>` · `preset=0` (no preset stroke) · `shot=1` (paste the screenshot at load) · `px=&py=` (a virtual pointer in page pixels) · `aim=k|T|Q|I|floor|P|Pm|F|D|k2|strip` (put the pointer on the stroke, the face portal, the occluder, the screenshot, the groove floor; since the fold of attack 1: the post's crossing, the probe beside it, the field, the drawing's circle, the second record's overlap with k, the strip the merge leaves without a predecessor) · **the box's construction:** `w=<rect width in m>` · `cut=1` (the boolean subtract on) · `anchor=origin|normalized|edge` (the attachment rule) · `onsplit=clip|detach` · **the post:** `post=0` (no post) · `chart=S0|S1` (its wall's chart: a = u, or a = (u − 100)/2) · `pr=<radius m>` · `panchor=origin|normalized` (what the ink keeps under a radius change) · `seam=unwrapped|modulo` (the source reading, or each knot reduced modulo C: the counterexample) · `nib=surface|naive` (the nib measured on the surface, or chart units read as mm) · `pd=<mm>` (the probe's distance along u from the crossing, default 15) · **the field:** `field=1` · `fx= fy= fz=` (its centre) · `fa=<a>` (the extent edit) · `hollow=1` (the inner leaf subtracted) · `fwork=<n>` (the CPU work limit; small values return unresolved) · **the second record:** `records=2` (cuts the top and authors k2 on the right part) · `merge=1` (then undoes the cut, so the lineage reads merged from 2) · `left=whole|trimmed` (k kept whole under clip, or destructively trimmed at cut time) · `order=lr|rl|none` (the composition of the overlap; none = needs-policy) · **the drawing:** `section=<h m>` (D below the portal, the section of S at y = h).

## What it is

The hardest case of the 3D starter as a thing to feel, the seam walker made visible, and, since the fold, the derived-part identity question as a thing to feel. The page (a planar space, px) holds a portal R onto a spatial space S (metres, Y up) through view V. In S: a solid B whose shape is what a saved construction CB evaluates to (a rectangle R0 on the ground, extruded to E, optionally cut by a slab C across the top from back to front); a sphere Q that can pass in front; a directional light; a planar surface Π attached to B's face `E.top` (millimetres, 1000 per metre) carrying a pen stroke k (samples with a width each, the swept round nib), by a saved **attachment rule** that says what the ink preserves when the face changes; and a portal T attached to B's face `E.front` showing a planar land L (a circle, a rect, a label) through view VL. Beside R the page can hold an image I, a screenshot of R.

Since the fold of the definer's attack 1 (the afternoon of the same day), S also holds: a post P, a cylinder from a saved construction CP (r 0.2 m, h 0.9 m, at (1.1, −0.2)) whose wall carries a mark kP authored in its own unwrapped domain across the wall's chart seam (the definer's four knots with pressure, at ten times their size; width 10 + 30·p² mm), with a saved chart parameter that re-describes the same wall (S1: a = (u − 100)/2) and two wrong readings for contrast; a field F, the definer's superellipsoid as a saved formula with parameters (a 0.2, b 0.1, c 0.1 m), optionally hollow, with an extent tool whose binding selects a, off by default; a second content record k2 on the top's right part while the top is split, so that undoing the cut merges two independently painted supports; and a section drawing D below the portal, live, with an analytic circle that carries its source relation. Every one of the pre-existing deep links prints a byte-identical address after the fold.

The panel is laid out as the waist. Top: the address the walker returns for the pointer, level by level, now with each face's **lineage** against the previous evaluation, and the diff against the last answer. Middle: what ran and at which rate (per edit, per view, per scale, per frame, hover). Bottom: the record, what is data above the waist, live: spaces, things, views, portals, the construction, the attachment rule, the stroke's sample count and revision; and one line under it, marked derived, saying what the construction evaluated to (blocks, faces, parts) and whether the attachment resolved. No triangles, no pixels, no device tolerance, no zoom in the record.

## The seam walker (what the address means)

`walk(pagePoint)` in the script. Page pixel → page world (the page's pan and zoom are the page's view) → is it inside I? then the answer is a pixel of I and nothing else, no seam. Inside R? → portal-local → the used viewport's pixel → `rayFromRegionPoint` with the same matrices the renderer uses (`cameraOf` serves both) → the nearest of `hitBlocks` (a slab test per evaluated block, each face tagged with its construction face and part) and `hitSphere` → on `E.top`: the attachment resolves (`resolveAttachment`: rule + the face's frame from the evaluation → origin, scale, offset) → `attachUV` → `classifyStroke` (the exact swept nib: the uneven-capsule signed distance per segment, min over segments) → the address ends at k with segment, t, arc length, width, and a lower bound on the distance to the edge; on `E.front`: T's normalized attachment → `hitLand` in L → the address ends at c, r or L's background; on `C.floor` (a face the cut generated): no attachment resolves, the address says so. Every level carries its coordinates. The footprint line on Π projects one Π millimetre and measures it in page pixels: the filler's tolerance on the face, per view, under the attachment rule.

Hover is `hover(reason)` = the walk over `(state.pointer, data, views)`, re-run on pointer move and whenever data or a view changes with the pointer held (`state.pendingHover = 'data' | 'view'`). An edit of the construction or of the attachment rule is data: it re-walks the hover under a still pointer. The diff line says `entered`, `left → entered`, or `still`, and names whether the pointer, the data or the view moved.

**What "agree by construction" covers, and what it does not.** `cameraOf` serving both the renderer and the walker guarantees the coordinate relation between the picture and the hit: a world point lands on the same portal pixel in both. It does not make the two agree about *meaning*: the fold found the renderer and the picker agreeing with each other through a wrong aspect (both read it from the lease) and through a wrong nib (both projected onto the centreline). Meaning is defined by the formulas the walker and the shader each implement and is checked apart: the classify is now the closed-form swept nib on both sides, and the framing is the portal's on both sides.

## The evaluator and the attachment (the fold's addition)

`evaluate(construction, previous)` runs per edit of CB and returns convex blocks whose faces name the construction face they belong to (`E.top`, `E.front`, `E.left`…, `C.floor`, `C.wall-`, `C.wall+`) and which part of it, plus a **lineage** per face against the previous evaluation: `unchanged`, `modified (1.00 → 1.40 m wide)`, `split into 2 by C`, `merged from 2 (the cut was undone)`, `generated by C`, `deleted`. The report goes to the per-edit rate line and the address.

`resolveAttachment(rule, face)` turns the rule and the evaluated face's frame into where Π sits: Π mm = ((x, z) − origin) · scale + offset, × 1000.

| rule | what the ink preserves | scale, offset |
|---|---|---|
| `origin` | its distances from the sketch's origin corner | 1, 0 |
| `normalized` | its proportions of the face | authored extents / current extents, 0 |
| `edge` | its distance from the +x edge | 1, (authored width − current width, 0) |

`onSplit: clip` keeps the authored stroke whole in Π and paints it only where a surviving part of `E.top` is under it (the shader runs the Π material on `E.top` parts only; a generated face gets no ink); `onSplit: detach` leaves k in the record on no face and the address says so. The same resolution feeds the shader's uniforms and the walker: one definition of where Π is. The portal T is an attachment too (`normalized`): widen the rectangle and the land stretches with the face; cut, and the notch cuts the picture.

## The post, the field, the second record and the drawing (the fold of attack 1)

**P's wall and its chart.** `postOf(CP)` gives the wall's authored chart S0: u = arc length in mm from the +x generator, counter-clockwise seen from +Y, v = height in mm; `S0(u, v) = (cx + r cos(u/R), v/1000, cz + r sin(u/R))`, C = 2πR. A saved chart parameter {offset o, scale s} describes the same wall by `S1(a, b) = S0(s·a + o, b)`; the transition φ(u, v) = ((u − o)/s, v), the period in a is C/s, the metric is diag(s², 1). `evaluateP(CP, previous)` returns the support and a **correspondence piece** against the previous evaluation: from (revision, wall, all) to (revision, wall, all), the relation (identity | reparameterization with φ, its metric and period | deformation with the generator and the height kept), forward/backward unique, the evidence (same points | declared deformation), error 0. `resolveWall(rule, evaluation)` turns the attachment's rule into the binding: under a chart change the authored-domain map composes with φ and nothing moves; under a radius change `origin` keeps millimetres from the generator (u kept) and `normalized` keeps proportions of the circumference (u scaled by C′/C). `postChart` maps a world point to the chart's (a, b); `postPreimages` lists the chart point's preimages u = s·a + o + k·C inside the authored domain (at most one for a domain narrower than C; several for content that wraps); `classifyMark` classifies an authored point against the mark with the same `capsuleSD` as k's and returns every segment inside as a contributor, so the self-crossing returns one mark with two source locations. The wall's material (class 4 in the main program) does the same per fragment: θ from atan2, the chart's a, the preimage loop over k ∈ {−1, 0, 1}, the capsule in millimetres on the surface; it also draws the chart's 100-unit grid and its cut (a = 0), so a reparameterization is visible as the grid and the cut moving under a mark that does not. The two wrong readings are the same code with different inputs: `seam=modulo` hands the shader and the walker knots with u reduced modulo C (the first segment's +160 mm becomes −1096.6 mm and the mark runs the other way round the post); `nib=naive` hands them φ-mapped knots and classifies in chart units (the mark half as wide along the wall; a point 25 mm along the circumference reads 12.5 and the naive nib puts it inside where the surface nib puts it 5.37 mm outside).

**F, the field.** `S.things.F` is a formula with parameters and what it supplies; no face. `hitField(ray, F, workLimit)` is the CPU reference: the quartic along the ray (`fieldPoly`), its real roots on the bounds interval by the derivative chain (`isolateRealRoots`: f‴ → f″ → f′ → f, every monotone piece bisected; a critical point where f = 0 is a contact, which a sign-change loop misses; a finite work limit returns the unfinished pieces as unresolved, never as empty space), the events of every leaf merged, the Boolean max(f_outer, −f_inner) ≤ 0 decided between consecutive events, the first material interval's start as the hit with its gradient normal and active leaf. The walker keeps an unresolved result in hand: if its unresolved interval starts before the winner's t, the address says the winner behind it is not definite and the leaf is `F:unresolved`. The GPU draws F's bounds box through a second program (`progF`) whose fragment shader runs the same isolation (`quarticRoots`, unrolled) from the eye through the fragment's world point and writes `gl_FragDepth` from the hit, so F occludes and is occluded like the blocks under the one depth test. No parity read exists yet between the two (fix list).

**k2 and the merge.** `records=2` cuts the top and authors k2 (a preset, 61 samples) in its own planar space Π2 whose origin is the right part's corner (world x 0.30, Π 800 mm), with its own rule. `merge=1` undoes the cut against that evaluation, so `evaluate` reports `merged from 2` and `mergeCorrespondence(previous, evaluated)` returns two restrictions (part 1 → x ∈ [0, 620] mm, part 2 → x ∈ [800, 1000] mm, evidence restricted old support) and the strip with no predecessor (620, 800). Both records bind to the one face: Ftop as before, Fright restricted to x ≥ 800. The shader evaluates both strokes on class-3 fragments (`uStroke2` on its own unit) and composes the overlap by `composition.order` (premultiplied source-over) or hatches it when the order is null; the walker classifies both (`classifyStroke2`) and prints the contributors and `composeAt`'s result, or needs-policy. `left=trimmed` clips k to the old parts' ranges (a destructively trimmed copy), so the strip stays blank after the merge; the default keeps k whole under clip, so it reappears there.

**D, the drawing.** `section=<h>` puts D below the portal, in page world: `sectionOf(h)` returns a rectangle per block the plane crosses (source: the block and its faces), the post's analytic circle (source: P wall ← CP.r), and the field's level curve lowered by 96 samples (marked not analytic). `hitD` returns the nearest curve with its parameter, its distance off and its source relation; for the circle the address adds the dimension's saved reaction (sets CP.r) and says that dragging the loop itself has no unique inverse.

## Measured on the bench (default view unless stated; DPR 1, headless SwiftShader, 2026-09-06 after the fold; the attack-1 rows after the patch of the afternoon, the earlier rows re-checked byte-identical then)

- `aim=k`: `[page (282.4, 179.7) px · R portal-local (222.4, 109.7), surface 420×330 used of a 448×384 lease, framing 1.273 · S: t = 3.232 m at world (0.00, 1.00, −0.20) · B solid, construction CB revision 0, face E.top, lineage from E · Ftop attachment resolved, rule origin, authored on 1000×1000 mm, face now 1000×1000 · Π (500.0, 302.0) mm, footprint 0.09 px per Π mm · k inside: segment 34, t 1.00, s 673.3 mm, width 30.0 mm, at least 15.00 mm from the edge]` — six levels, `entered k`.
- `aim=k&zoom=2.4`: the same page point, the same portal-local (222.4, 109.7), viewport 1008×792 in a 1024×832 lease, the same t, the same Π (500.0, 302.0), footprint 0.22 px per Π mm. The lease changed; the framing, the view and the value did not (the fold's repair: before it, the normalized portal x of one world point moved from 0.5324 to 0.5307 between the two leases).
- `aim=k&occ=0.36`: the same pointer → `Q at t = 2.260 m`, `entered Q`; four levels.
- `aim=k&w=1.4&anchor=origin`: `E.top modified (1.00 → 1.40 m wide)`; Π (500.0, 302.0) at world x 0.00: the ink kept its distances from the origin corner; the new strip of face is blank.
- `aim=k&w=1.4&anchor=normalized`: the same Π (500.0, 302.0) is now at world x 0.20; footprint 0.14 px per Π mm (a Π millimetre is 1.4 world millimetres along x): the ink stretched with the face.
- `aim=k&w=1.4&anchor=edge`: the same Π (500.0, 302.0) at world x 0.40: the ink slid with the +x edge.
- `aim=k&cut=1`: `E.top part 1 of 2, lineage split into 2 by C · attachment resolved on 2 parts (clip)`; k inside at the same Π; the derived line: `CB → 3 blocks, faces E.top×2 E.front E.bottom E.left E.right E.back C.floor C.wall- C.wall+`.
- `aim=k&cut=1&onsplit=detach`: `attachment detached: split into 2 by C and the rule says detach · k stays in the record, on no face`; leaf `B:E.top (ink detached)`.
- `aim=floor&cut=1&yaw=0.05&pitch=0.62`: `t = 3.175 m at world (0.21, 0.72, 0.00) · face C.floor, lineage generated by C · no attachment here: a generated face inherits no ink (the rule)`.
- `aim=T&cut=1&w=1.3`: `E.front modified (1.00 → 1.30 m wide) · T attachment normalized · face (0.500, 0.500) → L (128.0, 128.0) px → c, disc`; six levels, the walk continued into another land through a notched, widened face.
- `shot=1&aim=I`: `I pixel of the used viewport · no seam`; two levels (the screenshot copies the used viewport, not the lease).
- `aim=P`: `P solid, construction CP revision 0, face wall (r 0.200 m, C 1256.6 mm), chart S0: a = u, period 1256.6 mm · Fwall resolved, rule origin, authored unwrapped u ∈ [1136.6, 1376.6] × v ∈ [280, 520] mm · PW chart (a 0.0, b 400.0) → preimage u 1256.6 mm (k 1), footprint 0.07 px per mm on the surface · kP inside: segment 0, t 0.50, width 17.50 mm · segment 2, t 0.50, width 24.70 mm (2 source locations, one mark), at least 12.95 mm from the edge`, leaf kP (the definer's 1.75 and 2.47 at ×10).
- `aim=P&chart=S1`: the same world point and the same kP line; `chart S1: a = (u − 100)/2, period 628.3 mm in a, metric diag(4, 1) · correspondence from revision 0: same points, unique/unique` · `chart (a 578.3, b 400.0) → preimage u 1256.6 mm (k 0)`; derived: `wall reparameterized … the surface is the same (S1∘φ = S0)`.
- `aim=Pm` and `aim=Pm&chart=S1&nib=naive`: at 15 mm along u both say inside and disagree about the distance (1.96 vs 6.09 mm from the edge); `pd=25`: the surface nib `outside by 5.37 mm`, the naive nib `inside … at least 1.52 mm from the edge`, `a point 25 mm along the circumference reads 12.5`.
- `aim=P&seam=modulo`: `kP outside by 98.54 mm (nearest: segment 1, t 0.00, width 29.20 mm) · note: first segment A→B: unwrapped +160.0 mm; reduced modulo C: −1096.6 mm`.
- `aim=P&pr=0.22&panchor=origin`: `correspondence from revision 0: declared deformation, unique/unique (the generator and the height kept; C changed)`; the crossing keeps its 1256.6 mm from the generator, which is 327° round the bigger post, so the old page point finds `no preimage in the authored domain` and `yaw=2.04` finds it with the same two contributors. `panchor=normalized`: `authored unwrapped u ∈ [1250.3, 1514.3]`, `chart (a 0.0, b 400.0) → preimage u 1382.3 mm (k 1)`, the kP line identical to `aim=P`.
- `field=1&fx=0.5&aim=F`: `t = 2.236 m, local point (0.063, 0.051, 0.098), normal (0.02, 0.14, 0.99), crossing, active outer · events 2.236 outer crossing · 2.490 outer crossing; material [2.236, 2.490] m; complete, 78 evaluations`; `fa=0.1`: `t = 2.240 …`; `hollow=1`: `2.236 outer · 2.299 inner · 2.426 inner · 2.490 outer; material [2.236, 2.299] ∪ [2.426, 2.490] m; 176 evaluations`; `fwork=12`: `no definite answer: the work limit 12 left [2.233, 2.492] m unresolved before the nearest surface; the winner behind it is not definite`, leaf F:unresolved, then the face behind at t = 2.861 m.
- `field=1&fx=0.289&fz=0.237&aim=k`: the field in front of k, `t = 2.548 m`, leaf F, `material [2.548, 2.801] m; 98 evaluations` (the `occ=0.36` analogue).
- `records=2&aim=k2`: `E.top part 2 of 2, split into 2 by C · Fright resolved, rule origin, Π2 origin at world x 0.30 (Π 800 mm), restricted to its old support x ≥ 800 mm · k · k2 overlap: k inside (segment 67, t 0.76, width 15.6 mm) · k2 inside (segment 14, t 1.00, width 16.0 mm) · composed by order [k, k2], top k2, premultiplied rgba (0.124, 0.443, 0.518, 0.960)`.
- `records=2&merge=1&aim=k2`: `lineage: merged from 2 (the cut was undone) · correspondence: E.top part 1 (x ∈ [0, 620] mm) → E.top, restriction, restricted old support · E.top part 2 (x ∈ [800, 1000] mm) → E.top, restriction, restricted old support · no predecessor: E.top x ∈ (620, 800) mm`; `order=none`: `both records cover this point; no composition rule saved → needs-policy (the lineage word "merged" cannot choose)`; `order=rl`: `top k, premultiplied rgba (0.151, 0.292, 0.748, 0.960)`.
- `records=2&merge=1&aim=strip`: k inside at Π 706, segment 53 (kept whole under clip, it reappears in the strip); `left=trimmed`: `not on k: the strip has no predecessor and k is trimmed here · not on k2: k2 binds only to the old right part (Π x ≥ 800 mm)`.
- `section=0.5&aim=D` (and with `cut=1`): `D drawing, section of S at y = 0.5 m, unit mm, live · circle: r 200 mm about (1100, −200) mm, θ −0.00 rad, 0.0 mm off · source: P wall ← CP.r (analytic; the path floor gets a lowering at a declared error) · dimension r: a saved reaction sets CP.r from this dimension; dragging the loop itself has no unique inverse`.

## Repaired in the fold (both found by the review Sid pasted, both reproduced in Node against the committed functions before the change)

- **The classifier was not the declared swept disc.** The committed `classifyStroke` projected the point onto the centreline and read the width at the projection: on samples (0,0,w 2) → (10,0,w 18) it put (0, 1.5) outside by 0.5, while the disc at t = 0.2 (centre (2,0), radius 2.6) contains it; the brute-force minimum of |p − c(t)| − w(t)/2 is −0.10. The shader shared the approximation, so CPU/GPU agreement was agreement in the same error. Now: the uneven-capsule closed form on both sides (`capsuleSD`), exact membership; the readout's t is the minimiser on the winning segment; the inside distance is labelled a lower bound.
- **Lease rounding changed the projection.** `cameraOf` took its aspect from the allocated surface (448×384, aspect 1.167; 1024×832, aspect 1.231) while the portal's rect is 420×330 (aspect 1.273). Now the framing is the portal's aspect, the viewport is the used sub-rect (rect × zoom × DPR, cap scaling both axes) and the allocation is quantised around it; the page paints the used sub-rect; the screenshot copies it. Today's tree is closer than the bench was: its aspect comes from `ceil(w·s)/ceil(h·s)` (fact-base-2 Part 4), within a pixel of the rect's, but still from a derived pixel size rather than from the portal.

## Not on it (fix list, not reframes)

- Shadows: the light casts none, so "a mark's pixels depend on other marks" is shown by occlusion only. Bench 0 has the sun's depth surface with a counter that rises when the box moves and not on orbit; that is the receipt for "derived pictures on their own inputs", and the tree's own join is the shadow's separate dirty role (renderer.cljs:1134-1139). Bench 0 stays live for it.
- A doubly curved face: the post is developable (arc length is a chart with a metric of diag(s², 1) and no geodesic work); ink on a sphere needs a surface-distance region (membership, bounds, chart pieces, unresolved portions) that is not built. The chart-seam precursor itself is on the bench now (the post), built from the definer's attack 1.
- The path kind's real filler (bench 9's Slug port) on the face: the bench evaluates the swept nib as a distance per pixel; it is the exact nib now, but not the banded coverage lane. Lifting bench 9's shader onto the face is the natural next cut.
- The other split behaviour, split the centreline and add fresh caps at the groove, is not built; the page names it as the picture the `clip` rule does not produce.
- The merge is built with two records and a composition rule (attack 1). Not built: the same mark carried through a chart change and then a merge in one edit sequence on one support (the definer's closing receipt names the two together); the pickup brush's replay on the post (the painting variant on a curved host); a GPU-versus-walker parity read for the field (both run the same isolation; a depth read at the cursor would be the receipt).
- A portal from L back into S (the recursion bound) is not built; the bound is asserted on the page, not measured.
- Travel (re-rooting into S on a zoom crossing) is not built; the page root with a window is the only containment on the bench.
- Query modes (visible, geometric, nearest, sample) are not on it: the walk is visible-nearest only; no glass, volume or splat is on the bench to need candidates.
- The record is read-only; bench 9's editable-record textarea would let the definer paste cases. The extent tool is a saved binding printed in the address, not a drag yet.
- Not carried from bench 0, which stays in the directory as their receipt: the `surface` execution through the lease ladder, the `picture` portal with a fixed camera, the GPU id read at the cursor as the parity check, and the three wheel operations (magnify, lens, dolly) with the aperture, the lens and the effective focal scale read out.
