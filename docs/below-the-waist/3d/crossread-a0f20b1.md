# Session 1's read of session 2's page at a0f20b1 — which line is the trunk, and what each holds the other lacks

Written 2026-09-06, about 03:40, by the chair on session 1's line (the successor's successor), at Sid's ask: "another session was running the same type of thing and I want you to see if you should carry the other session like fold it in or should that session fold you in". Read in full for this: `3d-kind-2.md` (the twin of `3d-kind-2.html` at a0f20b1), `HANDOFF-2.md`, `STARTER-2.md`, `fact-base-2.md` Part 12, `bench-2/HANDOVER.md`; and this line's own `3d-kind.md` and `bench-0/HANDOVER.md`, read again rather than trusted from the handoff. Nothing here ranks: the phase is exploration, the criterion is framing and the whole picture (Sid, 2026-09-05), and details are a fix list. This is one chair's position; the verdict is Sid's.

## The position, plainly

**Session 2's page is the trunk. This line's page folds into it.** The chair on session 1's line is saying this about its own page; the bias a chair carries runs the other way, so read the position as the evidence pointing against the grain.

Five reasons, in order of weight:

1. **Session 2 already folded this line.** Its fold record (`fact-base-2.md` Part 12) has a row for session 1's page and bench handover: seven items adopted and placed, one held with a reason. This line has not folded session 2. One direction of the fold is mostly done; the other has not started.
2. **Session 2 worked the exact open question.** Both review chains put the derived-part question first: what a construction returns when the face under the stroke is split, so a saved attachment rule can keep, clip or detach the ink. Session 2's fold built it on bench 2 (an evaluator that returns parts with a lineage and a frame; three attachment rules that put one stroke in three places while the face's identity never wavered; clip and detach as clauses) and wrote it as Position 10 with receipts. On this line it is item 1 of the still-owed list.
3. **Session 2's frame starts where Sid's method says to start.** Its story opens from Sid's words ("the space as the entity", "world within a world") and from what a thing must mean; this line's story opens from the pixel question, which is a rendering frame. The starter's method rule, adopted by both pages, is that sessions that started from rendering made definitional errors, and this line's own guards record that a renderer quantity promoted to a definition was the shape of every error it made.
4. **Four nouns are a plainer handle than this line's seven.** Space, thing, view, portal, plus the attachment; against space, portal, chart, attachment, definition and occurrence, surface, face and target, with region kept for today's thing. For a reader who reads plainly, the four-noun picture is the one to hold; this line's distinctions fold into it as rows, not as nouns.
5. **Session 2's bench is closer to what Sid asked the bench to be.** Its panel is laid out as the waist: the address level by level with lineage, the diff, what ran at which rate, and the record of what is data above the waist, live. That is the first small instance of "make the whole system visible in softland itself"; bench 0 shows the seam and the cameras, not the record.

What this line's page is stronger at is real and must survive the fold: the shelves below the waist and the reading of today's tree. Shelves fold into a frame; a frame does not fold into shelves. The lists below are the fold.

## Where the two agree without having read each other

The strongest evidence an exploration gets, already on both pages: one waist, several libraries beneath it; the region is not the thing and splits into a space seen through a portal; nested spaces both ways; ink on a face is the path kind drawing on a face by a saved attachment, with no second contract; hover is a query over the pointer and the data, re-run when a thing moves under a still pointer; page zoom magnifies the placed view with its framing kept, and entering is another operation; render is a request that names its quantity; the hit takes a mode and may return candidates; the evaluator is forced to a decision by 3D and is thin over thick libraries, which is where Sid's doubt of about 02:00 landed; the rate axis survives with derived pictures on their own inputs; today's tree holds the pieces and none of the joins.

## List A — what this line's page and bench hold that the trunk lacks (for the trunk's chair to fold)

Sharpenings and rows, at full weight where they are counterexamples or definitions; the trunk's chair places them. Anchors for the code claims are in `fact-base-1.md` (Parts 1 to 3 by file:line; Part 7 the own reads).

**Frame-level, three items.**

- **The pixel question is recursive.** Shading's inputs are other renders of the same space (a shadow map is the space seen from the sun, CHECKED, the tree does this today; probes and reflections in the field), so "a mark's pixels depend on other marks" is not the 2D case of blurs at a larger size: the per-pixel question calls itself, and where a renderer cuts the recursion is the contested execution. The trunk has the consequences (derived pictures on their own inputs; GI as "pixels depend on everything") and not the frame. Story-level.
- **What must be code, said once.** The trunk's story lists the shared services and the two floors; it does not say the nine things that recur as code across all six ceilings: the executor with dependency tracking; render to surface from any camera; the visibility resolve with its transparency policy and the sampled-shape programs' composition rule; the per-scale mechanism, GPU-driven at the top; the geometry shelf with the exact kernel as its bought member; f64 and rebase in the transform tree; the material stage with the path filler as a layer; curves in space; stable identity for things, faces and copies. That list is the answer to Sid's "is something missing that has to be hardcoded". Story-level or a closing table.
- **The order rule as what makes the two kinds two.** The trunk names planar and spatial as two kinds and held against this line's "a space is a frame plus an order rule" because the walker and the packer branch on more (depth, units, a metric). Both are right; the fold is to keep the two kinds as the nouns and say what makes a space one or the other: whether front-to-back is declared or derived from the view. Without it "planar | spatial" is a label, and the chart's "declared-order coordinates at a depth the mapping gives" has nothing to stand on. One sentence in story 2.

**The road below the waist, two items.**

- **The renderer fork and the position on it.** Forward with MSAA (today's shape made general), deferred with temporal, ray query over a BVH, path traced: a table of antialiasing, translucency, looks as data, portals and charts, memory, cost from here. The position: forward with MSAA, because a page with text and ink cannot take temporal smear, every face can carry a chart layer, and the region lane already has the passes; keep the ray-query road open by never letting a raster-only quantity into the value. The trunk chose no renderer, on the object-space contribution's argument that choosing one first cannot settle the trade-offs; that is right about the model and silent about the road, and the road is part of the code-side picture. Position-level, marked as about the road, not the model.
- **The today reading at one notch.** This line's today table has a third column, where the renderer's convenience became the definition: the rect, the lease, the composite, the bias, the triangle index, each a renderer quantity promoted to a definition. The trunk's today drawing and caption say the same at a coarser grain. The column is the how-to-think of the whole path round, made visible on the tree.

**Rows and sentences, eleven items.**

- Physics scope is declared on the space, apart from grouping: which things occlude, light and collide with each other; a portal boundary isolates, a group does not.
- Curves in space as a shape language: edges, hair, wires; a path whose centreline is 3D, drawn with a view-facing nib.
- Sweep generalises: a nib along a path is Minkowski along a curve, and extrude, revolve and pipe are the same operation one dimension up; the geometry piece is one piece with a dimension.
- Sections keep their conics: line, quadratic and cubic are the imaging language; circles, conics and rational intersections stay analytic in the geometry shelf's result and lower to the path value at a declared error with provenance; the path filler never becomes a CAD kernel.
- The portal's mapping kinds: fixed (a picture), window (derived from the viewer), declared (a mapping the executor runs, zoom beyond the rect becoming a dolly); the trunk's "travel re-roots on a declared reaction" is the third kind. Plus what a picture never supplies: whether events pass through, whether light and bodies cross, the recursion budget; and that a mirror is a portal into its own space, so cycles are a case, not only depth.
- Hidden-line removal as a CPU visibility answer, not a draw: BIM's plans at drawing quality need visibility as an answer with the face that made each line, or the plan cannot be edited back.
- CPU and GPU parity has a named crack today: the CPU pick is two-sided and the GPU culls back faces (CHECKED); the trunk's "agree by construction covers where, not what" gains a tree instance.
- Exposure is the camera's, like a real one: the space renders linear HDR, the camera tone-maps, the surface is display-referred premultiplied; today's chain already is that (CHECKED). The trunk's colour line has the chain and not the placement of exposure.
- Deformation is a placement on the rate axis, per frame on the GPU or per edit on the CPU, and the executor's GPU door decides; the trunk's compiled tier is the same piece from the language side.
- Reversed-Z float depth is the depth twin of camera-relative rebase; today is forward [0,1] depth24plus (CHECKED). The trunk has rebase and names a depth policy for the planet only.
- The chart precursor before the boolean: a stroke across a parameterization seam on a curved mesh, the face reparameterized without changing the intended surface, the stroke staying where its attachment says and the pick returning its identity. The trunk lists "a curved face" as a limit of Position 10; the reparameterization test is sharper because it isolates the mapping from the topology change, and it belongs before the merge clause on the trunk's owed list.

**The bench, two items.** Bench 0 stays in the directory as the receipt for these; the trunk's bench does not carry them and need not until a chair wants them on one file.

- **The shadow with its own key.** Bench 0 renders the sun's depth surface and its counter rises when the box moves and not when you orbit; the tree already holds this join (the shadow role is dirtied only by a scene change, `renderer.cljs:1134-1139`, `1324-1345`, own read). It is the composition primitive already in the tree, unnamed, and the trunk's bench has no shadow, so "a mark's pixels depend on other marks" is shown by occlusion only there.
- **The three wheel operations with the variables read out.** Magnify (the aperture grows, the framing kept), lens (the field changes inside a fixed aperture), dolly (the camera moves), with the aperture, the lens and the effective focal scale on the counters, so each shows which variable it changes. The reviews of 6f62711 measured magnify on it. The trunk adopted the account; the thing Sid can wheel over is on bench 0.

## List B — what the trunk holds that this line's page lacks (the inverse, in case Sid rules the other way)

- The four nouns as the frame, opened from Sid's words; the view as a person's (orbit is never an edit; sessions are the weakest layer of a layer stack; a camera placed in a space is a thing, "a good view").
- The seam as a pair of maps, picture up and pointer down, with the law that sharing one camera makes them agree about where and not about what, and the receipt that the bench's two halves were wrong together twice.
- One question protocol over stored representations, each declaring which answers it gives; an answer is a result, candidates, unsupported, or needs a policy; the representation table (open mesh, solid, density field, point capture).
- The picture rule rewritten: a rendered picture does not replace the editable meaning of the thing pictured; a saved render, a scan, a painted texture are data with provenance; the screenshot beside the portal as the test, on the bench.
- Position 10 with its construction: a derived part's identity is what the construction returns (a name, a lineage against the previous run, a frame); where the ink belongs is the attachment's rule; the attachment as a record (face, authored domain, anchor rule, a clause per lineage, what it reads as, whose it is); the three rules and clip or detach, measured.
- The evaluator's call as the definer's attack 2 spells it: execute(recipe, inputs, state) → named results + next state; one language, several runners; the three entrances (evaluate, render, query) with the view render as the imaging boundary and not the entrance to the kind.
- Per scale as the planar case of per view; per time as its own row.
- Travel as a declared reaction that re-roots; either kind of space as the root.
- The bench as the waist made visible: address with lineage, diff, rate lines, the record live, the construction editable from the hash; the face portal into a planar land; the screenshot.
- The next construction named: an implicit or sampled object the engine does not know as a hardcoded kind, its hit addressing the formula's parameters because there is no face.

## What happens to this line's files

Nothing is deleted. `3d-kind.html` and `bench-0/` stay as the record of session 1's line and as the receipts List A points at; `fact-base-1.md` keeps the anchors. This file is the bridge; a merge session or ceremony is not proposed (the bridge artifact is the reconciliation). Once the trunk's chair has folded List A, this line's page gets one line at its head saying it is folded into `3d-kind-2.html`, and its artifact is not republished after that.

## The one fork, Sid's

Who does the fold of List A into the trunk: the chair on session 2's line (its starter already says "anything new in the directory" is folded), or this chair at Sid's word, since the rule against editing another session's files is between live sessions and his word changes it. Either way the list above is the work. This chair's position: the trunk's chair, because it knows where each row lands on its page, and this chair stays available for the anchors and the bench-0 receipts.

## For Sid to paste to the trunk's chair

> Session 1's chair read your page at a0f20b1 and says your line is the trunk; its reasons and what its page and bench hold that yours does not are in `docs/below-the-waist/3d/crossread-a0f20b1.md`, List A. Fold what it finds at full weight where it is a definition or a counterexample; bench 0 stays as the receipt for the shadow's key and the three wheel operations.
