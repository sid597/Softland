# 3D — the starter for session 2's successor (written 2026-09-06, end of session 2)

Essence of the exploration as it stands after session 2, in clean first person, with the view session 2 reached stated as a working basis to attack, not as conclusions; terrain as facts; the two ratified riders. One version, for the Claude lane that takes over session 2's view. Sid's words are on the page's ledger and in `path-kind/HANDOFF-9.md` §6, verbatim; none are quoted here.

`````
I am working through the engine under `src/app/client/` one kind at a time, at the level of inputs and outputs: what goes into a kind, what comes out, and whether the kind is really a library. The path kind has been worked to a picture in `docs/below-the-waist/path-kind/`. The 3D kind's round is running in `docs/below-the-waist/3d/`. Session 2 there reached a view; you take that view over and carry it forward. Feedback is about to arrive: the parallel sessions' pages sit in the same directory, the definer's attacks land there, and I will redline.

What I am trying to build is every tool, 2D and 3D. The way I want to get there is a waist: an engine and a few hardcoded primitives below it, and everything above defined as data, ECS-style, that humans and agents create, edit, save for reuse and build higher things from, without merge deadlocks. The store that merges is above this kind; what the kind owes it is stable identity and fine-grained diffs. Hover matters because it is where a person's focus is, and the hit answer comes at pointer rate for the whole page, nested regions included, and says only what changed. The hardest 3D tools are the ceilings, parametric CAD, procedural film tools, real-time open worlds, BIM, planet-scale geospatial, volumes and scans, and I want to build through them, not hack through them. The question is what below-the-waist layer should exist for everything, 2D and 3D in one answer, or a reason there are two.

The view I am carrying forward, and want attacked rather than assumed. A space is the entity, planar or spatial, with a unit; things live in spaces; a view is a camera onto a space and belongs to a person; a portal is a thing in either kind of space that shows a view of another space. Every seam is two maps, the picture up and the pointer down, and they must agree: what I see is what I hit. The general 3D object is a placed shape stored as its source made it, a mesh, a solid, a formula, a volume, points or splats, behind one set of answers over the several representations; the triangle is the leak only when it stands in for a source, and the picture is never data. Ink on a face is a planar space with a unit that the path kind draws in unchanged. The hit is an address through every level with the diff against the last answer, and hover is a value of the pointer, the data and the views, not an event. The page stays planar with a paint order and 3D relations live inside one spatial space; the root can be any space and travel re-roots on a declared reaction. One waist, two floors: identity and diffs, the seam walker, the compositor and the evaluator are shared, a planar floor and a spatial floor sit under them, and the residency ceiling is the condition under which that becomes two engines under one data model. Constructions, a feature history, an operator graph, a shader graph, a mate, are the kind's input, and the evaluator's language is the fork I close; my doubt about one executor stands, so the working shape is one language for constructions with several runners behind it and constraints as operations. The question nobody has worked is the identity of a derived part: ink on a face of a solid made by a construction, the sketch edited, the construction re-run, and what makes the new face the same face.

Terrain. `docs/below-the-waist/3d/HANDOFF-2.md` is session 2's handoff, with its still-owed list and the code facts that decide things. Its page is `3d-kind-2.html`, live at https://claude.ai/code/artifact/400dcba1-c53c-4eb2-bba8-7978dee838d7 , with a `.md` twin; every anchor is in `fact-base-2.md`. Its bench is `bench-2/seam-bench.html`, live at https://claude.ai/code/artifact/32169816-ae1c-4bbb-ba07-a9171eea419a , with `bench-2/HANDOVER.md` for the deep links, the headless check and the measured readouts; the hardest case runs on it. The same directory holds the parallel sessions' work, which session 2 did not read: session 1's `3d-kind.html`, `fact-base-1.md` and `bench-0/`, and another lane's `3d-kind-working-model.md`, `3d-object-space-and-query.md` and `HANDOFF-object-space-and-query.md`. `docs/below-the-waist/3d-ceilings-starter.md` names the ceilings and the current waist. In `docs/below-the-waist/path-kind/`: `HANDOFF-9.md` (its section 6 carries my words of 2026-09-06 on compositing, tools as data and the executor), `HANDOFF-8.md`, `path-kind.html`, the definer's `attack-1.md` (folded) and `attack-2.md` (unread by anyone). `vision/LOG.md` is my words on what Softland is for. The code is reached by the READMEs under `src/app/client/`, then namespace and function docstrings, then the code. Never `src/app/server/env.clj`. Positions on the page are marked and carry the condition under which each stops being the answer; the verdicts are mine.

Work the hardest case through whatever you propose, including the case the round ended on: a pen stroke on a face of an object inside a 3D region on a page, zoomed, orbited, hovered and clicked, an object passing in front, a portal inside the region showing another land, a screenshot of the region pasted beside it, and the face's solid re-made by an edited construction. Take it through both directions of the seam. Where does a named abstraction, surface, region, portal, scene, face, still hide unfinished work? After qualifying, say what decision about 3D's input and output remains and which exact question is still open. Where you take a position, on containment, on the general 3D object, on one waist or two, on the evaluator: what supports it, under what conditions does it become only a workable default rather than the answer, and what else in the picture changes with it, including in the path-kind picture?
`````
