# Opening prompt — framework package CONTRACT (Fable session)

**Runs in parallel with the Step-0 probe.** If `PROBE.md` hasn't landed, draft with pre-registered slots for its three numbers (apply-cost, wrap-line cost, scroll convention) and finalize when it does — the relation-kernel contract handled memory-claims exactly this way.
Model: Fable (contract work per D-006). One session. If the `3-way-design-framework-converge` session still has context, it writes this contract itself — it holds the full recon; this prompt is the fallback for a fresh boot.

## Boot

1. `.claude/skills/work-package/SKILL.md` — the contract checklist GOVERNS: traps ledger, acceptance gates whose one-line text carries every carve-out, coverage claims name their verb, input manifest, the `imp:<family>:` prefix-routing deliverable rule, STANDING/NOW mechanics.
2. `docs/current-mental-model/build/framework/ROAD.md` (v2) — the direction source. The contract realizes **Steps 1–2 plus the Step-4 kernel-object schema §** — not the whole road.
3. `docs/current-mental-model/build/framework/PROBE.md` — if present.
4. `docs/current-mental-model/decisions.md`: D-004 (relation kinds are a CLOSED enum — additions need explicit authorization; block-kernel's 3-kind addition is the precedent), D-008 A2 (the machine write path), D-009 (one substrate; registry entries as code-as-addressed-material), D-010 (approve-by-default). Plus the Trunk-5 STANDING PROPOSAL in the D-006 notes: the **mechanical fixture-fidelity gate** — this contract adopts it.
5. Precedent contracts: `build/relation-kernel/CONTRACT.md` (traps-ledger exemplar) · `build/sense-line-mvp/block-kernel/CONTRACT.md` (goldens + receipt discipline).
6. Source — verify every claim you bind, cite file:line: `rect_tree.cljc` · `ui_primitives.cljs` · `trail_face/wiring.cljs` · `runtime/render.cljs` · `electric_flow.cljc`.

## Deliverables

1. **`build/framework/CONTRACT.md`**, covering at minimum:
   - Purpose · consumers in order (Sid wearing faces → the design round → the orchestrator) · non-goals = ROAD's deliberately-not-building list, each phrased as an extension point.
   - Placement ruling: which new namespaces, and who may touch `electric_flow.cljc` (Wave-1 lane C is its sole owner).
   - **Grammar v0**: the five keys, validation rules, the arrangement-only guard, error-card semantics (malformed assembly → error-card rt-node, never a crash).
   - **Interpreter interface**: two-stage law (wear-time compile → data-change apply → frame-time sample only); carries `(view-instance, address)` from birth so Δ1 compliance is a rename when the Δ3 store lands.
   - **Registry**: runtime map `{keyword → builder-fn}`; persisted form = keyword/address kernel objects — never fn pointers.
   - **Artery protocol**: request shape (face + address + params), data-context atom, S1/S2 seam law as contract-level law, the harmonization ladder with rungs 3–4 explicitly form-break-gated.
   - **Assembly-as-kernel-object schema §** (implemented Wave 2, schema fixed now): object kind, status (candidate/worn/retired), provenance, lineage. Relation kinds: use existing D-004 kinds where honest (`based-on`, `new-direction`, `dead-end`, `elaborates`); any addition (e.g. `supersedes`, `pairs-with`) is a **named authorization item**, never a silent enum edit. If a new `imp:` prefix is minted, its `extract-object-key` routing + foreign-read gate is a named deliverable (skill rule).
   - **Traps ledger**: naive alternative → concrete failure → ruling, numbered, cited by number in code comments.
   - **Acceptance gates**: golden rt-tree discipline (JVM: assembly + fixture → golden), the sidebar structural-equality falsifier (assembly → interpreter → rt-tree ≡ existing builder output; runs at the wave's integration step), the fixture-fidelity mechanical gate, every one-liner carrying its carve-outs.
   - **Input manifest** (feeds the D-006 counterfactual probe).
   - **Handoff**: the three W1 lanes with fences, the W1-INT integration step, the wave's ONE batched end-gate (falsification-by-class + Fable gate) per the 2026-07-05 delivery-mode ruling.
2. **Finalize `docs/sessions/framework-w1-lane-prompts-2026-07-11.md`** — pin every ⟨§?⟩ reference to real contract sections. Only then are the lanes dispatchable.
3. **Draft (not enter) the two decision-log PROPOSED entries** for Sid's countersign: the middle regime (land's furniture as land's data — neither Regime 1 nor gated Regime 2) and the Regime-2 self-hosting test formulation (a design conversation produces a usable view without leaving the land or hand-translation).
4. Update `build/framework/NOW.md` STANDING to point at the contract; flip the board line; commit docs (docs branch only, docs-only commit).
