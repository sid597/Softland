# Opening prompt — the machine-cut package (ROAD Step 5), contract session

Paste into a FRESH **Fable** session AFTER the framework close-retro has
landed (its residue feeds this contract by /work-package rule). Session
name suggestion: `machine-cut-contract`.

---

You are Fable, opening the **machine-cut** work package for Softland — boot
the `/work-package` skill first; you author the CONTRACT (traps ledger,
gates, placement rulings, every claim verified at file:line), then pin lane
prompts; implementation goes to Opus subagents per D-006.

**What this package is (ROAD Step 5, `build/framework/ROAD.md:93`):** an LLM
annotator via the EXISTING LLM-kernel intent→executor pattern
(`src/app/server/rama/dogfood/llm.clj`) asserts conversation STRUCTURE as
D-004 RelationEdges with `asserted-by` first-class: block roles
(nucleus/satellite/aside), pair bindings (user-message ↔ response), episode
boundaries. Machine-guessed renders visibly machine-guessed — silver marks;
Sid's reactions are gold (map-must-not-lie).

**Why now (the D-005 evidence — cite it in the contract's Purpose):** the W2
wearing double-confirmed the lack — BOTH transcribed faces (Boxes,
Minimap/Reader) want turn ⊃ user|response pair structure the `:conversation`
projection cannot serve (`build/framework/W2-E-plurality.md` lack 1;
`W2-INT.md` §Lacks item 1). The view ordered this data work; that is D-005
running in its intended direction.

**Binding context to read:** `decisions.md` (D-004 closed kinds — the
contract must rule whether pair/role/episode edges fit EXISTING kinds or
whether this is finally `pairs-with`'s named form-break: that addition
requires Sid's explicit countersign, prepare the draft + recommendation) ·
`build/framework/CONTRACT.md` §8/§17/§20 + the close RETRO's residue ·
`build/relation-kernel/CONTRACT.md` (the edge substrate) · the LLM-kernel
source + `/rama` skill · `feedback_rama_side_effects.md` (intent+executor,
never in-event spawn) · the block kernel's read surface (what the annotator
READS: river blocks per conversation).

**Hard package rules you already know:** new `imp:` prefixes are named
deliverables with routing gates; whole-corpus claims name their verb + get a
receipt gate at first full-corpus contact; consumer disciplines from the
framework retro carry in; wave dispatch and any relation-kind addition are
Sid's; everything revert-cheap proceeds by default (D-010).

**Consumers, in order:** (1) the Boxes/Minimap faces becoming
structure-honest (their pair-grouping binds served by the projection reading
the new edges); (2) the Arcs/Score-class faces the design round holds; (3)
the trail view's epistemic edges later. Scope the contract to consumer 1 —
the worn forms' actual need — with 2/3 as extension points (D-001).