# Spec Room · Part 2 — the Mark Grammar (paste into a FRESH session)

Part 2 of the sense-line SPEC. **Part 1 (block grammar) is closed to LIVING v0.**
This session builds the **mark grammar**: the node kinds + the relations (edge
families) between them. Boot minimal; read lazily; read spans, never whole
transcripts.

## The backbone (the synthesis Part 2 stands on — read in order)

1. `docs/current-mental-model/build/sense-line-mvp/block-grammar.md` — **Part 1, the
   backbone.** Hold especially: **B-4** (marks/specs address *raw spans*, never
   block-IDs — the invariant that keeps marks and blocks separable); **B-2/B-3**
   (chunking is plural; **S2 is label-driven**, so the mark kinds defined here *feed
   S2's carving* — the one place Part 2 loops back into Part 1); **B-6** (S2 =
   epistemic-kind). Marks **target blocks** (spans); this is the block↔mark seam.
2. `docs/current-mental-model/sense-line-model.md` — the model. Fixed-point **#7**:
   labeling is emergence — description first, names earned by recurrence, **Sid names**.
3. `docs/current-mental-model/build/sense-line-mvp/DIRECTION.md` §0 + §3.1 — **SPEC ≠
   BENCHMARK** (this room defines the schema; marker-performance is a later, separate
   room) and the layering discipline: **two edge families kept SEPARATE — epistemic
   (supports/opposes/informs) · process (based-on/supersedes/produced)**. Combine by
   layering, never merging (the schema deaths of 2026-07-07).
4. `docs/current-mental-model/inquiry/specimen-01-graph-derivation-2026-07-07.md` —
   **the best-evidenced candidate kinds.** The epistemic layer maps clean onto
   **Q/C/E/Source**; the specimen *forces* three extensions with recurrence receipts:
   **purpose (+`serves`), test (+`invokes`), verdict (kill/accept)**. Read as the
   candidate set to pressure-test, not to adopt.
5. Sid's Roam notes + the Part-1 mapping: graph `softland`, block `((FFUM-rLVd))` and
   its `#[[Fable]]` children — the join between Sid's own thinking and Part 1's
   B-statements. (`specimen-01-this-chat` and `atomic-unit-2026-07-07` are available if
   a nuance is needed — read the span, not the whole.)

## Goal (definition of done)

The artifact stating the **mark grammar** — what node kinds, and what relations (edge
families) between them — folded with Part 1 into `SPEC.md`. All vocabulary is
scaffolding; names finalize by recurrence; Sid names.

## The live open question this room opens on

Do **`serves` / `invokes`** (from purpose / test) force a **THIRD edge family** beyond
epistemic + process — a teleological/intentional axis — or fold into the two? The
graph-derivation showed the dissolution is a **three-way interaction** (a `serves`-spine
+ a *missing* epistemic node), which is the standing argument that `serves` may not be
reducible. Watch it; do not rule it prematurely (#7). Also open: is **verdict** a node
kind or an event/edge; does **test** collapse into Question or gate distinctly.

## Lenses (Sid's — use emergently, never forced)

- **olog** as *notation* for writing the schema down: objects = kinds, morphisms =
  relations/aspects, and provenance paths that should agree must **commute** (an honesty
  constraint on the edge graph). Notation, not a rival ontology (DIRECTION §3.1).
- **panproto** — **READ here** (Sid's pointer, not yet metabolized):
  github.com/panproto/panproto + github.com/panproto/panproto-toolkit. Candidate mental
  model for the schema layer; fold in if it earns, park if it doesn't.

## Method

Reason-based discussion + apply candidate kinds/edges **by hand to ONE example chat**.
Default fixture: **this Part-1 session itself** (`spec-sense-line`) — the self-test /
strange-loop the block grammar names (block-grammar.md §4); it is dense in claims,
verdicts, purpose-mutations, and a dissolution-shaped hole, so it exercises the whole
candidate set. Alternative: the design sitting `7c80ce2a` (specimen-01 describes it
independently, so candidates can be checked against a description not derived from them).
Propose plural; **Sid rules**; never converge-and-sell. **SPEC ≠ BENCHMARK** — mark to
stress the schema (what forced a fit, what recurred with no kind, what kind never
fired), not to score a marker.

## Do NOT load

next-prompt.md, decisions.md in full, batons, or the Part-1 authoring transcript in full
(session `spec-sense-line` — on disk at `~/.claude/projects/-mnt-data-projects-Softland/*.jsonl`;
read the span, not the whole). Execution-altitude docs colonize the register.

## Register

Working session with Sid, close iteration; his in-flow verdicts are the gold; propose
plural options; no work-queues beyond the next concrete iteration. Explorers first,
implementers second — the spec has lasting effects on how the system *can* be.
