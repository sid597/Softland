# "Where we at, Softland" — orientation opener (baseline: post-Trunk-5 wave, 2026-07-06)

Paste-ready prompt for a fresh orientation session. Written at the Trunk-4
orientation session's close; anywhere this recap and the repo disagree, the
repo wins.

---

hey where we at? what is the current state of softland from a product
development pov? orient against the NEW baseline — a lot moved since the
trunk-4 opening:

the all-fronts marathon ran end to end. trunk-4 (fresh session) authored the
face-2 contract (build/face-2/CONTRACT.md, v1.1 after its validation round)
and dispatched four branches as background subagents — t4-room built the whole
R-2 tranche (bands 0-3 · lanes-from-edges · the honest unthreaded band · move
chips · new threads.cljc), t4-spine closed the backend seams (claimed-ms via
an explicit :claimed/at-ms key — it REFUSED :request/time-ms because that
defaults to wall clock and would stamp md arrivals as claims; my dangling-edge
doubt was REAL — /home/sid/projects/Softland is a symlink that split path
identity, root-caused + canonicalization now rebases through it; the
[GIT-SPINE] stats stopped lying twice — fresh/converged split + a ~300×
sibling-jsonl dedup fix; :workers-2 smoke passed; incremental-jsonl was
DELIBERATELY not built — the branch proved the instance-scoped cursor makes
cross-boot skipping semantically impossible and escalated the durability fork
instead: durable cluster vs durable spine-edge replay log, MINE to rule),
t4-substrate landed Δ7 slug glyphs, t4-bench ran PARTIAL (paused mid-grading,
batch 7+ outstanding; sonnet-5/haiku-4.5/codex-5.5 arms pre-registered at
pinned 127376b0). trunk-5 then ran the wave boundary: serial suite 204/2500/0
→ three falsification agents hunting by class → THREE GATES PASS with five
fixes applied at gate — the big one being the endpoint gap, a literal
recurrence of the wave-1 kill class (every fixture gate green while first
light would have rendered threadless), caught by a live-probe; plus phantom
move-chips on merge bystanders and same-ms tie ordering, both
latent-until-endpoints-live. four commits were staged on my word (room · spine
· substrate · docs) — CHECK git log for whether i gave it and what landed
after.

i booted first light: THREADS ARE REAL — real claimed clocks (2024 commits
render "claimed 2024-01 · arrived 07-06"), move chips fired, attested lines,
typed edges with provenance on every line. but the surface speaks MACHINE
DIALECT: raw 64-hex ids in chips and edge lines (display-name gap for
chat-conversation targets + unclamped text against the R-1 "unbounded label
bleed is the worse map-lie" ruling), walls of produced-edges that need
folds-with-counts (ledger 16), kraft connectors floating unlabeled, density
poor. my screenshot is the FIRST_LIGHT_R2 evidence; the legibility batch is
the next UI tranche and band-2 semantic text (sitting-3 item 3, the
commit-gesture question) is the deep cure. vocabulary adopted that session:
MACHINERY-DONE ≠ FORM-DONE — say which you mean, per front, always.

also: i NAMED THE WRITE SIDE (my verbatim in vision/LOG.md 2026-07-06) — edit
access to everything the land ingests and shows. the standing analysis:
LIVING material (docs/code — edit-in-place, versioned, D-003's commit
boundary IS the architecture, the watcher echo is the confirmation leg) vs
WITNESS material (transcripts/commits — annotate only, never mutate); the
durability fork is the FIRST write-side decision; design rides sitting-3
(doors + commit-gesture), build paced by D-008.5's own friction trigger —
read-only friction in daily use, which starts counting now that i live in the
threaded view.

so orient: read docs/sessions/next-prompt.md top blocks (the trunk-5 baton),
decisions.md D-001..D-009, BETS.md (did i sign the H1 arming entry? the
candidate image + the two t0 readings are on record), build/face-2/CONTRACT.md
header (countersigned or pending? flags at countersign: the Δ9 client-side
deviation · the §2.4 move-rule conjunct added at gate · band-line wording),
the three gate artifacts + the four BRANCH_REPORTs, runs/ for bench state, and
walk git log --oneline -50 — the commit messages narrate the arc deliberately.
VERIFY against disk anywhere this recap disagrees with the repo.

then tell me: where we are, what the frontier actually is, what is converging
vs genuinely open — the sitting residue (whichever of these i still haven't
done: commit word · H1 arming signature · face-2 countersign · the
durability/incremental-jsonl fork · importer provenance on source rows · bench
resume + LM thresholds · the intake sitting with the local-models +
behind-the-scenes + MEMORY-off-land families) · the legibility batch · band-2
semantic text · the write-side design · and when the face-2 wave actually
fires and what enters with it (Δ1/Δ2 birth laws, Δ3 store, camera → Δ13/Δ14/
Δ15 cascade, codex 5.5 xhigh fast in the builder pool). give me
machinery-done vs form-done per front (rama · ui · framework · benchmark).
and repaint the state-of-the-land page — same artifact url
(https://claude.ai/code/artifact/3a268c14-7e0a-46d9-8dd7-addd7f5dcd85), and
check how much of the 07-06 version has retired; the retirement audit is the
point, it always was.

please note i am not asking you to make explicit decisions ... it's
orientation for me and re-familiarization for you — take your read, explore,
keep threads open or converge at your discretion, and flag anywhere a used
form has broken against a closed decision.
