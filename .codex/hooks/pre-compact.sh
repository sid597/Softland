#!/bin/bash
echo "Context is being compacted. STOP current work immediately. Before doing anything else:
1. Save any unsaved decisions, architecture insights, bugs found, or patterns learned to memory files — if any exist and are relevant
2. Update any living docs that track progress (checklists, orientation docs, session logs, doc indexes) — if we are tracking those and they reflect what changed
3. Write a next-session prompt to docs/next-prompt.md — if there is unfinished work that a fresh session needs to pick up. Include: what was done, what is incomplete, key files touched, and specific TODOs with enough detail to resume without re-reading everything
Only update what actually needs updating. Do not create files or entries that serve no purpose."
