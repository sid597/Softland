---
name: project-first-record-research-application
description: "State of the first-record work (what the store must fix before its first record): one pass-on document, CORNERS.md; the research, pilot and first handoff retired to history/proposals/; fetched sources in a depot outside the repo (2026-09-21)."
metadata: 
  node_type: memory
  type: project
  originSessionId: 372bfb78-5c86-4eec-b482-7ebfc04edfee
  modified: 2026-09-21T14:15:42.000Z
---

Sid is deciding what his append-only, never-rewritten store must fix before its first record; what
he wants first is the conventions that would paint him into a corner. On 2026-09-21 two sessions
applied the eight research files (about 310k tokens). What is passed on is ONE document:
`src/proposal/first-record-2026-09-20/CORNERS.md`. It holds where the work stands, the picture
(planet, forest, tree and the two "to"s; given to Sid, not confirmed in words), six findings the
corners hang from, a page per corner, a redrawn ledger (a candidate; Sid has not ruled on it), what
Rama fixes physically per the rama research file, and what was and was not read. The first thing it
puts to Sid: whether the unit of admission is one fact or a saying, since names, order, the
envelope, the verdict and reads all hang from that.

Everything else was retired on Sid's word ("I will not tell any session to read them") to
`history/proposals/first-record-2026-09-20/` (README there): the eight research files, the
orchestrator's merge, the pilot, the first HANDOFF.md, and the Codex plans and per-corner bundles.
It is evidence to check a quote against, not a session's reading. The 363 MB of fetched pages and
PDFs are not in git; they sit in `/mnt/data/projects/research/softland/first-record-2026-09-20/`.

**Why:** a new session cannot see the chat, and Sid wants a small load handed on, not 310k tokens.
Reading fence he set for this work: never `research/loop/` or `research/sources/`.

**How to apply:** start a first-record session from CORNERS.md plus Sid's own text (the problem,
forced questions, one fact, handles 0 to 17), which he hands over himself. Treat the redrawn ledger
as evidence to test, not a position to defend. The economy is out of scope for now. The "running
answers never stored" pushback in it touches the caching rule and was deliberately left for the
fresh adversarial session CLAUDE.md describes. Related: [[feedback-large-research-loads-go-small]],
[[feedback-proposals-under-src-proposal]].
