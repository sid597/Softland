;; Hand fixture shaped EXACTLY per WP1 CONTRACT s6 (recent-activity feed).
;; Window selection is ARRIVAL-ONLY (WP1 s9.10); :order sorts WITHIN it.
;; Contains: a back-dated entry (claimed 60d before now, arrived in-window),
;; a nil-claimed transcript entry (honest), a dead-end-marked target, a
;; written-by-differs actor, and the standing :feed/uncovered declarations.
;; Salts as \uXXXX escapes only (S4).
;; 2026-07-05 re-derivation (git-spine WP2 P3, F-L3/G9): source-ingested and
;; transcript-file-updated targets now carry :display-name (basename of the
;; adjacent source-ref/file-path), exactly where the live feed constructors
;; emit it. relation-transition targets carry NONE - live emits none there.
{:fixture/now-ms 1782172800000

 :feed/address (trail/recent-activity {:window {:from-ms 1782086400000 :to-ms 1782172800000} :order :arrival})
 :feed/rendered-at-ms 1782172800000

 :feed/entries
 [{:entry/kind :relation-transition
   :entry/target {:id "oc:doc:9fdoc" :kind :doc}
   :entry/address (trail/context-bundle {:targets ["oc:doc:9fdoc"]})
   :time/claimed-ms 1782170000000
   :time/arrival-ms 1782171000000
   :entry/actor {:asserted-by "sid" :written-by "agent:claude-code/session-x"}
   :entry/detail {:kind :based-on :status :asserted :relation-id "rel:b1"
                  :from {:kind :doc :id "oc:doc:9fdoc"}
                  :to {:kind :doc :id "oc:doc:earlier"}}}

  ;; the back-dated entry: claimed 60 days before now, arrived in-window
  {:entry/kind :source-ingested
   :entry/target {:id "oc:doc:earlier" :kind :doc :display-name "april.md"}
   :entry/address (trail/context-bundle {:targets ["oc:doc:earlier"]})
   :time/claimed-ms 1776988800000
   :time/arrival-ms 1782172000000
   :entry/actor {:asserted-by nil :written-by nil}
   :entry/detail {:source-ref "docs/april.md"}}

  ;; nil claimed is honest (transcript file update)
  {:entry/kind :transcript-file-updated
   :entry/target {:id "oc:chat-conversation:chat:ab12" :kind :conversation
                  :display-name "ab12.jsonl"}
   :entry/address (trail/context-bundle {:targets ["oc:chat-conversation:chat:ab12"]})
   :time/claimed-ms nil
   :time/arrival-ms 1782169000000
   :entry/actor {:asserted-by nil :written-by nil}
   :entry/detail {:file-path "transcripts/ab12.jsonl" :conversation "chat:ab12"}}

  ;; dead-end-marked target
  {:entry/kind :relation-transition
   :entry/target {:id "oc:doc:deadend-doc" :kind :doc}
   :entry/address (trail/context-bundle {:targets ["oc:doc:deadend-doc"]})
   :time/claimed-ms 1782168000000
   :time/arrival-ms 1782168500000
   :entry/actor {:asserted-by "sid" :written-by nil}
   :entry/detail {:kind :dead-end :status :asserted :relation-id "rel:de1" :dead-end? true
                  :from {:kind :doc :id "oc:doc:deadend-doc"}
                  :to {:kind :doc :id "oc:doc:9fdoc"}}}

  ;; same family as 9fdoc (du:9fdoc:*) -> same lane; exercises the spine
  {:entry/kind :relation-transition
   :entry/target {:id "du:9fdoc:000003" :kind :block}
   :entry/address (trail/context-bundle {:targets ["du:9fdoc:000003"]})
   :time/claimed-ms 1782165000000
   :time/arrival-ms 1782166000000
   :entry/actor {:asserted-by "llm:opus-4-8/x" :written-by "agent:claude-code/session-y"}
   :entry/detail {:kind :confirms :status :asserted :relation-id "rel:v2"
                  :from {:kind :block :id "du:9fdoc:000003"}
                  :to {:kind :doc :id "oc:doc:evidence-1"}}}

  ;; ── R-2 extension (CONTRACT_R2 G3 fixture note: extend, never invent
  ;;    shapes; every entry below mirrors the live constructors in
  ;;    trail_view.clj - source-activity-entry for commit artifacts
  ;;    (source-ref "git-commit:<sha>", display-name = sha7),
  ;;    relation-activity-entry for the marks) ─────────────────────────────

  ;; the 3-commit :based-on chain (git-spine §3.A commit artifacts).
  ;; cmt-a3 is claimed EARLIEST but arrived LAST (a back-dated commit) so
  ;; :order claimed vs :order arrival flip within the thread (gate 8).
  {:entry/kind :source-ingested
   :entry/target {:id "oc:doc:cmt-a1" :kind :doc :display-name "aaa1111"}
   :entry/address (trail/context-bundle {:targets ["oc:doc:cmt-a1"]})
   :time/claimed-ms 1782000000000
   :time/arrival-ms 1782167000000
   :entry/actor {:asserted-by nil :written-by nil}
   :entry/detail {:source-ref "git-commit:aaa1111b2c3d4e5f60718293a4b5c6d7e8f9012" :derived-unit-count 1}}

  {:entry/kind :source-ingested
   :entry/target {:id "oc:doc:cmt-a2" :kind :doc :display-name "aaa2222"}
   :entry/address (trail/context-bundle {:targets ["oc:doc:cmt-a2"]})
   :time/claimed-ms 1782010000000
   :time/arrival-ms 1782167100000
   :entry/actor {:asserted-by nil :written-by nil}
   :entry/detail {:source-ref "git-commit:aaa2222b2c3d4e5f60718293a4b5c6d7e8f9012" :derived-unit-count 1}}

  {:entry/kind :source-ingested
   :entry/target {:id "oc:doc:cmt-a3" :kind :doc :display-name "aaa3333"}
   :entry/address (trail/context-bundle {:targets ["oc:doc:cmt-a3"]})
   :time/claimed-ms 1781990000000
   :time/arrival-ms 1782167200000
   :entry/actor {:asserted-by nil :written-by nil}
   :entry/detail {:source-ref "git-commit:aaa3333b2c3d4e5f60718293a4b5c6d7e8f9012" :derived-unit-count 1}}

  ;; the chain's lineage marks (relation-activity shape, entry 0 precedent)
  {:entry/kind :relation-transition
   :entry/target {:id "oc:doc:cmt-a2" :kind :doc}
   :entry/address (trail/context-bundle {:targets ["oc:doc:cmt-a2"]})
   :time/claimed-ms 1782167150000
   :time/arrival-ms 1782167150000
   :entry/actor {:asserted-by "import:git-spine" :written-by nil}
   :entry/detail {:kind :based-on :status :asserted :relation-id "rel:a21"
                  :from {:kind :doc :id "oc:doc:cmt-a2"}
                  :to {:kind :doc :id "oc:doc:cmt-a1"}}}

  {:entry/kind :relation-transition
   :entry/target {:id "oc:doc:cmt-a3" :kind :doc}
   :entry/address (trail/context-bundle {:targets ["oc:doc:cmt-a3"]})
   :time/claimed-ms 1782167250000
   :time/arrival-ms 1782167250000
   :entry/actor {:asserted-by "import:git-spine" :written-by nil}
   :entry/detail {:kind :based-on :status :asserted :relation-id "rel:a32"
                  :from {:kind :doc :id "oc:doc:cmt-a3"}
                  :to {:kind :doc :id "oc:doc:cmt-a2"}}}

  ;; a CROSS-LINK between the two threads (G4: :references NEVER merges
  ;; lanes - it renders as a kraft connector BETWEEN them)
  {:entry/kind :relation-transition
   :entry/target {:id "oc:doc:cmt-a1" :kind :doc}
   :entry/address (trail/context-bundle {:targets ["oc:doc:cmt-a1"]})
   :time/claimed-ms 1782167300000
   :time/arrival-ms 1782167300000
   :entry/actor {:asserted-by "sid" :written-by nil}
   :entry/detail {:kind :references :status :asserted :relation-id "rel:xr1"
                  :from {:kind :doc :id "oc:doc:cmt-a1"}
                  :to {:kind :doc :id "oc:doc:earlier"}}}

  ;; the fold family (G5): a doc + marks on its du: blocks - one card
  {:entry/kind :source-ingested
   :entry/target {:id "oc:doc:folddoc" :kind :doc :display-name "notes.md"}
   :entry/address (trail/context-bundle {:targets ["oc:doc:folddoc"]})
   :time/claimed-ms 1782100000000
   :time/arrival-ms 1782168800000
   :entry/actor {:asserted-by nil :written-by nil}
   :entry/detail {:source-ref "docs/notes.md" :derived-unit-count 2}}

  {:entry/kind :relation-transition
   :entry/target {:id "du:folddoc:000001" :kind :block}
   :entry/address (trail/context-bundle {:targets ["du:folddoc:000001"]})
   :time/claimed-ms 1782168900000
   :time/arrival-ms 1782168950000
   :entry/actor {:asserted-by "llm:opus-4-8/x" :written-by "agent:claude-code/session-y"}
   :entry/detail {:kind :confirms :status :asserted :relation-id "rel:fd1"
                  :from {:kind :block :id "du:folddoc:000001"}
                  :to {:kind :doc :id "oc:doc:evidence-2"}}}

  {:entry/kind :relation-transition
   :entry/target {:id "du:folddoc:000002" :kind :block}
   :entry/address (trail/context-bundle {:targets ["du:folddoc:000002"]})
   :time/claimed-ms 1782168960000
   :time/arrival-ms 1782168970000
   :entry/actor {:asserted-by "llm:opus-4-8/x" :written-by "agent:claude-code/session-y"}
   :entry/detail {:kind :confirms :status :asserted :relation-id "rel:fd2"
                  :from {:kind :block :id "du:folddoc:000002"}
                  :to {:kind :doc :id "oc:doc:evidence-3"}}}

  ;; two unedged docs (G3): band material - no lineage edges, never a lane
  {:entry/kind :source-ingested
   :entry/target {:id "oc:doc:loose-1" :kind :doc :display-name "loose-1.md"}
   :entry/address (trail/context-bundle {:targets ["oc:doc:loose-1"]})
   :time/claimed-ms 1782110000000
   :time/arrival-ms 1782168600000
   :entry/actor {:asserted-by nil :written-by nil}
   :entry/detail {:source-ref "docs/loose-1.md" :derived-unit-count 1}}

  {:entry/kind :source-ingested
   :entry/target {:id "oc:doc:loose-2" :kind :doc :display-name "loose-2.md"}
   :entry/address (trail/context-bundle {:targets ["oc:doc:loose-2"]})
   :time/claimed-ms 1782120000000
   :time/arrival-ms 1782168400000
   :entry/actor {:asserted-by nil :written-by nil}
   :entry/detail {:source-ref "docs/loose-2.md" :derived-unit-count 1}}]

 ;; the STANDING declaration (WP1 s6 named gaps) - gate 11 checks presence.
 ;; Shape matches the LIVE feed (gate-16 verified): :omission/kind +
 ;; :feed/gap keywords, :reason is a string.
 :feed/omissions
 [{:omission/kind :feed/uncovered :feed/gap :editor-revisions
   :reason "direct editor ObjectEditPayload revisions are not projected as feed activity (D-008: CLI is the write surface)"}
  {:omission/kind :feed/uncovered :feed/gap :session-metadata
   :reason "session model/session_id metadata is thin on the OC path; the feed renders what exists"}]}
