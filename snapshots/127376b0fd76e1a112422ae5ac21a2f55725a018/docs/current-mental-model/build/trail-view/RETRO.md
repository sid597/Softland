# WP1 trail-view — RETRO (light)

Closed 2026-07-05 by Fable under Sid's in-session time-box blanket ("use the
recommended option, note the others"). The alternative not taken: holding the
close for a dedicated retro session — rejected because the gate artifacts
already carry the detail; this retro is deliberately light and assertion-grade,
sourced from GATE_REVIEW.md, DIFF_FALSIFICATION_R1.md, the decisions.md D-006
notes, and the baton. It re-derives nothing.

## What the package proved

- Contract → phased impl → falsification → gate **inside 24 hours** under the
  amended (collapsed-cadence) process — the second consecutive package to do
  so. The 2026-07-05 "fresh context ≠ fresh session" ruling originated from
  this package's felt cost and is now the default shape.
- Criterion-3 (implementation contact): **Phase B survived with ZERO contract
  amendments.** Phase-0's F-1 conflict was caught at text-time and swept into
  CONTRACT v1.1 before any plan/code tokens (letter-ding / spirit-held,
  pre-recorded in the D-006 notes).
- 16/16 gates; final suite 3 tests / 302 assertions / 0 failures, re-run twice
  at gate. One gate gap found AT gate (gate 10's custody projection had no
  executed assertion) — closed by a reviewer-authored test-only block, green
  first run.
- Client-composition path ruled CONTRACT-SANCTIONED (§7 pre-named fallback).
  Platform finding banked: Rama 1.6.0 mirror `local-select>` rejects
  `:allow-yield?` (implementation-quirks updated in-session).

## Carried forward (not lost by closing)

- **8 non-blocking open doubts** with named falsifiers — live in
  GATE_REVIEW.md (top three: `:written-by` present-with-nil vs "omitted";
  text-projection arrow direction; feed-address round-trip untested).
- Display-name enrichment (F-L3, the importer-enrichment gap) — assigned to
  the WP2 window.
- Write-path pair — /assert affordance + OI-1 durable-runtime call —
  scheduled 2026-07-05 (see baton); H1's CONFIRM leg depends on it.

## Process lessons (delta over relation-kernel RETRO — that file still holds)

- **Double-dispatch collision**: two builders were dispatched onto Phase B
  across parallel sessions; one stood down with zero writes. Rule going
  forward: one orchestrating session OWNS dispatch; concurrent sessions own
  disjoint file sets, declared at start.
- Baton NOW ≤15 lines held. Per-round FAIL artifacts never overwritten held.
- Grep, never trust cited line numbers: kernel line numbers shifted 3× during
  the package (already encoded in the baton anchors; repeating because it
  fired repeatedly).
