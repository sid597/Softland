Source: `docs/current-mental-model/BETS.md`, section "The ladder → **H1 — ACTIVE · The trail view beats the wall**" (with a clarifying entry in the same file's "Verdict log").

H1's pre-registered evidence, as stated in the H1 entry:

- **KILL:** Two weeks after the view first renders (relations + timeline working), if new thinking panels still start on paper AND wall photos still get pasted into LLM chats. Grace clause: form-break fix cycles are the design (D-005), not failure — the clock measures *where new thinking starts*, not polish.

- **CONFIRM:** The photograph-and-paste ritual is replaced by context bundles from the view; at least half of new panels/threads start inside Softland.

Two clarifications from the same file that bear on when this clock runs:
- The **claim** H1 is testing: a trail view over Softland's own material (transcripts, docs, commits, typed relations) becomes Sid's daily sensemaking surface, beating the paper wall.
- The **arming event** (BETS.md "Verdict log," 2026-07-05, ruled by Fable under Sid's time-box authority and reversible on his review): the two-week clock does NOT start at first render. It arms at "the first render showing ≥1 typed relation over real material — import-derived or sid-via-agent (D-008) both count." This resolves an ambiguity between the KILL text's "(relations + timeline working)" condition and an earlier placeholder that read "starts when the trail view first renders."
