# Q: Has the H1 kill-clock started, and what event starts it?

## Answer

**No — the H1 kill-clock has NOT started** (as of the frozen snapshot, 2026-07-05). It was explicitly ruled *not started* at first light, and it **starts (arms) at the first render showing ≥1 typed relation over real material** — an *event*, not a date. Import-derived relations or sid-via-agent (D-008) relations both count.

## Detail and citations

1. **The ruling that the clock has not started** — `docs/current-mental-model/BETS.md`, "Verdict log (append-only)", entry **2026-07-05 — H1 clock: NOT started at first light; arming event pre-registered.** At first light (2026-07-05) the timeline rendered 167 docs with **zero typed relations** over real material (the F-L2 confetti finding), while H1's claim names "transcripts, docs, commits, typed relations." Starting the clock there would invite an ambiguous kill (form failed vs. nouns missing), so it was not started.

2. **The arming event, verbatim** — same BETS.md verdict-log entry: *"ARMING EVENT (an event, not a date): the clock arms at the first render showing ≥1 typed relation over real material — import-derived or sid-via-agent (D-008) both count."*

3. **Corroborated in** `docs/current-mental-model/build/view-mvp/FIRST_LIGHT.md`, "Addendum 2026-07-05 — clock ruling": the clock did NOT start at first light (zero typed relations rendered, F-L2), because the pre-registered KILL text conditions the window on "relations + timeline working." Arming event pre-registered: "**the first render showing ≥1 typed relation over real material** (import-derived or sid-via-agent both count)." FIRST_LIGHT.md remains the first-light record; the arming-event entry in BETS.md will be the clock-start record.

4. **Authority / status of the ruling** — Ruled by Fable under Sid's same-day in-session time-box authority ("use the recommended option but also note down others so I can review"); it is **reversible on Sid's review** (BETS.md verdict log; FIRST_LIGHT.md addendum). An alternative was recorded but not taken: starting the clock 2026-07-05 at first render with a grace clause absorbing the relations gap — rejected for the ambiguity above.

5. **What the H1 clock measures once armed** — `docs/current-mental-model/BETS.md`, "H1 — ACTIVE" section: **KILL** = two weeks after the view first renders (relations + timeline working), new thinking panels still start on paper AND wall photos still get pasted into LLM chats; **CONFIRM** = the photograph-and-paste ritual is replaced by context bundles from the view, with at least half of new panels/threads starting inside Softland.

6. **Arming is still pending as of the snapshot** — `docs/sessions/next-prompt.md` (baton, "NEXT (in order)", item 1): Sid's app boot runs replay→spine-sync→extract (ingesting commits + parent edges, i.e. the first typed relations over real material); "Threads render → screenshot the first kraft connector = the **H1 ARMING RECORD** (BETS verdict-log arming event)." This is listed as a not-yet-done next step, confirming the clock had not yet armed at the time of the snapshot.
