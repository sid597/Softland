# Answer

**NOT DERIVABLE FROM PROVIDED MATERIAL** (as a single commit).

## What the material does establish

The "read-only trail-view module (client wrappers + View-3 text projection)" is the WP1 (Track A) trail-view data-layer deliverable:

- The module is the new file `src/app/server/rama/trail_view.clj`, ns `app.server.rama.trail-view`, declared read-only by construction (no depots, no ETL topologies — gate 14). Source: `build/trail-view/CONTRACT.md` §2.
- Its §7 client wrappers (`read-context-bundle`, `read-recent-activity`, `read-conversation-trail`, `read-relation-detail`, `->address`/`resolve-address`, `current-verdicts`, `render-bundle-text`) and its §8 View-3 text projection (`render-bundle-text`, the `;; trail-text v0` face) are named in `build/trail-view/CONTRACT.md` §7–§8.
- This was built in WP1 Phase B and the package was CLOSED 2026-07-05.

On commits, the WP1 close entry states: "Code was already committed (`af0e0e2`/`fd59b78`/`63202b0`/`67f75eb`)". Source: `docs/sessions/next-prompt.md` (WP1 package CLOSED entry, 2026-07-05); the same four SHAs are consistent with `decisions.md` D-006 note and `build/trail-view/GATE_REVIEW.md` references.

## Why the exact single commit is not derivable

The material lists **four** WP1 commits (`af0e0e2`, `fd59b78`, `63202b0`, `67f75eb`) as "the code," but provides **no per-commit file/phase breakdown**. WP1 spans both the relation-kernel amendments (Phase A: custody/stance/activity in `relation_kernel.clj`) and the trail-view module (Phase B: `trail_view.clj`). Nothing in the provided files attributes `trail_view.clj` (the module + client wrappers + View-3 projection) to one specific SHA among the four.

(Distinct and not the answer: the later git-spine WP2 wave commit `a1fda21` "names P3+G11" *amended* `trail_view.clj` for display-name enrichment and the G11 test — it did not *produce* the module. Source: `docs/sessions/next-prompt.md` git-spine wave commit list.)

## What would be needed

A per-commit content mapping for the WP1 commit set — i.e., which of `af0e0e2`/`fd59b78`/`63202b0`/`67f75eb` introduced `src/app/server/rama/trail_view.clj` (e.g., the commit messages or `git show --stat` for those SHAs, or a RETRO/GATE_REVIEW line assigning the module to a specific commit). None of that per-commit attribution is present in the provided material.
