NOT DERIVABLE FROM PROVIDED MATERIAL

## What the material does establish

Custody recording — the addition of `envelope-actor-id` + `envelope-actor-type` fields to `RelationDecisionRow`, `RelationEventRow`, and `RelationEdgeRow`, copied verbatim from the envelope `:actor` at decision time — was introduced by the **trail-view WP1 Phase A1 amendment** to `relation_kernel.clj` (docs/current-mental-model/build/trail-view/CONTRACT.md §5.1 "Custody recording (I-7, I-14 — hard-gated BEFORE phase-1 daily use)"; sequenced FIRST as Phase A1). It was NOT in the original relation-kernel package: §5.1 records "Verified 2026-07-04: the kernel persists only the payload's `asserter-actor-id`; the envelope `:actor` is checked for retraction rights and dropped," and decisions.md (D-008; object-kernel-revision ruling) repeats that as of 2026-07-04 the kernel "persists only the payload asserter."

WP1's code was committed as part of the WP1 package close:
- decisions.md, D-006 evaluation notes, 2026-07-05 "WP1 package CLOSED" entry: "Code was already committed (`af0e0e2`/`fd59b78`/`63202b0`/`67f75eb`)."
- docs/sessions/next-prompt.md corroborates WP1 code (relation_kernel.clj custody fields among them) was committed at close.

## Why the specific commit is not derivable

The material lists the four WP1 commit hashes (`af0e0e2` / `fd59b78` / `63202b0` / `67f75eb`) as a group but does **not** map any single one of them to the Phase A1 custody amendment specifically (as opposed to Phase A2 stance kinds, Phase A3 activity+R3, or Phase B). Unlike the git-spine WP2 commits, which the baton labels per phase ("eac6dd5 spine P1P2 · ed2db8a route PW · a1fda21 names P3+G11 …"), the WP1 commits carry no per-phase attribution in the provided text.

## What would be needed

A per-commit changelog / commit-message breakdown for the WP1 close commits (`af0e0e2`, `fd59b78`, `63202b0`, `67f75eb`) — or the WP1 SESSIONS.md / GATE_REVIEW.md commit mapping — showing which hash carries the §5.1 custody-field change to `relation_kernel.clj`. Alternatively, `git log`/`git blame` over the `RelationDecisionRow`/`RelationEventRow`/`RelationEdgeRow` `envelope-actor-id` field, neither of which is included in the provided material.
