# Answer

**Where:** The git-spine work package's verified fact base is `INPUTS.md`, located in the same directory as the git-spine CONTRACT — i.e. `docs/current-mental-model/build/git-spine/INPUTS.md`.

**What caution builders carry about it:** Builders must **re-grep every cite before relying on it, because line numbers drift** — but the underlying facts stand (and the fact base is never to be re-gathered).

## Support

- `docs/current-mental-model/build/git-spine/CONTRACT.md` (v1.2 header paragraph) states: "`INPUTS.md` (same dir) is the verified fact base — builders **re-grep every cite before relying on it** (line numbers drift); the facts stand."
- `docs/current-mental-model/build/git-spine/INPUTS.md` (header): "Gathered 2026-07-05 by a fresh Opus recon agent (dispatched from the Fable HQ session), every load-bearing claim verified by direct read. **Never re-gather — re-grep cites before relying on them (line numbers drift), but the facts stand.**"
- `docs/current-mental-model/build/git-spine/CONTRACT.md` §8 (Builder verification duties), item 1: "Re-grep every INPUTS cite you rely on."
