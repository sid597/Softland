# Which face of the trail view renders first, and why?

**Answer: View 3 — the AI-agent-legible text projection (the "View 3 text bundles" / minimal text projection) renders first.**

## The ruling

`docs/current-mental-model/decisions.md` (Open questions queued for ruling — "Which face of the trail view renders first") records this as **RULED 2026-07-04** by Fable, on Sid's explicit delegation via Roam card ("I am not sure you decide"):

- **View 3 first** — "it is nearly coextensive with the data contract itself (context bundles + the two query topologies; a minimal text projection, not a pixel investment)."
- **The threaded/DAG timeline face second** (the 27-04 outline form) — "as Sid's first pixel surface for the read-only screenshot loop (INPUTS item 13/14)."
- **Canvas last** — "parked until the wall-replacement ambition earns activation."

## Why (grounds, verbatim from the ruling)

"**the first paying reader is the agent; the Sid-face rides the same queries View 3 defines, so the ordering costs nothing extra.**"

In other words, View 3 renders first because:
1. It is nearly the same artifact as the trail-view data contract itself (context bundles plus the two query topologies), requiring only a minimal text projection rather than any pixel investment.
2. The first paying reader is the agent (View 3 = agent-legibility).
3. The second face (Sid's threaded/DAG timeline) reuses the very queries View 3 defines, so choosing View 3 first adds no extra cost.

The earlier framing captured alongside the open question notes the cost ordering: "View 3 agent-legibility is cheapest; View 1 outline; View 2 canvas replaces the wall but costs most."

## Corroborating material

- `docs/current-mental-model/build/trail-view/CONTRACT.md` §1 (Consumers, in order): (1) **Agents — View 3 text bundles + the queries directly** ("The load-bearing face: minimal-token legibility"); (2) **Sid's threaded/DAG timeline face — second face, same queries, first pixel surface** ("face-order ruling, decisions.md 2026-07-04"); (3) DG-plugin teammates (later).
- `docs/sessions/next-prompt.md` (Interim working protocol, item 9): "Face order also RULED 2026-07-04 on Sid's delegation: **View 3 → threaded/DAG timeline → canvas** (decisions.md Open Questions)."
- `CLAUDE.md` / `docs/current-mental-model/decisions.md` D-002 also frames Panel 2's "View 3: AI agent" as part of the same first form — "the material must be legible to agents, not only to Sid."
