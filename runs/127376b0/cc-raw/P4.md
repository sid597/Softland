# Answer

**Who ruled it:** Fable.

**Under what authority:** under Sid's in-session time-box blanket (recorded as "use the recommended option, note the others" / "adopt recommended, note alternatives"), and the ruling is explicitly **reversible on Sid's review** — i.e., Fable ruled it, but not as a Sid-countersigned closure.

**What was decided:** The transcript↔commit join is represented as **durable import-asserted RelationEdges** (not projection-time joins). Specifically:
- `asserter-type :import`
- version-free actor-id `"import:git-spine"` (extractor version rides in `note` only, so relation-ids don't fork on upgrade)
- evidence refs pointing to the transcript **session + entry**
- shas **verified against the actual repo before asserting** (mention-only / unverified shas never asserted)
- exactness carried via a documented **note-grammar v1** (`note = "spine-v1|<basis>"`, basis ∈ `sha-verified` | `parent` | `file-write`), promoted to a structured kernel field only when a view demands rendering it (D-001)

**Alternative recorded but NOT taken:** projection-time joins — they have no storage and no staleness, but cannot carry `asserted-by` / exactness / retraction and would need new query topologies per join type.

The same ruling also answers the exactness-flag-semantics question queued alongside it (via note-grammar v1).

## Sources (all from the provided file)
- `docs/current-mental-model/decisions.md`, "Open questions queued for ruling" section — the bullet "**Transcript↔commit join representation — RULED 2026-07-05** (Fable, under Sid's in-session time-box blanket; reversible on his review)". This is the primary, direct statement of who ruled, the authority, and the decision.
- `docs/current-mental-model/build/git-spine/CONTRACT.md` §2 (Adjudications), item **2.1 "Durable edges, not projection-time joins"** and §2.4 (import actor identity version-free) — the full grounds/traps the decisions.md entry cites.
- `docs/sessions/next-prompt.md`, Cross-track section (Track A) — corroborates the same item as a "key adjudication queued: durable asserted edge vs projection-time query for transcript↔commit joins, + exactness-flag semantics."
