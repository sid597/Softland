# Answer to: The relation-activity projection + R3 query — which WP phase produced it, and in which commit did it land?

## Part 1 — Which WP phase produced it: DERIVABLE

**Phase A3 of the trail-view WP1 work package** ("A3 activity + R3").

The relation-activity projection + R3 query is defined in the trail-view WP1
contract as §5.3 "Relation-activity projection + R3 (feeds §6)" — introducing
`RelationActivityRow`, the `$$relation-activity-by-bucket` PState, and "R3 query
topology `relation-activity`" (FILE: `docs/current-mental-model/build/trail-view/CONTRACT.md` §5.3).

The work-package handoff maps this section to a specific phase:

> "**Phase A** (relation kernel): A1 custody (§5.1, FIRST), A2 stance kinds
> (§5.2), A3 activity + R3 (§5.3) — gates 9, 10, 15, 16."
> (FILE: `docs/current-mental-model/build/trail-view/CONTRACT.md` §12, Work-package handoff)

The baton confirms the phase was completed:

> "**State:** Phase A COMPLETE (A1 custody, A2 stance kinds, A3 activity+R3);
> gates 9, 10-kernel-half, 15, 16 green..."
> (FILE: `docs/sessions/next-prompt.md`, Track A NOW section)

So the producing phase is **A3** (the third relation-kernel-amendment phase of
trail-view WP1).

## Part 2 — In which commit did it land: NOT DERIVABLE FROM PROVIDED MATERIAL to a single commit

The material states the trail-view WP1 code was committed across **four**
commits, listed collectively without a per-phase/per-file breakdown:

> "2026-07-05, WP1 package **CLOSED** ... Code was already committed
> (`af0e0e2`/`fd59b78`/`63202b0`/`67f75eb`)..."
> (FILE: `docs/current-mental-model/decisions.md`, D-006 evaluation notes, 2026-07-05 WP1 CLOSED entry)

The A3 relation-activity + R3 amendment lands in `relation_kernel.clj` (per
CONTRACT §5 and §12: "Amended `src/app/server/rama/relation_kernel.clj` ... the
accepted-branch activity write, R3"), which is part of WP1. Therefore the
commit is one of those four WP1 code commits, but **the provided material does
not map the A3 amendment to a specific one of `af0e0e2` / `fd59b78` / `63202b0`
/ `67f75eb`** — they are given as an undifferentiated set.

(Note for disambiguation: the per-phase commit list in the delivery-session
baton — `eac6dd5 spine P1P2 · ed2db8a route PW · a1fda21 names P3+G11 · dc743d6
render R-1+clip fix · 7827f6f · 57b0113` — is the git-spine WP2 + trail-room
wave, NOT trail-view WP1; none of those is the A3 relation-activity+R3 work.)

**What would be needed** to pin the exact commit: a per-commit or per-phase file
mapping for the four WP1 commits (e.g., which of `af0e0e2`/`fd59b78`/`63202b0`/
`67f75eb` contains the `relation_kernel.clj` A3 amendment), which the provided
material does not include.
