# Answer to Question: Git-Spine Work Package Verified Fact Base

## Location
The git-spine work package's verified fact base is in `docs/current-mental-model/build/git-spine/INPUTS.md`.

## Citation
From CONTRACT.md (§1, lines 574-598):
> `INPUTS.md` (same dir) is the verified fact base — builders **re-grep every cite before relying on it** (line numbers drift); the facts stand.

From INPUTS.md header (lines 920-924):
> Gathered 2026-07-05 by a fresh Opus recon agent (dispatched from the Fable HQ session), every load-bearing claim verified by direct read. **Never re-gather — re-grep cites before relying on them (line numbers drift), but the facts stand.**

## Caution for Builders
Builders must **re-grep every cite before relying on it** because **line numbers drift**. However, the verified facts themselves stand as authoritative — the facts were gathered by direct read and are valid; only the line-number citations may shift as code evolves.

The caution is explicitly stated as a procedure: re-grep to locate current line numbers, but trust the facts recorded in INPUTS.md as the verified ground truth for this work package.
