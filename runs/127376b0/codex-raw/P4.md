Fable ruled it on 2026-07-05, under Sid’s in-session time-box blanket authority: “use the recommended option, note the others”; the ruling was recorded as reversible on Sid’s review. (docs/current-mental-model/decisions.md — “Open questions queued for ruling” / “Transcript↔commit join representation”; docs/current-mental-model/build/git-spine/CONTRACT.md §2 “Adjudications”)

The decision was: transcript→commit joins are durable, import-asserted `RelationEdge`s, not projection-time joins. They use `asserter-type :import` with version-free actor id `"import:git-spine"`, carry evidence refs to the transcript session and entry, assert only repository-verified SHAs, and encode exactness in the v1 note grammar, e.g. `note = "spine-v1|sha-verified"`. The alternative, projection-time joins, was explicitly recorded but rejected because it could not carry `asserted-by` / custody / exactness / retraction semantics and would require new query topology per join type. (docs/current-mental-model/decisions.md — “Open questions queued for ruling” / “Transcript↔commit join representation”; docs/current-mental-model/build/git-spine/CONTRACT.md §2.1 “Durable edges, not projection-time joins”, §2.2 “Exactness rides note”, §2.4 “Import actor identity is VERSION-FREE”)