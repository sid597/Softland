View 3, the agent-legibility face, renders first. More precisely: the first face is the View-3 text/context-bundle projection for agents, not Sid’s graphical timeline. The threaded/DAG timeline face comes second as Sid’s first pixel surface, and the canvas/wall-replacement face is parked last.

Why: the 2026-07-04 ruling says View 3 is “nearly coextensive with the data contract itself” because it is just context bundles, the two query topologies, and a minimal text projection rather than a pixel/UI investment. The same ruling gives the core reason: “the first paying reader is the agent,” and Sid’s later face rides the same queries View 3 defines, so doing View 3 first “costs nothing extra.” The trail-view contract matches this ordering: consumers are listed first as “Agents — View 3 text bundles + the queries directly,” with “minimal-token legibility,” and second as “Sid’s threaded/DAG timeline face,” the first pixel surface. (prompt.txt, `docs/current-mental-model/decisions.md`, “Open questions queued for ruling”; prompt.txt, `docs/current-mental-model/build/trail-view/CONTRACT.md`, §1 “Purpose and scope” and §8 “View-3 text projection”)