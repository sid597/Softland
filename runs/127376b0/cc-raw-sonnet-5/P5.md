**Question:** The relation-activity projection + R3 query: which WP phase produced it, and in which commit did it land?

**Phase — derivable:**
The relation-activity projection and its R3 query were produced by **Phase A3** ("activity + R3") of **WP1, the trail-view data contract**.

- `docs/current-mental-model/build/trail-view/CONTRACT.md` §5.3 is titled "Relation-activity projection + R3 (feeds §6)" and defines `RelationActivityRow`, `$$relation-activity-by-bucket`, and "R3 query topology `relation-activity`."
- CONTRACT.md §12 ("Work-package handoff") explicitly assigns it to a phase: "**Phase A** (relation kernel): A1 custody (§5.1, FIRST), A2 stance kinds (§5.2), **A3 activity + R3 (§5.3)** — gates 9, 10, 15, 16."
- `docs/sessions/next-prompt.md`, under the trail-view WP1 "NOW" log, confirms completion: "**State:** Phase A COMPLETE (A1 custody, A2 stance kinds, **A3 activity+R3**); gates 9, 10-kernel-half, 15, 16 green; suite 2 tests / 222 assertions / 0 failures; code + docs UNCOMMITTED (commits on Sid's word)."

**Commit — NOT DERIVABLE FROM PROVIDED MATERIAL** at the single-commit level.

The material establishes that WP1 as a whole (both Phase A, which includes A3, and Phase B) was committed as a set of four commits, per `docs/current-mental-model/decisions.md` (D-006 evaluation notes, 2026-07-05 WP1 package CLOSED entry): "Code was already committed (`af0e0e2`/`fd59b78`/`63202b0`/`67f75eb`)." No text in the provided material maps Phase A3 specifically to one of these four hashes (versus, e.g., Phase B's `trail_view.clj` work) — the commits are listed together as "WP1" without a phase-to-commit breakdown.

What would be needed to answer at the single-commit level: a phase-scoped artifact or commit log (e.g., a `PHASE_A3.md` phase artifact, or `git show`/`git log` output for `af0e0e2`, `fd59b78`, `63202b0`, `67f75eb`) showing which of the four commits actually contains the `relation_kernel.clj` changes for §5.3 (the `RelationActivityRow`, `$$relation-activity-by-bucket`, and R3 query addition) — none of which is included in this provided snapshot.
