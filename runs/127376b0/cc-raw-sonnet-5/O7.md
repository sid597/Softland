**Answer: View 3 (the agent-legible text projection) renders first.**

Per `docs/current-mental-model/decisions.md`, "Open questions queued for ruling" — "Which face of the trail view renders first" — **RULED 2026-07-04** (by Fable, on Sid's explicit delegation via Roam card, "I am not sure you decide"):

> **View 3 first** — it is nearly coextensive with the data contract itself (context bundles + the two query topologies; a minimal text projection, not a pixel investment); **the threaded/DAG timeline face second** (the 27-04 outline form) as Sid's first pixel surface for the read-only screenshot loop (INPUTS item 13/14); **canvas last**, parked until the wall-replacement ambition earns activation. Grounds: the first paying reader is the agent; the Sid-face rides the same queries View 3 defines, so the ordering costs nothing extra.

This is corroborated in `docs/current-mental-model/build/trail-view/CONTRACT.md` §1 ("Purpose and scope"), which lists consumers in order:

> 1. **Agents** — View 3 text bundles + the queries directly (H2 handoff, H3 machine benchmark). The load-bearing face: minimal-token legibility (I-3).
> 2. **Sid's threaded/DAG timeline face** — second face, same queries, first pixel surface (face-order ruling, decisions.md 2026-07-04).
> 3. **DG-plugin teammates** (H3 human half, later; adapter-shaped).

**Why View 3 first, in summary:**
- It requires no separate build — it is essentially the data contract itself (context bundles + the two query topologies) plus a deterministic text renderer (`render-bundle-text`, CONTRACT.md §8), not a pixel/UI investment.
- The first paying reader of the trail view is an agent, not Sid, so the agent-facing face is the load-bearing one and is prioritized.
- Because the Sid-facing threaded/DAG timeline face consumes the *same* query wrappers that View 3 defines, sequencing it second "costs nothing extra."
- Canvas (View 2, "replaces the wall") is explicitly last, parked until the wall-replacement ambition "earns activation."

This ordering also appears reflected in practice in `docs/sessions/next-prompt.md`, where WP1 (the data/View-3-adjacent contract) and its gates (e.g., gate 13, the View-3 text-projection style gate) are closed before the pixel-facing view-MVP (WP-B2, the threaded/DAG timeline face) proceeds to integration.
