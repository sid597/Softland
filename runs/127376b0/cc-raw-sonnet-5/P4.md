**Who ruled it, under what authority:** Fable ruled it, on 2026-07-05, under Sid's in-session time-box blanket ("use the recommended option, note the others" — the same delegated authority invoked elsewhere in this session). The ruling is recorded as reversible on Sid's later review.

**What was decided:** the transcript↔commit join is represented as **durable import-asserted RelationEdges** — not projection-time joins. Specifically:
- `asserter-type :import`
- version-free actor-id `"import:git-spine"`
- evidence refs pointing to the transcript session + entry
- shas verified against the repo before asserting (only repo-verified shas are asserted)
- exactness carried via a documented note-grammar v1 (`note = "spine-v1|<basis>"`, basis ∈ `sha-verified` | `parent` | `file-write`), to be promoted to a structured kernel field only when a rendered view actually demands rendering exactness differently (D-001 grounds)

**Alternative recorded, not taken:** projection-time joins — these would need no storage and incur no staleness, but cannot carry `asserted-by`/exactness/retraction, and would require new query topologies per join type.

The exactness-flag semantics question queued alongside this one was answered by the same ruling (note-grammar v1).

**Citations:**
- `docs/current-mental-model/decisions.md`, "Open questions queued for ruling" section, entry beginning "**Transcript↔commit join representation — RULED 2026-07-05**": "(Fable, under Sid's in-session time-box blanket; reversible on his review): **durable import-asserted RelationEdges** — asserter-type `:import`, version-free actor-id `"import:git-spine"`, evidence refs to transcript session + entry, shas verified against the repo before asserting, exactness via a documented note-grammar v1 ... Alternative recorded, not taken: projection-time joins ... Full grounds and traps: `build/git-spine/CONTRACT.md` §2."
- `docs/current-mental-model/build/git-spine/CONTRACT.md` §2.1 ("Durable edges, not projection-time joins") and §2.2 ("Exactness rides `note`, grammar v1") and §2.4 (import actor identity is version-free) corroborate the same ruling and its grounds/traps in full.
