**Answer:** The git-spine work package's verified fact base is `docs/current-mental-model/build/git-spine/INPUTS.md`.

**Caution builders carry:** Line numbers drift, so builders must **re-grep every cite before relying on it** — but the underlying facts themselves stand, and the file must **never be re-gathered**.

**Citations:**

- FILE: `docs/current-mental-model/build/git-spine/CONTRACT.md` (header/preamble): "Binding together with `decisions.md`. `INPUTS.md` (same dir) is the verified fact base — builders **re-grep every cite before relying on it** (line numbers drift); the facts stand."

- FILE: `docs/current-mental-model/build/git-spine/INPUTS.md` (header): "Gathered 2026-07-05 by a fresh Opus recon agent (dispatched from the Fable HQ session), every load-bearing claim verified by direct read. **Never re-gather — re-grep cites before relying on them (line numbers drift), but the facts stand.**"

- FILE: `docs/current-mental-model/build/git-spine/CONTRACT.md` §8 ("Builder verification duties (before writing code)"), item 1: "Re-grep every INPUTS cite you rely on." — this is the operational instruction builders follow, consistent with the caution above.
