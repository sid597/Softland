**Answer:** Not a single commit — the material records the whole WP1 package (which is what produced the read-only trail-view module: `trail_view.clj`, the client wrappers, and the View-3 text projection, i.e. CONTRACT.md's "Phase B") as closed and "already committed" across **four** commit hashes, given together with no per-hash breakdown:

> "2026-07-05, WP1 package **CLOSED** (executed by Fable under Sid's in-session
> time-box blanket ... Code was already committed (`af0e0e2`/`fd59b78`/`63202b0`/`67f75eb`); light retro at `build/trail-view/RETRO.md`..."
> — `docs/current-mental-model/decisions.md`, D-006 evaluation notes, 2026-07-05 entry "WP1 package CLOSED"

Context establishing that these four commits are the ones covering the trail-view module/wrappers/text-projection specifically (not just the relation-kernel amendments):
- `docs/current-mental-model/build/trail-view/CONTRACT.md` §12 "Work-package handoff" defines the deliverable in two phases: "**Phase A** (relation kernel): A1 custody, A2 stance kinds, A3 activity + R3" and "**Phase B** (trail-view module + wrappers + text projection + fixtures) — remaining gates." Both phases belong to the one WP1 package that decisions.md records as closed via those four commits.
- `docs/sessions/next-prompt.md` (WP1 track NOW log, 2026-07-05 entries) confirms Phase B (trail_view.clj + wrappers + text projection) went green and gate-reviewed PASS in the same session sequence that decisions.md then records as "package CLOSED."

**What is NOT derivable:** which of the four hashes (`af0e0e2`, `fd59b78`, `63202b0`, `67f75eb`) is specifically "the" commit for the trail-view module/wrappers/text-projection versus the relation-kernel custody/stance/activity amendments (Phase A) or docs/retro bookkeeping. Unlike the git-spine WP2 commit list in `next-prompt.md` (which labels each hash by component, e.g. "eac6dd5 spine P1P2 · ed2db8a route PW · a1fda21 names P3+G11..."), the WP1 close entry in decisions.md gives the four hashes as an undifferentiated set ("Code was already committed") with no component-to-hash mapping anywhere in the provided material.

Also worth noting: `next-prompt.md`'s "(superseded) SESSION-CLOSE BATON — 2026-07-05 HQ marathon" section separately lists `trail_view.clj` + `trail_view_test.clj` as still **uncommitted** at that point, annotated "(P3 + reviewer G11)" — but per `docs/current-mental-model/build/git-spine/CONTRACT.md` §3.D, "P3" there is the *git-spine* work package's display-name component (not WP1's Phase B), and "G11" there is git-spine's gate 11 ("first-thread render"), not relation-kernel's gate 11. That later, separate round of edits to `trail_view.clj` was committed in the git-spine delivery wave as `a1fda21` ("names P3+G11" — `next-prompt.md`, "BATON — 2026-07-05 delivery session"). So the file `trail_view.clj` itself was touched by commits from **two different packages** (WP1's four-hash close, and git-spine's `a1fda21`); the question as asked ("the read-only trail-view module — client wrappers + View-3 text projection") maps to the WP1 deliverable, whose commit is one of the undifferentiated four above, not `a1fda21`.

If a single exact hash is required, what would be needed: a component-to-commit mapping for the WP1 close (e.g., a `git log` on `af0e0e2`/`fd59b78`/`63202b0`/`67f75eb` showing which one touches `src/app/server/rama/trail_view.clj`) — not present anywhere in the provided material.
