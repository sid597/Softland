## Answer

**Next queued item after the trail-view data contract (per the baton's own Queue list):**

`docs/sessions/next-prompt.md`, section `## Queue (Sid sequences; per decisions.md Fable-window queue)`:

- Item 1: "**Trail-view data contract** (next Fable-window item) — the contract + acceptance gates for D-002's first form, consuming the relation kernel via its two query topologies. Open it per the new `/work-package` skill, in a FRESH session..."
- Item 2 (the item immediately following it in that same numbered list): "**D-003 Regime-1 spine** (needs Sid's go): transcript→commit/doc join extractor + git-commit-metadata adapter emitting `:produced`/`:based-on` assertions with evidence anchors, feeding the 27-04 trail view."

So, per that list, the next queued work item after the trail-view data contract is **the D-003 Regime-1 spine** (transcript→commit/doc join extractor + git-commit-metadata adapter).

**Is the baton's "(next Fable-window item)" label current? No.**

That Queue section sits below the header `# ⚡ BATON — 2026-07-05 delivery session (READ FIRST; supersedes everything below)`, which explicitly states it supersedes everything that follows it in the same file — including the older "(superseded) SESSION-CLOSE BATON — 2026-07-05 HQ marathon" material that this Queue list is part of.

The current (superseding) top section of `docs/sessions/next-prompt.md` shows both of these queue items already completed:

- The trail-view data contract's own work package (WP1) is recorded closed further down (still-superseded) text: "**CLOSED/BUILT TODAY:** WP1 **CLOSED** (light retro `build/trail-view/RETRO.md`)."
- Item 2 on the Queue list — the D-003 Regime-1 spine (transcript→commit/doc join extractor + git-commit-metadata adapter) — is the same work as the "git-spine WP2" package (`docs/current-mental-model/build/git-spine/CONTRACT.md` §1: "Give the trail view threads and names..."; scope item A: "Git commit metadata enters the object container as source artifacts (D-003 Regime-1...)"). The top of the baton file records: "**DONE THIS SESSION (delivery mode, Fable coded directly): git-spine WP2 + trail-room R-1 GATE-PASSED, FIXED, COMMITTED, CLOSED.**"

So by the time of the current (topmost, superseding) baton entry, both the trail-view data contract and the item queued directly after it have already been built and closed — the "(next Fable-window item)" label on the Queue's item 1 is stale.

Corroborating (and partially conflicting) evidence from a separate list: `docs/current-mental-model/decisions.md`, section "Open questions queued for ruling," has its own "Fable-window queue (per D-006)" line: "~~gate review of relation-kernel implementation~~ (done 2026-07-03, PASS) → trail-view data contract → hypothesis kill-conditions at first trail-view render → ~~succession document~~ (done 2026-07-03, pulled ahead per Sid's next-prompt sequencing...)." In this list the item named directly after "trail-view data contract" is "hypothesis kill-conditions at first trail-view render" (not struck through in that line), while "succession document" — listed after it — is marked done out of order. This is a different, higher-level queue than the next-prompt.md "Queue" list and does not by itself name "D-003 Regime-1 spine." Neither list's stale ordering is reconciled anywhere in the provided material, but both point to the same conclusion: work has moved past where the "next" labels say it is, so the labels are not current.

**Sources:**
- `docs/sessions/next-prompt.md` — top header "# ⚡ BATON — 2026-07-05 delivery session (READ FIRST; supersedes everything below)"; "CLOSED/BUILT TODAY: WP1 CLOSED..." line; "## Queue (Sid sequences; per decisions.md Fable-window queue)" items 1–2.
- `docs/current-mental-model/build/git-spine/CONTRACT.md` §1 (goal/scope, item A).
- `docs/current-mental-model/decisions.md` — "Open questions queued for ruling," Fable-window queue line.
