**Has the H1 kill-clock started? No — as of the material provided, it has NOT started.**

**What starts it:** a pre-registered **arming event**, not a date — "the first render showing ≥1 typed relation over real material — import-derived or sid-via-agent (D-008) both count."

## Evidence

**`docs/current-mental-model/BETS.md`, Verdict log, 2026-07-05 entry:**
> "H1 clock: NOT started at first light; arming event pre-registered." ... At first light (2026-07-05) the timeline rendered 167 docs with ZERO typed relations over real material (F-L2 confetti) — and H1's claim names transcripts, docs, commits, typed relations. Starting the clock there invites an AMBIGUOUS kill... **ARMING EVENT (an event, not a date): the clock arms at the first render showing ≥1 typed relation over real material — import-derived or sid-via-agent (D-008) both count.**

This entry explicitly overrides an earlier (rejected) alternative of starting the clock at 2026-07-05 first render — that alternative was recorded but "Rejected for the ambiguity above."

**`docs/current-mental-model/build/view-mvp/FIRST_LIGHT.md`, "Addendum 2026-07-05 — clock ruling":**
> Ruled the same day (BETS.md verdict log, under Sid's time-box blanket, reversible on his review): the clock did NOT start at first light — zero typed relations rendered over real material (F-L2), and the pre-registered KILL text conditions the window on "relations + timeline working". Arming event, pre-registered: **the first render showing ≥1 typed relation over real material** (import-derived or sid-via-agent both count). This file remains the first-light record; the arming-event entry in BETS.md will be the clock-start record.

**`docs/sessions/next-prompt.md`** corroborates the ruling ("H1 clock RULED: NOT started at first light; arming event = first render showing ≥1 typed relation over real material" — BETS.md verdict log, reversible on Sid's review) and shows the arming event had **not yet fired** by the end of the provided material: the baton's "NEXT (in order)" step 1 still lists, as future work, "Sid's app boot" to make threads render, after which "screenshot the first kraft connector = the **H1 ARMING RECORD** (BETS verdict-log arming event)." No later entry in the file records that this screenshot/arming record was produced.

## Summary
- Clock status: **NOT started** (explicitly ruled, not merely un-mentioned).
- Arming condition: first render of **≥1 typed relation over real material**, counting either import-derived relations (e.g., a git-spine `:based-on`/`:produced` edge) or a sid-via-agent assertion (per D-008).
- As of the last baton entries in `docs/sessions/next-prompt.md`, that arming event was still pending (queued as the next session's finish-line task), not yet recorded as having occurred.
