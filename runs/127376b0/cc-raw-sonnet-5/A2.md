No. It is explicitly superseded.

The WP1 STANDING block itself (`docs/sessions/next-prompt.md`, section "Active work package: trail-view WP1 (Track A) — data layer" → `## STANDING`) says:

> **Process:** one phase per FRESH session per `.claude/skills/work-package/SKILL.md` + `/rama` mechanics. Phase order: A1 custody → A2 stance kinds → A3 activity+R3 ... → B trail-view module + wrappers + text projection (remaining gates). Fresh-context validation, default-fail verdicts, never overwrite a FAIL artifact (per-round files).

But the `## NOW` log directly beneath it, first entry, states:

> **2026-07-05, Fable — baton COMPACTED, process AMENDED.** ... Ruling with Sid (decisions.md D-006 notes 2026-07-05): fresh context ≠ fresh session — QC layers run as fresh Opus subagents inside ONE orchestrating session; routing tables are advisory on model/effort, never binding on session structure; NOW entries cap ~15 lines. **STANDING's "one phase per FRESH session" line is superseded** (precedence: decisions.md wins; STANDING left unedited per protocol).

This is corroborated in `docs/current-mental-model/decisions.md`, under the D-006 evaluation notes, the 2026-07-05 entry titled "process form-break, ruled with Sid in-session — fresh context ≠ fresh session":

> Root cause: the 2026-07-04 token-economics routing ("no more Fable-priced wake-ups for mechanical phases") bound the fresh-CONTEXT QC requirement to fresh SESSIONS... **Ruling (Sid's countersign = his in-session selection of the collapsed plan):** QC layers keep their kill record and stay; session boundaries go. Default shape: one orchestrating session (Fable when already booted) runs phases with fresh-context Opus subagents as the validation/review layers; routing tables are advisory on model/effort, never binding on session structure; baton NOW entries capped ~15 lines.

So the governing rule right now is: **fresh-context validation/review layers must still run (as fresh-context Opus subagents), but they no longer need to be separate human sessions — one orchestrating session may run multiple phases.** The literal STANDING text ("one phase per FRESH session") is left unedited in the document (per the project's protocol that STANDING is frozen at package open and not edited while active), but it is not binding — the NOW entry itself states decisions.md's ruling wins over STANDING when the two conflict.

(Note: the same STANDING pattern — and identical 2026-07-05 supersession note — also appears for the WP-B2 Track B package in the same file, but the question refers specifically to WP1, cited above.)

Sources: `docs/sessions/next-prompt.md` (WP1 "## STANDING" process line; WP1 "## NOW" first entry, 2026-07-05 "baton COMPACTED, process AMENDED"); `docs/current-mental-model/decisions.md` (D-006 evaluation notes, 2026-07-05 "process form-break" entry).
