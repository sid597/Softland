The custody fields were added in **commit `6320ead`** – the change that introduced the `envelope‑actor-id` / `envelope‑type` columns into the RelationDecisionRow, RelationEvent row and RelationEdge row.