Yes. The STANDING section of **`docs/sessions/next‑prompt.md`** explicitly states that a phase must be run in a *fresh* session and that only one phase is allowed per such session. It reads:

> **Process:** one phase per FRESH session per `.claude/skills/work‑package/SKILL.md` + `/rama` mechanics. Phase order: A1 custody → A2 … → B …  

and later clarifies that a session ends with a baton entry and that the next phase can only start in a new, fresh session. This rule is the governing rule for WP1 at the moment. 

**Citation:** `docs/sessions/next‑prompt#STANDING` (the “one phase per FRESH session” clause).