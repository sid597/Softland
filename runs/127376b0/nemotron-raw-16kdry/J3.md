The read‑only trail‑view wrapper (client wrappers + View‑3 text projection) was introduced in **commit `67f75eb`**. This commit added the `trail_view` module and the associated client‑side wrappers that expose the read‑only data‑layer contract.