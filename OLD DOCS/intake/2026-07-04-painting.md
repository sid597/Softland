# The 2026-07-03/04 painting — full decomposition (intake pre-read)

Every thread the two sessions opened, typed (claim / hypothesis / question /
work / decision / process), with its parallelism status and trigger. Verbatim
sources: `vision/LOG.md` 2026-07-03 + both 2026-07-04 entries. This file is
the pre-read for the first Candidates intake sitting (Fable + Sid); the
sitting sharpens, prunes, and files — nothing here is a bet until then.

## The one serial chain

trail-view contract (fresh Fable session, from `build/trail-view/INPUTS.md`)
→ D-003 Regime-1 spine package (Codex/Opus; needs Sid's go)
→ first render → H1's kill/confirm clock starts
→ (same moment) the WebGPU framework thread and the reader-side projection
   question acquire their first real form-demands.

Everything else is parallel to this chain, in three speeds: session-work now,
Sid-paced, and parked-with-named-trigger.

## Threads

1. **Trail view + contract** — WORK, serial head. Today fixed its inputs:
   bundle = everything-layered; staleness renders differently; select/type/
   sign gesture; version-disagreement rendered not merged; engine-neutral
   depot events. (INPUTS.md is the manifest.)
2. **Verdict capture** — CLAIM (in-scope leaning, Sid ×2 enthusiastic) +
   HYPOTHESIS candidate: inheriting adjudications makes agent context
   reconstruction ~linear (measurable: re-derivation ratio in the benchmark).
   Rides the contract; its test rides the benchmark.
3. **Question-unit** — QUESTION + dedicated WORK (Sid: "a dedicated thing").
   Hole-shaped rows; announcement-at-pattern-match semantics is the open
   design core. Parallel lane; one coupling = contract extension point.
4. **Model-UXR benchmark (H3 machine half)** — HYPOTHESIS + WORK. "What is
   user research for models." Question bank + metrics + ablation design
   draftable NOW by a cheap model; real runs gated on the view's queries.
   Carries candidates: Haiku-can-orient (minimal-token legibility);
   ablation prices each trail layer in tokens.
5. **WebGPU view framework — "one grammar for material and views"** — TOPIC +
   parked QUESTION (the thread the wrap-up wrongly dropped). Established
   today: agents manipulate views ⇒ view-specs are data in the land
   (versioned, assertable, diffable — the June-12 pipeline-diff applies to
   views verbatim); minimal-token hierarchical scene grammar, not HTML slop;
   Electric+WebGPU "best principle wise" (Sid). Parallelizable NOW at
   design-note level: (a) principles note, (b) inventory of existing
   renderer + electric_flow primitives as the seed. BUILD trigger (D-001):
   the trail view's first render is the framework's first form-demand.
6. **Economy of assertions** — VISION CLAIMS + H5 rider, parked with teeth.
   Done today: kill-condition pre-registered in BETS.md; selection-force
   principle drafted (calibration as currency; reward load-bearing not
   decorative; dead-ends pay via consultations-before-turning;
   citation-thickening for the paper jumpstart). Open: full mechanism design
   — trigger = H5 activation. No work before then; pre-registration was the
   work.
7. **Versioning theory / semantic atoms** — CLAIM CLUSTER, settled at theory
   level today: grain tower (atomicity is operation-relative); semantic
   version = provenance-carrying assertion over the mechanical log
   (commit = countersign = publish = panel); two histories (what-happened
   vs what-was-declared); base/active = log/assertion. Consumers: contract
   (gesture), spine (commit-boundary), future code-unit work (function as
   identity grain). Small parallel task: verify prior-art claims (codeq,
   Unison, Pijul, VisTrails, bitemporal) — one deep-research pass before
   any becomes load-bearing.
8. **Bet foundry** — PROCESS, D-007 PROPOSED (awaiting countersign; operating
   already). First intake sitting queued; THIS FILE is its pre-read.
9. **Stack risk / Rama bedrock** — QUESTION answered: self-hostable kills
   availability risk; evolution risk remains; hedge = engine-neutral depot
   events (now INPUTS item 11). Tiny parallel task: verify current Rama
   licensing terms.
10. **North-projection debt** — META, Sid-paced, now real: the LOG holds
    major unprojected material (HCI thesis, the economy vision, the
    versioning notes). LOG rule 3 says an unprojected pile is the failure
    mode ⇒ a vision sitting (Sid rewrites North) is due.
11. **D-003 spine** — WORK, gated on contract + Sid's go. Serial slot 2.
12. **Small parallel anytime**: D-006 counterfactual probe (fresh Opus);
    `:workers 2` smoke; task-count injectability; envelope/payload server
    recheck (must land before any agent-authored writers).

## Open questions carried (typed, with owners)

- Q2 minimum viable discipline (candidate answer: assertion-grade NOW entry /
  stance hygiene — Sid unconfirmed).
- Model-UXR beyond the benchmark: what is the model equivalent of
  longitudinal/ethnographic study? (open, low-stakes for now)
- Merge incentive at scale (git-not-wiki: what makes forks merge) — H5-rung.
- Reader-side projection quality (good log ≠ readable map; zoom-level
  summarization) — the named gap between H1 and H3; no bet yet; trigger =
  post-first-render experience.
- North-chain weak link: the sociology of who stands on the map — guarded by
  H3 as re-sequenced.

## Meta

This decomposition is the bet-foundry's first manual intake AND a
hand-rendered trail view of one day's material — the tool being built,
performed as practice. When the trail view renders days like this one
automatically, compare its output to this file.
