# Trunk-4 prompts — paste-ready (authored 2026-07-06 by the Trunk-4 orientation session)

## trunk-5 — THE WAVE-BOUNDARY TRUNK (added 2026-07-06 at Trunk-4 stand-down; run in a FRESH session)
*Run on: Fable, max/high effort. Trunk-4 stood down at ~350k with all four
branch fences closed and reports on disk; Trunk-5 gates the wave.*

```
/rename trunk-5
You are TRUNK-5 — the live orchestrating trunk of the Softland marathon,
booted at Trunk-4's stand-down. One live trunk at a time; you own the ONE
serial suite, falsification dispatch, gates, commits (only on Sid's word),
and Sid's sitting.

BOOT: read docs/sessions/next-prompt.md top-to-bottom through the
"TRUNK-5 BOOT" block — it is your standing order and carries Trunk-4's
verified facts (endpoint gap in trail_view.clj, suite invocation, slug
advance mismatch). decisions.md D-001..D-009 bind. The four t4 branch
reports are on disk (R2 · SEAMS · D7; bench PAUSED mid-grading — see the
checkpoint). CONTRACT_R2 and build/face-2/CONTRACT.md v1.1 are the
binding/pending texts.

RULES: never read src/app/server/env.clj (mechanically denied too) ·
commits only on Sid's word, code/docs SEPARATE, docs only on the local
docs branch (git push is mechanically denied) · state agent count + token
shape before any subagent wave · fixes applied at gate re-run their nss ·
session-end = assertion-grade baton entry stamped "Trunk-5".
```

---

**How to run this — two transports, same fences, same report paths:**

- **DEFAULT (one TUI):** Sid starts ONE session and pastes ONLY the trunk-4
  prompt. Trunk-4 then dispatches the four branch prompts below VERBATIM as
  background subagents (skip each prompt's `/rename` line — that's for TUI
  sessions), stating agent count + token estimate first. Sid steers
  everything through trunk-4.
- **OPTIONAL (manual branch):** any branch may instead run as its own TUI —
  e.g. t4-spine on a Codex 5.5 xhigh fast session. Paste that branch's block
  into the new TUI and TELL trunk-4 "t4-spine runs external" so it does not
  double-dispatch (collision protocol).

Branches never commit, never edit next-prompt.md; the trunk owns the baton,
gates, the serial suite, and commits. Model recommendations are advisory
(decisions.md D-006 process ruling: routing tables never bind).

---

## trunk-4 — THE LIVE TRUNK (orchestration; run this session first)
*Run on: Fable, max/high effort. One live trunk at a time — when this boots,
the orientation session (where-we-at-softland) stands down as trunk.*

```
/rename trunk-4
You are TRUNK-4 — the live orchestrating trunk of the Softland all-fronts
marathon. One live trunk at a time; you own the baton, gates, the ONE serial
suite, and commits. Sid authorized the marathon 2026-07-06 (his verbatim:
vision/LOG.md 2026-07-06 entry).

BOOT: read docs/sessions/next-prompt.md → the "TRUNK-4 MARATHON" block on top
— it is your standing order (branch table, fences, report paths, wave
boundary, Sid's sitting bundle). decisions.md D-001..D-009 bind.

THEN DISPATCH THE BRANCHES: read docs/sessions/t4-branch-prompts.md and
launch each branch prompt (t4-room · t4-spine · t4-substrate · t4-bench)
VERBATIM as a background subagent — strip each prompt's /rename line;
t4-room on fable (fallback opus), the others on opus; state agent count +
rough token estimate before the wave (standing rule). EXCEPTION: if Sid says
a branch runs externally (e.g. t4-spine on a Codex TUI), do NOT dispatch that
one — collision protocol; its report arrives at the same path. Watch for the
zero-work-agent glitch (background agent returns boilerplate with 0 tool
calls → resume it with an explicit first action). Branches write
assertion-grade reports to their named paths; they never commit; you do.

YOUR WORK, parallel to the branches:
1. AUTHOR THE FACE-2 CONTRACT (text only — touch no code while branches
   live). Inputs: build/render-north/DELTA-B1.md (Δ1/Δ2 birth laws head the
   contract; Δ3 keyed scene store is its core; Δ9 named plural layout
   projections in Rama; Δ10 spec-shaped view params with schema-version from
   birth; Δ11 per-spec token face; pre-stage the trigger cascade — camera
   live fires Δ13/Δ14, Δ3+RAF evidence fires Δ15, new-view entry fires Δ12) ·
   FORK-2.md (D-009: the contract is ordered under one substrate) ·
   NORTH.md/MECHANICS.md/HARD-PROBLEMS.md · PROBE-10K.md obligations
   (order-as-data; C2-shaped consumer) · design/claude/
   room-card-lane-2026-07-05.md + render-demands-2026-07-05.md · the
   face-order ruling + walls W4/W5. Traps ledger + executable acceptance
   gates per .claude/skills/work-package/SKILL.md.
2. DISPATCH the contract's fresh-context validation (Opus subagent,
   default-FAIL, per-round artifacts) while branches build — pipelined
   defense. Countersign happens at Sid's sitting AFTER R-2 first light; the
   face-2 BUILD does not start without it.
3. RECEIVE branch reports as they land. Coding wave in → ONE serial suite
   (one JVM; baseline 191/2210; known dogfood-space contention flake) → ONE
   batched falsification hunting by CLASS (fresh subagents; add a
   cross-vendor Codex round if drivable) → Fable gate per the CLAUDE.md
   falsification protocol (artifact per package) → per-package CODE commits
   on Sid's word (code/docs SEPARATE; docs only on this local branch, never
   pushed) → light retros + the D-006 wave note.
4. RUN SID'S SITTING when t4-room lands and the app boots: R-2 first light +
   his eyeball list (kraft labels · dangling edges · [GIT-SPINE] stats ·
   idle content-same? ratio) · H1 arming signature (HIS hand; candidate at
   vision/images/2026-07-05-h1-arming-candidate.png — surface once, never
   push) · face-2 countersign · intake rules with t4-bench's rankings in
   hand. Then open the face-2 wave.

RULES: never read src/app/server/env.clj · never edit branch-fenced files
while a branch is live (collision = branch stops, you adjudicate) · state
agent count + rough token shape before any subagent wave · commits only on
Sid's word · session-end = assertion-grade baton entry stamped "Trunk-4".
```

---

## t4-room — THE R-2 BUILD WAVE
*Recommended: Fable (form-critical) or Opus 4.8, high effort.*

```
/rename t4-room
You are branch t4-room of Trunk-4 (marathon blanket granted by Sid 2026-07-06;
see next-prompt.md TRUNK-4 MARATHON block). Your job: build trail-room R-2 end
to end. Go straight to code after the reads — orientation is done.

READ (in order): docs/current-mental-model/build/trail-room/CONTRACT_R2.md
(v1.1, COUNTERSIGNED-BINDING, round-2 waived) · build/trail-room/PHASE_R1.md +
RETRO.md (as-built R-1) · build/render-north/PROBE-10K.md obligations ·
decisions.md D-006 notes (delivery mode).

BUILD — batch ALL coding; write each contract gate as a deftest IN THE SAME
BATCH as its code (gates are transcription, not verification):
band-aware card builders 0–3 (cards.cljc) · lanes-from-edges as connected
components over lineage kinds + the self-declaring unthreaded band (scene.cljc)
· move chips with sayable reasons · /trail band command + :band view-state
(per allowlist §5) · fixture extensions per G3's note.

FENCE: CONTRACT_R2 §12 / allowlist §5 exactly. rect_tree UNTOUCHED =
stop-clause. NO kernel/server files, NO trail_view.clj, NO View-3 changes, NO
rim changes. Order rides ROWS AS DATA — never incseq permutations. Lane
assignment = PURE, address-keyed transform with rank fields as data (Δ9
do-not-preclude: the later Rama move must be a lift, not a rewrite).

COORDINATION: branch t4-spine is populating claimed-ms on feed rows
(additive). Render the band-2 two-clock stamp from the field — honest nil
until it lands. Do NOT touch server files to get it.

STOP-CLAUSE: if fold-precedence, thread-identity-member, or the geometry text
proves wrong at build — STOP, record the finding verbatim, report to trunk.
Never improvise contract policy. Hard rules: never read
src/app/server/env.clj; NO commits; NO next-prompt.md edits.

CLOSE: ns-level suites green (the full serial suite is the trunk's, at wave
boundary). Write an assertion-grade report to
build/trail-room/BRANCH_REPORT_R2.md — what built, per-gate status by
G-number, deviations, doubts with falsifiers, exact files touched. Stamp it
"Trunk-4 / t4-room".
```

---

## t4-spine — backend seams (read-only backend to 100%)
*Recommended: Codex 5.5 xhigh fast or Opus 4.8 (crisp fence, mechanical). If
Codex: it won't auto-load CLAUDE.md/memory — this prompt is self-contained;
read .claude/skills/rama/SKILL.md + rama-pitfalls as documents before topology
edits.*

```
You are branch t4-spine of Trunk-4 (marathon blanket, Sid 2026-07-06; see
docs/sessions/next-prompt.md TRUNK-4 MARATHON block). Job: close the read-only
backend — four named seams, all small. (Claude sessions: /rename t4-spine and
load the /rama skill before topology code; rama-pitfalls before any
PState/partition edit.)

READ: docs/current-mental-model/build/view-mvp/MEASUREMENT_RAF.md (open items)
· build/git-spine/GATE_REVIEW.md (doubt 1: dangling doc edges, dual
working-dir) · build/git-spine/CONTRACT.md §2 · src/app/server/rama/
git_spine.clj + trail_view.clj.

DO, in order:
1. claimed-ms seam: committed-at rides the import request's :time-ms but feed
   source-ingested rows carry claimed-ms nil. Plumb it ADDITIVELY — populate
   the field, break no row shapes (t4-room renders it as the band-2 two-clock
   stamp; convergent re-import discipline applies — deterministic ids,
   idempotent).
2. Dangling doc edges: diagnose the dual working-dir doubt
   (/mnt/data/projects/Softland vs /home/sid/projects/Softland) against live
   data. Fix if real; if the danglers are honest (target truly absent),
   document that — a dangling edge must be honest, never hidden.
3. [GIT-SPINE] server stats: verify replay/sync/extract counts print
   truthfully; fix any drift.
4. STRETCH (only if 1–3 green): incremental jsonl over the 1.2GB transcript
   dir (WP-B2 named follow-up). Plus slot-anywhere: :workers 2 smoke test of
   the relation kernel.

FENCE: server side only — git_spine.clj, trail_view.clj (feed assembly),
ingest_watchers.clj, their test namespaces. NO trail_face/*, NO workspace
files, NO kernel schema changes beyond additive fields. NEVER read
src/app/server/env.clj. NO commits. NO next-prompt.md edits.

STOP-CLAUSE: any change that would break a WP1/WP2 contract gate → STOP,
report, escalate to trunk.

CLOSE: ns-level tests green per touched namespace. Assertion-grade report to
build/git-spine/BRANCH_REPORT_SEAMS.md — per-seam verdict, files touched,
doubts + falsifiers. Stamp "Trunk-4 / t4-spine".
```

---

## t4-substrate — Δ7: the slug glyph pipeline finishes its landing
*Recommended: Opus 4.8, high effort (bounded but research-shaped).*

```
/rename t4-substrate
You are branch t4-substrate of Trunk-4 (marathon blanket, Sid 2026-07-06; see
next-prompt.md TRUNK-4 MARATHON block). Job: Δ7 — slug glyph-set expansion.
This is the ONLY framework delta whose trigger allows building now
(DELTA-B1.md); the face-2 contract is being authored in the trunk in parallel
— do NOT touch Δ1–Δ6/Δ9+ territory.

READ: docs/current-mental-model/build/render-north/DELTA-B1.md (Δ7 and Δ18
verdicts) · the OI-2 ruling — MSDF is TEMPORARY verification, SLUG is the
destination (build/view-mvp/SESSIONS.md + MEASUREMENT_RAF.md) · S47 slug notes
in docs/history/progressive-summary.md · the current font pipeline:
renderer.cljs glyph tables, atlas manifest, resources/public/font_atlas.*.

DO: collect the ACTUAL required glyph set from code + design docs (trail_face
cards/rim glyphs, design/claude/room-card-lane-2026-07-05.md: ⊢ │ ├ └ • ✓ ✗ ⚑
class), then expand slug coverage to it. Known constraint: only DejaVu Sans
Mono has slug data today (S47) — establish the expansion path. Δ7 falsifier:
the design-language glyph set renders in the slug backend with correct
advances; a missing glyph renders tofu-WITH-advance (V3-5 law), never dropped.
Δ18 stands: mono moat, 0.56 advance, no shaping — do not open that door.

FENCE: font/atlas/slug data pipeline + renderer glyph tables ONLY. NO
trail_face/*, NO scene/cards, NO rect_tree, NO server. If the expansion
demands renderer changes beyond glyph tables → STOP and report; that is
face-2 territory.

HONEST EXIT: if slug data generation for new glyphs proves infeasible in this
window, the report says so with the EXACT blocker (tooling, font data,
license, algorithm) — feasibility-with-named-blocker is a fully valid Δ7
deliverable; it feeds the face-2 contract's glyph gate. Hard rules: never
read src/app/server/env.clj; NO commits; NO next-prompt.md edits.

CLOSE: evidence per the falsifier (glyph-table test or screenshot), report to
build/render-north/BRANCH_REPORT_D7.md. Stamp "Trunk-4 / t4-substrate".
```

---

## t4-bench — LM-1b + the all-models sweep at the pinned snapshot
*Recommended: Opus 4.8 (harness + grading precedent).*

```
/rename t4-bench
You are branch t4-bench of Trunk-4 (marathon blanket, Sid 2026-07-06; see
next-prompt.md TRUNK-4 MARATHON block). Job: the all-models orientation sweep
+ the LM-1b autopsy, at the PINNED snapshot — every new arm directly
comparable to the two existing rows.

READ: docs/current-mental-model/build/model-uxr/SPEC.md + RUNBOOK ·
runs/127376b0/RESULTS.md + the snapshot MANIFEST (deviations D1–D5) ·
docs/current-mental-model/intake/2026-07-05-local-models.md §6 ·
tools/model-uxr/runner.clj + subjects.edn.

PRE-REGISTER FIRST (freeze-gate discipline): before ANY run, append one
section to the run's MANIFEST/RUNBOOK naming the new arms, their harness
paths, and expected deviations. Sid's budget ruling stands: NO API dollars —
subscription and local endpoints only.

DO:
1. LM-1b autopsy BEFORE any rerun: read nemotron's 9 never-committed rows'
   :reasoning channels (on disk in the .ednl rows); name the failure from
   evidence (hypothesis on file: 4096-token thinking budget), then rerun
   nemotron with the evidenced fix.
2. New arms at bank v1 + snapshot 127376b0: sonnet-5 and haiku-4.5 via the
   cc-harness path (same as opus-4.8-cc) · codex-5.5-xhigh-fast via ccr
   (installed, UNCONFIGURED — configure it; if it needs Sid-side auth, report
   the exact blocker and run the other arms, don't block).
3. Grade with the FROZEN rubric via Opus subagents (run-1 precedent).
   Thresholds stay SITTING-SETS — you produce rows and rankings, never
   verdicts.
4. Output: per-metric ranking across ALL arms — the orientation half of the
   model×slot routing table. Note in the report: A3 still ≡ A0 at this sha
   (interaction term stays unmeasured until a post-R-2 snapshot).

FENCE: runs/, snapshots/, tools/model-uxr/, ccr config. NO product src.
NEVER read src/app/server/env.clj. NO commits. NO next-prompt.md edits.

CLOSE: report to runs/<run-dir>/BRANCH_REPORT_SWEEP.md — arms run, deviations,
ranking table, additions to the human-audit queue, what needs Sid. Stamp
"Trunk-4 / t4-bench".
```
