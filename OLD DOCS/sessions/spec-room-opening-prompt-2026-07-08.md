# Spec Room — opening prompt (paste into a FRESH session)

Spec room for the sense-line MVP. Read, in order, before anything else:

1. `docs/current-mental-model/sense-line-model.md` — the model. Hold its fixed
   points, especially #7: labeling is emergence — description first, names
   earned by recurrence, Sid names.
2. `docs/current-mental-model/build/sense-line-mvp/DIRECTION.md` §0 + §3.1 —
   **SPEC ≠ BENCHMARK** (this room produces the spec; model-performance is a
   different, later room) and the layering discipline: block → mark kinds →
   two edge families (epistemic · process — kept separate) → episode. Combine
   by LAYERING, never merging. Blocks are addresses+offsets into raw text,
   never copies. Chunking and labeling are separable stages.
3. `docs/current-mental-model/inquiry/specimen-01-this-chat-2026-07-07.md` +
   `specimen-01-graph-derivation-2026-07-07.md` — the hand-made exemplar and
   the three forced extensions (purpose/serves · test/invokes · verdict) with
   recurrence receipts: the best-evidenced candidate kinds.
4. `docs/current-mental-model/design/claude/atomic-unit-2026-07-07.md` — what
   died and why (the five-field merge; the verified Q/C/E/Source base — DG is
   input vocabulary for the epistemic family, never the frame).

**Goal (definition of done, Sid's words):** the artifact stating what the spec
is — what kinds of nodes, what relations between them — plus the block grammar
(what a good chunk break looks like).

**Method:** reason-based discussion + apply candidates to ONE example chat via
manual pass; iterate. Sid rules on everything; propose plural options, never
converge-and-sell.

**First move:** propose 2–3 candidate block-break rules and a scaffold
kind-set; apply them by hand to a ~30-message stretch of the example chat;
show the marked output; then we argue.

**Example chat:** Sid picks. Default suggestion: the design sitting
`7c80ce2a` — specimen-01 describes it independently, so candidates can be
checked against a description not derived from them. Transcripts live at
`~/.claude/projects/-mnt-data-projects-Softland/*.jsonl` — read SPANS lazily,
never load a whole transcript.

**Do NOT load:** next-prompt.md, decisions.md in full, batons, or the
authoring session's transcript (`18d63935` — on disk if a specific nuance is
needed; read the span, not the whole).

**Register:** working session with Sid, close iteration; his in-flow verdicts
are the gold; no work-queues beyond the next concrete iteration.
