# Starter for the next session on the frame (16 September 2026)

Made with the prompt-maker skill: the raw in Sid's first person, spliced from
this chat, then flavorings to append. Sid decides what to append. Presented
as two turns because the folder is a load before it is a question: turn one
at low effort, then `/effort max`, then turn two.

## Turn one

This session is self contained. Read only `src/proposal/frame-2026-09-15/`
and do not read outside it. In this order: `README.md`;
`What-is-this-proposal-about.md`, which has verbatim my questions and your
replies through part three; `LEDGER.md`; `PARTS-4-8.md`; the headings and the
envelope block of `NOTATIONS.md`. `PICTURE.md` is the proposal itself; open
it when a part's wording needs checking. Reply with one line: what you read
and the byte total.

## Turn two

I want to be put in a position to respond to this proposal. We are going
layer by layer, top down, from how it should be, correcting my mental model.
Each layer true to the content below it but one abstraction up. Layer up does
not mean dilution or a loose summary. Part by part, because a proposal has
parts, and each part carries what it proposes, what it settles, what it leaves
open, and what it asks of me. I am a human, not an AI that can load things
into my brain at once. Write in the words I think in. Nothing referenced
before it has been introduced.

Where we are: layer −1, the top-layer recognition, and parts one and two are
done. The ledger has what I ruled and at what weight and points at where I
said it. Part three was delivered and I have not responded to it. Parts four
to eight are in `PARTS-4-8.md` in the same form as one to three.

One part at a time. Present the part from the file and stop there until I say
it reads right. The "you said" framing is dropped. Do not carry ECS; keep
things simple, as in not naming an abstraction and one implementation of it
together. Say tool, running answer, offer. When I say agree or nothing more,
tell me what changed in the ledger and open the next part. Nothing lands on
disk until I say; at the end, preview the write-set in one message so I can
veto in one line.

Start by asking me for my response to part three.

## Flavorings

One sentence each on what that room might find that this chat is missing.
The folder is handed in the raw, so "read nothing on disk" in a line means
nothing beyond the folder.

Claude, widest: takes the folder as the whole world and checks nothing, so it
would carry the parts in the verbatim's voice and hand every pushback of mine
back as a question, uncoloured by what the code looks like.

Claude, one mechanism deep: would go to one file when a claim needs a receipt,
which this chat never did, so it could tell me whether one row of part eight's
table, say Inland's admission as a gate with actor sets, is what the proposal
says it is.

Claude, whole system: would check both sides of part eight's table with
receipts and come back as a report, which is the verification this chat
declared it had not done.

Codex: does what the words say, so with the folder attached it would find the
places where the ledger's wording outruns my words in the verbatim, which is
the one thing a compression cannot see about itself.

```
read nothing on disk, no code, never src/app/server/env.clj.
read nothing on disk; code only if a claim needs a receipt, scoped to one file, say why you went; never src/app/server/env.clj.
read nothing on disk; code only if a claim needs a receipt; never src/app/server/env.clj.
attached: src/proposal/frame-2026-09-15/. i am asking whether the walk through parts one to three settled what LEDGER.md says it settled, and what i want from it is every place the ledger's wording goes past my words in What-is-this-proposal-about.md.
```
